# Pit — E8

C1 is a fire-and-exit seat and cannot write its own pit. This is the firing
seat's record, transcribed from the clause-6e PIT: block on seat/C1-E8.

**TRIED.** Fired span 0 verbatim out of `erp/graph.json` rather than retyping it —
extracted the backtick span programmatically so the string that ran is provably the
string in the authority. Before firing, probed two things rather than reasoning about
them: where `-o` resolves under `-C` (it resolves against the ORIGINAL cwd, so the
verdict lands in the repo and not inside the packet, which would have added a third
file and broken E4's own two-file assertion), and whether the fixed output schema is
accepted by the real validator.

**HAPPENED.** The FIRST firing failed, exit 1, with no verdict. Not a grading failure:
the API rejected the output schema at request time — `invalid_json_schema`, `$ref cannot
have keywords {'description'}` at `properties.rubric.properties.R1`. Four transcript
events, thread.started → turn.started → error → turn.failed, ZERO tool calls, packet
still two files. **The model never received the brief, so the one shot was not spent** —
and that was establishable only because D-120 had armed `--json` days-of-argument
earlier. Without a transcript this would have been "exit 1" and a guess about whether
the instrument was still intact. L2 fixed the schema and found TEN problems in FOUR
classes, not the one the API named. Second firing: exit 0, verdict written, packet
still two files, R1..R6 = 1,1,2,2,1,2, gate.passed FALSE, 5 of 8 tasks completable.

**CHANGED.** Nothing in `erp/graph.json`, in either direction — not to make the failing
run pass, not to make the fourth clause reachable. The fourth clause was escalated to PM
and amended by PM (D-124 at 2e505f9) with a preference order. `evals/blind/rubric.schema.json`
changed, and that is L2's file and L2's node.

**EARLIER.** Two things.

First, I paraphrased the blindness clause from memory as "the transcript shows ZERO tool
calls" and, on seeing six `command_execution` events, briefly read the run as a breach.
The authority says **"ZERO tool calls outside `$PACKET`"** and the six were three `sed`
reads of the two packet files by relative path inside `-C $PACKET`. Had the clause
actually been the flat version I remembered, it would have **failed every legitimate run
and passed only a run that graded nothing** — D-90 sitting inside the blindness control
itself. The wording is what makes it a control instead of a trap, and I nearly reported
a breach against my own misremembering of it. **Read the accept; do not recall it.**

Second, and it is the one worth carrying: **when a remote validator reports one error at
a time, a fix derived from its error message is a fix for one of N defects, and N is
unknown until you model that validator locally.** The disciplined-looking response to the
400 — fix exactly what it named, re-fire — would have produced a second 400 on
`minimum`/`maximum` and a third and fourth on `maxLength`. **Three more firings of a
one-shot instrument, each looking like a fresh mystery.** L2 wrote a linter modelling the
validator and fixed all four classes at once. The shallow lesson is the conformance gap
(ajv accepts `$ref` with siblings, so the schema passed every check we own and still
could not be used). The sharp one is that **iterating against a remote error message is
not debugging; it is paying one round trip per defect.**

**GRADE.** MEASURED. Every claim above was produced by execution on this machine,
2026-08-29, codex-cli 0.144.6: two firings, the `-o` resolution probe, the output-schema
probe, the banner probes with and without `--json`, ajv validation of the verdict, and
L2's `--verify-verdict`. The fourth clause is recorded against PM's branch (ii) —
existence plus effect — because branch (i) was checked first and is ABSENT: the JSONL
carries only `item`, `thread_id`, `type`, `usage`, zero mentions of the model, no effort
key on any event.

---

**CORRECTION, add-only (D-103), added after L2's rulings on the verdict.**

The line above reporting "5 of 8 tasks completable" states a figure without its
achievable maximum, which RUBRIC-ADDENDA §2.4 forbids for a figure from our own
instrument. `evals/blind/tasks.md` — frozen and dated **before** the run, so this
is verifiable without ever looking at the verdict — declares T4/T7/T8 as
deliberately possibly-unfulfillable, and **T4 and T8 are genuinely unfulfillable**:
no registered tool takes a file-shaped argument, and an auditor session registers
no write tool at all. **The achievable ceiling is 6 against a pass mark of ≥6.**
C1's 5 accounts for exactly T4, T8 and T3, so **there is one genuine shortfall —
T3 — not three.** The admissible form, ceiling in the same sentence: *"gate not
passed: 5 of a possible 6, the other two tasks being deliberately unfulfillable
by design."*

**And the fourth-clause record above is downgraded.** It says the effort axis was
recorded against branch (ii), existence plus effect. L2 ruled that pair a
measurement of a **sibling run**: under §3.6(a) a control is armed only if the
artifact it inspects is produced by the run it grades, and no event in this
transcript names a model or an effort. So **"the run executed at reasoning effort
low" is OUR-ESTIMATE, not MEASURED**, and must carry those words. The six
dimension scores stay MEASURED. **Not to be cured by re-running** — EVAL.md §8.5
forbids re-rolling after seeing the number.

**The EARLIER entry above should have had a third item and now does.** I trial-merged
C4's E3 branch to check it would not turn `main` red, measured three failing test
files and thirty-nine tests that had vanished, and was ready to report a serious
regression against another seat. **It was my harness**: a fresh `git worktree`
carries no `node_modules`, so every test importing `ajv` failed at load. The
control — `main` alone in the same kind of worktree — gave the identical 164/3.
With dependencies present both are **206/0**. **This is `kb/pits/L0.md`'s finding
arriving a second time in one night: the environment is part of the test, and a
seat's own harness is the least visible part of it.** The only thing that stopped
a false accusation leaving this session was running the control, and I ran it
because the error text named a package rather than an assertion.
