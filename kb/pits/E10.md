# Pit — E10

C4 is a fire-and-exit Codex seat. Transcribed from the clause-6e `PIT:` block on
`seat/C4-E10`, in C4's own words, with an L1 addendum at the end marked as such.

**TRIED.** Added executable per-case mutants, reran every enforced must-fail case, and
required its exact refusal to become a permitted outcome.

**HAPPENED.** The deployed-origin baseline stayed green 11/11; all 10 must-fail
mutations flipped and the report recorded all 11 suite rows.

**CHANGED.** `eval.mjs` now accepts `run --suite negative --verify-controls`, validates
`brokenBy` against `evals/mutants`, fails surviving controls, and writes the mutation
report.

**EARLIER.** Treat `brokenBy` as an exact executable contract from the start, and
separate must-fail controls from positive guard rows before designing the report.

**GRADE.** Green — acceptance exited 0 with 10/10 must-fail flips and 11/11 report rows.

---

**L1 ADDENDUM, 2026-08-29, marked as not C4's words.**

**This node is the answer to a question E3 could not answer about itself.** E3 went green
eleven-for-eleven against the deployed origin, and a suite that passes proves only that
nothing tripped it — **not that it would have noticed if something had.** E10 applies each
case's declared `brokenBy` mutation and **fails the run if the case does not flip from
refused to permitted.** Ten of ten flipped. That converts E3's eleven passes from
*nothing went wrong* into *the detectors work*, which is D-108's rule — prove the
detector still returns the POSITIVE — pointed at an entire suite rather than one check.

**Two verification notes, because C4 made a claim about its own run that I checked
rather than took.** C4 reported "five unrelated inspector failures — `Chrome exited
early (null)`". **Merged into `main` and run here: 221 pass, 0 fail, zero `not ok`
lines.** The failures were headless Chrome being unavailable in C4's sandbox, not a
regression, and C4's characterisation was accurate.

**And this was the first node gated with `tools/merge-gate.mjs --accept-merged`**, added
tonight because E3 exposed that the gate ran predicates against the base checkout: E10's
accept invokes a subcommand that exists only on its own branch, so in the base tree it
could not have passed no matter how correct the node was. **The gate would have reported
a red predicate for a green node — the worst direction for a gate to be wrong, because
it teaches you to distrust the gate.**
