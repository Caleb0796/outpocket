# Pit — S12

*Reported by I3 in its own words; L1 transcribed, did not compose. Late to the record for the
same reason as [[S5]] — written in conversation, never sent.*

**TRIED.** Built `server/locks.mjs` as x-freeze layer 2: `assertUnlocked(reportId)` throws 423
`E_SIGN_IN_PROGRESS` — the call any future mutating write route must make first, since **none
existed yet** (S4's own precedent for this build order). Wired `locks.acquire()` into
`sign.mjs`'s `open()` **directly adjacent to the snapshot build, no `await` between them, so
"no window" is true by construction** rather than by test. Wired `locks.release()` into
decline/commit/expiry. Made `sign_request.revision` server-owned:
`locks.getRevision(reportId, claimed)` seeds from the caller's claim **only the first time** a
`report_id` is seen, then ignores further claims; `bumpRevision()` stood in for "an accepted
mutation" until S2 landed real ones.

**HAPPENED.** `sign-lock.test.mjs` 8/8, re-run 5x clean **including the genuine-concurrency
race test — two real HTTP opens through `Promise.all`, not a single-threaded assertion dressed
up as a race.** Full suite 152/152 at merge, `sha256sum -c FREEZE.md` unchanged (never touched
`erp/contracts/**`).

**CHANGED.** `server/locks.mjs` (new), `server/sign.mjs` (locks param + acquire/release call
sites + revision resolution), `tests/acceptance/sign-lock.test.mjs` (new). Did not touch
`index.mjs` at merge time — `createSignGate`'s default locks kept the wiring inside `sign.mjs`
alone.

**EARLIER.** Read `graph.json`'s S12 node and `erp/CONTRACTS.md` §11 check 9 before writing
anything, and confirmed via `kb/pits/S4.md` that "no HTTP write routes yet" was an
already-endorsed reading for this exact situation — **so I did not fabricate a fake write route
in production code to make the 423 check "literal".** S2 built the real ones two nodes later
and now uses this same lock.

**GRADE.** Green, on estimate (2.0h).
