# Pit — S5

*Reported by I3 in its own words; L1 transcribed, did not compose. Late to the record — see the
note at the foot.*

**TRIED.** Built the server-owned sign-decision state machine in `server/sign.mjs`:
`open -> answered(signed|declined) -> committed | expired`, `signed_by` from the session cookie,
`at` from the server clock, **never from the request body**. Shipped the two-call handshake
(`src/page/sign-bridge.js`) as default per `contingencies[4]`, suspend-until-signed kept behind
the same switch as this node's own negative control. Minted `confirm_token` (DOM-only delivery;
`peekConfirmTokenForDialog` is a documented test-only stand-in for that) and a distinct
agent-visible ticket, **each rejected in the other's slot**. Added `E_ALREADY_ANSWERED` (409)
and `E_NOT_SIGNED` (409). Wrote N-15/N-16/N-21 as real curl-level scenarios against a real
`http.Server`.

**HAPPENED.** `sign-state.test.mjs` 16/16 at merge, full suite 139/139, `sha256sum -c FREEZE.md`
clean after the scheduled re-record, contracts-check green.

**CHANGED.** `server/sign.mjs`, `tests/acceptance/sign-state.test.mjs`,
`src/page/sign-bridge.js`, plus the pre-authorized `DEV-E3-eval-case-known-open` edit:
`eval-case.schema.json` `examples[1].controlStatus` `known-open -> enforced`, with FREEZE.md's
manifest line re-recorded in the same commit.

**EARLIER.** Hit the ownership hook on that scheduled frozen-file edit, **did not bypass it**,
and self-filed DEV-E3 — the schema, `erp/CONTRACTS.md` and `erp/graph.json` all pre-named that
exact ticket ID as authorization before the node existed to file it. Confirmed all four gate
commands rather than assuming green. Left one thing **explicitly OPEN in the module's own
header rather than scoping it out silently**: `commit()` does not re-canonicalise against live
state — that is S6, now unblocked by S12.

**GRADE.** Green, as scoped.

---

**Why this arrived late (L1's note, not I3's).** I3 wrote these five fields in an earlier turn
as ordinary conversation text and never sent them over `SendMessage`, so they never reached the
foreman session and the row sat at `pits:PENDING` for a day. **A report composed but not
transmitted is indistinguishable, from the receiving end, from a report never written** — the
same shape as every other finding this sprint, applied to the reporting channel itself. The
contract template already says "REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS
GREEN"; that instruction exists because this has now happened four times.
