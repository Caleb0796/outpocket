# Pit — E4

*L2's five fields, delivered in its COMMIT under clause 6e. L1 transcribed, did not compose.*

**TRIED.** Ran `accept-gate E4 --list` **BEFORE building**, as instructed, and it immediately
showed span [4] is a backticked prose fragment rather than a command. Built the hermetic home
first because it is the only output with a measurable environment behind it, then the brief,
then the tasks, then the schema, then the builder that guards all three. **Ran every accept
clause individually with `--run-clean` rather than trusting a suite.**

**HAPPENED.** All four real clauses green. **THE FINDING I DID NOT EXPECT WAS IN MY OWN
DETECTOR:** the admissibility scan's workflow-order pattern
`/descriptions?[^.]{0,60}\border\b/` **FIRED ON MY OWN SCHEMA**, matching
`"description": "One record per task, in task order"` — where "task order" means T1 through T8
and has nothing to do with tool call order. **A FALSE POSITIVE, caught only because the
self-test requires the REAL files to pass and not merely the injected ones.** Narrowed to
require "tool" adjacency. Second: span [1] `ls -1` passes from the repo root and proves
nothing, flagged not fixed. Third: the export carries two repo identifiers that cannot be
removed, so `EVAL.md` §8.2's export grep is unsatisfiable as written.

**CHANGED.** Five files, all declared E4 outputs, ownership clean. **NOTHING ELSE. No product
code, no answer-key file, no `erp/*.md`.**

**EARLIER — and it is the MIRROR of D-90 rather than an instance of it.** D-90 says prove the
check can return the NEGATIVE. **I nearly shipped a detector that was half-tested, because the
other half matters just as much: PROVE IT STILL RETURNS THE POSITIVE ON THE REAL CORPUS.** All
four injected contamination cases fired and I would have called it proven. The case that
caught the bug was the clause asserting **the real files still pass**. **A DETECTOR THAT FIRES
ON EVERYTHING IS EXACTLY AS SATISFIABLE AS ONE THAT FIRES ON NOTHING — and the false-positive
direction is the one that gets an instrument DISABLED by the next person rather than merely
ignored.**

**GRADE.** Green on all four real clauses, each run individually with `--run-clean`. Both
self-tests follow D-90 by **ADDING the forbidden thing to a valid subject, never deleting the
subject** — and since every `blind-home.sh` assertion is an absence assertion, all four
satisfiable by an empty home, its self-test **requires an EMPTY HOME TO BE REJECTED**.

**Standing caveat, not softened:** `tools/blind-home.sh` CANNOT see the ~3.7 KB
`<recommended_plugins>` block — it is pulled in by `auth.json`, which the script must copy, and
does not come from `$BH/config.toml`. The honest sentence is that **the blind run is ISOLATED
FROM OUR REPOSITORY, NOT BYTE-REPRODUCIBLE RUN TO RUN.** No byte count is published as a
constant, and the retracted 32,412 → 11,217 pair is not requoted.

---

**L1's note on clause [1].** I merged this with span [1] verified BY HAND rather than by the
predicate, exactly as I graded D1: the packet has **2 entries, both byte-identical to their
sources, and `git rev-parse` inside it exits 128 — outside any repo.** The span itself is
decoration: its prose says "`ls -1` IN IT prints 2", "in it" is not something a shell can
bind, the extracted span is a bare `ls -1`, and **it exits 0 from the repo root on any packet
or none.** Demonstrated. With PM.
