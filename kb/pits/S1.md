# Pit — S1

**TRIED.** Read `erp/contracts/eval-case.schema.json`'s `persona` enum directly
(`["none", "chen", "ruiz"]`) rather than trusting the dispatch text's retyped
spelling, before writing a single line of `server/index.mjs` — the dispatch
itself warned "check that file, do not invent the spelling," and `src/erp.js`'s
existing `PERSONAS` array (`chen`/employee, `ruiz`/auditor) confirmed the same
two ids independently. Built a single-file Node server on the built-in
`node:http` module — no framework, matching the node's own title — with a
`createApp()` factory closing over an in-memory `Map<sid, personaId>` rather
than module-level state, so `tests/acceptance/session.test.mjs` can spin up
independent servers with independent sessions inside one test process without
port or state collisions. `POST /api/login` validates `persona` with
`Object.hasOwn(PERSONAS, personaId)` rather than `PERSONAS[personaId]` or the
`in` operator — a bracket/`in` lookup against a plain object resolves
inherited members, so a client sending `persona: "constructor"` or
`"__proto__"` would otherwise read back a truthy, wrong value instead of being
refused. Wrote a matching negative test for exactly that. Wrote
`erp/contracts/session.contract.md` describing the cookie mechanism, the two
personas, and the one-instance constraint this node's own graph notes
(shared by `S6`'s TOCTOU closure). Cross-checked the write set against
`erp/contracts/tool-surface.contract.md`'s S2/S4 authorization boundary to
confirm `S1` does not reach past its own scope: no route here does anything
but establish and read back a session.

**HAPPENED.** `node --test tests/acceptance/session.test.mjs` exits 0, 8/8
subtests pass — under a bare shell and again under `FORCE_COLOR=3`
(see [[force-color-3-in-seat-env]] and `kb/pits/L0.md`, since any exit-code-only
predicate is unaffected by the colour issue but re-running costs nothing and
rules it out cleanly rather than by argument). The three accept-line
behaviors are each a named test: `Set-Cookie` carries `HttpOnly` and
`SameSite=Lax`; `GET /api/me` with that cookie returns
`{persona:"chen", role:"employee"}`; without any cookie, or with a garbage
`sid`, it returns 401. A further test reads
`erp/contracts/eval-case.schema.json` at run time (not a hard-coded copy) and
asserts its persona enum is exactly `{none, chen, ruiz}`, so a future edit to
that frozen file would fail this suite rather than silently drift. Ran
`node --test` with no path argument afterward: 48/48 pass repository-wide, so
nothing here disturbed `S11`'s canonicaliser suite or the T6 surface tests.
Ran `node tools/lint-layer0.mjs`: it flags pre-existing banned wording and
retracted claims in `.team/contracts/G4.txt` and `kb/pits/S10.md`, unrelated to
any file this node touched, and none of `server/index.mjs`,
`erp/contracts/session.contract.md`, or `tests/acceptance/session.test.mjs` are
named in its output.

**CHANGED.** Nothing outside this node's three declared outputs
(`server/index.mjs`, `erp/contracts/session.contract.md`,
`tests/acceptance/session.test.mjs`) — all newly created, none pre-existing.
No route beyond `/api/login` and `/api/me`; no logout, no expiry, no
persistence across restart, no CSRF token beyond `SameSite=Lax` — the contract
doc names these as this node's explicit non-goals so a later node does not
mistake their absence for an oversight.

**EARLIER.** Nothing from an earlier attempt at this node — this is `S1`'s
first dispatch, unblocked by the `S10` freeze that merged at `225daa6`. One
thing worth carrying forward for whoever builds `S2`/`S4`/`S5` on top of this
server: the session lookup lives entirely inside `createApp()`'s closure, on
purpose, so a route handler can never reach a persona except through the same
cookie-to-session path this contract documents — there is no back door via a
request body or query string for a downstream node to be tempted into adding
under time pressure. If that changes, the contract doc's line about it needs
to change with it, not just the code.

**GRADE.** MEASURED. `node --test tests/acceptance/session.test.mjs`, Node
v22.23.1, in this worktree (`seat/I3-S1`, branched from `main` at `0f3cebd`,
which includes the `S10` freeze at `225daa6` and `S11` at `17680ea`) on
2026-08-29: 8 pass, 0 fail, identically under a bare shell and
`FORCE_COLOR=3`. The full-suite 48/48 and the lint-hook output above are read
directly off those two runs, not inferred.
