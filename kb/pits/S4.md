# Pit — S4

Reported by **I3** under D-31, with the green, transcribed by L1.

**TRIED.** Built `server/envelope.mjs`: `buildViolationEnvelope(violation, ctx)` lifts
`src/policy.js`'s bare `{code, severity, field, message, fix}` into the full
`violation.schema.json` envelope, **looking up `rule_id`/`fix_class` from S3's frozen
`POLICY_DOCUMENT.rules` rather than re-typing a second rule table** — so the envelope cannot
drift from what the policy document actually enforces. Two things are enforced at construction
time rather than documented: `human_exception_required`/`not_reimbursable` violations must carry
empty `candidates`, and every `fix` runs through the `x-fixLint` substring scan **read live from
the schema's own `x-fixLint.bannedSubstrings`** rather than hardcoded, so a deviation-ticketed
change to that list needs no code edit here.

Since no HTTP write routes exist yet — S2 and T2 build those, downstream — read "every write
route" as **every rule the policy engine can raise across every write operation**, and wrote
`tests/acceptance/envelope.test.mjs` as one real scenario per rule, **R01 through R19**, each a
genuine `validateLine`/`validateReport` call feeding `buildViolationEnvelope`, each
ajv-2020-12-validated against the frozen schema. `tests/fix-lint.test.mjs` implements and
verifies the lint itself: rejects every `x-lintFailingExamples` instance the schema documents
(including the spike's live defect verbatim), catches all 19 banned substrings
case-insensitively, and confirms every schema `examples[]` fix and the ported `CAP_TRANSPORT`
fix pass clean.

**HAPPENED.** `envelope.test.mjs` 6/6, `fix-lint.test.mjs` 7/7, full suite **82/82** (69 prior +
13 new), no regressions. `sha256sum -c erp/contracts/FREEZE.md` clean. The repo's own pre-commit
ownership hook ran on commit and printed OK for all three files as declared S4 outputs.

**One early failure, and the direction of the fix is the point.** I had asserted the
`CAP_TRANSPORT` envelope's `message` matched the schema's ILLUSTRATIVE worked-instance prose
verbatim. It does not — `src/policy.js` keeps the spike's own wording ("exceeds…cap" against the
schema's "is above…limit") — and **only `fix` was ever required to match, because `fix` is what
`x-fixLint` grades.** The assertion was wrong, not the message. Fixed the assertion to check the
real text.

**CHANGED.** Three new files only: `server/envelope.mjs`, `tests/acceptance/envelope.test.mjs`,
`tests/fix-lint.test.mjs`. Did not touch `src/policy.js`, `src/erp.js` or any write-tool code in
`src/page/**` — those are downstream consumers (T2, and whoever wires the eventual server write
routes) and none are S4's declared outputs.

**EARLIER.** Read `CONTRACTS.md` §4 before writing anything, specifically for the exact envelope
shape and the `RECEIPT_REQUIRED`/`CAP_TRANSPORT` worked instances, then cross-checked against
them. **Checked for an existing reusable fix-lint implementation before writing one**
(`tools/lint-layer0.mjs` is a different check, unrelated) rather than assuming none existed.
Deliberately avoided `entity_id "ln_3"` anywhere in the fixtures — `violation.schema.json`'s own
`x-invalidExamplesNote` documents that id as a historically confused one, re-keyed off it on
purpose during an earlier repair.

**GRADE.** Green, 1.5 h as scoped.

---

**L1's note.** The clause said "every write tool" and there are no HTTP write routes yet. The two
easy readings were both available — call it vacuous, or wait for S2/T2 — and I3 took neither. On
a day when this project priced three separate instances of exit 0 satisfied by a check that
discovered nothing, **finding the non-vacuous reading of an under-determined clause is the
instinct worth naming.** And correcting its own assertion rather than the message is the harder
direction: the tempting move is always to make the artifact match the test.
