# Pit — S7

Two clause-6e blocks, both verbatim from I3, bolded at the field headers and
otherwise unedited. The first is S7 as built; the second is the CONFLICT
RESOLUTION with D-118, which produced a property neither branch could test alone.

## From `56a875a` — S7 as built

PIT:
**TRIED.** Read sign.mjs's own "NOT THIS NODE" note before writing anything —
it already named exactly what its in-memory stand-in was standing in for
and what shape server/chain.mjs needed to have (same OCF-1 formula, same
digest prefix) to be a real replacement rather than a second, parallel
implementation living beside it. Reused CHAIN_DIGEST_PREFIX/GENESIS_DIGEST
by moving their canonical definition into chain.mjs and re-exporting from
sign.mjs, rather than defining them twice.
**HAPPENED.** chain.test.mjs 3/3. Full suite 209/209. FREEZE.md unaffected —
no contract touched. Both sabotage directions verified and reverted:
always-true failed the 2 detection tests; always-false failed all 3,
including the positive-control test built for exactly that failure mode.
**CHANGED.** server/chain.mjs (new). server/sign.mjs: chainHead/chainSeq
locals removed, replaced by an injectable `chain` option and calls to
chain.append(); the artifact's chain_head now reads chainEntry.entry_digest
directly. server/index.mjs: one new route (GET /api/daybook), one line
exposing signGate.chain. tests/acceptance/chain.test.mjs (new).
**EARLIER.** Chose commit-TIME (chain.append()'s own now(), matching S8's
ledger's own `at` discipline) over the sign-time `rec.at` the old stand-in
used for the entry's timestamp — a deliberate, small behaviour change from
the stand-in's own choice, since the stand-in was explicitly documented as
NOT the real thing and its exact field choices weren't binding; stating
this rather than letting the substitution look untouched.
**GRADE.** Green, on estimate (1.5h, cut 3 honored).

## From `131ccec` — the merge with D-118

PIT:
**TRIED.** Did the merge in a worktree with both branches as local refs
(`git merge seat/I3-D118 --no-commit --no-ff` on top of seat/I3-S7),
resolved the one real conflict (both branches' doc-comment additions to
createSignGate's option list — purely additive, kept both), then read the
auto-merged commit() body in full rather than assuming a clean merge
meant a correct one. Wrote the two "no phantom chain entry on refusal"
tests BEFORE re-running any sabotage, since that property is exactly what
L1 named as the thing no existing test could see.
**HAPPENED.** Full suite 217/217. All four sabotages (2 from S7, 2 from
D-118) re-applied individually against the combined code and reverted;
each broke exactly the test(s) built for it, and sabotage 3 additionally
broke the new cross-cutting test, confirming the merge is load-bearing,
not cosmetic. FREEZE.md unaffected.
**CHANGED.** server/sign.mjs: merged createSignGate's option docs (additive)
and confirmed the auto-merged commit() body's check order (policy ->
recon -> chain.append) explicitly in a new comment block, rather than
leaving it implicit. tests/acceptance/chain.test.mjs: two new tests
proving no phantom entry on either refusal path. server/index.mjs,
server/routes/policy.mjs, tests/acceptance/sign-state.test.mjs: took
D-118's versions as-is (no conflicts there).
**EARLIER.** L1 was right that a clean git merge is not evidence of a correct
one — the auto-merge got the ordering right here, but I would not have
known that without reading the merged commit() body line by line and then
building a test that could only fail if it were wrong. The instinct worth
keeping: after any merge that touches control flow two independently-
written suites both exercise, the question isn't "does it compile" or
even "do both suites pass" — it's "is there a property true of the
combination that is false of either half alone," and if so, write that
test before declaring the merge done.
**GRADE.** Green. One branch, both features, the interaction proven rather
than assumed.
