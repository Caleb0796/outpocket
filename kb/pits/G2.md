# Pit — G2

Transcribed VERBATIM from the clause-6e `PIT:` block on `seat/I4-G2` (6cced04).
I4s own words, reformatted in no way: an earlier transcription of mine reflowed
the field headers and corrupted a sentence, which is not a risk worth taking with
someone elses report.

```
PIT:
**TRIED.** Unknown to this session for the ORIGINAL G2 work — the merge commit
(ab4831b) carries a different Claude-Session id than this one, and I am the
I4 seat, not that session; there is no transcript of mine to draw TRIED or
HAPPENED from, and reconstructing one from the diff is exactly the artifact
D-31 exists to prevent (kb/pits/H1.md's precedent, and my own repeated
reports saying the same thing).

**HAPPENED.** Same reasoning as TRIED — only the merge commit's gate summary
survives ("Gate 1 accept PASS... Gate 3 ownership PASS 2/2. Suite green."),
which is a gate result, not a report.

**CHANGED.** From the diff alone, the only source that doesn't require memory:
README.md gained the two demo credentials and both open-app paths. Later
and separately, in this session, I changed the three lines above that
section that the original G2 work never touched and its test never checked
(README's stale "Status: planning" line and the two "(not yet created)"
tags) — that fix and its own reasoning live in commit 1742a11 on this
branch, not restated here.

**EARLIER.** The field with something real to say, because the later find sits
in G2's own territory: readme-credentials.test.mjs asserts exactly the two
things it was written for — the login lines and the open-app paths — and
nothing else in the file. The status line and the repo-layout table sat one
screen above the very text the test does check, false for two days across
dozens of merged nodes, and nothing failed, because nothing was ever asked
to look. Every other defect found this sprint was a document claiming MORE
than was measured; this one claimed dramatically LESS, and that inversion is
exactly why no instrument built to catch over-claiming ever saw it. The
lesson for myself an hour before I'd have needed it: a merged-and-green
document can still be stale precisely where nobody wrote an assertion, and
the gate's silence there means "not checked," never "true" — and a report's
own formatting can carry the same failure mode as the artifact it describes,
which is the second half of this lesson, learned writing this very commit.

**GRADE.** TRIED/HAPPENED: EVIDENCED-UNRECOVERABLE, same class as H5 — not a
fresh finding. EARLIER: MEASURED, against the current tree and the current
test file, by me, in commit 1742a11, restated here under the header format
that actually gets read.
```
