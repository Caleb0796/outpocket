# Pit — V1

Human-gated node. Mechanical gate run by L1; **human second gate performed by QA under D-46**,
reported field by field, transcribed here by L1 under D-31.

**TRIED.** *(QA)* Read `evidence/V1.png`'s "WHAT THIS PAGE FOUND ABOUT ITSELF" panel field by
field against `evidence/V1.json` — **the panel, not the "COPY THIS INTO VERDICT.JSON" block**,
because checking the file against the block the page rendered from the same value is checking
the file against itself.

**HAPPENED.** *(QA)* **PASS.** All six panel-checkable fields agree exactly:

| field | panel | json | |
|---|---|---|---|
| `origin` | webmcp-probe.onrender.com | same | match |
| `modelContextPresent` | document.modelContext → PRESENT | `true` | match |
| `navigatorAliasPresent` | navigator.modelContext → undefined | `false` | match (transcribed) |
| `chromiumMajor` | 151 | `151` | match (transcribed) |
| `isSecureContext` | true | `true` | match (transcribed) |
| `toolCount` | tools registered → 5 | `5` | match |
| `chatgptModel` | *no panel row exists for it* | `"Sol"` | **cannot corroborate** |

**`chatgptModel` is not panel-checkable and QA did not claim to have verified it.** The page
cannot detect its driving model. `observedAt` likewise has no panel row. So the human gate
verifies **six of the eight recorded fields by construction**, and the model identity is
**attested by the operator, not by the page** — recorded in `V1.notes` per D-48 before the gate
ran, so the limit was known going in rather than discovered by it.

**CHANGED.** *(QA)* Nothing. This is review duty on I1's node, not a node QA owns — no files
written, no commits made. **D-46: "QA" in an accept is a REVIEW DUTY, not node ownership.**

**EARLIER.** *(QA)* N/A — first read of this artifact.

**GRADE.** *(QA)* MEASURED — ajv passes, both gates run.

---

**L1's record.** Mechanical gate re-run on main at merge time: ajv 2020-12 of `evidence/V1.json`
against `erp/contracts/probe-verdict.schema.json` exit 0; `test -s evidence/V1.png` exit 0. Both
gates required; the human read is the second gate, never the only one, and both are now on the
record.

**THE RESULT.** `document.modelContext` is **PRESENT** in the ChatGPT built-in browser on a plain
HTTPS origin **the account had never visited**. Contingency 0 did not fire and D2 stays cut rank
1 — a custom domain does not become mandatory. And the built-in browser is **Chromium 151** while
the Chrome V0 measured is **152**: two different engines, the `navigator` alias absent on both,
which corroborates HANDOVER §3 rule 1 on a second engine and is a stronger result than V0 alone.
V0's own notes said under R-8 that "151 is not present on this machine and the previous framing
asked about a version nobody can test." It is now measured.

**THIS NODE'S ROW WAS MISSING FROM THE RECORD FOR NINETY MINUTES AND ROUGHLY TEN MERGES**, and
the cause is a new one rather than clause 6d's. QA **did** report — its verdict is in its own
transcript, addressed to me, in the shape I asked for. **The message never arrived.** That is not
"a seat finished and went idle without reporting", which D-55 rules cannot be fixed by asking
seats to report harder; it is a report that was sent and lost in transit. The branch sweep does
not catch it either, because **V1 produced no branch** — its evidence landed inside I1's D-48
merge, so there was nothing for a branch sweep to find and nothing for the merge log to hang a
row on. Caught by the peer session `mcp-6d` comparing `merges.txt` against `graph.state.json.done`
and finding 21 rows that did not include V1.

The generalisable form, and it is a gap in my own loop: **clause 6d sweeps BRANCHES, and a node
whose artifacts arrive inside another node's merge has no branch.** Two registers that must agree
— the merge log and the state file — with the sweep watching only one of them.
