# Pit — F2

Verbatim from UX on `seat/UX-F2` (d47aa70), bolded at the field headers and
otherwise unedited.

PIT:
**TRIED.** Read S8's store header first, as the brief said, then probed the real
store to learn its shapes rather than assuming them — which is how I found the
mislabel guard by tripping it.
**HAPPENED.** 8/8 first run. The still frame reads at a glance. The one thing F2
cannot deliver is live data, because the store it renders is not wired to the
page.
**CHANGED.** src/page/ui/editor.js and tests/acceptance/editor.test.mjs (F2
outputs), plus src/page/index.html for the tag and the panel's baseline styling.
**EARLIER.** What I would tell myself an hour earlier: THE MISLABEL GUARD FOUND ME
BEFORE I FOUND IT. My first probe of S8's store threw E_PROVENANCE_MISLABEL
because I wrote {source:'agent', actor:'chen'} without a tool — I had reached
for the shape I expected rather than the shape the module documents. That error
is the exact thing F2 exists to make visible, committed by me, in the first
thirty seconds of using the module. The useful form: WHEN A MODULE REFUSES YOUR
FIRST CALL, THE REFUSAL IS USUALLY THE MODULE'S THESIS. I could have read the
guard as friction and worked around it by relabelling my write; instead it
taught me that agent identity is not a thing this system records (R-21), which
is a fact I then needed for the renderer.
Second: I nearly rendered `unset` fields as cells with data-source="unset",
which would have broken my own clause 1 while looking more complete. More
complete and wrong is the shape of most of what I have caught this week.
**GRADE.** GREEN.
