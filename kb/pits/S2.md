# Pit — S2

*Reported by I3 in its own words at the merge gate (D-74/D-31); L1 transcribed, did not compose.*

**TRIED.** Built against all four of the remedies in the brief, not just the accept text. **No
HTTP write routes existed yet** (S4's own precedent), so I built the minimal real routes needed
to make the check non-vacuous: six report-content routes
(create/open/add-line/update-line/remove-line/link-receipt — deliberately shallow, no
FX/itemization/policy-engine wiring, that is a separate node's depth) plus gating on
`POST /api/sign`. `authorizeWrite(session)` throws 403 `E_ROLE_FORBIDDEN` for anything but
employee, **checked before body parsing** on every route. `curl-403.sh` reads `WRITE_ROUTES` via
a live `node --input-type=module` import (not retyped), **asserts the table's length before
iterating**, and for every entry runs the auditor case (exactly 403, exact code) **beside the
employee positive control** (asserted not-403 AND pinned to the real expected success status) —
then **I sabotaged `authz.mjs` twice (always-throw, empty table) to watch the script fail
correctly before trusting it green.** Also wired each content route to S12's
`signGate.locks.assertUnlocked()`, so `erp/CONTRACTS.md` check 9 is now true against a real
endpoint rather than a stand-in.

**HAPPENED.** `curl-403.sh` exit 0, 3 clean reruns, **both sabotage negative-controls failed as
designed** (14/14 failures on always-403; empty-table guard tripped). Full suite 161/161 —
including one break I caused myself: gating `/api/sign` to employee-only broke my own
already-merged S12 concurrency test (`sign-lock.test.mjs`), which raced chen vs. ruiz. **Fixed
by racing two independent chen sessions instead — same invariants, different fixture — rather
than leaving the gate weaker to avoid touching prior work.**

**CHANGED.** New: `server/authz.mjs`, `tests/acceptance/curl-403.sh`. Modified:
`server/index.mjs` (six routes + two authz gate sites + a `blockedByLock` helper),
`tests/acceptance/sign-lock.test.mjs` (one fixture swap, documented inline). First commit
attempt tripped the ownership hook's banned-wording lint — I had quoted two retracted phrases
verbatim while explaining why they are retracted, instead of paraphrasing; reworded both
comments, reverified clean, recommitted.

**EARLIER.** Read `erp/CONTRACTS.md` §7.4/§11 check 8, `kb/pits/S4.md`'s "no HTTP write routes
yet" precedent, and `src/page/tools/compile.js`'s `writeTools()` (seven tools,
`readOnlyHint !== true`, R-20) before deciding what the table should contain — **that is where
the 7-route count and the create→open→add→update→link→remove→submit ordering came from, not
guesswork.** Deliberately excluded `/respond` and `/commit` from the table **with a written
reason** (untestable honest positive control, since `confirm_token` never appears in a JSON
response) rather than including them and faking the positive control.

**GRADE.** Green, on estimate (2.0 h as scoped, plus the S12 fixture fix it forced).
