# Pit — D3

Transcribed VERBATIM from the clause-6e `PIT:` block on `seat/I4-D3` (580cf2f).
I4s own words, reformatted in no way: an earlier transcription of mine reflowed
the field headers and corrupted a sentence, which is not a risk worth taking with
someone elses report.

```
PIT:
**TRIED.** Read the accept literally before writing anything: it names no push,
no deploy, no channel to L1 — just an idle duration, a status code and a
bound, and a digest equality. Kept the pre-check (confirming the S9 baseline
matched before starting the clock) as a SEPARATE step from the measurement
itself, on L1's explicit instruction that the two must not overlap — the
pre-check is one request, made once, before survive.mjs is ever invoked; the
tool's own two requests are the only ones that happen inside the window.

**HAPPENED.** Idle window 05:09:45 -> 05:39:45 UTC, live host, zero requests to
it from this session in between (confirmed by the empty gap in my own
command log). Cold request: 200 in 280.5ms. State-digest: exact match
against the pre-check's confirmed-clean baseline. Both PASS. origin/main did
not move during the window. A second attempt, launched after a later message
implied the baseline might be stale, was stopped before it made any request
to the server — the correction arrived showing the original baseline had
already been confirmed clean, so nothing about that stop cost the
measurement anything.

**CHANGED.** tools/survive.mjs (D3's declared output, written from nothing —
it did not exist before this node) and evidence/survive.json. The load-
bearing design choice, which belongs here rather than in anyone else's merge
message: survive.mjs takes --expect-digest as an explicit argument rather
than fetching S9's baseline itself. A tool that fetched its own comparison
value would need to touch the server at least once more than strictly
necessary, and on a node whose entire subject is what happens when nothing
touches the server, that extra touch is not neutral — it is the fabricated-
input trap, applied to the instrument doing the measuring rather than to the
system under test. Passing the digest in means the only two requests
survive.mjs ever makes, across its whole run, are the two it reports on.

**EARLIER.** What I'd tell myself an hour before I needed it: an escalation's
supporting facts carry the same evidence burden as the escalation itself,
and the escalation's own urgency is what makes those facts feel too obvious
to check. I was told twice, with reasoning attached, that my baseline was
stale — and both times the actual answer was already sitting in my own
committed evidence file, unread by the person asserting otherwise. I didn't
argue past what I'd measured, but I also didn't think to point back at my
own prior commit as the fastest way to settle it; I re-derived the same
confirmation from scratch instead of citing where it already lived. A
measurement that's already on record is worth checking before building a
response to a claim about it.

**GRADE.** MEASURED, against the live host, 2026-08-30, one clean 30-minute
window with its build named. The design choice in CHANGED is a claim about
the tool's behavior, not a separate measurement — verified by reading the
source, not run-tested as a negative control.
```
