# Pit — H2

Reported by **I1** under D-31, in the same message as the green accept, and transcribed by L1.

**TRIED.** Read the merged `evidence/V0.json`, `harness/probe-v0.mjs` and `tools/chrome.mjs`
before writing a line, then wrote a THROWAWAY SCOUT in the job tmp dir to measure the WebMCP CDP
surface on a one-tool page **before designing around it**. Then built `harness/drive.mjs` with
three modes (gate, `--list`, `--exec`), launching through `tools/chrome.mjs` rather than
re-deriving flags, so scenario `cdp` is the graded arm and scenario `none` is the negative
control. The gate serves its own one-tool fixture from 127.0.0.1 as a string constant inside
`drive.mjs`, because clauses (iii) and (v) are stated over a page registering exactly one tool,
V5's origin registers five, and `harness/drive.mjs` plus `evidence/H2-reachability.json` are the
only declared outputs — a fixture file would have been a fourth.

**HAPPENED.** All six clauses green on the first run and again on an independent second run; no
red at any point. **The scout paid for itself three times**, and each one would otherwise have
shipped as a wrong assertion:

- `WebMCP.toolsAdded` fires **ONCE**, carrying a `tools` **ARRAY**, with `frameId` on each tool
  object rather than on the envelope. A driver expecting one event per tool mis-asserts clause (v).
- That channel spells the annotations `readOnly` / `untrustedContent` where the page JS wrote
  `readOnlyHint`.
- An unknown tool name is a **CDP-level REJECTION at send time** (`-32602 "Tool not found"`),
  never a `toolResponded` with status Error — so `--exec` converts a rejected send, not a failed
  response. Unresolved names are sent to the browser unchanged so the `-32602` is the browser's
  answer and not a name check of ours.

The artifact is self-proving on clause (iv): `cdpDomainEnabled` reads true on BOTH arms — flagged,
and flagless with zero tools and no page API.

**RECORDED BUT NOT CLAIMED**, and the restraint is the point: the remote `--exec whoami` returned
`cookiePresent`/`cookieMatches` true **with no consent prompt** under plain Chrome + CDP. That is
consistent with the consent gate being a ChatGPT *client policy* rather than a Chrome behaviour —
but it is **NOT V3's owed re-confirmation**, which binds to the real server (S1). V3's open item
stays open.

**CHANGED.** Three files, all mine: `harness/drive.mjs` (new), `evidence/H2-reachability.json`
(new), `evidence/V2.json` (one authored line, the D-40 reword). Nothing in `erp/graph.json`, in
either direction. No pit file, per D-31.

**EARLIER.** **L1's base fix works and still has a race.** The dispatch named `c9fc6ef`; by the
time I read it main was at `62ae6f2` and by the time my worktree cut it was `ed53532`. `c9fc6ef`
is an ancestor of both and the intervening diff touched only `server/`, `tests/acceptance/`,
`erp/contracts/` and `kb/pits/S1.md` — nothing I own — so I branched from current main and said
so, rather than reconstructing a tree that had already moved twice, **which is the H1 defect
wearing the opposite face.** Naming a base fixes stale-by-a-lot; it cannot fix stale-by-an-hour
on a day where a merge lands every hour. Better form: *"current origin/main, which must contain
`<sha>`."*

Second, cheaper than it sounds: **measuring the event shape with a 100-line throwaway before
writing the real file cost about ten minutes and removed every guess from `drive.mjs`.** Every
previous revision of this node's brief had the calling convention wrong; the brief that arrived
had it right, and the scout **confirmed** all of it rather than discovering any of it.
**Information first is not only for the V lane.**

**GRADE.** MEASURED. Chrome 152.0.7977.64, node v22.23.1, `--headless=new`, a clean dedicated
`--user-data-dir` per launch: two full gate runs, three CLI spans, the 58-test suite, the lint,
the check-ownership run and the scout. No agent connected in any run — renderer-side readings
only. The one graded **UNVERIFIED**: the `remoteOrigin` arm is RECORDED, NOT GATED, on purpose,
because it registers five tools and cannot satisfy clauses (iii) and (v), and **a sleeping
free-tier instance must not be able to fail a regression gate about Chrome.**

---

**L1's note.** I1 flagged that my dispatch contradicted itself — the header said "do not commit a
pit" (D-31) while my standing brief block still read "write it on your branch, no pit file no
merge". I1 followed the header and cited `check-ownership` showing `kb/pits/**` DENY for its own
seat. The brief block had been stale since D-31 and is corrected. That is the second time today a
seat was handed a contradiction I authored, and both times the seat resolved it correctly and
told me.
