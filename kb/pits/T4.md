# Pit — T4

*Reported by QA in its own words; L1 transcribed, did not compose. Late to the record for the
same reason as [[S5]] and [[S12]] — composed in conversation, never transmitted.*

**TRIED.** Merged `origin/main` (e1bf38b) into `seat/QA-T4` — clean, no conflicts. Ran
`node --test tests/acceptance/conformance.test.mjs` against the merged tree. It drives
`makeWorld()`/`buildCleanReport()` through all six canonical states S0–S5 and for each state's
`toolset.surface()` asserts: description ≤ `DESC_BUDGET`, annotation keys a subset of
`{readOnlyHint, untrustedContentHint}`, no banned output-schema key, and every tool the frozen
`erp/contracts/tool-surface.contract.md` §2 table marks read-only carries `readOnlyHint: true`
— **table copied verbatim into the test, not derived from runtime.**

**HAPPENED.** 1 pass, 0 fail. The test also asserts non-empty surfaces per state and that
`toolset.state()` actually reports the state id the harness meant to build, **so it is not a
vacuous pass on an empty or misbuilt surface.**

**CHANGED.** Nothing beyond re-running the existing test post-merge — no product code touched,
no test edits needed.

**EARLIER.** This node was rescheduled Day 1 → Day 3 (R-19) because **the six canonical states
did not exist until T2 drove the real registration lifecycle against F1's page**; on Day 1 only
T1's static list existed and four of six states were unreachable, so the predicate was
unrunnable then. `DESC_BUDGET` is imported as the literal 500 from
`src/page/tools/defs.js:26`, matching the accept text's own number — graded **PUBLISHED**
(official budget), not MEASURED, since I did not derive 500 from runtime behaviour.

**GRADE.** PASS.

---

**A live consequence QA raised itself, recorded here because it is the pit's most useful
output (L1's note).** QA asked — rather than assumed — whether the D-77 freeze bump had landed
after the tree it verified against, since that bump moves every per-state count. It had. And
the answer is that **the verbatim copy is now stale**: `conformance.test.mjs:23` reads
"Sixteen tools" and §2 is now seventeen. The test is green today *only because
`explain_missing_tool` does not exist yet*, and when T3 lands it will keep passing **while
silently checking one tool fewer than the contract names.** A hand-copied table that drifts is
exactly what I3 designed `curl-403.sh` to avoid by reading the route table out of the server's
own export — *"then the no-gaps clause is checking your copy against your copy."* The copy was
the right call when T4 ran (it pins what the contract SAID, independently of what the code
does, which is the point of a conformance test); the missing half is a check that the copy
still matches its source.
