# Pit — H1

**THIS IS NOT THE SEAT'S OWN REPORT, AND THAT IS THE FIRST THING TO KNOW ABOUT IT.** D-31 says
the seat reports five fields and L1 transcribes them, and says plainly that a pit L1 composed is
worse than no pit. I1 was stopped before it reported, so its five fields do not exist and cannot
be recovered — a stopped seat's context is gone. What follows is marked field by field for where
it came from. Nothing below is presented as I1's words.

**TRIED.** *(From the branch and the commit message, not from I1.)* Built `tools/chrome.mjs` with
one flag spelling per scenario and `tests/acceptance/launcher.test.mjs` to assert it, including
an explicit NEGATIVE test for the prefix trap — `--scenario cdp` must print
`--enable-features=WebMCP` and must NOT print the `WebMCPTesting` spelling, which a naive
substring assertion would pass on, since the first is a prefix of the second.

**HAPPENED.** *(Verified by L1 at the gate, independently of any report.)*
`node --test tests/acceptance/launcher.test.mjs` = **10 tests, 10 pass, 0 fail**. Run directly:
`--scenario cdp` emits `--enable-features=WebMCP`, `--scenario manual` emits
`--enable-features=WebMCPTesting`, and a grep for the Testing spelling in the cdp output returns
**0**. The prefix trap is genuinely covered.

**CHANGED.** *(From the diff.)* Two files, both declared H1 outputs, nothing else. Nothing in
`erp/graph.json`.

**EARLIER — AND THIS FIELD IS L1's, ABOUT L1, BECAUSE THE SEAT'S IS UNRECOVERABLE.** I1
deadlocked on an interactive menu it raised about the `.team/contracts/G4.txt` lint leak. The
failure was **real on its base and already dead on main**: its worktree sat 17 commits behind,
on a `SWEEP_EXCLUDE` that predated the G4 sweep fix, and its own proposed patch would have
regressed the class rule back to a literal list — the exact thing the principle was written to
prevent. **A seat working from a stale base can correctly diagnose a bug that no longer exists
and propose a fix that undoes the real one.** I dispatched H1 and never told I1 to merge main
first, and the sweep fix landed after it started. **A seat's base is part of its brief, and I do
not state it.** From here every dispatch says which main to start from.

The second half is the cost recorded above: **D-31 makes the pit depend on the seat surviving to
report it, and nothing in the protocol captures the five fields before a seat can be stopped.**
That is now the third time today a seat-lifecycle action cost something — `claude rm` on a busy
UX seat destroyed F0's draft, `core.hooksPath` blocked QA mid-commit, and here a stop cost a pit
outright. The fix is cheap and is not in place: **ask for the five fields as part of the merge
request, not after it.**

**GRADE.** **MEASURED for the accept and the launcher behaviour** — the 10/10 run, both
`--print-flags` outputs and the prefix-trap grep were executed by L1 on this machine, 2026-08-29,
in I1's own worktree at `b924b45`. **UNVERIFIED for anything about what I1 experienced or
intended**, because I1 did not report and this file must not invent it.

---

**A process note L1 is recording rather than adjudicating.** The H1 commit was made by the
monitoring peer session, not by I1, and its message says so: *"Committed by the monitoring
session under the user's full-authority grant, because I1 deadlocked."* The work is I1's staged
work, it is unmodified, the accept is green and I verified it myself before merging. It is
recorded here because `erp/charters/L1.md` names exactly one case where a party other than the
owning seat commits on a seat branch — C2's build-arm carve-out — and this is not that case. It
is not an accusation and nothing about the artifact is in doubt; it is a fact about how the
commit came to exist, and W files on evidence while PM prices it.
