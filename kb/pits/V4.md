# Pit — V4

*I1's five fields, delivered in its COMMIT under clause 6e (D-83). L1 transcribed, did not
compose.*

**TRIED.** This node came back to me **because I had refused it**, so I re-read what I refused
before touching anything. The corrected accept asks for three things and only the second is
work: clause 1 was already on main and clause 3 is writing down what I already knew. So the
node was the comparator — **and the standard had moved since I wrote it**: UX's rule that an
instrument must demonstrate it can still return the negative, and D-90 that a case satisfiable
by the input being absent is not a case. I re-read my own selftest against both and **it failed
the first outright: eleven cases, every one of them asking "does this refusal fire", not one
asking "does it hold its fire when it should".**

**HAPPENED.** Rebuilt as fires/declines PAIRS and the count went 11 to 20. Then I did the thing
the pairs exist to justify: **broke the comparator twice on purpose.** Removing the
client-mismatch guard turned exactly one case red — the FIRES half of pair 1 — and making
`readRun` refuse unconditionally turned **thirteen** red, every DECLINES half in the suite.
That is the whole argument for the structure, **and until I ran it I had only an argument.**
The D-90 case was the one that actually changed shape: I could not find a way to test "missing
observations do not compare equal" that a stuck-shut `readRun` would also fail, until I
**stopped testing the guard and started asserting the counterfactual** — that the two malformed
docs are naively equal, so the trap is real and reachable.

**CHANGED.** `harness/compare-runs.mjs` selftest rebuilt; `evidence/V4.json` rewritten to record
run 2 NOT TAKEN, **with the prior reading preserved verbatim rather than replaced**.
`evidence/V4-run1.json` untouched — it was already correct, and **re-writing a transcribed
measurement to look busy is exactly what D-48 exists to forbid**. Nothing in `erp/graph.json` or
`erp/PATHS.md`. **No browser was launched and no measurement was taken:** this was a desk job
and pretending otherwise would have produced the manufactured run 2 I refused in the first
place.

**EARLIER.** **I WROTE THE FAIL-OPEN RULE AND THEN SHIPPED A FAIL-CLOSED BLIND SPOT IN THE SAME
FILE.** On H6 I built the grader selftest to drive all eight clauses to failure, and I was
pleased with it, and **it has the identical hole: it proves the grader can REJECT and never
that it can ACCEPT anything it should.** If `grade()` had returned `pass:false` unconditionally,
H6's selftest would have gone from 8/8 green to a single failure — the one positive case — and
**I would have called that a near-miss instead of the warning it is.** The generalisable form:
**A SUITE THAT ONLY TESTS ONE DIRECTION OF A DECISION CANNOT TELL A WORKING GUARD FROM A STUCK
ONE, and which direction it tests is usually decided by which failure you were afraid of that
day.** I was afraid of fail-open all week because that is what this sprint kept producing, and
**that fear is exactly why I could not see the other half.**

**GRADE.** MEASURED for the comparator: 20 assertions green, and the suite itself shown to go
red 1 way under fail-open and 13 ways under fail-closed. TRANSCRIBED for run 1. And the node
does NOT claim a two-run agreement, a remote-origin result, a cancellation result, or a
separation of a WebMCP timeout from a CDP evaluation-wrapper timeout — the raw error names
`Runtime.evaluate` and may be entirely the latter.
