# Pit — S6

*Reported by I3 in its own words at the merge gate (D-74/D-31); L1 transcribed, did not compose.*

**TRIED.** `recanon.mjs`'s `reconcile(signedSnapshot, liveReport)` rebuilds the signed snapshot
with `report` swapped for CURRENT live state and recomputes its OCF-1 digest through
`src/canonical.js` — **same canonicaliser, never a second one.** Wired into `sign.mjs`'s
`commit()`: fetch live state via `opts.getLiveReport`, **synchronous with no `await` before the
comparison, the same "true by construction" argument as S12's lock.** Mismatch throws
`E_SNAPSHOT_MISMATCH` (409) carrying both `signed_digest` and `recomputed_digest`. No
live-state entry for a `report_id` (every existing sign-state/sign-lock test's synthetic ids) is
**SKIPPED, not a false mismatch** — which is why all 175 prior tests kept passing unmodified.

**THE REAL FINDING WHILE BUILDING IT.** The literal accept scenario — "mutate one line through a
second request, then submit" — **cannot be driven through S2's real content routes, because
S12's lock already refuses that exact mutation with 423 before S6 would ever run.** I proved
that first, against the real unmodified server, inside the same script (PART 2a). So this
node's actual value is the case the lock is not there to catch, and PART 2b exercises it
honestly: a small test-only bootstrap wires the real `sign.mjs`/`recanon.mjs` to a report store
deliberately NOT behind S12's lock, **documented plainly as such rather than silently routing
around the real lock to fake a pass.**

**HAPPENED.** `toctou.sh` exit 0, 3 clean reruns. PART 1 (`reconcile()` unit-level) proves the
five canonicalisation attacks — key order, whitespace, unicode NFC/NFD, numeric formatting,
duplicate keys — are all **false-positive-safe** (an honestly-reformatted report is never
rejected) alongside two genuine content-change cases that ARE caught. **Sabotaged `reconcile()`
(forced always-ok) and correctly failed 4/4 of the assertions that matter before trusting it
green.** Full suite 175/175, FREEZE.md unchanged.

**CHANGED.** New: `server/recanon.mjs`, `tests/acceptance/toctou.sh`. Modified:
`server/sign.mjs` (`getLiveReport` option, `reconcile()` call in `commit()`, `SignError` gained
an optional `detail` field, `recomputed_digest` added to the chain-entry stand-in),
`server/index.mjs` (default `signGate` now wires `getLiveReport` to its own `findReport`; both
sign-route and commit-route error responses now spread `err.detail`). Removed a duplicate
`findReport` definition S2 had left further down the file once I needed it earlier in scope.

**EARLIER.** **Reread S5's own header before touching anything** — it named `commit()`'s missing
re-canonicalisation as explicitly open and named this node as its home, so I knew exactly what
NOT to relitigate (verdict recomputation stays out of scope; only `report` is checked, since
that is the one field a write route can mutate). Read `erp/CONTRACTS.md`'s TOCTOU passage and
**confirmed the "single server instance" precondition before writing the true-by-construction
claim, rather than asserting it and hoping.**

**GRADE.** Green, on estimate (2.0h).
