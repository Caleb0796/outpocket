# Pit — S3

**TRIED.** Ported `countinghouse/src/policy.js` into `src/policy.js` with the two required
changes: `FX` moved from float USD-per-unit rates to integer micro-USD (`EUR: 1_090_000`, etc.),
`toUsdCents` recomputed as `round(cents * micros / 1_000_000)`, and the one live fix-lint
violation (`CAP_TRANSPORT`'s "Split legitimate multi-trip charges into one line per trip.") was
rewritten to the schema's own example fix text. Built `POLICY_DOCUMENT` as a literal object
matching `erp/contracts/policy.schema.json examples[0]` field-for-field (19 rules `R01`-`R19`,
`limits_cents` in snake_case, sorted `categories`, the frozen `non_reimbursable_labels` word
list) and derived `POLICY_DIGEST`/`POLICY_CANONICAL_BYTES` from it via `src/canonical.js`,
rather than hand-computing and hardcoding the digest. Rebuilt `policyForAgent()` to the
schema's `x-agentView` projection shape (no decimals, `policy_digest_prefix`, no `rules`).
Wrote `server/routes/policy.mjs` with a pure `verifyPolicyDocument(document, lock)` and a
`createPolicyHandler(document, lock)` factory so the exact same verification code that runs at
server policy load (module import, `export const policyHandler = createPolicyHandler()`) can
also be exercised in a test against a deliberately doctored document, and wired one line into
`server/index.mjs` before the 404 fallback. Wrote `tests/policy-lock.test.mjs` against that
interface. Verified the digest by hand first (`node -e` importing `src/policy.js` before writing
any tests) rather than trusting the literal object was correct by inspection.

**HAPPENED.** `node --test tests/policy.test.mjs` = 15/15. `node --test tests/canonical.test.mjs`
= 16/16, unaffected by the migration (it does not import `src/policy.js`). `node --test
tests/policy-lock.test.mjs` = 4/4, including the doctored-document refusal running through the
real `createPolicyHandler` production code path. `npm test` (whole suite, no `--` filter per
R-23) = 62/62, up from the 58/58 recorded on origin/main before this branch — the 4 new
policy-lock tests, nothing else moved. The curl acceptance predicate, run verbatim against a
live `node server/index.mjs` on port 8971, printed `ALL CHECKS PASS` and exited 0: version
`2026-08.1`, 19 rules `R01..R19` in order, every `code` matching the named-string pattern, no
decimal number anywhere after `version`/`effective_from` were deleted from the scan. Recomputed
digest: `sha256:b7ccc1ff9fdadb66399f48b26617a53572dd793ac7c57af55d72929561965b38` over 2458
canonical bytes — byte-exact match to the frozen `policy-versions.json` entry on the first
attempt (the literal document was built directly from the schema's `examples[0]`, not from the
old spike's field names, which is what made this land exact rather than needing iteration).
`sha256sum -c erp/contracts/FREEZE.md` still exits 0 — no frozen file touched.

**CHANGED.** `src/policy.js` (FX to micros, `toUsdCents`, one fix string, `policyForAgent()`
rebuilt, `POLICY_DOCUMENT`/`POLICY_DIGEST`/`POLICY_CANONICAL_BYTES` added), `server/index.mjs`
(three lines: import + one dispatch line for `/api/policy`, before the existing 404 fallback —
`/api/login` and `/api/me` untouched). New files: `server/routes/policy.mjs`,
`tests/policy-lock.test.mjs`. Did not touch `erp/contracts/**`, `src/erp.js`, `src/tools.js`,
`src/page/**`, or any file outside this node's declared outputs.

**EARLIER.** Read `erp/CONTRACTS.md` §8 and the R-33 `x-versionDiscipline` block in
`policy.schema.json` before writing the loader, specifically to get the "declines to serve any
policy at all, not the doctored one" behavior right rather than inferring it from the ACCEPT
predicate's prose alone. Read `src/page/tools/defs.js` before touching `FX`'s shape, since it's
outside my ownership and imports `FX`/`policyForAgent` from this file — confirmed it only calls
`Object.keys(FX)` (key names unchanged, only values became integers) and
`JSON.stringify(policyForAgent())`, so no coordination was needed with whoever owns that file.
One thing I noted but did not act on, flagged rather than silently resolved: the schema's
`non_reimbursable_labels` field description reads as if it were the actual alcohol-detection
list, but the frozen 20-word example doesn't include "chianti", "merlot", "cabernet",
"margarita", "beers", "cocktails", or "whisky" — all of which existing ported tests require the
detector to catch. Kept the runtime `ALCOHOL_RE` broader than the frozen document field and
said so in a code comment; did not narrow the regex to match the frozen list (that would fail
existing tests) and did not widen the frozen list (that would break the pinned digest — it's
frozen, not mine to edit).

**GRADE.** Green. All four ACCEPT clauses pass as specified, verbatim, with no predicate
relaxed or reinterpreted.
