# Pit — S8

*I3's five fields, delivered in its COMMIT under clause 6e. L1 transcribed, did not compose.*

**TRIED.** **Read `erp/contracts/provenance.schema.json`'s full `x-derivation` and `x-fieldSets`
before writing anything** — **the dispatch's own `{value, source, ts, actor}` shorthand
undersells it.** The frozen ledger record is
`{schema, seq, id, at, entity, entity_id, field, source, actor, tool, value_digest, supersedes,
revision?}` — **`value_digest`, not `value`, precisely so the ledger never stores a value
twice.** Built the mislabel refusal around the source↔tool↔actor consistency triangle **the
schema's own field descriptions already state** (agent implies a tool name; human/seed/unset
forbid one; agent's actor is always literally `'agent'`) **rather than inventing a separate
rule.**

**HAPPENED.** 6/6. Full suite 206/206. **Every real ledger record the store produces validates
against the frozen schema via a real ajv-2020 compile (17/17), not just my own field-presence
assertions.** FREEZE unchanged — implemented against the schema, did not touch it. Two
sabotages verified and reverted: "flip everything to human" failed 5/6 as expected; removing
the mislabel check failed **exactly the 1 test built for it.**

**CHANGED.** `server/provenance.mjs`, `server/store.mjs`, `tests/acceptance/provenance.test.mjs`
— all new. **Did not touch `server/index.mjs` or S2's write routes**, and flagged that
integration gap explicitly rather than leaving it implicit: S2 does not list S8 as an input and
S8 does not list S2, and the accept predicate is the test file rather than an HTTP check.
Whoever wires it should read `store.mjs`'s header on the ts/digest warning first.

**EARLIER.** Took the `ts` warning seriously: it is wall-clock but **stored once at write time
and never recomputed on read**, so it does not reproduce H6's per-read-drift failure mode by
itself. Documented in the module header that **the frozen schema's signed-snapshot provenance
is field→source ONLY** — never `at`/`actor`/`value` — for whoever wires this into anything
digested next.

**GRADE.** 6/6, 206/206, 17/17 real records valid against the frozen schema by ajv.

---

**L1's independent check of the sabotage claim.** My first attempt was **too blunt to prove
anything**: a crude edit broke the module load, so the run reported 1 test / 1 failure — an
import error, not a discriminating assertion. Precisely disabling **only** the mislabel guard
(every `throw new ProvenanceError` neutralised, module still parsing) gives:

    # tests 6  # pass 5  # fail 1
    not ok 4 - D-100: the ledger refuses to record an agent write as human, or a human write as agent

**Exactly one test, and exactly the one built for it.** Restored: 6/6. I3's claim confirmed
rather than relayed — and my own first attempt is the second sabotage-that-proved-nothing I
have written today.
