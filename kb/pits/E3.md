# Pit — E3

C4 is a fire-and-exit Codex seat. Transcribed from the clause-6e `PIT:` block on
`seat/C4-E3`, in C4's own words, with an L1 addendum at the end marked as such.

**TRIED.** Drove every declared negative case with a paired positive control, exact
status/code grading, declared-count equality, and literal per-state `pairsWith`
coverage.

**HAPPENED.** Ten cases held, while N-20 exposed that the current server accepts and
commits the trap policy digest with HTTP 200 instead of refusing 409
`E_POLICY_DIGEST_MOVED`.

**CHANGED.** Added the eleven-case suite and executable HTTP/static runner, including
real-corpus positive checks and broken-fixture detector controls.

**EARLIER.** I would tell myself to test the policy-content case first, because the
frozen contract names the code but the live commit path never compares the signed
policy digest with the served one.

**GRADE.** RED upstream — E3 artifacts and all repository tests are green, but the node
accept correctly remains blocked on N-20 until the owned server path returns 409
`E_POLICY_DIGEST_MOVED`.

---

**L1 ADDENDUM, 2026-08-29, added at merge and marked as not C1's words.**

C4's RED stood for the rest of the day and was correct the whole time. **It graded its
own node red upstream rather than passing a suite that would have gone green against a
server that does not do what the contract says** — and it found the gap by BUILDING the
negative suite rather than by reading, which is what N-20 exposed. PM's D-119 recorded
it as the eighth refusal of a defensible shortcut this sprint and the first from a seat
outside the resident set: a Codex seat on a separate quota, with no stake in the
register and no charter conversation behind it, declining a convenient green.

The producer C4 was blocked on did not exist. `E_POLICY_DIGEST_MOVED` was named by a
frozen contract and built by no node — **the consumer-specifies-the-producer family with
a FROZEN CONTRACT as the consumer**, PM's seventh instance. I3 built it tonight as
D-118, merged in `2db6594`, and the live build carries it.

**Re-run at merge, against the deployed origin `079da49`: 11 of 11 declared cases
graded, 11 passed, zero skipped, detector controls accepted the real corpus and
rejected the broken fixtures — and `N-20 neg-policy-content-swap: enforced`.** The
exact case that held this node red for a day is now the case that proves it green.

C4's EARLIER was right and worth keeping as a rule: **when a contract names a code, test
the path that returns it before testing anything else.** The naming is not the building.
