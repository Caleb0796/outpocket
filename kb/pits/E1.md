# Pit — E1

Reported by **C4** under D-31 — the first Codex seat booted in this sprint — transcribed by L1.

**TRIED.** Ported S11's canonicaliser **source-identically** into `webmcp-eval-kit/src/canon.mjs`,
added exact-source and seven-vector verification, and built the deterministic CLI self-test
pipeline.

**HAPPENED.** `npm ci` and `npm test` exit 0: **12/12 tests pass, including all seven vectors.**
`--version` prints `0.1.0`; `--selftest` reports `tool_count=1 byte_identical=true`. The no-network
preload form also passes.

**CHANGED.** Five files, 320 lines: the eval-kit CLI, package and lockfile, the source-identical
OCF-1 port, and the no-net test/preload.

**EARLIER.** The first CLI entry-point check would not have handled an installed symlink reliably;
final review replaced it with real-path comparison. The initially absent eval-kit was confirmed to
be **E1's own output, not a missing H2 merge** — checked rather than assumed.

**GRADE.** VERIFIED. Exact source comparison returns 0, all seven canonical strings and digests
match byte-for-byte, the non-empty two-run pipeline is deterministic, and the ownership hook
reports 5 paths checked with 0 violations.

---

**L1's note.** R-11 says `src/canonical.js` is the ONLY canonicaliser and the eval-kit's copy is a
PORT of it. C4 satisfied that **by identity rather than by agreement**: measured at the gate,
`src/canonical.js` and `webmcp-eval-kit/src/canon.mjs` are **5087 bytes each and byte-identical.**
A port that merely reproduces the seven vectors could drift the first time either side is edited;
one that is the same bytes cannot drift silently, and the kit ships an exact-source check that
fails if it ever does. That is the strongest available reading of the ruling.

Accept spans 0–2 exit 0. Span 3 is the bare fragment `--selftest`, not a command; the runner
flags it and a non-zero there is the shell rejecting a fragment, not the node failing.

**First Codex dispatch of the sprint and it needed no correction.** Booted in a dedicated
worktree at `../wt-C4` per the charter's C2/C3/C4 rule, with the evaluator profile and network
flag verified present before launch rather than assumed. It reported five fields unprompted and
committed no pit file, which is D-31 followed by a seat that had never been briefed on it before
tonight.
