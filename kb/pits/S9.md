# Pit — S9

Reported by **I3** under D-31, transcribed by L1.

**TRIED.** Added a static handler and a deterministic reseed path in `server/index.mjs` — same
single file, no new module — plus `server/seed.mjs` and `GET /api/state-digest`, so the digest is
a property of the seeded state rather than of one process's memory.

**HAPPENED.** Byte-identical digest **across a genuine process restart**:
```
boot 1  -> {"digest":"sha256:525b30a0…c51870"}   curl exit 0
restart -> {"digest":"sha256:525b30a0…c51870"}   curl exit 0
diff /tmp/d1 /tmp/d2                              exit 0
```
That is the whole claim — the seed is deterministic, not merely stable within one process.
Existing session tests unaffected; full suite green.

**CHANGED.** `server/index.mjs`, `server/seed.mjs`, `server/routes/state-digest.mjs`. Nothing
outside S9's declared outputs.

**EARLIER.** Read the node's own accept before writing, so the restart was part of the design
rather than something bolted on to satisfy a gate afterwards.

**GRADE.** MEASURED, on this machine, 2026-08-29.

---

**L1's note, and it is about my own gating rather than the node.** The three accept spans use
`$URL` and assume a booted server. Run bare they exit `3`, `3`, `0` — **which reads at a glance
like two failures and a pass, and is actually a complete non-test**: `$URL` was unbound, `curl`
never made a request, and `diff` compared two empty files and agreed. I nearly graded the node
off those bare exit codes. The run above is the first restart-to-restart comparison that means
anything, and it only happened because the pattern of vacuous passes had already been priced four
separate times today.

Found by the clause-6d branch sweep, not by a report — the fifth unreported finish of the day.
