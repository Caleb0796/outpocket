# Pit — D-118

Not a graph node: a PM ruling (D-122 unparked it) implemented as an S5 follow-up.
Verbatim from I3 on `78aea96`, bolded at the field headers, otherwise unedited.

PIT:
**TRIED.** Read x-policyBinding.theAttack/theFix/negativeControl in full
before writing anything — the dispatch's own summary is accurate but the
frozen prose has the reasoning for WHY version and digest are separate
codes (one has an innocent explanation, one doesn't), which shaped the
check order. Wrote the null-claim test before the implementation, per the
false-positive warning already being the headline risk, rather than
writing the happy path first and the edge case as an afterthought.
**HAPPENED.** sign-state.test.mjs 23/23 (17 prior + 6 new). Full suite
212/212. FREEZE.md unaffected — no contract touched, only implemented
against one. Two reverted sabotages: disabling the whole check broke
exactly the 2 "moved" tests and nothing else; dropping only the null
guard broke exactly the 1 test built for that specific gap.
**CHANGED.** server/routes/policy.mjs (getServedPolicy/SERVED_POLICY, additive
— policyHandler's own behaviour and export are untouched), server/sign.mjs
(getServedPolicy option + the two new checks in commit(), placed before
S6's recon check), server/index.mjs (one line wiring the real server's
default gate to SERVED_POLICY), tests/acceptance/sign-state.test.mjs (6
new tests, already an S5 output — no ownership collision).
**EARLIER.** Checked graph.json before assuming D-118 needed its own
authorization: it doesn't — server/sign.mjs, server/index.mjs and
server/routes/policy.mjs are already S1/S3/S5 outputs I own, so this
landed as a normal edit to owned files rather than another deviation
ticket. The one genuine risk (the null-as-moved false positive) was
caught by test-first discipline, not by review after the fact — worth
remembering that D-106 (the crash) and this near-miss are the same shape:
an existing, correct `undefined`-vs-`null` distinction elsewhere in this
exact file, and a new check almost re-broke it independently.
**GRADE.** Green. Both directions of D-100 exercised, D-108's binding half
verified before I trusted the refusal half, QA's decidability question
answered with a test rather than an assertion.
