# Pit — E5

*Reported by C4 in its own words in its run output; L1 transcribed, did not compose. C4's
contract predated clause 6e, so this arrived as report text rather than in the commit — the
last node to do so.*

**TRIED.** Ran the two declared accounting cases under the required preload, with deliberate
`dns.lookup` and socket-connect attempts inside the eval process, while reconciling all six
exported states and scanning the eval source.

**HAPPENED.** Both denial attempts threw the tagged error; 2/2 cases passed, covering six
states and 49 descriptions, and one real source file was scanned. **The first package test
exposed that standalone `inputSchema` canonicalization mishandled `$schema`;** I corrected it
through OCF-1's `inputSchema` carve-out.

**CHANGED.** The preload now identifies the exact in-process prototype-patch mechanism and
throws a tagged error. The runner now **proves denial fired, rejects replaced hooks**,
validates T5 accounting/totals and forbidden keys, binds results to the declared case IDs, and
emits explicit verdicts and scan counts.

**EARLIER.** I would tell myself that **OCF-1 permits dollar-prefixed schema keys only after
entering a property literally named `inputSchema`; `canon(tool.inputSchema)` is not
equivalent.** I would also **test the replaced-hook world before trusting any function-name
marker** — a marker that identifies a hook by its name is satisfied by anything wearing that
name.

**GRADE.** Case count 2 declared / 2 emitted / 2 passed: **MEASURED**. Both denial controls
firing and byte identity: **MEASURED**. OCF-1 byte counts: **MEASURED**. `tokensApprox`:
**OUR-ESTIMATE**. The 500-character description limit remains **PUBLISHED guidance**, not
browser enforcement.

---

**Verified independently by L1 rather than taken (note, not C4's).** The predicate cannot
check the thing that matters, so I ran it myself: **`node webmcp-eval-kit/bin/eval.mjs --suite
accounting` WITHOUT the preload exits 1 with `E_NETWORK_DISABLED`** — the denial genuinely
fires, and a run where it never armed FAILS LOUDLY rather than passing quietly. Byte-identity
holds over **2 declared / 2 emitted cases carrying verdicts**, not over an empty file. The
header names the mechanism — *"prototype patch on net.Socket.prototype.connect and dns.lookup,
in-process, via --import"* — rather than a boolean `network: false`, so a later reader can
check it. And `webmcp-eval-kit/src/` exists and is non-empty, so the negative grep is a search
that actually ran.
