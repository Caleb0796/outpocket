# Pit — V6

Reported by **PM** under D-31, in its own words, transcribed by L1.

**TRIED.** Node V6 as written, after ruling on eight other seats' nodes first.

**HAPPENED.** V6 is my own cut-0 node and **it sat READY AND UNSTARTED for the whole session.**
Every other seat on the board had work; the only unstarted ready node was the graph owner's own.
L1 found it, not me.

**CHANGED.** Built it in one pass; the register was already conformant, so the whole node was the
checker plus its selftest.

**EARLIER.** `ready.mjs` prints cut and day but **does not RANK**, and it does not know which seat
is reading it. I ran it a dozen times today and **never once filtered it to `owner=PM`.** One line
would have shown me.

**GRADE.** MEASURED — the checker exits 0 on the register and 0 on 15 selftest cases.

---

**L1's gate notes.** Six rows, 6 MEASURED, 0 UNVERIFIED; 15 selftest cases, 0 unexpected; suite
152/152.

**R-43's trap is real and the build is honest about it.** `/^\|\s*\**V6\b/` matches BOTH
`V6-consent-gate` AND this node's own id, because `-` is a word boundary — so a checker using `\b`
there would call a register keyed on the wrong thing well-formed. The implementation uses an exact
`KEYS` array and an exact `id === 'V6'` rejection; the only `\b` in the file is the **comment
explaining the trap**. I grepped, found the hit, and read it in context before believing it — the
third time today that distinction mattered.

**Clause (5) ships exercised rather than merely written**, and that is the point worth keeping:
every row is MEASURED today, so the UNVERIFIED branch has **no live fixture**. Rather than let it
ship unexercised, it carries 15 selftest cases with four on that branch — **and two of the fifteen
must PASS.** A checker that rejects everything is not a checker. That is the vacuous-pass class
inverted, and it is the same bar I asked C4 to hold `--suite` to.

**Clause (4) checks the RAW line's final bytes before any splitting**, so `… MEASURED |` fails
where a cell-trimming check would wave it through.

`evidence/UNKNOWNS.md` needed no change — already well-formed under all six clauses, including the
deliberate missing final pipe.
