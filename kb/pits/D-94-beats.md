# Pit — D-94, the storyboard beats ruling (L2)

*L2's five fields, delivered in its COMMIT under clause 6e. L1 transcribed, did not compose.
Not a graph node — a ruling L2 was seated to make.*

**TRIED.** Read `erp/RUBRIC.md` end to end before ruling on anything, then **measured every
load-bearing fact instead of reading it**: ran `curl-403.sh` rather than trusting its header
comment, ran `check-ownership --who` and `--seat` before believing **its own charter**, ran
`accept-gate E4 --list` before believing the predicate, and **executed F6's parser regex
against both column placements rather than eyeballing it**. Deliberately ruled on §1.2 ALONE
and left §1.4 unwritten until the ruling was composed, **so the clause could not be reaching
back to win an argument it was present for.**

**HAPPENED.** All three beats rule ENOUGH IF, 1.0 h total, documents only. **The two findings
worth more than the rulings both came from running something:** the SURFACE column is
INVISIBLE to F6 when appended, and its own charter granted it an edit — `erp/RUBRIC.md` —
that `check-ownership` DENIES. It did not force the second. And **SB-04's verdict turns
ENTIRELY on a fact found in a header comment**: `compile.js` compiles the surface from
(role × report-state × verdict) and **POLICY IS NOT AN AXIS**, so beat 1's causal chain is
real but INDIRECT, and "the refusal names the policy version" is only true via a second agent
turn. UX needs that BEFORE the shoot, not after.

**CHANGED.** `erp/RUBRIC-ADDENDA.md` — new, this seat's file under D-103.

**EARLIER.** Ownership resolution and file existence are different questions, and the tool
answers the first: `check-ownership` resolves whether a seat MAY write a path, which is
well-defined for a path that does not exist yet. L1's verification was right and answered the
question it asked — **another instance of D-104's class.**

**GRADE.** All three beats ENOUGH IF, on §1.2 alone, before §1.4 existed. The two mechanism
findings are MEASURED — both regex placements executed, both ownership directions run.

---

**Why this pit matters more than its verdict (L1's note).** A cold seat, one hour old, **ran
the parser regex of a node it was not reviewing** and found that the ruling it had been asked
to make would have landed as decoration: appended as a 7th column, the six-group regex matches
the first six and **SURFACE IS SILENTLY IGNORED** — so D-94 lands as nothing, and worse, three
miscategorised shots could then go green. **A seat discovering that the ruling it was handed
would not have worked, before executing it.** And it found a FOURTH miscategorisation, SB-07,
which is green today and would have stayed green after D-94 landed, with the wrong surface
recorded.
