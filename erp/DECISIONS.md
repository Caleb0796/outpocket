# Decisions register — outpocket sprint A

Rulings of record. Created by node **L0** on Day 0 (2026-08-28); node **V6** appends its
unknowns rows to this same file on Day 2 and does not create it.

The D-17 row below is reproduced verbatim from `erp/RUNBOOK.md` §2, *The D-17 row, literally*.
`L0` accept gate (1) matches it with `^\|\s*D-17\s*\|[^\n]*human_hours_per_day\s*=\s*([0-9.]+)`,
requires the captured value to be `2.5` or `3.0`, and requires `erp/graph.json`
`capacity.human_hours_available` to equal that value x 5.5. If you change a digit here, change
`capacity.human_hours_available` in the same edit or the gate fails — which is what it is for.

D-17 was RULED by the user directly on 2026-08-28, before any seat was dispatched. L0 does not
decide it; L0 records it.

## Rulings

| ID | Date | Ruling | Consequence |
|---|---|---|---|
| D-17 | 2026-08-28 | human_hours_per_day = 3.0 | RULED by the user, directly, before any seat was dispatched. 3.0 x 5.5 days = 16.5 h available; 15.875 required; 0.625 h spare. NOTHING IS CUT — all 62 horizon-A nodes stay in scope. The 2.5 h/day branch and its 27-node amputation set survive in capacity.human_budget_sensitivity as a contingency, not as the plan of record. |
| D-31 | 2026-08-29 | The pit file `kb/pits/<node-id>.md` is **written and committed by L1**; the building seat **reports** its five fields with the merge request | Resolves a contradiction internal to the merge gate: clause 6 required a file that clause 3 forbade its author to write, so **every seat that satisfied gate 6 violated gate 3, mechanically, on every merge**. No node lists a pit in `outputs` (ownership clause (a) is empty *by design* — `PATHS.md §2.8` keeps the ritual unbudgeted), and the longest-matching glob `kb/pits/**` resolves to L1 (clause (b)). Of the three available resolutions this is the only one that invents no rule: adding the pit to 68 nodes' `outputs` contradicts §2.8's deliberate unbudgeting, and a `file_ownership` row resolving `<id>` to the owning node's seat would make one row of a static glob→seat table dynamic. **`conventions.ownership_rule` is unchanged, `file_ownership` is unchanged, no node's `outputs` are touched.** Cost: one commit per merge, on L1. L1 transcribes the seat's own words and names the seat; a pit L1 composed is worse than no pit. **L1's T6 merge is AFFIRMED.** L1 merged T6 with `DENY kb/pits/T6.md` recorded in the merge commit rather than waived quietly, and that was the right call: the alternative was blocking Day 1's leverage node on a bookkeeping contradiction that was not I2's fault, and recording the violation in the open is what made this ruling possible. The violation is retired by this row, not excused — under D-31 it could not have occurred. |
| D-32 | 2026-08-29 | **D-17's second half: the human budget is a TOTAL and the schedule does not spread it. D4 is NOT shortened. The lever is the user's attention, and Day 6 needs an 08:00 PT start.** | ARITHMETIC, regenerated against `capacity.schedule_A.days` after R-42/D-30: **Day 1 = 2.0 h** (`V1`), **Day 5 = 4.0 h** (`D4`), **Day 6 = 4.5 h** (`G1` 0.5 + `D5` 2.0 + `D6` 2.0); the other four days carry zero. Against the ruled 3.0 h/day that is **1.0 h over on Day 5, 1.5 h over on Day 6, 2.5 h over in total** against **0.625 h** of sprint-wide spare. **No rank of the ladder touches it** — `G1 V1 D4 D5 D6` are all cut 0, so firing `T3 T5 F3 F5 E1–E10` and every other rank frees exactly zero of these hours. Two levers exist and I rule on both. (1) **D4 is not shortened.** It is 4.0 h of the highest-leverage deliverable in the sprint — HANDOVER §5 concludes the video IS the deliverable, a missing video is a disqualification item, and 24 of 420 surveyed repos posted one. Trading that for 1.0 h, when the binding constraint is Day 6's wall clock and not Day 5's, is the wrong trade. `D4.hours` stays 4.0 and its accept is unchanged byte for byte. (2) **Day 6 is over on WALL CLOCK, not only on budget**, and this is the fact that was not previously stated: the window from the 09:00 PT freeze to the 13:00 PT deadline is **4.0 hours** and Day 6 carries **4.5 h** of strictly-sequential human-gated work (`G1` → `D5` → `D6`). No lever I hold closes a wall-clock gap. **Day 6 starts at 08:00 PT, giving a 5.0 h window for 4.5 h of work with 0.5 h of slack.** That is the single ASK to the user and it is the whole of it. **Not ruled, and deliberately left to the user:** moving `G1` from Day 6 to Day 5 evening would buy 0.5 h of Day-6 wall clock for ~12 h of early publication — D-30 was ruled by the user, so overturning it is not PM's. |
| D-33 | 2026-08-29 | Two grades in `erp/graph.json` regrade from OUR-ESTIMATE to MEASURED on the first green `G0` run; three published edge totals are corrected by one | `node tools/ready.mjs` exists (node `G0`) and all seven modes exit 0. `conventions.cut_invariant_grade`: **120 edges, 109 hard, 101 qualifying, 8 cross-horizon exempt, 0 violations** — the published `121/110/102` was the accounting of the PRE-D-30 graph, and R-42/D-30's deletion of the hard, same-horizon `G1 → G3` edge is the whole of the discrepancy. `critical_path_A.grade` becomes **MEASURED-DERIVATION OVER OUR-ESTIMATE INPUTS**: `--path` reproduces the twelve nodes and totals 29.5 h, equal to `capacity.graph_depth_hours`, but every `hours` field remains an estimate — **29.5 h is exact arithmetic over estimates and is not evidence the sprint takes 29.5 hours.** These regrades ride with L1's `G0` merge; they are licensed by a checker that is in the repository, which is what the earlier grade withheld. |
| D-34 | 2026-08-29 | The three stale restatements of R-42/D-30 are regenerated; `graph.json` is affirmed as correct and is not reopened | `--check-tables` exited 1 on four rows with one root cause: `G1` was still on Day 1 in three sibling documents. Regenerated: `erp/GRAPH.md` `G3.inputs` → `[T6, L0]`; `erp/PLAN.md` `G1` moved from the Day-1 table to the Day-6 table; `erp/RUNBOOK.md` day-1 count 16 → 15 and day-6 count 2 → 3. **Propagated beyond the four checker rows, because the checker only reads tables and the prose was worse:** `RUNBOOK.md` §3 listed `G1` among today's nine and instructed the human to flip both repositories public **today, five days early** — corrected, with the `G1` walkthrough retained and banner-marked DAY 6. Also corrected: the `G1 → G3` edge marked deleted in `GRAPH.md` and `PLAN.md`; six restatements of `121 edges`; the per-day human split in `PLAN.md`, `TEAM.md` and `RISK.md`; the `RISK.md` Day-1 23:59 trigger that demanded a Day-6 node be green on Day 1 and so fired by construction; and `README.md`'s claim that the human budget is an assumption nobody has ruled on. `erp/FACTS.md` is append-only by its own §16 rule, so its three stale figures are superseded in a new dated §17 rather than edited in place. `--check-tables` now exits 0. |
| D-35 | 2026-08-29 | **No count from `--check-accept-paths` is restated in any file the mode reads.** The runner prints them; the documents assert only the invariants that survive an edit | **The measurement is not idempotent under publication, and this was demonstrated, not argued.** The mode tokenizes the `accept` strings in `erp/graph.json`, so its own published output becomes part of the corpus it measures. I republished the 2026-08-29 run into `G0.accept` with the seven directory prefixes named — as the accept ordered — and the re-run counted **FOURTEEN** prefixes, because naming seven prefixes adds seven path tokens. Removing the names returned it to seven. Every figure restated in that predicate is a fixed point that the next prose edit anywhere in the file breaks; the retired `179 resolved / 19 exemption hits` and `182 / 152 / 7` publications were therefore not merely superseded by the R-43/R-44 accept edits, they were **unstable by construction**, which is the deeper reason they drifted. What `G0.accept` asserts now is the part that holds across edits: **exit 0, ZERO UNRESOLVED, the prefix class printed separately from the verbatim class rather than folded into it (R-36), and every one of the 8 exemption entries used at least once.** All four hold. **One item is referred back to L1 and is not resolved here:** I could not reproduce L1's distinct-class split (L1 published 189 VERBATIM and 8 EXEMPT; from a clean worktree I measure 157 VERBATIM and 21 EXEMPT against the same 8 exemption entries and the same 31 hits, with 7 PREFIX reproducing exactly). The likely cause is a transcription of exemption *entries* where the runner prints *pairs*, but I am not ruling on L1's own instrument's output — L1 re-reads it. It changes nothing: both runs exit 0 with zero unresolved, and under this ruling neither figure is published anywhere. |
| D-36 | 2026-08-29 | **The prose register is NOT mechanised, no node is created for it, and `G4` is not expanded to cover it.** The control is relocated to the charters instead | L1's addendum is correct and its number supersedes mine: the stale-`G1` set is **fifteen sites in two registers**, not four. Four are table rows (`--check-tables` reads these) and eleven are prose (it cannot). The two instruments had **zero overlap** — neither would have found the other's — and that is a limitation of `G0`, not a defect in the peer sweep. **I am not creating a node.** It would cost agent-hours plus review overhead against **0.625 h** of sprint-wide spare, and no rank of the ladder frees any of it. **I am also not bolting it onto `G4`**, and the reason is a category error worth naming rather than a scheduling one: `G4`'s `--assert-register` greps `kb/webmcp/RETRACTED.txt` for **overclaims about the product** that this project lost the right to assert — "structural guarantee", "the tool surface is the boundary". A stale planning statement is not an overclaim, `G4`'s lint does not read `erp/`, and pointing a submission-integrity hook at schedule drift would degrade both. **What replaces it is a control at the point of action rather than a checker over a corpus**, which is the better instrument here because the hazard is a *seat booting a node*, not a document being wrong: `erp/charters/QA.md` now states that `G3.inputs` is `[T6, L0]`, that `G1` is not an input, and that the fix for an auth failure is `gh auth status` and never a request to run `G1`; `erp/charters/I4.md` — the seat that OWNS `G1` — now opens that node's entry with **DAY 6, NOT DAY 1**, and says in terms that the node is *ready* from Day 0 and that ready is not scheduled; and `erp/RUNBOOK.md`'s dispatch cross-check now subtracts ready-but-not-scheduled nodes by name. A charter is re-read at every boot; a manual is read once. **Accepted limitation, stated plainly: nothing mechanically proves that prose in `erp/**.md` agrees with `erp/graph.json`, and after today nothing will until a second instance appears.** The trigger for revisiting is exactly that — a second ruling that goes stale in prose while the tables stay green. One instance is a defect cleared by hand; two is a missing instrument, and at two I will pay for the node. |
| D-37 | 2026-08-29 | **Three ownership collisions of the D-31 class, ruled in two halves. The `erp/` half is a wrong DECLARATION; the `.team/` half is D-31 applied again. No new rule in either.** | Raised by L1 after its gate DENIED PM on three paths in PM's own rulings commit — and raised rather than waived, which is the second time today that choice paid. **`erp/` half:** `L0.outputs` listed `erp/graph.json`, `erp/PATHS.md` and `erp/DECISIONS.md`, so ownership clause (a) resolved all three to L1 and denied **the author of the authority** in favour of the seat that committed it once on Day 0. The model is right; the declaration was wrong. **A node's `outputs` lists what that node AUTHORS — bringing a file into version control is a git act, not authorship.** The three are removed from `L0.outputs`; `erp/RUBRIC.md` STAYS, because R-16 makes its content genuinely L0's, and that distinction is the whole ruling. `erp/DECISIONS.md` is a `V6` output and `V6` is PM's, so clause (a) now resolves it to PM by the same rule that used to deny PM. L0 runs once and is done, so this creates no future denial for L1 and the Day-0 creation stands as executed. **`.team/` half:** `.team/**` resolves to L1, and `check-ownership.mjs` reads a diff's FILE LIST, so it cannot see that PM owns the `VERDICT`/`VERDICT_NOTE` lines of a ticket W wrote the rest of. Per-line ownership is not expressible and I am not making it so. **PM reports the verdict word and note; L1 writes them** — D-31 exactly, and it invents nothing, because **L1's own merge gate clause 7 already requires that `git log main` show only L1 as committer.** "The glob-owner commits, the author reports" was never a special case for pits; it is what this repository has always done, now written where it was ambiguous. Same for `.team/log/inbound-PM.jsonl`. **L1's merge of the rulings commit with the three DENYs recorded is AFFIRMED**, on the same reasoning as T6. |
| D-38 | 2026-08-29 | **L1's retraction of the `--check-accept-paths` distinct-class figures is ACCEPTED, and the evidence standard it wrote for itself is adopted project-wide** | L1 disclosed, unprompted, that the `222 pairs / 189 VERBATIM / 8 EXEMPT` split in its escalation was **never run**: it patched the mode to print a distinct-pair accounting, then wrote the figures by reasoning from the raw run instead of executing the patched mode once. The figures are retracted. I reproduce its corrected direction — `7 PREFIX` and `21 EXEMPT` against the retracted `8`, which was never possible, since there are 8 exemption ENTRIES and many more (node, token) PAIRS over them, and pairs is what the line counts. **This is not harmless because D-35 had already removed the counts.** Nothing downstream broke, but that is luck, not control: the figure was labelled MEASURED and sent to the seat that owns the authority, and had I propagated it into `graph.json` before running the mode myself, a fabricated number would now be the published accounting of the project's own instrument. It is Day 0's `FORCE_COLOR` pit with the sign flipped — there an observation with an invented explanation, here an explanation with an invented observation. **ADOPTED AS BINDING ON EVERY SEAT, in L1's own words: a figure goes into a message only if a command printed it in this session and the seat can point at the line.** Grades `MEASURED` and `VERIFIED` already mean this; what was missing was the statement that reasoning correctly from a real run does not produce a measurement. Note also, per D-35: I re-ran the mode twice more and got 207 and then 206 pairs, because the corpus includes the documents. **The class counts are unpublishable by construction; only exit 0, zero unresolved, the separated prefix class, and every exemption used are stable.** |
| D-39 | 2026-08-29 | **W's threshold is loosened, in writing, effective now. Its silence today is an instrument failure, not a clean crew.** | My charter tells me to watch this and it fires today. `.team/deviations/` holds **exactly one** ticket. It was filed on Day 0, and it was filed by **L1 about itself**. Across Day 1 — seven merges (`T6 S10 S11 V0 G4 G3 G0`), one node rejected once and reworked, four stale-restatement clusters spanning fifteen sites in three registers, a merge gate whose clauses 3 and 6 contradicted each other on every merge, and a fabricated measurement sent between seats — **W filed nothing.** Every deviation the project actually caught today was self-disclosed by the seat that made it, or found by a peer sweep. That is the failure mode my charter names: an adopt rate near zero read as compliance when it is really an instrument that has stopped reporting. It is worse than mis-calibration, because W is not predicting my verdicts — it is not producing evidence at all. **Loosened, and W's brief is restated: file on the EVIDENCE, never on the significance. If a seat corrects itself, that is a ticket. If a document disagrees with `erp/graph.json`, that is a ticket. If a gate is waived, deferred, or passed with a violation recorded, that is a ticket — L1 recording a DENY in a merge commit is exactly the shape you file on, and it happened four times today without one.** Do not wait for harm; the whole point of the register is that PM prices it and W does not. Zero tickets on a seven-merge day is a reading I cannot use, and the adopt/send-back/debt split is uncomputable from a single row. |
| D-40 | 2026-08-29 | **BW-22 is NOT narrowed and NOT weakened. `erp/charters/UX.md` is corrected instead — the charter was prescribing the banned string.** | L1 offered two defensible fixes and declined to choose, correctly. I choose the charter. **BW-22 is right and evidence-backed** — `erp/RISK.md` §2 grades it MEASURED and supplies its replacement column, *"revocation prevents the next invocation; an in-flight execute runs to completion"*, which shows the row's intent is to eliminate the ambiguous verb entirely, not merely to demand a qualifier. The charter's *"Copy must be precise: revocation blocks the next call"* used the banned verb while teaching the rule. **Narrowing BW-22 to accommodate it would be relaxing a working gate to route around a document — the exact move I refused in `DEV-L0-gate4-banner-grep`, and the consistency matters more than either fix.** MEASURED, and it changes the shape of the finding: **`erp/**` is on the lint's EXCLUDE array**, so the charter never failed a gate and never could. The hazard is not that the charter fails — it is that the charter **teaches a seat to write a string that fails everywhere else**, which is why it surfaced only when L1 quoted it into `kb/pits/F0.md` and the pre-commit blocked the commit. **I swept all of `erp/**` against every WORD and RETRACTED row: 121 hits, and 120 of them are legitimate** — a document that bans a phrase must name it, which is why `BANNED.txt` and `RISK.md` are excluded in the first place. Every other charter hit reads *"do not write X"*, *"do not claim X"*. **`UX.md:204` was the only PRESCRIPTIVE instance in the entire tree**, and L1 found it. Corrected to the sanctioned phrasing plus the rule L1 arrived at independently and which is now binding on every charter: **name the rule id, do not quote the banned string.** |
| D-41 | 2026-08-29 | **The unenforced "on the spot" restriction: I4's dispatch AFFIRMED, and a defect in `graph.json` that is mine, not I4's** | L1's dispatch is right and it is inside the line D-36 drew: a product overclaim IS `G4`'s business where planning drift is not, so the row, the `--selftest` case and the sweep for other unenforced phrase restrictions all belong to `G4`. Verified rather than relayed, per D-38: a file containing the sentence passes `tools/lint-layer0.mjs` with **exit 0**, and the phrase is in **neither** registry. **The part L1 did not find is worse and is mine:** `graph.json` did not merely omit the enforcement, it **asserted** it — *"G4's wording lint carries the banned form"* — so for two days the authority stated that a ruled restriction was enforced when nothing enforced it, and every seat quoting the authority inherited that. Corrected to state the **requirement** rather than the **state**. **This is the cut-invariant rule on a new surface: an authority says what must be true and a checker proves it; an authority that asserts a checker's state is a claim with nothing behind it.** I am extending the D-33 prohibition accordingly — *do not grade MEASURED on a checker that has not been written* now also reads *do not assert enforcement by a registry row that does not exist.* **On D-36's trigger, and I am refining it rather than stretching it:** this is a different class from the one D-36 priced. D-36 was authority-vs-prose drift, which is expensive to mechanise. This is *a ruling that never reached an enforcement registry*, and that set is small, enumerable and cheap to check — the ruled phrase restrictions are countable and `G4` already reads both registries. **If I4's sweep returns more than one missing row, I buy the checker for THIS class, scoped to phrase restrictions reaching the registry, and not the general prose problem.** That is a much smaller instrument than the one D-36 declined, and the distinction is the reason I can afford it. |
| D-42 | 2026-08-29 | **I4's three wording questions: two rows APPROVED, one REFUSED. The refusal is the ruling that matters.** | **(1) N-01, APPROVED** as a WORD row on the literal `distinguishes revoked from unknown`. Narrow domain jargon, low collision, and `erp/FACTS.md:761` states the fact it protects — a revoked tool and a never-registered tool are indistinguishable to the client. Residual under-catch is accepted and named: a seat writing *"the browser can tell a revoked tool from an unknown one"* is not caught. **An under-catching row is correct here; an over-catching one would not be.** (2) **S9/Kernel 5, APPROVED on the FRAMING literal `evidence of rarity`, and I4's reasoning is affirmed in full and is the better half of this ask.** Anchoring on `0/420` would ban the numeral, and the numeral is the *honest citation of the measured gap* (0/420 strict per-field provenance, 53% generic audit/ledger) — **a row that forbids the true statement is worse than no row.** The violation is the novelty framing, not the count. I4's classification is also right: this is a WORD row of the BW-05..08 novelty family, not a RETRACTED/RC row, because RETRACTED registers claims this project *asserted and lost the right to assert*, and this is a prohibition on framing that was never asserted. Note the novelty family (BW-05..08, BW-31, BW-32) already carries most of this load, which is why one narrow row is proportionate. **(3) FACTS.md §1(a), the flag/empty-surface restriction — REFUSED. No row is added, and the refusal is deliberate.** There is no literal that separates the false claim from a true one. `produces an empty surface` is a TRUE statement about a page with WebMCP absent — `V1`'s whole finding is that judges may see a page with no tools at all — so that row would ban an accurate sentence about a different subject. The violation is a *flag token adjacent to an empty-surface claim*, and a WORD row is a literal substring with no way to express adjacency. **This is exactly the trap I4 correctly identified in (2), one step further on: in (2) a literal would have banned a true number; here it would ban a true sentence.** I4 spotted its own trap and I am refusing the one it did not. The restriction stands unrelaxed and is enforced by review, not by lint. **It becomes the first entry in a DECLARED-UNENFORCEABLE list, which is the point:** D-41's defect was a restriction whose absence from the registry was *silent*. A declared absence with a ruled reason is not that defect. |
| D-43 | 2026-08-29 | **D-41's trigger has FIRED — I4's sweep returned THREE, not one. I am buying the checker I priced, at the scope I priced it. And BW-33 lands only AFTER the four rewords.** | **The number is 3.** I said in D-41 that more than one missing row buys the checker for this class, so it is bought, and it is scoped exactly as ruled: *for every ruled phrase restriction, either a registry row exists, or a ruled exemption is recorded in this file.* Not the general prose problem D-36 declined — that scope distinction is the whole reason this one is affordable. **Home: the `G4` follow-up already in flight with I4** — not a new node, because a new node must be scheduled and `--check-schedule` asserts 62/62 exactly once, and I am not doing graph surgery at the end of Day 1 for a 0.5 h check. **Cost, stated rather than waved at: 0.5 agent-hours, review overhead 0.05 x 0.5 = 0.025 human-hours, against 0.625 h of spare. 0.600 h remains. Nothing is cut and no rank fires.** The check turns D-42(3) from a hole into a declared exemption, which is the only reason I can refuse a row with a clear conscience. **SEQUENCING, and this is an ordering decision, not a preference — mcp-6d is right and I am ruling it binding: the four rewords land on `main` BEFORE I4's BW-33 row merges.** A registry row that bans a string four tracked files still carry turns every seat's pre-commit red repo-wide the instant it lands. I4 declining to bypass its own gate to commit the row is correct and is affirmed. **One correction to mcp-6d's owner map, and it is the same error it made on `charters/QA.md`: it applied longest-glob, but `conventions.ownership_rule` says a node's `outputs` BEAT the glob.** `evidence/UNKNOWNS.md` is a declared `V6` output and `V6` is PM's — MEASURED by `node tools/check-ownership.mjs --seat PM`, which returns `OK evidence/UNKNOWNS.md [declared output of V6 (owner PM)]` and DENIES the other three. **PM's file is reworded in this commit**, preserving the V6 byte-literal row shape (ends in `MEASURED`, no trailing pipe, pipe count unchanged). The remaining three are `docs/VIDEO-SCRIPT.md` (UX), `evidence/V2.json` (I1) and `kb/pits/F0.md` (L1), dispatched through L1. **`evidence/V2.json` is safe to reword and here is the boundary, because next time it may not be:** the phrase sits in an authored `consequence` field, not in captured measurement bytes. **Raw measured output is NEVER reworded to satisfy a lint** — if a future collision lands on real captured bytes, evidence stands and the linter gains a narrow mechanism by ruling. Editing evidence to make a gate green is the one move that is never available. |
| D-44 | 2026-08-29 | **BW-33 is RE-ANCHORED on the assertion, not the bare fragment. L1's counter-argument is AFFIRMED: a direction to a human producer must name the string. No document rewords, and no exemption mechanism is added.** | L1 raised the strongest case against its own pit rule and escalated it instead of absorbing it, which is the right instinct and the reason this is ruled rather than settled by fiat. **The argument is correct and I am adopting it: a line that says "do not say X" must name X.** The pit rule — *name the rule id, do not quote the string* — is right for a **pit**, whose reader is a seat that can look up an id; it is wrong for `docs/VIDEO-SCRIPT.md`, whose reader is a human writing copy at the moment they are least able to go look something up. **Different artifacts, different readers, different rules**, and a rule that makes a production direction unusable to protect a lint has the priority backwards. **The fix belongs in the ROW's wording, and MEASURED, a separating literal exists** — which is what makes this different from D-42(3), where I refused a row precisely because none did. Tested against the real assertion and all three real prohibition sentences: the bare fragment `on the spot` catches the assertion and **false-positives on 3 of 3** prohibitions; **`changes on the spot` catches the assertion and false-positives on 0 of 3.** BW-33 is therefore re-anchored on a closed, verb-led family: **`changes on the spot`, `updates on the spot`, `appears on the spot`, `refreshes on the spot`, `arrives on the spot`** — each with its own `--selftest` case, the set extended only by ruling. **Consequence: `docs/VIDEO-SCRIPT.md` and `evidence/V2.json` pass AS WRITTEN and are NOT reworded.** The D-43 sequencing constraint is discharged, not by three edits, but by the row no longer being wrong. `evidence/UNKNOWNS.md` (mine) and `kb/pits/F0.md` (L1's, at 7b0c7d3) are already reworded and **stay reworded** — the pit's rework also fixed a staleness (it claimed the phrase was in neither registry, which BW-33 closes) and stands on that merit alone. **THE GENERAL RULE, and this is the fourth instance today so it is systemic, not incidental: a ban pattern must distinguish ASSERTING a claim from FORBIDDING it. Anchor on the claim's predicate, never on its noun phrase.** BW-22 (charter prescribed the literal), the rarity row (a literal `0/420` would ban the honest citation), D-42(3) (no separating literal exists, so no row), and now BW-33. Where a separating literal exists, the row uses it; where none does, there is no row and the restriction is declared unenforceable. Neither case ever contorts the document. |
| D-45 | 2026-08-29 | **L1's D-41 check is a FALSE POSITIVE. The correction IS on `main`. And L1 should not book this as a third failure — its discipline was right and the instrument was ambiguous.** | MEASURED, on `origin/main`: `git show origin/main:erp/graph.json \| grep -o ".\{130\}G4's wording lint carries the banned form.\{130\}"` returns **my own retraction** — *"D-41, 2026-08-29 — THIS SENTENCE PREVIOUSLY ASSERTED that G4's wording lint carries the banned form. IT DID NOT. MEASURED 2026-08-29: a file containing that sentence passes tools/lint-layer0.mjs with exit 0..."* The uncorrected assertion is gone; what `grep -c` counted was the correction quoting the old claim in order to retract it. **This is D-44's rule biting the verification of D-44's own subject matter — the fourth instance today, and the one that proves the pattern is structural.** I am ruling on the self-assessment too, because getting it wrong is expensive: **L1's first two disclosures were real and its standard is right, but this one is not a third.** L1 ran a command, in-session, and reported what it printed — that is exactly what D-38 demands. The failure was the instrument's, not the seat's. **A seat that starts distrusting its own verified checks is worse off than one that never ran them**, and I would rather absorb a false positive than have L1 stop reporting. What is worth carrying is narrower and is now the standard: **to verify a correction, READ THE SENTENCE IN CONTEXT — `grep -o` with a window, or `sed -n`. Never `grep -c` a string you have just written a retraction of.** A count cannot tell assertion from retraction, and a corrected file legitimately contains the words it corrects. **Also correcting my own number, D-38 applying to me:** D-43 recorded I4's sweep as returning **three**; it returned **FOUR** — three needing a wording ruling (D-42) plus one clean and addable with no judgment, **IR-16(b), *"--headless=new enables WebMCP with no flag"*, retracted under R-30 with zero registry coverage. APPROVED as a RETRACTED/RC row**, and no accept edit is needed: `G4.accept` already reads *"at least these five entries"*, so a sixth is admitted by the predicate as written. The trigger fired at more than one either way and the checker stays bought; the count is corrected because it is wrong, not because it changes anything. |
| D-46 | 2026-08-29 | **`V1`'s human gate is QA's. "QA" in an accept is a REVIEW DUTY, not node ownership — and QA's refusal was correct.** | Both documents were right about what they each said. `V1.accept` says verbatim *"HUMAN SECOND: QA re-reads the screenshot against the JSON and a mismatch fails the node"*; QA's charter lists four nodes and `V1` is not among them, because **`V1` is I1's node and always was.** The gap is that a charter's node list enumerates what a seat OWNS and has never enumerated **review duties**, so no document said a seat could be named as a reviewer in another seat's accept. **The authority is not over-reaching and the charter is not under-listing nodes — the charter was under-listing DUTIES.** Ruled: QA reads `evidence/V1.png` against `evidence/V1.json` and a mismatch fails the node; `V1` does not join QA's four. **General rule, now binding: an accept MAY name a seat as a second reader, and any accept that does so must have that duty written into that seat's charter — PM's obligation.** Written into `erp/charters/QA.md`. **QA's refusal is AFFIRMED and I want it on the record as correct behaviour, not friction:** it declined a request from an unfamiliar session name for work outside its listed nodes, which is the same instruction-source discipline I1 and L1 applied on Day 0. Work reaches a seat from L1; a peer message carries no dispatch authority. **Why the second reader must not be the seat that produced or corrected the evidence:** the mechanical gate (ajv against `probe-verdict.schema.json`, plus `test -s evidence/V1.png`) proves the JSON is well-formed, never that it describes the PNG. `evidence/V1.json` was corrected pre-gate from a stale 00:56 localhost reading to the remote transcription — the R-44 provenance class, caught before it became a passed gate — and the seat that made that correction is exactly the seat that must not also certify it. `V1` gates three contingencies and `V2`/`V3`/`V4` are input-blocked behind it, so this is Day 2's first dispatch. |
| D-47 | 2026-08-29 | **The nine-ticket backlog is adjudicated. Split: 6 adopt, 2 debt, 1 send-back — 67% adopt.** | Authored by PM; **L1 transcribes into the `VERDICT`/`VERDICT_NOTE` lines under clause 6b (D-37)**, because `.team/**` is L1's and the checker cannot express per-line ownership. **DEV-001 — adopt.** The pit file was unownable by the seat required to write it; ruled at D-31, authority edited (`PATHS.md` §2.8, `charters/L1.md`, `TEAM.md`), zero rework for I2. **DEV-002 — adopt.** Three collisions of the same class on PM's own commit; ruled at D-37, `L0.outputs` corrected. **DEV-003 — adopt.** `G4.accept` named one exclusion mechanism while the node needs three; clause 1 (sweep exits 0) and clause 2 (fixture exits 1) are the same file with two required outcomes and are unsatisfiable without a swept-versus-named distinction. **The plan was wrong, the work was right** — `G4.accept` now names `EXCLUDE`, `REGISTER_FILES` and `SWEEP_EXCLUDE`, so the next reader cannot repeat the confusion that caused the rejection. **DEV-004 — debt**, node `G0`. Cost: a project-wide verification standard that did not previously exist (D-38), and one figure retracted before it propagated. **Deliberately NOT adopt and deliberately NOT send-back, and the distinction is the ladder doing its job: the DISCLOSURE is what keeps it off send-back; the BREACH is what keeps it off adopt.** Grades MEASURED and VERIFIED already meant "run it" — no plan gap excuses it — but L1 disclosed unprompted, retracted in its own pit, and nothing propagated. Pricing self-disclosure at zero teaches fabrication is free; pricing it as send-back teaches seats not to disclose. **DEV-005 — send-back**, and it is the only one. **(1) Failing clause:** `G5.accept` clause 3 as amended today — the hook must ACTUALLY FIRE; a real `git commit` staging a path the seat may not write must be rejected by git. The original clause proved only that the DIRECTORY was registered and that the script exits correctly **when invoked by hand**; git only ever invokes a hook named `pre-commit`, so all three clauses passed while the product was **inert**. **(2) Rework deadline: Day 2, 09:00 PT.** **(3) Branch: `G5`'s — L1 marks it blocked.** The fix is chaining, not logic: the script is correct and must be invoked from `.githooks/pre-commit` alongside G4's lint. **DEV-006 — adopt, and it is the most valuable ticket in the batch.** `T1.accept` checked the top-level-registration invariant at the one moment it could not fail — T1 runs before any `registerTool` call site exists — with the tool's own vacuity arm (`--selftest`) switched off, and no later node re-invoked it. **The clause is moved to `T2`, where call sites first appear and the invariant becomes falsifiable, with `--selftest` mandatory.** Zero rework for I2 on T1; a vacuous check becomes a real one. **DEV-007 — adopt.** The one-worktree rule that binds every building seat lived only in `charters/L1.md`. Now in `TEAM.md`, binding on all of them. Recovery was a reflog and luck, not a control. **DEV-008 — debt**, node `F0`. Cost: ~1 h of UX agent work destroyed and re-done, no permanent loss. A real cost was incurred, so it is debt and not adopt; `RUNBOOK` already said to re-dispatch seats **at `done`**, so the rule existed. **DEV-009 — adopt.** Enabling `core.hooksPath` switched G4's lint on for every seat at once and blocked QA on a failure QA did not introduce. Nobody's node was wrong; the rollout was, and no document governed rollout. **This is the same defect as the BW-33 blast radius ruled hours later (D-43) — twice in one day is what makes it a rule**, now in `TEAM.md`: a repo-wide gate goes on last, after the repo is already green. **On the split: 67% adopt is healthy and it is evidence W is calibrated, not lenient.** Six tickets moved the authority; the two debts name real costs; the one send-back is the one node whose product did nothing. **Six of the ten are against L1, filed by L1's own instrument about L1.** |
| D-48 | 2026-08-29 | **`evidence/V1.json` is COMPLETED from the screenshot before QA gates it. The built-in browser is Chromium 151, not the installed 152, and that fact currently exists only in a PNG.** | L1 read the screenshot against the JSON and explicitly did **not** perform the gate or merge the node, because D-46 assigns it to QA and that ruling was open. **That is exactly right and is affirmed: producing evidence for a gate is not performing it.** The mechanical gate is green (ajv exit 0, `test -s` exit 0) and four of five required fields corroborate to the millisecond. **The finding that matters is what the JSON leaves out.** `probe-verdict.schema.json` defines optional `chromiumMajor`, `navigatorAliasPresent`, `isSecureContext`, `tools` and `registrationError`; `V1.json` carries only the five required fields — verified by reading both. The PNG shows `chromiumMajor 151`, `navigator.modelContext undefined`, `isSecureContext true`. **The ChatGPT built-in browser is Chromium 151. The installed Chrome `V0` measured is 152. Different engines** — and `V0.notes` says in terms, under R-8, that *"151 is not present on this machine and the previous framing asked about a version nobody can test."* **We now have that version, on the surface that actually decides the submission, and the alias reads `undefined` there too — corroborating HANDOVER §3 rule 1 on a SECOND engine, a stronger result than `V0` alone, currently recorded nowhere but a screenshot.** **Ruled: I1 populates the optional fields from the screenshot already taken, and QA gates the completed file.** Sequencing is deliberate — transcribe first, gate once; the human gate has not run, so nothing is re-opened and `V1.accept` is NOT amended. **This is the OPPOSITE of D-43's prohibition and the distinction must not blur: D-43 forbids editing raw measured output to make a gate GREEN; this records measured facts the file omitted, into fields the schema was built to hold.** **Second, recorded rather than assumed away: `chatgptModel` is the one required field the screenshot CANNOT corroborate.** The page cannot detect which model drives the built-in browser; the string appears only in the copy-block the page renders from a value it was given. QA's read therefore verifies four of five required fields and cannot verify the fifth **by construction** — a limit on what this gate can prove, not a defect in the evidence. It matters because Sol/Terra is a MEASURED constraint that scopes the node (Luna disables WebMCP). **`chatgptModel` is attested by the OPERATOR, not by the page**, and is now written into `V1.notes` so no later reader mistakes it for a verified field. |
| D-49 | 2026-08-29 | **The record of a merge is part of the merge. Same class as W's unpushed branch, and that is twice.** | `.team/log/merges.txt` was FOUR rows behind `graph.state.json` — eleven rows against fifteen done — so any seat auditing from the record undercounted by four; `F0`, `G6`, `H1` and `S1` merged hours ago and their lines were never written. Found by mcp-6d with the measurement attached, corrected by L1 at `e45146f`. **L1's own diagnosis is the ruling: the merge is gated by four mechanical clauses; the RECORD of the merge is gated by nothing.** This is the same defect as W's nine tickets sitting unpushed on a branch while L1 and I both read the main checkout and concluded the instrument was dead — **gated work, ungated record, and in both cases the people reading the record drew a confident wrong conclusion from it.** I drew mine three times, in writing, to the user. **Binding: a merge is not complete until its row is in `merges.txt` and its branch is pushed — the record is a clause of the merge, not bookkeeping after it.** No new checker is bought for this; it belongs in L1's existing merge gate, which already has eight clauses and is the only place every merge passes through. |
| D-50 | 2026-08-29 | **`--smoke-login` and the missing static route are ONE defect with two instances: the CONSUMER specified what the PRODUCER was never told to build. Neither predicate is rewritten and nobody is sent back.** | Verified, not relayed: `--smoke-login` appears in exactly three places (`F1.accept` plus two restatements of it), **zero** times in `H2.accept`, and `grep -c smoke-login harness/drive.mjs` returns **0**. Separately MEASURED: `GET /` returns 404 while `GET /api/login` returns 200 with the cookie — `server/index.mjs` serves no static files. **Nobody did anything wrong in either case. H2 is green on all six clauses and S1 does exactly what its accept required.** (1) **The mode is specified in the PRODUCER: a 0.5 h follow-up on `harness/drive.mjs`, owner I1.** Inventing it into `F1.accept` would be PM writing a spec into the consumer for the second time, which is precisely how the defect was created. **`F1.accept` is NOT rewritten and NOT weakened** — a predicate that cannot run is broken, not strict, and shrinking F1 to fit what exists is the silent descope my charter forbids. (2) **The missing edge is ADDED: `H2 → F1`, hard.** `F1.inputs` was `[S1]` only, so nothing crossed and **both checkers were correct and blind** — `--check-schedule` had no edge to order and `--check-accept-paths` happily resolved `harness/drive.mjs` as a declared H2 output. **This is the same shape as the three edges v2.0.0 had to add for `T2`**, and it is the second time an unstated dependency hid behind two green instruments. (3) **The static route is a SCOPE ADDITION to S1, 0.5 h, owner I3 — not a send-back**, and the distinction from `G5` (DEV-005) is exact: G5's product was **inert**, S1's product **works and its accept was incomplete**. And it is not scope I invented: the graph's own edge contracts already say so — `S1 → T2` reads *"server/index.mjs — there is no server to serve the page under test"* and `F1 → D1` reads *"src/page/index.html — something to serve"*. I am matching the accept to a contract the authority already carries. **THIS ONE IS THE MOST URGENT ITEM ON THE BOARD: it blocks `T2`, which is ON THE CRITICAL PATH (`S4 → T2 → H3`), as well as `F1` and `D1`.** **Cost: 1.0 agent-hour total, review overhead 0.05 × 1.0 = 0.05 h, against 0.600 h spare after D-43. 0.550 h remains. Nothing is cut and no rank fires.** **Edge accounting, re-MEASURED and flagged because it will mislead a reader: the totals are 121/110/102 again — the SAME numbers published before D-30 — by coincidence, not reversion.** D-30 deleted `G1 → G3` (121→120), D-50 adds `H2 → F1` (120→121). Two different edges, one net; `inputs`↔`edges` bijective 121/121; D-33's correction stands. Restatements updated in `README.md`, `GRAPH.md`, `PLAN.md`, `PROPOSAL-workflow-tools.md`, and appended to `FACTS.md` under its own append-only rule. **`--check-tables` caught my own edge addition** — `FAIL erp/GRAPH.md:520 F1.inputs table [S1] graph.json [H2,S1]` — which is R-22 working on the graph owner, and is the answer to anyone who thinks the restatement checker is bureaucracy. |
| D-51 | 2026-08-29 | **UX's generalisation of D-40 is ADOPTED, and UX was right to decline L1's counter-argument.** | L1 asked whether a cue sheet read by a human under time pressure earns an exception to D-40. **UX's answer is better than the question: the objection was against the SHAPE L1 proposed, not against D-40.** A bare pointer to a registry row is a pointer with no payload — but D-40 never said the id is all you may write. **The rule, now binding wherever D-40 forces a literal out: replace it with the RULE THE LITERAL INSTANTIATES, never with a bare pointer.** Lead with the positive instruction, complete and usable on its own; then name the semantic class, so the lookup is optional rather than load-bearing. UX also observed that L1's own `kb/pits/F0.md` reword already did this instinctively, and L1 asked for it to be written down rather than left as a habit two seats happen to share — which is the right instinct and is why this row exists. Note this sits alongside, not against, **D-44**: where a ban pattern can be re-anchored on the claim's predicate, the document does not have to change at all, and `docs/VIDEO-SCRIPT.md` passes as written. D-51 governs the cases where a rewrite is genuinely required. **Two seats reached the same rule independently and a third asked for it to be recorded; that is the strongest evidence a convention is real rather than imposed.** |
| D-52 | 2026-08-29 | **The user rules a ONE-TIME EXEMPTION for Days 5 and 6. D-17's 3.0 h/day stands as the norm; Day 5 is 4.0 h and Day 6 is 4.5 h as a ruled exception. `D4` keeps its full scope. THIS CLOSES THE BUDGET HALF OF MY ASK AND NOT THE WALL-CLOCK HALF.** | **Provenance, stated honestly and graded lower than D-17's: RELAYED to PM by session `mcp-6d`, not heard by PM directly.** D-17 is graded *"RULED by the user, directly, before any seat was dispatched"*; this row is **RULED by the user, RELAYED — and CONFIRMED DIRECTLY 2026-08-29 ("确认无误", the recorded content put back to them verbatim), so this row is RE-GRADED to DIRECTLY-RULED and the caution below is discharged**. I am recording and acting on it because it matches what the arithmetic recommended and nothing about it is destructive — but per R-44, the provenance class this project keeps rediscovering, It was flagged for confirmation and the user confirmed it verbatim. **Any seat may now cite this row as directly ruled.** The provenance ladder worked exactly as R-44 intends: recorded at the lower grade, acted on, flagged, confirmed, re-graded. **The ruling itself is accepted and D-32 is superseded on its first half only:** the levers I offered were the user's attention or shortening `D4`, and the user chose neither by choosing to absorb the overage. `D4` keeps 4.0 h, `D4.accept` is unchanged, no node's scope moves, total accepted overage 2.5 h across the final two days. **THE HALF THIS DOES NOT CLOSE, AND IT IS THE ONE I RAISED SECOND BECAUSE IT IS THE HARDER ONE: Day 6 is short on WALL CLOCK, not only on budget.** MEASURED from the authority just now: `G1 → D5 → D6` is a **hard chain** (`G1→D5` contract *"the public repo URL"*, `D5→D6` contract *"the submitted Devpost entry"*), so its 0.5 + 2.0 + 2.0 = **4.5 hours are strictly sequential and cannot be parallelised**, against a window from the 09:00 PT freeze to the 13:00 PT deadline of **4.0 hours** (`RISK.md` §7.4). **An exemption grants HOURS. It cannot grant CLOCK.** 4.5 h of sequential work does not fit in 4.0 h of wall time however the budget is ruled. **AND THE FIX COSTS NOTHING, which is why this is a correction rather than an escalation: the 09:00 freeze is a CEILING ON CODE CHANGES, not a START TIME for submission work.** `RISK.md:1028` says only that *"Only `D4`'s tail, `D5` and `D6` may run after it"* — nothing forbids them running **before** it, and `G1` (flip two repos public) touches no code and needs no freeze at all. **Starting at 08:00 PT gives a 5.0 h window for 4.5 h of work with 0.5 h of slack, and requires no exemption, no scope change and no ruling from anyone.** That is the whole remaining ask and it is now one line: **begin Day 6 at 08:00 PT, not 09:00.** Recording it here on Day 1 rather than discovering it at 09:05 on Day 6, when `D5` is the only thing left and there is no scope left to cut. |
| D-53 | 2026-08-29 | **08:00 start APPROVED. Readiness-triggering ADOPTED for the tail. `D4` and `G1` may pull earlier freely. `D5` — the SUBMIT — may not, until one named unknown is closed.** | The user asked the sharpest question of the day: *"不可能提前吗？感觉现在已经到day 2的工作了"* — can the tail pull earlier, given the machine line is running ahead of the calendar. **The ORDER argument is correct and I have verified it rather than accepted it: `capacity.schedule_A` assigns ORDINAL day numbers and `--check-schedule` proves `day(u) <= day(v)` on every hard edge — it proves ORDER, never DATES.** Shifting the whole tail earlier changes the calendar mapping and not one day number, so every instrument stays green and every invariant holds: `G1` stays immediately ahead of `D5` (R-42/D-30 pins its POSITION in the sequence, not its date, and an early tail moves public-flip and submission together — which also SHORTENS the public exposure window D-30 was protecting, so it is strictly more consistent with that ruling, not less). **ADOPTED: L1 triggers the human windows on READINESS SIGNALS, not on dates, and gives the user one day's notice through the relay so they can plan. `D4` and `G1` pull earlier with no further ruling.** **`D5` IS DIFFERENT AND IT IS THE ONE THING I WILL NOT WAVE THROUGH: SUBMISSION IS THE ONLY IRREVERSIBLE ACT IN THE GRAPH.** Everything else in this sprint can be re-run, re-shot, re-merged or reverted. **The authority records NOTHING about whether a Devpost entry can be edited after it is submitted** — I searched `FACTS.md` and `RISK.md` and there is no such fact at any grade, so I will not assert one in either direction. **The whole trade turns on it.** If an entry CAN be revised before the deadline, submitting on Sep 1 is strictly better: the entry is banked, disqualification risk collapses, and Sep 2–3 becomes real improvement time. If it CANNOT, submitting on Sep 1 **converts two days of buffer into forfeited improvement time** — the worst version of this trade, taken for a benefit we would not have needed. **NARROWED, because the risk is smaller than it first looks and precision matters here: `D6.accept` verifies only ASSETS the entry POINTS AT — the repo page logged out, the MIT licence, the video URL returning 200 with an audio stream, the live URL and its banner. Every one of those stays fixable after submission.** The irreversibility is confined to the Devpost entry's own four answers and fields. **ACTION, and it is cheap: I4 owns `D5` and submission compliance — I4 reads the contest rules and reports whether a submitted entry can be edited before the deadline, as a PUBLISHED fact with a date.** Until that lands, `D5` holds its position relative to the deadline while everything ahead of it pulls earlier — which costs nothing, because pulling `D4` earlier is where almost all of the benefit is. **The 08:00 PT start is APPROVED and stands as the rule for whichever calendar day the tail lands on**, so the wall-clock fix survives the reschedule rather than being an artifact of Sep 3. |
| D-54 | 2026-08-29 | **L1 is right: the NOTICE is now the long pole on the tail. Fire it on ANTICIPATED readiness, not achieved readiness.** | Ruled, and it is L1's judgement adopted rather than a change to D-53. If `D4` becomes ready this evening, one day's notice means the earliest useful human window is tomorrow **regardless of readiness** — so readiness-triggering buys nothing unless the trigger fires ahead of the signal it waits for. **The asymmetry decides it, and it is not close: a notice fired too early costs the user a planned half-day that slips by a day. A notice fired too late costs a READY human-gated node with no human**, on a cut-0 node that is 27% of the entire human budget with zero tolerance for a re-shoot. Warn early. **Concrete trigger, so this is checkable rather than a disposition: `D4.inputs` are `H6`, `D1`, `F0` — `F0` is done. L1 fires the "your window opens tomorrow" call the moment `H6` and `D1` are BOTH in flight with no named blocker**, not when they merge. The same shape applies to `D5`/`D6` on the final day. A false warning is a cheap apology; a late one is the failure this whole schedule exists to prevent. |
| D-55 | 2026-08-29 | **Four seats finished and went idle without reporting today. The fix is the SWEEP, not a louder instruction to report — a process that has ended cannot report.** | I4, I1, QA and I3 all completed work that then sat on branches with no signal to L1; QA's tripwire flip and I3's `S4` were both found by the D-49 branch sweep rather than by a report. **L1's framing is exactly right and worth preserving: the gap is not seats failing to WORK, it is seats finishing without a REPORT.** **The structural point, which is why "tell seats to report harder" is the wrong ruling: a seat that finishes and goes idle is a process that has ENDED. You cannot rely on a report from something that is no longer running.** Push-based reporting fails precisely at completion, which is the one moment the report matters. **The control must be PULL-based, and it already exists: the D-49 branch sweep becomes a standing clause of the dispatch loop, run after every merge alongside `ready.mjs`, not an incident response.** D-49 was merged an hour before it caught two real cases, which is the strongest evidence available that it was the right rule. **This is the same family as D-49 and the stale-read failures in D-56: gated work, ungated record, and a reader drawing a confident conclusion from a gap.** No new instrument is bought — `git branch --merged` and `ready.mjs` are both one command and both current. |
| D-56 | 2026-08-29 | **PM's own rule, and the third instance of the day's most persistent failure: RECOMPUTE IN THE TURN YOU PUBLISH.** | I closed a report to the USER with *"the critical path has not advanced since `S10`"* and *"`T2` is blocked on `S1`'s static route"*. **Both were false when I wrote them.** Re-measured with `node tools/ready.mjs` against `erp/graph.state.json`: **19 nodes done, BURNED 14.0 / 29.5, head is `T2` — dispatched, not blocked.** `S1`'s static route, `S4` and `F1` all merged while my message was in flight; the path advanced three nodes and 7.0 h. I stated it as the thing outranking everything else, which is the worst sentence in a report to be wrong about, because it is the one the reader acts on. **My charter said the ready set is recomputed "every morning". That is not strong enough and never was.** It is now: `READY`, `BLOCKED` and `BURNED` come from a run of `ready.mjs` **in the same turn that publishes them** — never from a teammate's message, never from my own earlier block, never from a checkout merged an hour ago. **A long turn is exactly when this bites, because the world moves inside it, and the five-line block is the one artifact the human reads as current by construction.** **Three instances today, one shape: "W has filed nothing" against a ten-ticket register; L1's `grep -c` against my own retraction; and this. The register was fine every time and the reader was stale every time.** L1 flagged it rather than letting it pass to the user uncorrected, which is the second time today it has caught a claim of mine before it did damage. |
| D-57 | 2026-08-29 | **The Devpost finding is PUBLISHED and it points the right way. The D5 hold is LIFTED CONDITIONAL on a UI check — not on the rules text alone. And "submit a throwaway entry" is REFUSED as the way to make that check.** | **Finding, PUBLISHED, recorded with its source because a paraphrase would lose the load-bearing clause:** `https://webmcp.devpost.com/rules` §6 *"Submission Modifications"*, accessed 2026-08-29, two independent fetches with identical wording — *"Prior to the end of the Submission Period, you may save draft versions of your submission on Devpost to your portfolio before submitting the Submission materials to the Hackathon for evaluation. Once the Submission Period has ended, you may not make any changes or alterations to your Submission, but you may continue to update the Project in your Devpost portfolio."* The only stated restriction is anchored to **"once the Submission Period has ended"** — 2026-09-03 13:00 PT — **not to the act of submitting.** §6 names no separate lock at submit-time. Also recorded so nobody re-derives it: §6's one narrow post-period exception is sponsor-permitted fixes for IP infringement, PII exposure or inappropriate material, and even then *"the modified Submission must remain substantively the same as the original."* **I4's caveat is correct and I am ruling WITH it, not around it: the rules do not establish that Devpost's UI actually offers a re-edit control on an already-submitted entry, as opposed to only pre-submit drafts.** **THIS IS THE DAY'S PATTERN A FOURTH TIME, AND IT IS NOW A GRADING RULE: PERMITTED IS NOT POSSIBLE. A rule that ALLOWS something is not evidence that the MECHANISM exists.** Sibling to D-33 (*do not grade MEASURED on a checker that has not been written*) and D-41 (*do not assert enforcement by a registry row that does not exist*). Today: the authority claimed enforcement by a missing registry row; `G5`'s three clauses passed while git never invoked the hook; `T1`'s clause passed on zero call sites; and now a rule permits an edit whose control is unverified. **In every case the rule was right and the mechanism was absent — and this is the only one where finding out late is irreversible.** **RULED: (1) Everything ahead of `D5` pulls earlier NOW, per D-53 — that is where nearly all the benefit was. (2) `D5` moves the moment someone confirms the edit control EXISTS post-submit. (3) THE CHECK IS DOCS-FIRST: Devpost's own help documentation, which touches no account and costs nothing. (4) I REFUSE the throwaway-submission route and it is not the user's call to make casually — submitting a decoy entry to a LIVE hackathon is an outward-facing act against the real contest, risks reading as a duplicate or spam entry under rules we have not read, and would be taken on the account that carries our actual submission. We do not de-risk an irreversible act with a different irreversible act.** (5) **If the UI question never resolves, the hold simply stands and costs us NOTHING** — `D5` runs at its scheduled position with the full 08:00 window (D-52/D-53) and we lose only an insurance policy we were never counting on. **A note on how this answer arrived, because it is the counter-example to the day's standing failure:** I4 answered in the vocabulary asked for, QUOTED the sentence rather than paraphrasing, verified with two independent fetches, drew the boundary of its own finding unprompted, and produced a report rather than a file where a file was not warranted. That is the D-38 standard met without being reminded of it. |
| D-58 | 2026-08-29 | **`--assert-flips` goes to I1 as an H2 follow-up, NOT to H3 — and the reason is a DEADLOCK, not a preference. T2 is not sent back. The missing `H2 → T2` edge is added.** | Verified independently: `grep -o '--[a-z][a-z0-9-]*' harness/drive.mjs` lists `--gate --list --exec --smoke-login --scenario --url --input --headed --headless --enable-features`. **`--assert-flips` is absent; `--smoke-login` is PRESENT, so D-50's follow-up shipped and worked.** T2's spans 1 and 2 are green. **T2 is NOT sent back and its accept is NOT rewritten**: I2 delivered its own scope and the failing span names a mode of another node's artifact — the same disposition as `F1` under D-50, for the same reason. **`erp/PROPOSAL-workflow-tools.md:285` assigns `--assert-flips` to `H3` as "0 h, text only". THAT ASSIGNMENT IS REJECTED, and a proposal document is not an authority.** MEASURED, and it is decisive rather than a matter of taste: **`H3.inputs` is `[T2]`. H3 runs AFTER T2.** Putting a mode that T2's accept depends on into H3 makes **T2's span 0 unsatisfiable forever** — a deadlock across the two head nodes of the remaining critical path, shipped as a scheduling note in a proposal nobody re-reads. `harness/drive.mjs` is `H2`'s declared output; a mode of it belongs to H2's producer, which is exactly why the `--smoke-login` follow-up landed there and succeeded. **Ruled: 1.0 agent-hour to I1, bundled into the same file it just touched.** **THIS BLOCKS `T2`, THE HEAD OF THE CRITICAL PATH, with `H3` immediately behind it — highest-urgency item on the board.** **Edge added: `H2 → T2`, hard.** `T2.inputs` was `[T1,H1,F1,S1,S4]` with no `H2`, so nothing crossed — **the second missing edge of exactly this shape today**, after `H2 → F1`. Re-MEASURED: 122 edges, 111 hard, 103 qualifying, 8 exempt, 0 violations, bijective 122/122; restatements and `FACTS.md` updated. **`--check-tables` again caught my own edge addition** (`GRAPH.md:347 T2.inputs`), which is now twice in one evening. |
| D-59 | 2026-08-29 | **The fourth register is real and I am BUYING the instrument. Three instances in one day, one on the critical-path head, is past any threshold I set.** | L1 recorded the pattern rather than proposing a fix, which was the right call and leaves the pricing to me. **The register: nothing verifies that a MODE, FLAG or SUBCOMMAND named in an accept predicate is produced by anything.** `--check-accept-paths` resolves `harness/drive.mjs` happily — **the PATH exists; the MODE is not a path.** `--check-schedule` had no edge to order because the dependency was never drawn. Both instruments were correct and blind, twice. **D-36 declined the prose register because its corpus is unbounded. D-41 refined the trigger for a small enumerable class. This class is small, enumerable and purely mechanical — every `--flag` token adjacent to a repo-relative script path in an accept — so it is priced like D-41's, not like D-36's, and its trigger fired three times over.** **BOUGHT: `node tools/ready.mjs --check-modes`, a G0 follow-up, owner L1, 1.0 agent-hour.** Definition, so it is buildable without a second ruling: for every `(script-path, --flag)` pair extracted from an accept — **(a)** if the script exists on disk, the flag literal must appear in it; **(b)** if it does not yet exist, the node declaring it as an output must be a hard input of the node whose accept names it. **Stated honestly, it catches TWO of today's three instances and not the third:** `--smoke-login` and `--assert-flips` are flags; `S1`'s missing static route was a CAPABILITY described in prose, and no flag-based checker reaches it. **I am buying an instrument that closes two thirds of a class, and saying so, rather than claiming it closes the class.** The third kind was caught by a human reading an edge contract against a running server, and nothing cheap replaces that. **Cost: 1.0 agent-hour, review overhead 0.05 h, plus D-58's 1.0 h at 0.05 h — against 0.550 h spare. 0.450 h remains. Nothing is cut and no rank fires.** |
| D-60 | 2026-08-29 | **D-57's condition is MET on both halves. The `D5` hold is LIFTED — `D5` may pull earlier with the tail. But `D6` does NOT move with it, and that is a new constraint early submission creates.** | **PUBLISHED, second source, obtained the way D-57 specified and by the route it approved:** `https://help.devpost.com/article/123-how-to-edit-a-submission`, Devpost Help Center, accessed 2026-08-29, cross-checked between a search snippet and a direct fetch — *"You can access and make changes to your submission until the submission deadline. Once the deadline passes, you can still make changes to your project but these changes will not be reflected in your hackathon submission, only in the project within your portfolio."* The UI affordance is named, not assumed: "Manage projects" / "Edit project", or "My projects" → the project. **So the RULE permits it (§6) and the PRODUCT provides a working control for it, both to 2026-09-03 13:00 PT — and the two sources agree on the cutoff without either being inferred from the other.** The "permitted is not possible" gap that D-57 refused to close by assumption is closed on its own terms. **No live-account action was taken and the throwaway-submission route stayed refused.** **HOLD LIFTED: `D5` joins the readiness-triggered tail under D-53.** **THE NEW CONSTRAINT, and it is created BY the lift rather than resolved by it: `D6` must NOT follow `D5` immediately any more.** `D6` is the freeze rehearsal — it verifies the PUBLIC state a judge sees: repo page logged out, MIT licence, video URL 200 with an audio stream, live URL and banner. **If `D5` submits on Sep 1 and we then keep improving through Sep 3 — which is the entire point of submitting early — then a `D6` run on Sep 1 verifies a state we subsequently change, and certifies nothing.** The `D5 → D6` edge is hard, which constrains ORDER and never adjacency, and `--check-schedule` only asserts `day(D5) <= day(D6)`. **Ruled: `D5` pulls earlier as readiness allows; `D6` stays at the END, as the final-state check, and is the last thing that runs before the deadline.** That strictly increases safety over the original plan, where `D6` ran once immediately after a submission nothing could follow. **If the tail pulls earlier, the sequence is: `G1` → `D5` (early, banked) → continued improvement → `D6` (last, against the final public state).** Recording it now because it is exactly the kind of thing that would otherwise be discovered by running `D6` on Sep 1, passing it, and shipping a Sep 3 state nobody re-checked. **On provenance: L1 explicitly declined to treat my condition as satisfied by its own reading of a peer's report, and said so. That is correct — I set the condition and the lift is mine — and it is the D-52 provenance ladder applied by a seat to itself without being asked.** |
| D-61 | 2026-08-29 | **`--check-modes` found two more defects on its FIRST RUN, hours after I bought it. Both ruled: `--fallback` joins I1's bundle, `E1 → E5` is drawn. And the gap L1 raised is real — `D6` gains the clause that closes it.** | **The instrument paid for itself before it was a day old, which is the answer to whether D-59 was worth 1.0 h.** Verified by running it myself: 31 (script, flag) pairs extracted, 22 resolved against a script on disk carrying the literal, 6 deferred to a producer that is a hard input or the node's own output, 3 violations. **(1) FOURTH instance of the consumer-specifies-the-producer defect: `H3.accept` names `harness/drive.mjs --fallback` and the flag is absent.** `H3` is the node immediately BEHIND `T2` on the critical path, so left alone this surfaced tomorrow as a blocked head node with `H3` already dispatched and less slack than tonight. **Ruled to I1, bundled with `--assert-flips` (D-58) and `--smoke-login` (D-50, shipped) — three modes, one file, 1.5 agent-hours total.** Edge `H2 → H3` added. **(2) THIRD missing edge of the shape that hid `H2 → F1` and `H2 → T2`: `E5.accept` names `webmcp-eval-kit/bin/eval.mjs`, which `E1` declares, and `E5.inputs` was `[T5]` only. `E2` and `E3` name the same script and BOTH carry their `E1` hard edge — so `E5` was the odd one out rather than the pattern, which is precisely why no human reading found it and a checker comparing sibling nodes did.** Edge `E1 → E5` added. **Re-MEASURED: 124 edges, 113 hard, 105 qualifying, 8 exempt, 0 violations, bijective 124/124. FOUR missing hard edges of one shape drawn in a single evening; three of the four point at `harness/drive.mjs`.** **(3) L1's raised gap is REAL and I am ruling it rather than noting it: nothing owned re-checking the submission's OWN four answers after an early submit.** Every clause of `D6` verifies an ASSET THE ENTRY POINTS AT — repo page, licence, video URL, live URL. **That gap was harmless while submission was the last act. D-60 made it the gap of the whole plan, because the entry can now be banked on day one and diverge from `docs/DEVPOST.md` for two days while every existing check stays green.** **`D5` gains an output, `evidence/D5-entry-sync.txt`, recording the sha256 of `docs/DEVPOST.md` as last pasted into the live entry; `D6` gains a clause asserting that sha equals the current one.** Change `DEVPOST.md` after the entry was last updated and they diverge and `D6` fails. **Deliberately a SHA COMPARISON and NOT a human confirmation: "the operator confirms the answers still match" is a human judgement standing as an acceptance predicate, which is the exact defect `D4` was rewritten to remove.** L1 raised this rather than assuming it, one round after raising the pattern that bought the instrument, and it is the last gap of the day found the way the first one was — by someone asking what nothing owns. |
