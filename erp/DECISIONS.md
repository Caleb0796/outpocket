# Decisions register — outpocket sprint A

Rulings of record. Created by node **L0** on Day 0 (2026-08-28); node **V6** appends its
unknowns rows to this same file on Day 2 and does not create it.

The D-17 row below is reproduced verbatim from `erp/RUNBOOK.md` §2, *The D-17 row, literally*.
`L0` accept gate (1) matches it with `^\|\s*D-17\s*\|[^\n]*human_hours_per_day\s*=\s*([0-9.]+)`,
requires the captured value to be `2.5` or `3.0`, and requires `erp/graph.json`
`capacity.human_hours_available` to equal that value x 5.5. If you change a digit here, change
`capacity.human_hours_available` in the same edit or the gate fails — which is what it is for.

D-17 was RULED by the user directly on 2026-08-28, before any seat was dispatched. L0 does not
decide it; L0 records it.

## Rulings

| ID | Date | Ruling | Consequence |
|---|---|---|---|
| D-17 | 2026-08-28 | human_hours_per_day = 3.0 | RULED by the user, directly, before any seat was dispatched. 3.0 x 5.5 days = 16.5 h available; 15.875 required; 0.625 h spare. NOTHING IS CUT — all 62 horizon-A nodes stay in scope. The 2.5 h/day branch and its 27-node amputation set survive in capacity.human_budget_sensitivity as a contingency, not as the plan of record. |
| D-31 | 2026-08-29 | The pit file `kb/pits/<node-id>.md` is **written and committed by L1**; the building seat **reports** its five fields with the merge request | Resolves a contradiction internal to the merge gate: clause 6 required a file that clause 3 forbade its author to write, so **every seat that satisfied gate 6 violated gate 3, mechanically, on every merge**. No node lists a pit in `outputs` (ownership clause (a) is empty *by design* — `PATHS.md §2.8` keeps the ritual unbudgeted), and the longest-matching glob `kb/pits/**` resolves to L1 (clause (b)). Of the three available resolutions this is the only one that invents no rule: adding the pit to 68 nodes' `outputs` contradicts §2.8's deliberate unbudgeting, and a `file_ownership` row resolving `<id>` to the owning node's seat would make one row of a static glob→seat table dynamic. **`conventions.ownership_rule` is unchanged, `file_ownership` is unchanged, no node's `outputs` are touched.** Cost: one commit per merge, on L1. L1 transcribes the seat's own words and names the seat; a pit L1 composed is worse than no pit. **L1's T6 merge is AFFIRMED.** L1 merged T6 with `DENY kb/pits/T6.md` recorded in the merge commit rather than waived quietly, and that was the right call: the alternative was blocking Day 1's leverage node on a bookkeeping contradiction that was not I2's fault, and recording the violation in the open is what made this ruling possible. The violation is retired by this row, not excused — under D-31 it could not have occurred. |
| D-32 | 2026-08-29 | **D-17's second half: the human budget is a TOTAL and the schedule does not spread it. D4 is NOT shortened. The lever is the user's attention, and Day 6 needs an 08:00 PT start.** | ARITHMETIC, regenerated against `capacity.schedule_A.days` after R-42/D-30: **Day 1 = 2.0 h** (`V1`), **Day 5 = 4.0 h** (`D4`), **Day 6 = 4.5 h** (`G1` 0.5 + `D5` 2.0 + `D6` 2.0); the other four days carry zero. Against the ruled 3.0 h/day that is **1.0 h over on Day 5, 1.5 h over on Day 6, 2.5 h over in total** against **0.625 h** of sprint-wide spare. **No rank of the ladder touches it** — `G1 V1 D4 D5 D6` are all cut 0, so firing `T3 T5 F3 F5 E1–E10` and every other rank frees exactly zero of these hours. Two levers exist and I rule on both. (1) **D4 is not shortened.** It is 4.0 h of the highest-leverage deliverable in the sprint — HANDOVER §5 concludes the video IS the deliverable, a missing video is a disqualification item, and 24 of 420 surveyed repos posted one. Trading that for 1.0 h, when the binding constraint is Day 6's wall clock and not Day 5's, is the wrong trade. `D4.hours` stays 4.0 and its accept is unchanged byte for byte. (2) **Day 6 is over on WALL CLOCK, not only on budget**, and this is the fact that was not previously stated: the window from the 09:00 PT freeze to the 13:00 PT deadline is **4.0 hours** and Day 6 carries **4.5 h** of strictly-sequential human-gated work (`G1` → `D5` → `D6`). No lever I hold closes a wall-clock gap. **Day 6 starts at 08:00 PT, giving a 5.0 h window for 4.5 h of work with 0.5 h of slack.** That is the single ASK to the user and it is the whole of it. **Not ruled, and deliberately left to the user:** moving `G1` from Day 6 to Day 5 evening would buy 0.5 h of Day-6 wall clock for ~12 h of early publication — D-30 was ruled by the user, so overturning it is not PM's. |
| D-33 | 2026-08-29 | Two grades in `erp/graph.json` regrade from OUR-ESTIMATE to MEASURED on the first green `G0` run; three published edge totals are corrected by one | `node tools/ready.mjs` exists (node `G0`) and all seven modes exit 0. `conventions.cut_invariant_grade`: **120 edges, 109 hard, 101 qualifying, 8 cross-horizon exempt, 0 violations** — the published `121/110/102` was the accounting of the PRE-D-30 graph, and R-42/D-30's deletion of the hard, same-horizon `G1 → G3` edge is the whole of the discrepancy. `critical_path_A.grade` becomes **MEASURED-DERIVATION OVER OUR-ESTIMATE INPUTS**: `--path` reproduces the twelve nodes and totals 29.5 h, equal to `capacity.graph_depth_hours`, but every `hours` field remains an estimate — **29.5 h is exact arithmetic over estimates and is not evidence the sprint takes 29.5 hours.** These regrades ride with L1's `G0` merge; they are licensed by a checker that is in the repository, which is what the earlier grade withheld. |
| D-34 | 2026-08-29 | The three stale restatements of R-42/D-30 are regenerated; `graph.json` is affirmed as correct and is not reopened | `--check-tables` exited 1 on four rows with one root cause: `G1` was still on Day 1 in three sibling documents. Regenerated: `erp/GRAPH.md` `G3.inputs` → `[T6, L0]`; `erp/PLAN.md` `G1` moved from the Day-1 table to the Day-6 table; `erp/RUNBOOK.md` day-1 count 16 → 15 and day-6 count 2 → 3. **Propagated beyond the four checker rows, because the checker only reads tables and the prose was worse:** `RUNBOOK.md` §3 listed `G1` among today's nine and instructed the human to flip both repositories public **today, five days early** — corrected, with the `G1` walkthrough retained and banner-marked DAY 6. Also corrected: the `G1 → G3` edge marked deleted in `GRAPH.md` and `PLAN.md`; six restatements of `121 edges`; the per-day human split in `PLAN.md`, `TEAM.md` and `RISK.md`; the `RISK.md` Day-1 23:59 trigger that demanded a Day-6 node be green on Day 1 and so fired by construction; and `README.md`'s claim that the human budget is an assumption nobody has ruled on. `erp/FACTS.md` is append-only by its own §16 rule, so its three stale figures are superseded in a new dated §17 rather than edited in place. `--check-tables` now exits 0. |
| D-35 | 2026-08-29 | **No count from `--check-accept-paths` is restated in any file the mode reads.** The runner prints them; the documents assert only the invariants that survive an edit | **The measurement is not idempotent under publication, and this was demonstrated, not argued.** The mode tokenizes the `accept` strings in `erp/graph.json`, so its own published output becomes part of the corpus it measures. I republished the 2026-08-29 run into `G0.accept` with the seven directory prefixes named — as the accept ordered — and the re-run counted **FOURTEEN** prefixes, because naming seven prefixes adds seven path tokens. Removing the names returned it to seven. Every figure restated in that predicate is a fixed point that the next prose edit anywhere in the file breaks; the retired `179 resolved / 19 exemption hits` and `182 / 152 / 7` publications were therefore not merely superseded by the R-43/R-44 accept edits, they were **unstable by construction**, which is the deeper reason they drifted. What `G0.accept` asserts now is the part that holds across edits: **exit 0, ZERO UNRESOLVED, the prefix class printed separately from the verbatim class rather than folded into it (R-36), and every one of the 8 exemption entries used at least once.** All four hold. **One item is referred back to L1 and is not resolved here:** I could not reproduce L1's distinct-class split (L1 published 189 VERBATIM and 8 EXEMPT; from a clean worktree I measure 157 VERBATIM and 21 EXEMPT against the same 8 exemption entries and the same 31 hits, with 7 PREFIX reproducing exactly). The likely cause is a transcription of exemption *entries* where the runner prints *pairs*, but I am not ruling on L1's own instrument's output — L1 re-reads it. It changes nothing: both runs exit 0 with zero unresolved, and under this ruling neither figure is published anywhere. |
| D-36 | 2026-08-29 | **The prose register is NOT mechanised, no node is created for it, and `G4` is not expanded to cover it.** The control is relocated to the charters instead | L1's addendum is correct and its number supersedes mine: the stale-`G1` set is **fifteen sites in two registers**, not four. Four are table rows (`--check-tables` reads these) and eleven are prose (it cannot). The two instruments had **zero overlap** — neither would have found the other's — and that is a limitation of `G0`, not a defect in the peer sweep. **I am not creating a node.** It would cost agent-hours plus review overhead against **0.625 h** of sprint-wide spare, and no rank of the ladder frees any of it. **I am also not bolting it onto `G4`**, and the reason is a category error worth naming rather than a scheduling one: `G4`'s `--assert-register` greps `kb/webmcp/RETRACTED.txt` for **overclaims about the product** that this project lost the right to assert — "structural guarantee", "the tool surface is the boundary". A stale planning statement is not an overclaim, `G4`'s lint does not read `erp/`, and pointing a submission-integrity hook at schedule drift would degrade both. **What replaces it is a control at the point of action rather than a checker over a corpus**, which is the better instrument here because the hazard is a *seat booting a node*, not a document being wrong: `erp/charters/QA.md` now states that `G3.inputs` is `[T6, L0]`, that `G1` is not an input, and that the fix for an auth failure is `gh auth status` and never a request to run `G1`; `erp/charters/I4.md` — the seat that OWNS `G1` — now opens that node's entry with **DAY 6, NOT DAY 1**, and says in terms that the node is *ready* from Day 0 and that ready is not scheduled; and `erp/RUNBOOK.md`'s dispatch cross-check now subtracts ready-but-not-scheduled nodes by name. A charter is re-read at every boot; a manual is read once. **Accepted limitation, stated plainly: nothing mechanically proves that prose in `erp/**.md` agrees with `erp/graph.json`, and after today nothing will until a second instance appears.** The trigger for revisiting is exactly that — a second ruling that goes stale in prose while the tables stay green. One instance is a defect cleared by hand; two is a missing instrument, and at two I will pay for the node. |
| D-37 | 2026-08-29 | **Three ownership collisions of the D-31 class, ruled in two halves. The `erp/` half is a wrong DECLARATION; the `.team/` half is D-31 applied again. No new rule in either.** | Raised by L1 after its gate DENIED PM on three paths in PM's own rulings commit — and raised rather than waived, which is the second time today that choice paid. **`erp/` half:** `L0.outputs` listed `erp/graph.json`, `erp/PATHS.md` and `erp/DECISIONS.md`, so ownership clause (a) resolved all three to L1 and denied **the author of the authority** in favour of the seat that committed it once on Day 0. The model is right; the declaration was wrong. **A node's `outputs` lists what that node AUTHORS — bringing a file into version control is a git act, not authorship.** The three are removed from `L0.outputs`; `erp/RUBRIC.md` STAYS, because R-16 makes its content genuinely L0's, and that distinction is the whole ruling. `erp/DECISIONS.md` is a `V6` output and `V6` is PM's, so clause (a) now resolves it to PM by the same rule that used to deny PM. L0 runs once and is done, so this creates no future denial for L1 and the Day-0 creation stands as executed. **`.team/` half:** `.team/**` resolves to L1, and `check-ownership.mjs` reads a diff's FILE LIST, so it cannot see that PM owns the `VERDICT`/`VERDICT_NOTE` lines of a ticket W wrote the rest of. Per-line ownership is not expressible and I am not making it so. **PM reports the verdict word and note; L1 writes them** — D-31 exactly, and it invents nothing, because **L1's own merge gate clause 7 already requires that `git log main` show only L1 as committer.** "The glob-owner commits, the author reports" was never a special case for pits; it is what this repository has always done, now written where it was ambiguous. Same for `.team/log/inbound-PM.jsonl`. **L1's merge of the rulings commit with the three DENYs recorded is AFFIRMED**, on the same reasoning as T6. |
| D-38 | 2026-08-29 | **L1's retraction of the `--check-accept-paths` distinct-class figures is ACCEPTED, and the evidence standard it wrote for itself is adopted project-wide** | L1 disclosed, unprompted, that the `222 pairs / 189 VERBATIM / 8 EXEMPT` split in its escalation was **never run**: it patched the mode to print a distinct-pair accounting, then wrote the figures by reasoning from the raw run instead of executing the patched mode once. The figures are retracted. I reproduce its corrected direction — `7 PREFIX` and `21 EXEMPT` against the retracted `8`, which was never possible, since there are 8 exemption ENTRIES and many more (node, token) PAIRS over them, and pairs is what the line counts. **This is not harmless because D-35 had already removed the counts.** Nothing downstream broke, but that is luck, not control: the figure was labelled MEASURED and sent to the seat that owns the authority, and had I propagated it into `graph.json` before running the mode myself, a fabricated number would now be the published accounting of the project's own instrument. It is Day 0's `FORCE_COLOR` pit with the sign flipped — there an observation with an invented explanation, here an explanation with an invented observation. **ADOPTED AS BINDING ON EVERY SEAT, in L1's own words: a figure goes into a message only if a command printed it in this session and the seat can point at the line.** Grades `MEASURED` and `VERIFIED` already mean this; what was missing was the statement that reasoning correctly from a real run does not produce a measurement. Note also, per D-35: I re-ran the mode twice more and got 207 and then 206 pairs, because the corpus includes the documents. **The class counts are unpublishable by construction; only exit 0, zero unresolved, the separated prefix class, and every exemption used are stable.** |
| D-39 | 2026-08-29 | **W's threshold is loosened, in writing, effective now. Its silence today is an instrument failure, not a clean crew.** | My charter tells me to watch this and it fires today. `.team/deviations/` holds **exactly one** ticket. It was filed on Day 0, and it was filed by **L1 about itself**. Across Day 1 — seven merges (`T6 S10 S11 V0 G4 G3 G0`), one node rejected once and reworked, four stale-restatement clusters spanning fifteen sites in three registers, a merge gate whose clauses 3 and 6 contradicted each other on every merge, and a fabricated measurement sent between seats — **W filed nothing.** Every deviation the project actually caught today was self-disclosed by the seat that made it, or found by a peer sweep. That is the failure mode my charter names: an adopt rate near zero read as compliance when it is really an instrument that has stopped reporting. It is worse than mis-calibration, because W is not predicting my verdicts — it is not producing evidence at all. **Loosened, and W's brief is restated: file on the EVIDENCE, never on the significance. If a seat corrects itself, that is a ticket. If a document disagrees with `erp/graph.json`, that is a ticket. If a gate is waived, deferred, or passed with a violation recorded, that is a ticket — L1 recording a DENY in a merge commit is exactly the shape you file on, and it happened four times today without one.** Do not wait for harm; the whole point of the register is that PM prices it and W does not. Zero tickets on a seven-merge day is a reading I cannot use, and the adopt/send-back/debt split is uncomputable from a single row. |
| D-40 | 2026-08-29 | **BW-22 is NOT narrowed and NOT weakened. `erp/charters/UX.md` is corrected instead — the charter was prescribing the banned string.** | L1 offered two defensible fixes and declined to choose, correctly. I choose the charter. **BW-22 is right and evidence-backed** — `erp/RISK.md` §2 grades it MEASURED and supplies its replacement column, *"revocation prevents the next invocation; an in-flight execute runs to completion"*, which shows the row's intent is to eliminate the ambiguous verb entirely, not merely to demand a qualifier. The charter's *"Copy must be precise: revocation blocks the next call"* used the banned verb while teaching the rule. **Narrowing BW-22 to accommodate it would be relaxing a working gate to route around a document — the exact move I refused in `DEV-L0-gate4-banner-grep`, and the consistency matters more than either fix.** MEASURED, and it changes the shape of the finding: **`erp/**` is on the lint's EXCLUDE array**, so the charter never failed a gate and never could. The hazard is not that the charter fails — it is that the charter **teaches a seat to write a string that fails everywhere else**, which is why it surfaced only when L1 quoted it into `kb/pits/F0.md` and the pre-commit blocked the commit. **I swept all of `erp/**` against every WORD and RETRACTED row: 121 hits, and 120 of them are legitimate** — a document that bans a phrase must name it, which is why `BANNED.txt` and `RISK.md` are excluded in the first place. Every other charter hit reads *"do not write X"*, *"do not claim X"*. **`UX.md:204` was the only PRESCRIPTIVE instance in the entire tree**, and L1 found it. Corrected to the sanctioned phrasing plus the rule L1 arrived at independently and which is now binding on every charter: **name the rule id, do not quote the banned string.** |
| D-41 | 2026-08-29 | **The unenforced "on the spot" restriction: I4's dispatch AFFIRMED, and a defect in `graph.json` that is mine, not I4's** | L1's dispatch is right and it is inside the line D-36 drew: a product overclaim IS `G4`'s business where planning drift is not, so the row, the `--selftest` case and the sweep for other unenforced phrase restrictions all belong to `G4`. Verified rather than relayed, per D-38: a file containing the sentence passes `tools/lint-layer0.mjs` with **exit 0**, and the phrase is in **neither** registry. **The part L1 did not find is worse and is mine:** `graph.json` did not merely omit the enforcement, it **asserted** it — *"G4's wording lint carries the banned form"* — so for two days the authority stated that a ruled restriction was enforced when nothing enforced it, and every seat quoting the authority inherited that. Corrected to state the **requirement** rather than the **state**. **This is the cut-invariant rule on a new surface: an authority says what must be true and a checker proves it; an authority that asserts a checker's state is a claim with nothing behind it.** I am extending the D-33 prohibition accordingly — *do not grade MEASURED on a checker that has not been written* now also reads *do not assert enforcement by a registry row that does not exist.* **On D-36's trigger, and I am refining it rather than stretching it:** this is a different class from the one D-36 priced. D-36 was authority-vs-prose drift, which is expensive to mechanise. This is *a ruling that never reached an enforcement registry*, and that set is small, enumerable and cheap to check — the ruled phrase restrictions are countable and `G4` already reads both registries. **If I4's sweep returns more than one missing row, I buy the checker for THIS class, scoped to phrase restrictions reaching the registry, and not the general prose problem.** That is a much smaller instrument than the one D-36 declined, and the distinction is the reason I can afford it. |
| D-42 | 2026-08-29 | **I4's three wording questions: two rows APPROVED, one REFUSED. The refusal is the ruling that matters.** | **(1) N-01, APPROVED** as a WORD row on the literal `distinguishes revoked from unknown`. Narrow domain jargon, low collision, and `erp/FACTS.md:761` states the fact it protects — a revoked tool and a never-registered tool are indistinguishable to the client. Residual under-catch is accepted and named: a seat writing *"the browser can tell a revoked tool from an unknown one"* is not caught. **An under-catching row is correct here; an over-catching one would not be.** (2) **S9/Kernel 5, APPROVED on the FRAMING literal `evidence of rarity`, and I4's reasoning is affirmed in full and is the better half of this ask.** Anchoring on `0/420` would ban the numeral, and the numeral is the *honest citation of the measured gap* (0/420 strict per-field provenance, 53% generic audit/ledger) — **a row that forbids the true statement is worse than no row.** The violation is the novelty framing, not the count. I4's classification is also right: this is a WORD row of the BW-05..08 novelty family, not a RETRACTED/RC row, because RETRACTED registers claims this project *asserted and lost the right to assert*, and this is a prohibition on framing that was never asserted. Note the novelty family (BW-05..08, BW-31, BW-32) already carries most of this load, which is why one narrow row is proportionate. **(3) FACTS.md §1(a), the flag/empty-surface restriction — REFUSED. No row is added, and the refusal is deliberate.** There is no literal that separates the false claim from a true one. `produces an empty surface` is a TRUE statement about a page with WebMCP absent — `V1`'s whole finding is that judges may see a page with no tools at all — so that row would ban an accurate sentence about a different subject. The violation is a *flag token adjacent to an empty-surface claim*, and a WORD row is a literal substring with no way to express adjacency. **This is exactly the trap I4 correctly identified in (2), one step further on: in (2) a literal would have banned a true number; here it would ban a true sentence.** I4 spotted its own trap and I am refusing the one it did not. The restriction stands unrelaxed and is enforced by review, not by lint. **It becomes the first entry in a DECLARED-UNENFORCEABLE list, which is the point:** D-41's defect was a restriction whose absence from the registry was *silent*. A declared absence with a ruled reason is not that defect. |
| D-43 | 2026-08-29 | **D-41's trigger has FIRED — I4's sweep returned THREE, not one. I am buying the checker I priced, at the scope I priced it. And BW-33 lands only AFTER the four rewords.** | **The number is 3.** I said in D-41 that more than one missing row buys the checker for this class, so it is bought, and it is scoped exactly as ruled: *for every ruled phrase restriction, either a registry row exists, or a ruled exemption is recorded in this file.* Not the general prose problem D-36 declined — that scope distinction is the whole reason this one is affordable. **Home: the `G4` follow-up already in flight with I4** — not a new node, because a new node must be scheduled and `--check-schedule` asserts 62/62 exactly once, and I am not doing graph surgery at the end of Day 1 for a 0.5 h check. **Cost, stated rather than waved at: 0.5 agent-hours, review overhead 0.05 x 0.5 = 0.025 human-hours, against 0.625 h of spare. 0.600 h remains. Nothing is cut and no rank fires.** The check turns D-42(3) from a hole into a declared exemption, which is the only reason I can refuse a row with a clear conscience. **SEQUENCING, and this is an ordering decision, not a preference — mcp-6d is right and I am ruling it binding: the four rewords land on `main` BEFORE I4's BW-33 row merges.** A registry row that bans a string four tracked files still carry turns every seat's pre-commit red repo-wide the instant it lands. I4 declining to bypass its own gate to commit the row is correct and is affirmed. **One correction to mcp-6d's owner map, and it is the same error it made on `charters/QA.md`: it applied longest-glob, but `conventions.ownership_rule` says a node's `outputs` BEAT the glob.** `evidence/UNKNOWNS.md` is a declared `V6` output and `V6` is PM's — MEASURED by `node tools/check-ownership.mjs --seat PM`, which returns `OK evidence/UNKNOWNS.md [declared output of V6 (owner PM)]` and DENIES the other three. **PM's file is reworded in this commit**, preserving the V6 byte-literal row shape (ends in `MEASURED`, no trailing pipe, pipe count unchanged). The remaining three are `docs/VIDEO-SCRIPT.md` (UX), `evidence/V2.json` (I1) and `kb/pits/F0.md` (L1), dispatched through L1. **`evidence/V2.json` is safe to reword and here is the boundary, because next time it may not be:** the phrase sits in an authored `consequence` field, not in captured measurement bytes. **Raw measured output is NEVER reworded to satisfy a lint** — if a future collision lands on real captured bytes, evidence stands and the linter gains a narrow mechanism by ruling. Editing evidence to make a gate green is the one move that is never available. |
| D-44 | 2026-08-29 | **BW-33 is RE-ANCHORED on the assertion, not the bare fragment. L1's counter-argument is AFFIRMED: a direction to a human producer must name the string. No document rewords, and no exemption mechanism is added.** | L1 raised the strongest case against its own pit rule and escalated it instead of absorbing it, which is the right instinct and the reason this is ruled rather than settled by fiat. **The argument is correct and I am adopting it: a line that says "do not say X" must name X.** The pit rule — *name the rule id, do not quote the string* — is right for a **pit**, whose reader is a seat that can look up an id; it is wrong for `docs/VIDEO-SCRIPT.md`, whose reader is a human writing copy at the moment they are least able to go look something up. **Different artifacts, different readers, different rules**, and a rule that makes a production direction unusable to protect a lint has the priority backwards. **The fix belongs in the ROW's wording, and MEASURED, a separating literal exists** — which is what makes this different from D-42(3), where I refused a row precisely because none did. Tested against the real assertion and all three real prohibition sentences: the bare fragment `on the spot` catches the assertion and **false-positives on 3 of 3** prohibitions; **`changes on the spot` catches the assertion and false-positives on 0 of 3.** BW-33 is therefore re-anchored on a closed, verb-led family: **`changes on the spot`, `updates on the spot`, `appears on the spot`, `refreshes on the spot`, `arrives on the spot`** — each with its own `--selftest` case, the set extended only by ruling. **Consequence: `docs/VIDEO-SCRIPT.md` and `evidence/V2.json` pass AS WRITTEN and are NOT reworded.** The D-43 sequencing constraint is discharged, not by three edits, but by the row no longer being wrong. `evidence/UNKNOWNS.md` (mine) and `kb/pits/F0.md` (L1's, at 7b0c7d3) are already reworded and **stay reworded** — the pit's rework also fixed a staleness (it claimed the phrase was in neither registry, which BW-33 closes) and stands on that merit alone. **THE GENERAL RULE, and this is the fourth instance today so it is systemic, not incidental: a ban pattern must distinguish ASSERTING a claim from FORBIDDING it. Anchor on the claim's predicate, never on its noun phrase.** BW-22 (charter prescribed the literal), the rarity row (a literal `0/420` would ban the honest citation), D-42(3) (no separating literal exists, so no row), and now BW-33. Where a separating literal exists, the row uses it; where none does, there is no row and the restriction is declared unenforceable. Neither case ever contorts the document. |
| D-45 | 2026-08-29 | **L1's D-41 check is a FALSE POSITIVE. The correction IS on `main`. And L1 should not book this as a third failure — its discipline was right and the instrument was ambiguous.** | MEASURED, on `origin/main`: `git show origin/main:erp/graph.json \| grep -o ".\{130\}G4's wording lint carries the banned form.\{130\}"` returns **my own retraction** — *"D-41, 2026-08-29 — THIS SENTENCE PREVIOUSLY ASSERTED that G4's wording lint carries the banned form. IT DID NOT. MEASURED 2026-08-29: a file containing that sentence passes tools/lint-layer0.mjs with exit 0..."* The uncorrected assertion is gone; what `grep -c` counted was the correction quoting the old claim in order to retract it. **This is D-44's rule biting the verification of D-44's own subject matter — the fourth instance today, and the one that proves the pattern is structural.** I am ruling on the self-assessment too, because getting it wrong is expensive: **L1's first two disclosures were real and its standard is right, but this one is not a third.** L1 ran a command, in-session, and reported what it printed — that is exactly what D-38 demands. The failure was the instrument's, not the seat's. **A seat that starts distrusting its own verified checks is worse off than one that never ran them**, and I would rather absorb a false positive than have L1 stop reporting. What is worth carrying is narrower and is now the standard: **to verify a correction, READ THE SENTENCE IN CONTEXT — `grep -o` with a window, or `sed -n`. Never `grep -c` a string you have just written a retraction of.** A count cannot tell assertion from retraction, and a corrected file legitimately contains the words it corrects. **Also correcting my own number, D-38 applying to me:** D-43 recorded I4's sweep as returning **three**; it returned **FOUR** — three needing a wording ruling (D-42) plus one clean and addable with no judgment, **IR-16(b), *"--headless=new enables WebMCP with no flag"*, retracted under R-30 with zero registry coverage. APPROVED as a RETRACTED/RC row**, and no accept edit is needed: `G4.accept` already reads *"at least these five entries"*, so a sixth is admitted by the predicate as written. The trigger fired at more than one either way and the checker stays bought; the count is corrected because it is wrong, not because it changes anything. |
| D-46 | 2026-08-29 | **`V1`'s human gate is QA's. "QA" in an accept is a REVIEW DUTY, not node ownership — and QA's refusal was correct.** | Both documents were right about what they each said. `V1.accept` says verbatim *"HUMAN SECOND: QA re-reads the screenshot against the JSON and a mismatch fails the node"*; QA's charter lists four nodes and `V1` is not among them, because **`V1` is I1's node and always was.** The gap is that a charter's node list enumerates what a seat OWNS and has never enumerated **review duties**, so no document said a seat could be named as a reviewer in another seat's accept. **The authority is not over-reaching and the charter is not under-listing nodes — the charter was under-listing DUTIES.** Ruled: QA reads `evidence/V1.png` against `evidence/V1.json` and a mismatch fails the node; `V1` does not join QA's four. **General rule, now binding: an accept MAY name a seat as a second reader, and any accept that does so must have that duty written into that seat's charter — PM's obligation.** Written into `erp/charters/QA.md`. **QA's refusal is AFFIRMED and I want it on the record as correct behaviour, not friction:** it declined a request from an unfamiliar session name for work outside its listed nodes, which is the same instruction-source discipline I1 and L1 applied on Day 0. Work reaches a seat from L1; a peer message carries no dispatch authority. **Why the second reader must not be the seat that produced or corrected the evidence:** the mechanical gate (ajv against `probe-verdict.schema.json`, plus `test -s evidence/V1.png`) proves the JSON is well-formed, never that it describes the PNG. `evidence/V1.json` was corrected pre-gate from a stale 00:56 localhost reading to the remote transcription — the R-44 provenance class, caught before it became a passed gate — and the seat that made that correction is exactly the seat that must not also certify it. `V1` gates three contingencies and `V2`/`V3`/`V4` are input-blocked behind it, so this is Day 2's first dispatch. |
| D-47 | 2026-08-29 | **The nine-ticket backlog is adjudicated. Split: 6 adopt, 2 debt, 1 send-back — 67% adopt.** | Authored by PM; **L1 transcribes into the `VERDICT`/`VERDICT_NOTE` lines under clause 6b (D-37)**, because `.team/**` is L1's and the checker cannot express per-line ownership. **DEV-001 — adopt.** The pit file was unownable by the seat required to write it; ruled at D-31, authority edited (`PATHS.md` §2.8, `charters/L1.md`, `TEAM.md`), zero rework for I2. **DEV-002 — adopt.** Three collisions of the same class on PM's own commit; ruled at D-37, `L0.outputs` corrected. **DEV-003 — adopt.** `G4.accept` named one exclusion mechanism while the node needs three; clause 1 (sweep exits 0) and clause 2 (fixture exits 1) are the same file with two required outcomes and are unsatisfiable without a swept-versus-named distinction. **The plan was wrong, the work was right** — `G4.accept` now names `EXCLUDE`, `REGISTER_FILES` and `SWEEP_EXCLUDE`, so the next reader cannot repeat the confusion that caused the rejection. **DEV-004 — debt**, node `G0`. Cost: a project-wide verification standard that did not previously exist (D-38), and one figure retracted before it propagated. **Deliberately NOT adopt and deliberately NOT send-back, and the distinction is the ladder doing its job: the DISCLOSURE is what keeps it off send-back; the BREACH is what keeps it off adopt.** Grades MEASURED and VERIFIED already meant "run it" — no plan gap excuses it — but L1 disclosed unprompted, retracted in its own pit, and nothing propagated. Pricing self-disclosure at zero teaches fabrication is free; pricing it as send-back teaches seats not to disclose. **DEV-005 — send-back**, and it is the only one. **(1) Failing clause:** `G5.accept` clause 3 as amended today — the hook must ACTUALLY FIRE; a real `git commit` staging a path the seat may not write must be rejected by git. The original clause proved only that the DIRECTORY was registered and that the script exits correctly **when invoked by hand**; git only ever invokes a hook named `pre-commit`, so all three clauses passed while the product was **inert**. **(2) Rework deadline: Day 2, 09:00 PT.** **(3) Branch: `G5`'s — L1 marks it blocked.** The fix is chaining, not logic: the script is correct and must be invoked from `.githooks/pre-commit` alongside G4's lint. **DEV-006 — adopt, and it is the most valuable ticket in the batch.** `T1.accept` checked the top-level-registration invariant at the one moment it could not fail — T1 runs before any `registerTool` call site exists — with the tool's own vacuity arm (`--selftest`) switched off, and no later node re-invoked it. **The clause is moved to `T2`, where call sites first appear and the invariant becomes falsifiable, with `--selftest` mandatory.** Zero rework for I2 on T1; a vacuous check becomes a real one. **DEV-007 — adopt.** The one-worktree rule that binds every building seat lived only in `charters/L1.md`. Now in `TEAM.md`, binding on all of them. Recovery was a reflog and luck, not a control. **DEV-008 — debt**, node `F0`. Cost: ~1 h of UX agent work destroyed and re-done, no permanent loss. A real cost was incurred, so it is debt and not adopt; `RUNBOOK` already said to re-dispatch seats **at `done`**, so the rule existed. **DEV-009 — adopt.** Enabling `core.hooksPath` switched G4's lint on for every seat at once and blocked QA on a failure QA did not introduce. Nobody's node was wrong; the rollout was, and no document governed rollout. **This is the same defect as the BW-33 blast radius ruled hours later (D-43) — twice in one day is what makes it a rule**, now in `TEAM.md`: a repo-wide gate goes on last, after the repo is already green. **On the split: 67% adopt is healthy and it is evidence W is calibrated, not lenient.** Six tickets moved the authority; the two debts name real costs; the one send-back is the one node whose product did nothing. **Six of the ten are against L1, filed by L1's own instrument about L1.** |
| D-48 | 2026-08-29 | **`evidence/V1.json` is COMPLETED from the screenshot before QA gates it. The built-in browser is Chromium 151, not the installed 152, and that fact currently exists only in a PNG.** | L1 read the screenshot against the JSON and explicitly did **not** perform the gate or merge the node, because D-46 assigns it to QA and that ruling was open. **That is exactly right and is affirmed: producing evidence for a gate is not performing it.** The mechanical gate is green (ajv exit 0, `test -s` exit 0) and four of five required fields corroborate to the millisecond. **The finding that matters is what the JSON leaves out.** `probe-verdict.schema.json` defines optional `chromiumMajor`, `navigatorAliasPresent`, `isSecureContext`, `tools` and `registrationError`; `V1.json` carries only the five required fields — verified by reading both. The PNG shows `chromiumMajor 151`, `navigator.modelContext undefined`, `isSecureContext true`. **The ChatGPT built-in browser is Chromium 151. The installed Chrome `V0` measured is 152. Different engines** — and `V0.notes` says in terms, under R-8, that *"151 is not present on this machine and the previous framing asked about a version nobody can test."* **We now have that version, on the surface that actually decides the submission, and the alias reads `undefined` there too — corroborating HANDOVER §3 rule 1 on a SECOND engine, a stronger result than `V0` alone, currently recorded nowhere but a screenshot.** **Ruled: I1 populates the optional fields from the screenshot already taken, and QA gates the completed file.** Sequencing is deliberate — transcribe first, gate once; the human gate has not run, so nothing is re-opened and `V1.accept` is NOT amended. **This is the OPPOSITE of D-43's prohibition and the distinction must not blur: D-43 forbids editing raw measured output to make a gate GREEN; this records measured facts the file omitted, into fields the schema was built to hold.** **Second, recorded rather than assumed away: `chatgptModel` is the one required field the screenshot CANNOT corroborate.** The page cannot detect which model drives the built-in browser; the string appears only in the copy-block the page renders from a value it was given. QA's read therefore verifies four of five required fields and cannot verify the fifth **by construction** — a limit on what this gate can prove, not a defect in the evidence. It matters because Sol/Terra is a MEASURED constraint that scopes the node (Luna disables WebMCP). **`chatgptModel` is attested by the OPERATOR, not by the page**, and is now written into `V1.notes` so no later reader mistakes it for a verified field. |
| D-49 | 2026-08-29 | **The record of a merge is part of the merge. Same class as W's unpushed branch, and that is twice.** | `.team/log/merges.txt` was FOUR rows behind `graph.state.json` — eleven rows against fifteen done — so any seat auditing from the record undercounted by four; `F0`, `G6`, `H1` and `S1` merged hours ago and their lines were never written. Found by mcp-6d with the measurement attached, corrected by L1 at `e45146f`. **L1's own diagnosis is the ruling: the merge is gated by four mechanical clauses; the RECORD of the merge is gated by nothing.** This is the same defect as W's nine tickets sitting unpushed on a branch while L1 and I both read the main checkout and concluded the instrument was dead — **gated work, ungated record, and in both cases the people reading the record drew a confident wrong conclusion from it.** I drew mine three times, in writing, to the user. **Binding: a merge is not complete until its row is in `merges.txt` and its branch is pushed — the record is a clause of the merge, not bookkeeping after it.** No new checker is bought for this; it belongs in L1's existing merge gate, which already has eight clauses and is the only place every merge passes through. |
| D-50 | 2026-08-29 | **`--smoke-login` and the missing static route are ONE defect with two instances: the CONSUMER specified what the PRODUCER was never told to build. Neither predicate is rewritten and nobody is sent back.** | Verified, not relayed: `--smoke-login` appears in exactly three places (`F1.accept` plus two restatements of it), **zero** times in `H2.accept`, and `grep -c smoke-login harness/drive.mjs` returns **0**. Separately MEASURED: `GET /` returns 404 while `GET /api/login` returns 200 with the cookie — `server/index.mjs` serves no static files. **Nobody did anything wrong in either case. H2 is green on all six clauses and S1 does exactly what its accept required.** (1) **The mode is specified in the PRODUCER: a 0.5 h follow-up on `harness/drive.mjs`, owner I1.** Inventing it into `F1.accept` would be PM writing a spec into the consumer for the second time, which is precisely how the defect was created. **`F1.accept` is NOT rewritten and NOT weakened** — a predicate that cannot run is broken, not strict, and shrinking F1 to fit what exists is the silent descope my charter forbids. (2) **The missing edge is ADDED: `H2 → F1`, hard.** `F1.inputs` was `[S1]` only, so nothing crossed and **both checkers were correct and blind** — `--check-schedule` had no edge to order and `--check-accept-paths` happily resolved `harness/drive.mjs` as a declared H2 output. **This is the same shape as the three edges v2.0.0 had to add for `T2`**, and it is the second time an unstated dependency hid behind two green instruments. (3) **The static route is a SCOPE ADDITION to S1, 0.5 h, owner I3 — not a send-back**, and the distinction from `G5` (DEV-005) is exact: G5's product was **inert**, S1's product **works and its accept was incomplete**. And it is not scope I invented: the graph's own edge contracts already say so — `S1 → T2` reads *"server/index.mjs — there is no server to serve the page under test"* and `F1 → D1` reads *"src/page/index.html — something to serve"*. I am matching the accept to a contract the authority already carries. **THIS ONE IS THE MOST URGENT ITEM ON THE BOARD: it blocks `T2`, which is ON THE CRITICAL PATH (`S4 → T2 → H3`), as well as `F1` and `D1`.** **Cost: 1.0 agent-hour total, review overhead 0.05 × 1.0 = 0.05 h, against 0.600 h spare after D-43. 0.550 h remains. Nothing is cut and no rank fires.** **Edge accounting, re-MEASURED and flagged because it will mislead a reader: the totals are 121/110/102 again — the SAME numbers published before D-30 — by coincidence, not reversion.** D-30 deleted `G1 → G3` (121→120), D-50 adds `H2 → F1` (120→121). Two different edges, one net; `inputs`↔`edges` bijective 121/121; D-33's correction stands. Restatements updated in `README.md`, `GRAPH.md`, `PLAN.md`, `PROPOSAL-workflow-tools.md`, and appended to `FACTS.md` under its own append-only rule. **`--check-tables` caught my own edge addition** — `FAIL erp/GRAPH.md:520 F1.inputs table [S1] graph.json [H2,S1]` — which is R-22 working on the graph owner, and is the answer to anyone who thinks the restatement checker is bureaucracy. |
| D-51 | 2026-08-29 | **UX's generalisation of D-40 is ADOPTED, and UX was right to decline L1's counter-argument.** | L1 asked whether a cue sheet read by a human under time pressure earns an exception to D-40. **UX's answer is better than the question: the objection was against the SHAPE L1 proposed, not against D-40.** A bare pointer to a registry row is a pointer with no payload — but D-40 never said the id is all you may write. **The rule, now binding wherever D-40 forces a literal out: replace it with the RULE THE LITERAL INSTANTIATES, never with a bare pointer.** Lead with the positive instruction, complete and usable on its own; then name the semantic class, so the lookup is optional rather than load-bearing. UX also observed that L1's own `kb/pits/F0.md` reword already did this instinctively, and L1 asked for it to be written down rather than left as a habit two seats happen to share — which is the right instinct and is why this row exists. Note this sits alongside, not against, **D-44**: where a ban pattern can be re-anchored on the claim's predicate, the document does not have to change at all, and `docs/VIDEO-SCRIPT.md` passes as written. D-51 governs the cases where a rewrite is genuinely required. **Two seats reached the same rule independently and a third asked for it to be recorded; that is the strongest evidence a convention is real rather than imposed.** |
| D-52 | 2026-08-29 | **The user rules a ONE-TIME EXEMPTION for Days 5 and 6. D-17's 3.0 h/day stands as the norm; Day 5 is 4.0 h and Day 6 is 4.5 h as a ruled exception. `D4` keeps its full scope. THIS CLOSES THE BUDGET HALF OF MY ASK AND NOT THE WALL-CLOCK HALF.** | **Provenance, stated honestly and graded lower than D-17's: RELAYED to PM by session `mcp-6d`, not heard by PM directly.** D-17 is graded *"RULED by the user, directly, before any seat was dispatched"*; this row is **RULED by the user, RELAYED — and CONFIRMED DIRECTLY 2026-08-29 ("确认无误", the recorded content put back to them verbatim), so this row is RE-GRADED to DIRECTLY-RULED and the caution below is discharged**. I am recording and acting on it because it matches what the arithmetic recommended and nothing about it is destructive — but per R-44, the provenance class this project keeps rediscovering, It was flagged for confirmation and the user confirmed it verbatim. **Any seat may now cite this row as directly ruled.** The provenance ladder worked exactly as R-44 intends: recorded at the lower grade, acted on, flagged, confirmed, re-graded. **The ruling itself is accepted and D-32 is superseded on its first half only:** the levers I offered were the user's attention or shortening `D4`, and the user chose neither by choosing to absorb the overage. `D4` keeps 4.0 h, `D4.accept` is unchanged, no node's scope moves, total accepted overage 2.5 h across the final two days. **THE HALF THIS DOES NOT CLOSE, AND IT IS THE ONE I RAISED SECOND BECAUSE IT IS THE HARDER ONE: Day 6 is short on WALL CLOCK, not only on budget.** MEASURED from the authority just now: `G1 → D5 → D6` is a **hard chain** (`G1→D5` contract *"the public repo URL"*, `D5→D6` contract *"the submitted Devpost entry"*), so its 0.5 + 2.0 + 2.0 = **4.5 hours are strictly sequential and cannot be parallelised**, against a window from the 09:00 PT freeze to the 13:00 PT deadline of **4.0 hours** (`RISK.md` §7.4). **An exemption grants HOURS. It cannot grant CLOCK.** 4.5 h of sequential work does not fit in 4.0 h of wall time however the budget is ruled. **AND THE FIX COSTS NOTHING, which is why this is a correction rather than an escalation: the 09:00 freeze is a CEILING ON CODE CHANGES, not a START TIME for submission work.** `RISK.md:1028` says only that *"Only `D4`'s tail, `D5` and `D6` may run after it"* — nothing forbids them running **before** it, and `G1` (flip two repos public) touches no code and needs no freeze at all. **Starting at 08:00 PT gives a 5.0 h window for 4.5 h of work with 0.5 h of slack, and requires no exemption, no scope change and no ruling from anyone.** That is the whole remaining ask and it is now one line: **begin Day 6 at 08:00 PT, not 09:00.** Recording it here on Day 1 rather than discovering it at 09:05 on Day 6, when `D5` is the only thing left and there is no scope left to cut. |
| D-53 | 2026-08-29 | **08:00 start APPROVED. Readiness-triggering ADOPTED for the tail. `D4` and `G1` may pull earlier freely. `D5` — the SUBMIT — may not, until one named unknown is closed.** | The user asked the sharpest question of the day: *"不可能提前吗？感觉现在已经到day 2的工作了"* — can the tail pull earlier, given the machine line is running ahead of the calendar. **The ORDER argument is correct and I have verified it rather than accepted it: `capacity.schedule_A` assigns ORDINAL day numbers and `--check-schedule` proves `day(u) <= day(v)` on every hard edge — it proves ORDER, never DATES.** Shifting the whole tail earlier changes the calendar mapping and not one day number, so every instrument stays green and every invariant holds: `G1` stays immediately ahead of `D5` (R-42/D-30 pins its POSITION in the sequence, not its date, and an early tail moves public-flip and submission together — which also SHORTENS the public exposure window D-30 was protecting, so it is strictly more consistent with that ruling, not less). **ADOPTED: L1 triggers the human windows on READINESS SIGNALS, not on dates, and gives the user one day's notice through the relay so they can plan. `D4` and `G1` pull earlier with no further ruling.** **`D5` IS DIFFERENT AND IT IS THE ONE THING I WILL NOT WAVE THROUGH: SUBMISSION IS THE ONLY IRREVERSIBLE ACT IN THE GRAPH.** Everything else in this sprint can be re-run, re-shot, re-merged or reverted. **The authority records NOTHING about whether a Devpost entry can be edited after it is submitted** — I searched `FACTS.md` and `RISK.md` and there is no such fact at any grade, so I will not assert one in either direction. **The whole trade turns on it.** If an entry CAN be revised before the deadline, submitting on Sep 1 is strictly better: the entry is banked, disqualification risk collapses, and Sep 2–3 becomes real improvement time. If it CANNOT, submitting on Sep 1 **converts two days of buffer into forfeited improvement time** — the worst version of this trade, taken for a benefit we would not have needed. **NARROWED, because the risk is smaller than it first looks and precision matters here: `D6.accept` verifies only ASSETS the entry POINTS AT — the repo page logged out, the MIT licence, the video URL returning 200 with an audio stream, the live URL and its banner. Every one of those stays fixable after submission.** The irreversibility is confined to the Devpost entry's own four answers and fields. **ACTION, and it is cheap: I4 owns `D5` and submission compliance — I4 reads the contest rules and reports whether a submitted entry can be edited before the deadline, as a PUBLISHED fact with a date.** Until that lands, `D5` holds its position relative to the deadline while everything ahead of it pulls earlier — which costs nothing, because pulling `D4` earlier is where almost all of the benefit is. **The 08:00 PT start is APPROVED and stands as the rule for whichever calendar day the tail lands on**, so the wall-clock fix survives the reschedule rather than being an artifact of Sep 3. |
| D-54 | 2026-08-29 | **L1 is right: the NOTICE is now the long pole on the tail. Fire it on ANTICIPATED readiness, not achieved readiness.** | Ruled, and it is L1's judgement adopted rather than a change to D-53. If `D4` becomes ready this evening, one day's notice means the earliest useful human window is tomorrow **regardless of readiness** — so readiness-triggering buys nothing unless the trigger fires ahead of the signal it waits for. **The asymmetry decides it, and it is not close: a notice fired too early costs the user a planned half-day that slips by a day. A notice fired too late costs a READY human-gated node with no human**, on a cut-0 node that is 27% of the entire human budget with zero tolerance for a re-shoot. Warn early. **Concrete trigger, so this is checkable rather than a disposition: `D4.inputs` are `H6`, `D1`, `F0` — `F0` is done. L1 fires the "your window opens tomorrow" call the moment `H6` and `D1` are BOTH in flight with no named blocker**, not when they merge. The same shape applies to `D5`/`D6` on the final day. A false warning is a cheap apology; a late one is the failure this whole schedule exists to prevent. |
| D-55 | 2026-08-29 | **Four seats finished and went idle without reporting today. The fix is the SWEEP, not a louder instruction to report — a process that has ended cannot report.** | I4, I1, QA and I3 all completed work that then sat on branches with no signal to L1; QA's tripwire flip and I3's `S4` were both found by the D-49 branch sweep rather than by a report. **L1's framing is exactly right and worth preserving: the gap is not seats failing to WORK, it is seats finishing without a REPORT.** **The structural point, which is why "tell seats to report harder" is the wrong ruling: a seat that finishes and goes idle is a process that has ENDED. You cannot rely on a report from something that is no longer running.** Push-based reporting fails precisely at completion, which is the one moment the report matters. **The control must be PULL-based, and it already exists: the D-49 branch sweep becomes a standing clause of the dispatch loop, run after every merge alongside `ready.mjs`, not an incident response.** D-49 was merged an hour before it caught two real cases, which is the strongest evidence available that it was the right rule. **This is the same family as D-49 and the stale-read failures in D-56: gated work, ungated record, and a reader drawing a confident conclusion from a gap.** No new instrument is bought — `git branch --merged` and `ready.mjs` are both one command and both current. |
| D-56 | 2026-08-29 | **PM's own rule, and the third instance of the day's most persistent failure: RECOMPUTE IN THE TURN YOU PUBLISH.** | I closed a report to the USER with *"the critical path has not advanced since `S10`"* and *"`T2` is blocked on `S1`'s static route"*. **Both were false when I wrote them.** Re-measured with `node tools/ready.mjs` against `erp/graph.state.json`: **19 nodes done, BURNED 14.0 / 29.5, head is `T2` — dispatched, not blocked.** `S1`'s static route, `S4` and `F1` all merged while my message was in flight; the path advanced three nodes and 7.0 h. I stated it as the thing outranking everything else, which is the worst sentence in a report to be wrong about, because it is the one the reader acts on. **My charter said the ready set is recomputed "every morning". That is not strong enough and never was.** It is now: `READY`, `BLOCKED` and `BURNED` come from a run of `ready.mjs` **in the same turn that publishes them** — never from a teammate's message, never from my own earlier block, never from a checkout merged an hour ago. **A long turn is exactly when this bites, because the world moves inside it, and the five-line block is the one artifact the human reads as current by construction.** **Three instances today, one shape: "W has filed nothing" against a ten-ticket register; L1's `grep -c` against my own retraction; and this. The register was fine every time and the reader was stale every time.** L1 flagged it rather than letting it pass to the user uncorrected, which is the second time today it has caught a claim of mine before it did damage. |
| D-57 | 2026-08-29 | **The Devpost finding is PUBLISHED and it points the right way. The D5 hold is LIFTED CONDITIONAL on a UI check — not on the rules text alone. And "submit a throwaway entry" is REFUSED as the way to make that check.** | **Finding, PUBLISHED, recorded with its source because a paraphrase would lose the load-bearing clause:** `https://webmcp.devpost.com/rules` §6 *"Submission Modifications"*, accessed 2026-08-29, two independent fetches with identical wording — *"Prior to the end of the Submission Period, you may save draft versions of your submission on Devpost to your portfolio before submitting the Submission materials to the Hackathon for evaluation. Once the Submission Period has ended, you may not make any changes or alterations to your Submission, but you may continue to update the Project in your Devpost portfolio."* The only stated restriction is anchored to **"once the Submission Period has ended"** — 2026-09-03 13:00 PT — **not to the act of submitting.** §6 names no separate lock at submit-time. Also recorded so nobody re-derives it: §6's one narrow post-period exception is sponsor-permitted fixes for IP infringement, PII exposure or inappropriate material, and even then *"the modified Submission must remain substantively the same as the original."* **I4's caveat is correct and I am ruling WITH it, not around it: the rules do not establish that Devpost's UI actually offers a re-edit control on an already-submitted entry, as opposed to only pre-submit drafts.** **THIS IS THE DAY'S PATTERN A FOURTH TIME, AND IT IS NOW A GRADING RULE: PERMITTED IS NOT POSSIBLE. A rule that ALLOWS something is not evidence that the MECHANISM exists.** Sibling to D-33 (*do not grade MEASURED on a checker that has not been written*) and D-41 (*do not assert enforcement by a registry row that does not exist*). Today: the authority claimed enforcement by a missing registry row; `G5`'s three clauses passed while git never invoked the hook; `T1`'s clause passed on zero call sites; and now a rule permits an edit whose control is unverified. **In every case the rule was right and the mechanism was absent — and this is the only one where finding out late is irreversible.** **RULED: (1) Everything ahead of `D5` pulls earlier NOW, per D-53 — that is where nearly all the benefit was. (2) `D5` moves the moment someone confirms the edit control EXISTS post-submit. (3) THE CHECK IS DOCS-FIRST: Devpost's own help documentation, which touches no account and costs nothing. (4) I REFUSE the throwaway-submission route and it is not the user's call to make casually — submitting a decoy entry to a LIVE hackathon is an outward-facing act against the real contest, risks reading as a duplicate or spam entry under rules we have not read, and would be taken on the account that carries our actual submission. We do not de-risk an irreversible act with a different irreversible act.** (5) **If the UI question never resolves, the hold simply stands and costs us NOTHING** — `D5` runs at its scheduled position with the full 08:00 window (D-52/D-53) and we lose only an insurance policy we were never counting on. **A note on how this answer arrived, because it is the counter-example to the day's standing failure:** I4 answered in the vocabulary asked for, QUOTED the sentence rather than paraphrasing, verified with two independent fetches, drew the boundary of its own finding unprompted, and produced a report rather than a file where a file was not warranted. That is the D-38 standard met without being reminded of it. |
| D-58 | 2026-08-29 | **`--assert-flips` goes to I1 as an H2 follow-up, NOT to H3 — and the reason is a DEADLOCK, not a preference. T2 is not sent back. The missing `H2 → T2` edge is added.** | Verified independently: `grep -o '--[a-z][a-z0-9-]*' harness/drive.mjs` lists `--gate --list --exec --smoke-login --scenario --url --input --headed --headless --enable-features`. **`--assert-flips` is absent; `--smoke-login` is PRESENT, so D-50's follow-up shipped and worked.** T2's spans 1 and 2 are green. **T2 is NOT sent back and its accept is NOT rewritten**: I2 delivered its own scope and the failing span names a mode of another node's artifact — the same disposition as `F1` under D-50, for the same reason. **`erp/PROPOSAL-workflow-tools.md:285` assigns `--assert-flips` to `H3` as "0 h, text only". THAT ASSIGNMENT IS REJECTED, and a proposal document is not an authority.** MEASURED, and it is decisive rather than a matter of taste: **`H3.inputs` is `[T2]`. H3 runs AFTER T2.** Putting a mode that T2's accept depends on into H3 makes **T2's span 0 unsatisfiable forever** — a deadlock across the two head nodes of the remaining critical path, shipped as a scheduling note in a proposal nobody re-reads. `harness/drive.mjs` is `H2`'s declared output; a mode of it belongs to H2's producer, which is exactly why the `--smoke-login` follow-up landed there and succeeded. **Ruled: 1.0 agent-hour to I1, bundled into the same file it just touched.** **THIS BLOCKS `T2`, THE HEAD OF THE CRITICAL PATH, with `H3` immediately behind it — highest-urgency item on the board.** **Edge added: `H2 → T2`, hard.** `T2.inputs` was `[T1,H1,F1,S1,S4]` with no `H2`, so nothing crossed — **the second missing edge of exactly this shape today**, after `H2 → F1`. Re-MEASURED: 122 edges, 111 hard, 103 qualifying, 8 exempt, 0 violations, bijective 122/122; restatements and `FACTS.md` updated. **`--check-tables` again caught my own edge addition** (`GRAPH.md:347 T2.inputs`), which is now twice in one evening. |
| D-59 | 2026-08-29 | **The fourth register is real and I am BUYING the instrument. Three instances in one day, one on the critical-path head, is past any threshold I set.** | L1 recorded the pattern rather than proposing a fix, which was the right call and leaves the pricing to me. **The register: nothing verifies that a MODE, FLAG or SUBCOMMAND named in an accept predicate is produced by anything.** `--check-accept-paths` resolves `harness/drive.mjs` happily — **the PATH exists; the MODE is not a path.** `--check-schedule` had no edge to order because the dependency was never drawn. Both instruments were correct and blind, twice. **D-36 declined the prose register because its corpus is unbounded. D-41 refined the trigger for a small enumerable class. This class is small, enumerable and purely mechanical — every `--flag` token adjacent to a repo-relative script path in an accept — so it is priced like D-41's, not like D-36's, and its trigger fired three times over.** **BOUGHT: `node tools/ready.mjs --check-modes`, a G0 follow-up, owner L1, 1.0 agent-hour.** Definition, so it is buildable without a second ruling: for every `(script-path, --flag)` pair extracted from an accept — **(a)** if the script exists on disk, the flag literal must appear in it; **(b)** if it does not yet exist, the node declaring it as an output must be a hard input of the node whose accept names it. **Stated honestly, it catches TWO of today's three instances and not the third:** `--smoke-login` and `--assert-flips` are flags; `S1`'s missing static route was a CAPABILITY described in prose, and no flag-based checker reaches it. **I am buying an instrument that closes two thirds of a class, and saying so, rather than claiming it closes the class.** The third kind was caught by a human reading an edge contract against a running server, and nothing cheap replaces that. **Cost: 1.0 agent-hour, review overhead 0.05 h, plus D-58's 1.0 h at 0.05 h — against 0.550 h spare. 0.450 h remains. Nothing is cut and no rank fires.** |
| D-60 | 2026-08-29 | **D-57's condition is MET on both halves. The `D5` hold is LIFTED — `D5` may pull earlier with the tail. But `D6` does NOT move with it, and that is a new constraint early submission creates.** | **PUBLISHED, second source, obtained the way D-57 specified and by the route it approved:** `https://help.devpost.com/article/123-how-to-edit-a-submission`, Devpost Help Center, accessed 2026-08-29, cross-checked between a search snippet and a direct fetch — *"You can access and make changes to your submission until the submission deadline. Once the deadline passes, you can still make changes to your project but these changes will not be reflected in your hackathon submission, only in the project within your portfolio."* The UI affordance is named, not assumed: "Manage projects" / "Edit project", or "My projects" → the project. **So the RULE permits it (§6) and the PRODUCT provides a working control for it, both to 2026-09-03 13:00 PT — and the two sources agree on the cutoff without either being inferred from the other.** The "permitted is not possible" gap that D-57 refused to close by assumption is closed on its own terms. **No live-account action was taken and the throwaway-submission route stayed refused.** **HOLD LIFTED: `D5` joins the readiness-triggered tail under D-53.** **THE NEW CONSTRAINT, and it is created BY the lift rather than resolved by it: `D6` must NOT follow `D5` immediately any more.** `D6` is the freeze rehearsal — it verifies the PUBLIC state a judge sees: repo page logged out, MIT licence, video URL 200 with an audio stream, live URL and banner. **If `D5` submits on Sep 1 and we then keep improving through Sep 3 — which is the entire point of submitting early — then a `D6` run on Sep 1 verifies a state we subsequently change, and certifies nothing.** The `D5 → D6` edge is hard, which constrains ORDER and never adjacency, and `--check-schedule` only asserts `day(D5) <= day(D6)`. **Ruled: `D5` pulls earlier as readiness allows; `D6` stays at the END, as the final-state check, and is the last thing that runs before the deadline.** That strictly increases safety over the original plan, where `D6` ran once immediately after a submission nothing could follow. **If the tail pulls earlier, the sequence is: `G1` → `D5` (early, banked) → continued improvement → `D6` (last, against the final public state).** Recording it now because it is exactly the kind of thing that would otherwise be discovered by running `D6` on Sep 1, passing it, and shipping a Sep 3 state nobody re-checked. **On provenance: L1 explicitly declined to treat my condition as satisfied by its own reading of a peer's report, and said so. That is correct — I set the condition and the lift is mine — and it is the D-52 provenance ladder applied by a seat to itself without being asked.** |
| D-61 | 2026-08-29 | **`--check-modes` found two more defects on its FIRST RUN, hours after I bought it. Both ruled: `--fallback` joins I1's bundle, `E1 → E5` is drawn. And the gap L1 raised is real — `D6` gains the clause that closes it.** | **The instrument paid for itself before it was a day old, which is the answer to whether D-59 was worth 1.0 h.** Verified by running it myself: 31 (script, flag) pairs extracted, 22 resolved against a script on disk carrying the literal, 6 deferred to a producer that is a hard input or the node's own output, 3 violations. **(1) FOURTH instance of the consumer-specifies-the-producer defect: `H3.accept` names `harness/drive.mjs --fallback` and the flag is absent.** `H3` is the node immediately BEHIND `T2` on the critical path, so left alone this surfaced tomorrow as a blocked head node with `H3` already dispatched and less slack than tonight. **Ruled to I1, bundled with `--assert-flips` (D-58) and `--smoke-login` (D-50, shipped) — three modes, one file, 1.5 agent-hours total.** Edge `H2 → H3` added. **(2) THIRD missing edge of the shape that hid `H2 → F1` and `H2 → T2`: `E5.accept` names `webmcp-eval-kit/bin/eval.mjs`, which `E1` declares, and `E5.inputs` was `[T5]` only. `E2` and `E3` name the same script and BOTH carry their `E1` hard edge — so `E5` was the odd one out rather than the pattern, which is precisely why no human reading found it and a checker comparing sibling nodes did.** Edge `E1 → E5` added. **Re-MEASURED: 124 edges, 113 hard, 105 qualifying, 8 exempt, 0 violations, bijective 124/124. FOUR missing hard edges of one shape drawn in a single evening; three of the four point at `harness/drive.mjs`.** **(3) L1's raised gap is REAL and I am ruling it rather than noting it: nothing owned re-checking the submission's OWN four answers after an early submit.** Every clause of `D6` verifies an ASSET THE ENTRY POINTS AT — repo page, licence, video URL, live URL. **That gap was harmless while submission was the last act. D-60 made it the gap of the whole plan, because the entry can now be banked on day one and diverge from `docs/DEVPOST.md` for two days while every existing check stays green.** **`D5` gains an output, `evidence/D5-entry-sync.txt`, recording the sha256 of `docs/DEVPOST.md` as last pasted into the live entry; `D6` gains a clause asserting that sha equals the current one.** Change `DEVPOST.md` after the entry was last updated and they diverge and `D6` fails. **Deliberately a SHA COMPARISON and NOT a human confirmation: "the operator confirms the answers still match" is a human judgement standing as an acceptance predicate, which is the exact defect `D4` was rewritten to remove.** L1 raised this rather than assuming it, one round after raising the pattern that bought the instrument, and it is the last gap of the day found the way the first one was — by someone asking what nothing owns. |
| D-62 | 2026-08-29 | **The mount is an F1 follow-up to UX, merged TOGETHER with T2. `index.html` is NOT added to `T2.outputs`. And the diagnosis is sharper than "a missing mount": THE EDGE WAS ALWAYS THERE AND THE CONTRACT WAS NOT ENOUGH.** | Verified rather than accepted, and it changes the finding: **`F1 → T2` EXISTS, hard, and its contract already names `src/page/index.html`** — *"there is no page to register tools on until F1 exists."* So this is **not** a missing edge, unlike the four drawn earlier tonight. The edge is drawn, the contract names the right file, **and it still never said what must be TRUE of that file: that `index.html` must LOAD `register.js`.** It loads `./ui/shell.js` and nothing else; `register.js` is named only in comments in both files. **The file is correct and is never executed. THAT IS THE SIGNATURE OF THIS FIFTH INSTANCE AND IT IS ITS OWN CLASS: all 124 edge contracts name a FILE and then describe the requirement in PROSE, and the prose is unchecked by anything.** **Ruled: (1) the mount is an F1 FOLLOW-UP, 0.25 agent-hours, owner UX** — `index.html` or `ui/shell.js`, which is the same file one step over and also F1's. **(2) `index.html` is NOT added to `T2.outputs`, and I am rejecting that shape explicitly: two nodes declaring the same output would give two seats write access to one file and destroy the determinism the whole ownership rule exists for.** It would also be the fifth time today the fix for consumer-specifies-producer was to do it again. **(3) F1 IS NOT REOPENED, REJECTED, OR AT FAULT — and this matters because it would be easy to read as UX's oversight. `register.js` is a T2 output and DID NOT EXIST when F1 ran; a script tag pointing at it would have 404'd. F1 could not have mounted it. It is an ordering artifact of a mount point no node owned.** **(4) The follow-up and T2 MERGE TOGETHER AS ONE GATE — neither is valid alone**, exactly as L1 already merged F1's spans with QA's tripwire flip; the precedent is L1's own and it is the right one. **(5) The `F1 → T2` contract is strengthened to state the requirement, not just the filename.** **T2 is not sent back: I2's declared scope is green, DEV-006 is vindicated on it, and `T2.outputs` is `[src/page/register.js]` — I2 owns not one byte of the page its accept requires an effect on.** **Credit where it decided the hour: I1's `--assert-flips` diagnostic distinguished BY NAME between "the producer has not landed" and "the producer landed and the effect is absent."** Without that line this was an hour of bisecting. A mode bought to unblock a node paid for itself again as a diagnosis. |
| D-63 | 2026-08-29 | **The capability-in-prose class has now cost TWO nodes. I am buying HALF an instrument for it and naming which half — `--check-orphans`. L1 was right that no cheap instrument closes the class; it is wrong that none closes a PIECE of it.** | L1 recorded the pattern, declined to propose an instrument, and said plainly it did not believe a cheap one exists rather than inventing one. **That was the right disclosure and it is why I can price this honestly instead of optimistically.** The class — a capability described in prose that nothing verifies — has cost `S1`'s missing static route and now `T2`'s unexecuted `register.js`, and both blocked the head of the critical path. **I priced `--check-modes` at "two of three" and said so; the same discipline applies here and gives a different answer: I can close ONE of these two, mechanically and cheaply, and not the other.** **BOUGHT: `node tools/ready.mjs --check-orphans`, a G0 follow-up, owner L1, 1.0 agent-hour, capped — if it cannot be done in the hour, report back rather than expand it.** Definition: **every declared node output under `src/**` must be REFERENCED from a non-comment context** — an `import`/`require`, a `<script src>`, or an entry point named in `package.json` scripts or in an accept predicate. Comments are stripped before matching, **which is the whole trick and the reason a naive grep would have passed this one: `register.js` IS mentioned in both `index.html` and `shell.js`, in comments, and a checker that counted mentions would have called it mounted.** **STATED LIMIT, and it is the same shape as D-59's: this catches AN ARTIFACT THAT IS NEVER REFERENCED. It does NOT catch a CAPABILITY THAT WAS NEVER BUILT — `S1`'s static route was not an orphaned file, it was a missing behaviour in a file that exists and is loaded, and nothing cheap reaches that. One of the two, not both.** **Cost: 1.0 agent-hour plus D-62's 0.25, review overhead 0.0625 h against 0.450 h spare. 0.387 h remains. Nothing is cut and no rank fires.** **Running total of instruments bought today: `--check-restrictions` (D-43), `--check-modes` (D-59), `--check-orphans` (D-63) — 2.5 agent-hours against a class of defect that has now blocked the critical-path head three separate times. The honest note is the one from D-61 restated: I bought each one an instance later than I should have.** |
| D-64 | 2026-08-29 | **L1 is right: I am behind again, and this is the sixth. Finding 2 is NOT a definition gap — it is a live defect that FAILS `D6` ON THE MORNING OF THE DEADLINE. The same follow-up mounts two files. `src/scenarios.js` is deleted.** | **The sixth instance was invited by my own words and L1 used them exactly as instructed. It is the correct reading and I am not softening it: three instruments, each bought an instance late, and the definition of the third was mine and was wrong.** I wrote *"an import/require"* into D-63 without distinguishing a PAGE mount from a TEST import. L1 implemented what I ruled; the ruling had the hole. **Verified myself, and it is worse than reported: `src/page/index.html` has exactly ONE script tag — `./ui/shell.js`, line 159 — and `shell.js` imports neither file. `src/page/register.js` (T2/I2) and `src/page/env-banner.js` (H5/I1) are BOTH named in `index.html` only in comments, and BOTH never execute. Line 31 is a comment mapping `#env-banner` to the file, line 54 is CSS, line 92 is the div carrying the literal placeholder `environment — not yet checked`.** **THE CONSEQUENCE IS NOT ABSTRACT: `D6.accept` requires *"the live URL returns 200 and the first screen shows the env banner."* Unmounted, a judge sees the placeholder forever and D6 fails on the morning of the deadline — the one node with no room behind it.** **Ruled: the D-62 UX follow-up MOUNTS BOTH FILES. Same file, same edit, same 0.25 h — do not dispatch UX twice.** The env-banner half may land immediately (`env-banner.js` is already on main); the `register.js` half merges with T2 as one gate per D-62; the commit split is L1's call as merge mechanics. **`H5` is NOT sent back and is NOT at fault**, on exactly the reasoning that protected T2 and F1: its accept ran its test and passed, and nothing anywhere asked whether the page loads it. **The register.js shape a second time, in the same file, from a different node, hidden behind a test import — and the only existing check that would have caught it runs on the deadline.** **`src/scenarios.js`: DELETED, and removed from `L0.outputs`.** It is referenced by nothing in the seven-directory corpus, absent from `package.json`, named by no accept, and **no node plans to use it** — the only other graph mention of "scenarios" is `H3`'s `harness/scenarios/happy.json`, a different file. L0 ported the spike wholesale and this came with it. Dead weight in a repository judges may read. **If L1 finds a real consumer before deleting, report back rather than delete.** |
| D-65 | 2026-08-29 | **L1's narrowing is BOUGHT, capped, and it is the question the class actually asks: PAGE-MOUNTED, not merely REFERENCED.** | L1 proposed it, said it might fit the hour, asked for the same cap, and did not build it without a ruling — for the third time tonight. **BOUGHT: `--check-orphans` gains a page-reachability arm, G0 follow-up, owner L1, 1.0 agent-hour, CAPPED — if it does not fit, report back rather than expand.** Definition, and it is narrower than D-63's on purpose: **every declared node output under `src/page/**` must be reachable from `src/page/index.html` through NON-COMMENT imports and script tags, TRANSITIVELY. A test import does not satisfy it.** That is the difference between "something references this file" and "the page a judge opens executes this file", and the whole class lives in that gap. **This closes both known instances — `register.js` and `env-banner.js` — where D-63's arm closed one and let the other through on a technicality of my own drafting.** It still does not reach `S1`'s missing static route, which was a capability absent from a file that exists and loads; **that remains unmeasured and I am not pretending otherwise.** **A note on how the tool was built, because it is the counter-example to my own record tonight: L1 ran the checker before shipping it and found its own corpus wrong — `src/samples.js` and `src/page/env-banner.js` reported as orphans because `tests/` was not walked. A checker INVENTING violations is the failure its conservative-stripper rule was written against, arriving through the corpus rather than the parser. It also wrote seven stripper cases, discovered ONE OF ITS OWN TESTS WAS WRONG, and fixed the test rather than the tool.** Corpus 20 files → 45. **Cost: 1.0 agent-hour, review overhead 0.05 h against 0.387 h spare. 0.337 h remains. Nothing is cut.** |
| D-66 | 2026-08-29 | **Seventh instance. The page tree has been UNSERVABLE since T1 landed and every gate passed. Fix: widen the served root to `src/`, keep the guard. Re-homing and duplication are both REJECTED, and the reason is architectural.** | Verified: `src/page/register.js:62` imports `../erp.js`; `src/page/tools/compile.js:30` imports `../../erp.js`; `src/page/tools/defs.js:23` imports `../../policy.js`. All three reach above a page root served at `src/page/`, so `GET /erp.js` is 404, the module graph dies at the first upward import, and nothing registers. **L1 asked whose fix it is and said it was genuinely unclear. Measuring the importers settles it: `src/policy.js` and `src/erp.js` are imported by `server/envelope.mjs`, `server/routes/policy.mjs`, `tests/`, `harness/drive.mjs` AND the page.** **RE-HOMING under `src/page/` and DUPLICATION are both rejected on architecture, not convenience: ONE policy engine shared by server and client is the point of this project — the server re-checks at commit against the same rules the page compiled — so a second copy is a second source of `POLICY_VERSION` and canonicalisation truth and puts the whole S lane at risk.** A build step is unbudgeted and rejected. **RULED: widen the served root from `src/page/` to `src/`, keeping the traversal guard intact at the new root, and route `GET /` to `page/index.html`. 0.25 agent-hours, owner I3, S1 scope.** **The objection L1 raised — that widening weakens the guard — does not survive one fact: `G1` makes this repository PUBLIC under MIT at submission. There is nothing in `src/` to conceal. The guard exists to stop ESCAPING the served tree, not to hide source, and `server/` is a SIBLING of `src/` and stays unreachable at the new root** — to be confirmed by the guard's existing test rather than assumed. Every relative import resolves one segment up unchanged. **`T1` is NOT sent back and its imports are NOT edited: the files exist, the paths resolve, and T1's tests import them from Node where they resolve fine. What was absent was a serving topology able to deliver them — S1's scope, not I2's.** Seventh instance, seventh time the fix belongs in the producer. **L1 refused four tempting shortcuts, and the fourth is the one worth naming: it did not move its OWN L0 outputs to make a downstream node pass.** |
| D-67 | 2026-08-29 | **L1's correction to D-65 is ACCEPTED BEFORE THE BUILD, which is the only useful time — and my draft would have PASSED tonight's defect.** | I drafted the arm as *"reachable from `index.html` through non-comment imports and script tags, transitively."* **Walk that on disk and every hop resolves: `index.html → register.js` ✓, `register.js → ../erp.js` ✓ BECAUSE THE FILE EXISTS. The check passes and the browser 404s.** The class is not "is this referenced" and not "is it page-reachable" — **it is "does the page's module graph resolve UNDER THE SERVING TOPOLOGY", and that requires resolving each import against the SERVED ROOT rather than the filesystem.** **ADOPTED into the D-65 arm at no extra cost: every resolved import in the page's transitive graph must fall INSIDE the served root.** **ONE CONDITION OF MY OWN, and it is the difference between an instrument and a restatement: the checker must READ THE SERVED ROOT FROM THE SERVER'S OWN CONFIGURATION, never hardcode it.** D-66 is about to move that root from `src/page/` to `src/`; a checker carrying today's answer as a literal would encode the defect's shape and go stale the moment it is fixed — which is R-22's problem in a tool instead of a document. **This is the third time tonight L1 has told me an instrument's definition was wrong, and the first time it did so BEFORE the hour was spent.** I have now been corrected on the scope of every instrument I bought today. **The pattern is mine and it is stable enough to name: I keep drafting checkers against the instance in front of me instead of the class I already named, and the person who catches it is the one who has to build it.** The standing instruction holds and I want it used again. **Also recorded, a THIRD reporting failure mode distinct from D-55's: `V1`'s gate PASSED, durably, in QA's own transcript — the report was SENT AND LOST.** Not a seat that failed to run, not a seat that finished without reporting, but a report that was made and did not arrive. The branch sweep found the truth; nothing else would have. |
| D-68 | 2026-08-29 | **F3 clause 2 is CORRECTED IN KIND and STRENGTHENED, not relaxed. Option (b), with the routing corrected: the `erp.js` fix is L1's file, not I3's.** | UX and L1 both picked (b) and both are right, but the deciding argument is UX's on the merits and it comes from the FROZEN CONTRACT ITSELF — verified verbatim: `violation.schema.json`'s `rule_id` description says the R- and S- namespaces *"are kept apart so that a policy version bump changes expense validation and nothing else."* **An unknown receipt id is a BAD ARGUMENT, not an expense-policy violation** — `rule_id`, `fix_class` and `policy_version` mean nothing for it. So the literal predicate demanded inventing an R-prefixed policy rule, breaking `POLICY_DOCUMENT`'s canonical bytes and the digest pinned in `policy-versions.json`, and scheduling a frozen-file deviation nobody budgeted, **for a bad-argument error — violating the exact reason the namespace split exists, as stated in the contract's own text.** **This is why (c) is wrong IN KIND rather than merely expensive, and why amending is CORRECTING rather than DESCOPING.** The distinction matters and I will hold it: a predicate that is unsatisfiable because it demands a category error is broken, not strict; **a predicate that is merely inconvenient stays as written, and I have refused to touch several today.** **The amended clause is STRICTLY STRONGER THAN OPTION (a): link_receipt on an unknown receipt must (i) fail, (ii) name the id, (iii) not mutate the report, AND (iv) surface a code DISTINCT from the unknown-LINE case.** Clause (iv) is what (a) drops and it is the real defect underneath — **`src/erp.js:314` and `:316` throw the same generic `NOT_FOUND` for two different conditions, which is what produces a confident wrong diagnosis later.** **ROUTING CORRECTED, and it would have cost a gate rejection: `src/erp.js` is a declared L0 output and L0 is L1's. `check-ownership --seat I3` DENIES it; `--seat L1` passes. It was attributed to I3 twice in the escalation.** 0.25 agent-hours, owner **L1**. **UX committed the test RED rather than relaxing it and costed three resolutions instead of choosing one — the correct handling of a predicate a seat cannot satisfy, and the opposite of the silent-descope my charter names as a category W files against me.** |
| D-69 | 2026-08-29 | **EIGHTH instance — and the first found by an instrument rather than a person. `--suite` to C4 as an E1 follow-up; it blocks THREE nodes.** | MEASURED by `--check-modes`, hours after D-59 bought it: `E1` is done, `webmcp-eval-kit/bin/eval.mjs` exists and implements only `--selftest` and `--version`, and **`E2`, `E3` and `E5` all name `--suite`. Three consumer nodes blocked by one missing mode in one producer.** 0.5 agent-hours, owner C4, specified in the PRODUCER as every one of these has been. **The instructive part is WHEN it became visible: while `eval.mjs` did not exist, `--check-modes` correctly DEFERRED all three to a producer that is a hard input — clause (b) of its own definition. It became checkable the moment the file landed and failed immediately.** That is the instrument behaving exactly as designed on a case no human was looking at, and it is the first of the eight found without a person driving a running system or reading a diff. **Two nights ago this would have surfaced on Day 3 as three simultaneously blocked eval nodes with the E-lane already dispatched.** **Running count: eight instances of one family in one day. Four fixed in the producer with an edge drawn, one by widening a served root, one by a mount, one by a mode bundle, and this one caught by machine before anyone looked.** **Cost: 0.5 agent-hours, review overhead 0.025 h, plus D-68's 0.25 h at 0.0125 h, against 0.337 h spare. 0.299 h remains. Nothing is cut and no rank fires.** |
| D-70 | 2026-08-29 | **Third stale board — and the precise failure is narrower and more useful than "stale": I named a WORK ITEM as a blocker, which is what let me skip computing one.** | I published *"T2 waits on I3's served root"* and *"25 done"*. Measured in this turn: **27 done, and T2 has NO unmet hard inputs — every one of them merged two hours earlier.** **D-56 made me recompute `READY` and `BURNED` and I did; I did NOT recompute `BLOCKED`, I inferred it from a teammate's earlier message.** **But my charter already forbade the specific move: *"a node you call blocked must name the node id it waits on."* "The served root" is a WORK ITEM, not a node id.** Had I obeyed the clause as written I would have had to derive the id from T2's unmet hard inputs and the answer was **NONE**. **The loose noun is what allowed the stale inference — a rule that demands an id forces a computation; a rule satisfied by a phrase does not.** Tightened in `charters/PM.md`: **BLOCKED names node ids and nothing else, computed by `ready.mjs` in the same turn.** **And the status I actually needed did not exist: a node with no unmet inputs that is still not done is NOT blocked — it is dispatched, or waiting on its own uncommitted work.** Those call for different actions, and calling the second "blocked" **points the reader at an innocent seat** — here at I3, which had already delivered. **Three stale-board failures today, and this is the first where the rule that would have caught it was already written and I had simply satisfied its letter.** |
| D-71 | 2026-08-29 | **A RELAYED "it is fixed" IS NOT A COMMIT, and L1 was right to re-gate rather than take it. Plus one real hazard: `git merge -m` with backticks silently does not merge.** | **A peer relayed that I2 had fixed `T2`. L1 re-gated instead of accepting it and measured: no commit touches `src/page/register.js` since `c563cdd`, `seat/I2-T2`'s tip is a merge of main, the worktree is clean, and the flip walk still reports `registered ZERO tools`. THE FIX DOES NOT EXIST IN THE REPOSITORY.** This is the assert-versus-exists class arriving through a relay of a seat's report — **the same shape as a merge log naming pits that were never written, and the same shape as `--check-modes`'s whole reason for existing: a claim that something is done is not the thing being done.** **Ruled, and it applies to me first: a report of a fix is EVIDENCE THAT SOMEONE BELIEVES IT IS FIXED. The artifact is the commit. L1 gates on the artifact, always, and no relay — mine included — substitutes for it.** I have relayed seat reports in this register all day; they are recorded as reports and none of them is a gate. **Second, and it is a live hazard rather than a lesson: L1's first attempt to merge my rulings used `git merge -m` with BACKTICKS in the message. The shell executed them and THE MERGE SILENTLY DID NOT HAPPEN** — no error a reader would notice, and a board that looks unchanged for a reason that has nothing to do with the work. L1 hit it hours ago, moved to message files, and regressed the moment a message felt short. **Recorded because this register is full of backticks by convention, so every seat quoting a ruling into a commit message is one keystroke from it. Message files without exception — `-F` and a heredoc, never `-m` with prose containing backticks.** |
| D-72 | 2026-08-29 | **`V6` is BUILT. `tools/check-unknowns.mjs` exits 0 on the real register and 0 on a 15-case selftest. And L1 is right that I should not have needed telling.** | **The failure first, because it is the one worth recording: `V6` is MY node, cut 0, and it sat READY AND UNSTARTED all day while I ruled on eight other seats' work.** Every other seat on the board had work; the only unstarted ready node was the graph owner's own. **A node that closes unknowns is a poor thing to discover late in a sprint whose entire method is closing them**, and nothing in `ready.mjs` was going to tell me — it prints cut and day, it does not rank, and it does not know which seat is reading it. L1 flagged it unprompted. **DELIVERED: `tools/check-unknowns.mjs`, implementing the byte-literal predicate clause for clause.** (1) locates the four-column table; (2) asserts **EXACTLY six contiguous rows**, not a floor; (3) compares the ordered first cells to `["V0","V1","V2","V3","V4","V6-consent-gate"]` **by exact string, never a regex** — R-43's trap is that `/^\|\s*\**V6\b/` matches BOTH `V6-consent-gate` AND this node's own id, because `-` is a word boundary, so a `\b` checker would call a register keyed on `V6` well-formed; (4) checks the **RAW line's final bytes** before any splitting, so `… MEASURED |` fails where a cell-trimming check would pass it; (5) requires `fallback:<node-id>` on every `UNVERIFIED` row, compared exactly against `graph.json`; (6) rejects duplicate ids, extra cells, duplicate fallback tokens, a bare `V6`, and a status token anywhere but the terminal position. **Measured: the real register passes — 6 rows, 6 MEASURED, 0 UNVERIFIED.** **CLAUSE (5) HAS NO LIVE FIXTURE and the accept says so, so it ships exercised rather than merely written: `--selftest` runs 15 cases, four of them on the UNVERIFIED branch, and TWO OF THE FIFTEEN MUST PASS — a checker that rejects everything is not a checker, which is the `exit 0 satisfied by a check that discovered nothing` class inverted.** All 15 behave as specified. `evidence/UNKNOWNS.md` needed no change: it was already well-formed under all six clauses, including the deliberate missing final pipe. **Pit reported to L1 under D-31 rather than committed here.** |
| D-73 | 2026-08-29 | **`V4`'s accept is CORRECTED IN KIND. V4 is NOT made human-gated, and it is NOT cut — and both rejections have arithmetic behind them.** | I1 read the accept carefully, concluded it cannot be satisfied honestly, and **parked it rather than manufacturing a run 2. That is exactly what the ladder should reward** and the opposite of the silent descope my charter names. **The defect is real:** run 1's 22.267 s is the ChatGPT built-in browser's own CDP-wrapper timeout — a property of THAT client. **On plain Chrome over CDP the HARNESS IS the client, so its timeout is a parameter we CHOOSE: measuring it would be circular, would discover nothing, and would then agree with itself.** And a run 2 on a different client cannot be compared at 20% anyway — `compare-runs.mjs` refuses two runs naming different clients, **by I1's own design, because that is a client mismatch and not a measurement dispute.** So the only faithful run 2 is a second built-in-browser run, needing a human, on a node the listing does not gate. **REJECTED (a), making V4 human-gated, with the arithmetic: +1.5 h takes the human-gated total from 10.5 to 12.0 and BREAKS THE ONE-TIME EXEMPTION THE USER RULED AT D-52 — for a CUT RANK 3 node, the FIRST thing the ladder deletes, whose finding is already banked and whose design consequence is already MERGED.** That is the worst available trade for an hour of the user's attention. **REJECTED (b), cutting V4 by id:** legitimate — it is cut 3 and nothing downstream needs it — but I1 has already delivered `harness/compare-runs.mjs` with an 11/11 selftest, and **cutting would discard built work to avoid editing a predicate that was wrong.** **AMENDED, and it is a correction under D-68's line rather than a relaxation: a predicate demanding a measurement that is CIRCULAR ON THE ONLY CLIENT A MACHINE CAN DRIVE is broken, not strict.** The node now requires run 1 transcribed **with its client named** (D-48 precedent), `--selftest` exit 0 proving the comparator — including the different-clients refusal, missing observations not comparing equal, a no-timeout run not averaging with a numeric one, malformed files rejected — and `evidence/V4.json` recording the second run as **NOT TAKEN**, with the reason and the standing selection. **The node does not claim a two-run agreement and must not.** **Nothing is at risk: `contingencies[4]` is PROVISIONALLY SELECTED and `S5` has SHIPPED the two-call handshake. A second run would upgrade "provisionally selected" to "fired"; it would not change one line of code.** |
| D-74 | 2026-08-29 | **FOUR reports made and lost in transit. The pit is captured AT the merge gate, synchronously — asking for one and waiting is not a channel.** | `kb/pits/H5.md` is unrecoverable and the row stays **PENDING**. I1 evidenced that rather than asserting it: commit `2d2854b` carries a different session id of the same seat, its body says the five fields *were* reported, and a sweep of every transcript for an assistant message containing both `TRIED` and `GRADE` alongside H5/env-banner returns **zero hits**. **It declined to reconstruct the pit from the diff, which is correct and which it had already refused once — a pit reconstructed by someone who did not live the work is a fabrication with a true-looking shape, and this register has spent the day pricing exactly that.** **Four now: QA's V1 verdict, I3's S9, I4's, and this. D-55 ruled that a process which has ENDED cannot report; this is the stronger case — a report from a process that was STILL RUNNING also did not arrive.** **Ruled: L1 captures the five fields IN THE MERGE EXCHANGE ITSELF, at the moment it gates, rather than requesting them and awaiting a reply. If they are not in hand when the gate passes, the row is written PENDING and stays PENDING.** **A PENDING row is honest and a reconstructed one is not**, and the freeze check (`ls kb/pits/*.md | wc -l` equals nodes done) must therefore be read as a COVERAGE figure, not a pass — I would rather ship six honest gaps than sixty plausible fabrications. **`2d2854b`'s commit body carries real technical content in that seat's own words and MUST NOT be relabelled a pit** — I1 offered that unprompted and it is right: a commit message is not a pit, and moving it would launder provenance to close a count. |
| D-75 | 2026-08-29 | **READING (b). `explain_missing_tool` is on the LIVE PAGE and deliberately OUTSIDE the counted export. Nothing is unfrozen, T3 is dispatchable, and the exclusion is now ASSERTED rather than filtered.** | L1 leaned (b) on the merits and declined to rule, correctly. **Two facts settle it and neither is in either reading as stated. First: T3's accept concerns `--list` and `--exec` — THE LIVE PAGE — and says nothing whatever about the export. Second: NO PREDICATE ANYWHERE ASSERTS export == live surface.** So (b) satisfies T3's accept as written, in full, with no freeze change. **On the merits (b) is also stronger than L1 put it: the export is not a census of registered tools — `E4` copies `artifacts/tools.export.json` into C1's blind packet and THAT FILE IS C1's ENTIRE WORLD. A tool whose description announces that absences are explainable hands a blind evaluator the mechanism it is being tested on.** **REJECTED (a), with the blast radius: `x-requiredStates` to 2/6/14/13/7/7, `FREEZE.md` re-cut, `E2`'s SET EQUALITY changed under C4 without C4 asking, the artifact regenerated, `E5`'s drift guard fired — and `T5` IS MERGED AND PUSHED, so it goes red the moment T3 lands. One 1.5 h cut-2 leaf would take down three merged-or-ready nodes and force the first mid-sprint freeze amendment.** **T3 was cuttable at rank 2 and I did not cut it: the node is right; only the unstated compatibility question was missing.** **L1's real worry — that (b) becomes "exactly the kind of quiet filter this sprint keeps getting burned by" — is the part I ruled hardest on, and `tool-export.schema.json` is `additionalProperties: false`, so the exclusion CANNOT be declared inside the artifact. It is therefore declared in the AUTHORITY and made checkable: T3's accept now also requires `export-surface.mjs` to exit 0 with `x-requiredStates` UNCHANGED, `grep -c explain_missing_tool artifacts/tools.export.json` to return 0, and the exclusion to be a NAMED CONSTANT in the source with its reason — never an inline filter.** Both halves are now checked: registered on the page, absent from the export. **`tools/export-surface.mjs` is a T5 output and T5 is I2's, the same seat as T3, so the filter lands in its own file.** |
| D-76 | 2026-08-29 | **L1's diagnosis of the edge is the finding, and it is bigger than T3: A SOFT EDGE ANSWERS ORDERING AND SAYS NOTHING ABOUT COMPATIBILITY.** | L1's words: *"soft meant T5 does not need T3 to build, and nobody wrote down what happens when T3 arrives AFTERWARDS at a surface the export has already been frozen against. The edge kind answered the ordering question and hid the compatibility one."* **That is exactly right and it is a defect in the graph's own semantics rather than in anyone's node.** `hard` and `soft` both express ORDER. **Neither expresses whether the producer's LATER ARRIVAL is compatible with what the consumer has already frozen — and that is a different question that no edge kind in this graph can state.** The `T3 → T5` contract now states it: T3 touches the live page only, the export excludes it, `x-requiredStates` is unchanged, so T5, E2 and E5 are unaffected by T3 landing at any time. **Rule, now binding: every SOFT edge's contract must state the compatibility answer, not just the ordering one.** **AND I AM DELIBERATELY NOT BUYING AN INSTRUMENT FOR THIS, which is a change in my own behaviour today: there are 11 soft edges, and compatibility is a DOMAIN JUDGEMENT about what a later arrival does to a frozen artifact — not a mechanical property a checker can read.** I have bought three instruments today, each one instance late, and the corrective for that is not to buy a fourth reflexively for a class that is not mechanical. **I will review the other ten soft-edge contracts myself and state each compatibility answer; that is my time, not a seat's, and it is bounded at eleven.** |
| D-77 | 2026-08-29 | **D-75 IS OVERTURNED. Reading (a) is ruled: `explain_missing_tool` is part of the published surface and the freeze IS amended. I2 argued against my ruling after it was made, and it was right.** | **Two facts I did not have or did not check. (1) `x-requiredStates`' OWN NOTE says the six counts *"are MEASURED by reading the countinghouse spike surface compiler"* — a PRE-PORT SPIKE THAT HAD NO ABSENCE REGISTER, because T3 did not exist. They are a stale measurement, never a decision about where `explain_missing_tool` belongs. Treating them as a decision gives a deleted compiler veto power over a live design choice, which inverts what a freeze is for: a freeze catches ACCIDENTAL surface change, and the sanctioned mechanism for an intentional reviewed one is a bump.** **(2) `erp/RISK.md` §5 — MY OWN FILE — names this exact hazard: *"The blind export can drift from the live surface. Seat C1 grades a file; judges see a page. If the two diverge, C1's verdict is worthless in exactly the direction that flatters us."* Its stated mitigation is that T5 *"is generated at runtime from the same registration path the page uses."* D-75 would have MANUFACTURED that drift deliberately, in the flattering direction, and broken the mitigation by construction.** **AND MY CONTAMINATION ARGUMENT WAS BACKWARDS: C1's worst moment is "the tool I need is not here, so I give up", and the absence register is the DESIGNED ANSWER to exactly that. Hiding it from the blind seat withholds the feature built for the blind seat's hardest moment.** **HOW I GOT IT WRONG IS THE DAY'S OWN PATTERN, COMMITTED BY ME, IN A RULING ABOUT THAT PATTERN: I searched for a PREDICATE asserting `export == live`, found none, and concluded no requirement existed. The requirement was stated in PROSE, in my own risk register. I checked the mechanical register and not the prose one.** **I2 raised the strongest counter to its own position unprompted** — `tool-surface.contract.md` line 91 names T3 inside the frozen document — and it does not survive: that clause defines what the register REPORTS (*"a name in §2 that is NOT in the current state's row"*), presupposing §2 is the full catalogue, and is silent on where the register lives. **AMENDMENT SCOPE, from I2 who owns the generator: §1 rows and §2 catalogue; `x-requiredStates` → 2/6/14/13/7/7; two hashes in `FREEZE.md`; `tools/export-surface.mjs` needs ZERO changes because it reads membership from `compile.js` and counts from the schema; two integer literals in `tests/surface.test.mjs`; `distinct_tool_count` 16 → 17. R-20 is unaffected — the register is READ-ONLY, so S2 stays at SEVEN write tools.** **L1 asked which node owns E2's set update: NONE. E2 and E5 self-correct, because their expected sets are taken FROM the export.** |
| D-78 | 2026-08-29 | **The stale note in the frozen schema IS corrected — and it rides the D-77 re-cut at zero marginal cost, which dissolves L1's objection.** | MEASURED post-T6: S4 and S5 differ by **TWO names each way** — S4 only `create_expense_report`, `open_expense_report`; S5 only `get_report`, `get_day_book` — not "exactly one name". True when written; T6 made it false. **The argument it supports is unaffected and slightly STRONGER: set equality over count equality, because a count assertion passes even if the auditor is handed the employee's surface, and two names differing makes that case better. Stale justification, not stale requirement; E2 is correct as written.** **L1 leaned toward leaving it, on the ground that unfreezing for a comment sets a precedent it would rather not have available at 02:00 on Day 5. That reasoning is sound and I would have taken it — but L1 wrote before knowing D-77 overturns D-75, and the freeze is now being amended ANYWAY for a substantive reason. The note correction costs one line inside a re-cut that is already happening, so the precedent it feared is not created.** **And L1 supplied the argument against its own lean without being asked: T5's pit records that what caught the crossover was a REDUNDANT restatement of the same six numbers in a second frozen document — which is an argument FOR keeping redundant restatements accurate.** A false sentence inside the authority for the six counts is exactly what a later seat or a judge reads as load-bearing. The `graph.json` copy is corrected in this commit; the frozen copy is corrected by L1 in the D-77 re-cut. **Both copies move together — the option I rejected outright was fixing only one, which leaves them disagreeing with the frozen one wrong.** |
| D-79 | 2026-08-29 | **W gets `.team/deviations/** ` and PM gets `.team/verdicts/**`. W had NO ownership rows at all, so every file it wrote was a violation by construction — and L1's gate NARRATED its objection instead of enforcing it.** | **The authority defect: `graph.json.nodes` filtered to owner W is EMPTY and no `file_ownership` glob mentioned W, so the longest match sent W's tickets to L1 — the seat W exists to hold to account, and the seat named in SIX of the first ten. W could not file a ticket without tripping the check, and the only way anyone landed one was to ignore the check. A rule that is broken every time it applies is worse than no rule.** Verified fixed: `check-ownership --seat W` on `DEV-014.md` now returns 0 violations. **PM's verdicts move OUT of the ticket body into `.team/verdicts/DEV-<id>.md`, because per-line ownership is not expressible and every alternative denies someone: W cannot own a file PM must append to, and L1 MUST NOT hold the pen on tickets filed against L1.** This **retires D-37's transcription hop for verdicts** — PM writes them directly — which also removes the rewording risk that hop carried. Inline verdicts already merged stay valid; the checkpoint accepts either. **L1's own report is the sharper half and I am recording it in its words: the ownership check on DEV-014 PRINTED `DENY` and the merge proceeded, because the check and the merge were separate statements and only the exit status was ECHOED. "THE GATE DID NOT REJECT THE MERGE; IT NARRATED ITS OWN OBJECTION AND WAS IGNORED."** Same family as everything else today — **a check that RAN and a check that was ENFORCED are two different things**, which L1 has spent the day telling seats. Its remedy is right and is a tool rather than a resolution to be careful: `tools/merge-gate.mjs` enforces all four clause-6 conditions in one command, exits 1 if any fails, and is proven in BOTH directions. |
| D-80 | 2026-08-29 | **NEW NODE `F7` — the signature provider is never installed. A capability this project exists to demonstrate was NOBODY'S NODE. Cut 0, UX, 1.0 h, scheduled Day 3.** | Verified independently, four greps: `setSignatureProvider` is DEFINED at `register.js:100`, EXPORTED at `:326`, and **CALLED NOWHERE**; `createSignBridge` is called **only from tests**; `sign-dialog.js` has **zero imports**; `sign-bridge.js` holds zero references to `outpocketSignDialog`. **`index.html:292` goes further and DESCRIBES wiring that does not exist.** **NO SEAT IS AT FAULT AND THAT IS WHY IT IS A GRAPH DEFECT RATHER THAN A DEVIATION: `sign-bridge.js` (S5) is deliberately DOM-FREE — which is why its tests are as good as they are — `sign-dialog.js` (F4) is deliberately DOM-ONLY and correctly publishes rather than reaching for a transport, and `register.js` (T2) exposed the seam. Each seat scoped its module correctly and THE GLUE IS A FOURTH PIECE NOBODY WAS ASKED FOR.** Bolting it onto T2 or F4 would be wrong for precisely the reason it fell through. **Owner is UX because the recorded intent says so — `register.js:97` reads *"F4 installs the real dialog by calling setSignatureProvider once"*.** **THE ACCEPT CARRIES THE DAY'S TWO HARDEST-WON LESSONS: (2) the wire must carry a DECISION, not merely a path — dialog stubbed to APPROVE completes, stubbed to DECLINE refuses, and the two outcomes must DIFFER by a named reason, because PRESENT IS NOT USEFUL; and (3) a NEGATIVE CONTROL — with the provider not installed the test must FAIL, so the suite cannot pass on a page where the wiring was silently removed.** **SEVERITY NARROWED HONESTLY, because L1's framing overstates one part: the SECURITY property is NOT at risk. The server refuses an unsigned commit and that is enforced and tested — S12 returns 423, S6 returns 409 `E_SNAPSHOT_MISMATCH`, E3's N-15 asserts 409 `E_NOT_SIGNED` against the live URL. What is missing is the PAGE-SIDE DEMONSTRATION. The claim is true; the demo of it is absent.** **This is the class `--check-orphans` PRINTS that it cannot catch, in its own output — an artifact never referenced is caught, a capability never built is not. S1's static route was the last one.** |
| D-81 | 2026-08-29 | **I drew a FALSE EDGE and caught it before committing. `F7 → D1` is not true, and believing it would have cost a forty-site cascade.** | I first drew `F7 → D1`, reasoning that the live site a stranger drives should have the sign gate wired. **Then I read D1's accept: it is a PURE DEPLOY CHECK — Origin-Agent-Cluster absent, 200, `/version` equals `git rev-parse HEAD`, exactly one instance — and it names NO FEATURE AT ALL. It is satisfied by a page with no sign gate whatever.** The edge asserted a dependency the predicate does not support. **The cost of believing it was measured, not imagined: `--path` recomputed the critical path at 30.5 h REROUTED through the sign lane, which would have obsoleted 18 figures in `graph.json` and about 20 restatements across PLAN, GRAPH and EVAL — a forty-site propagation for an edge that is false.** **With the correct edges the path is UNCHANGED at 29.5 h.** **THE TRUE CONSUMERS ARE `D4` AND `F6`, and the evidence is `docs/STORYBOARD.md`: `SB-10` "Signature dialog" and `SB-11` "The human confirms" are ON-CAMERA shots, and F6 resolves every shot id to a selector matching an element on the built page — SB-10's `[data-worst-case]` and SB-11's `[data-signature-line]` live inside the dialog F7 mounts.** **What saved it was the rule I have enforced on everyone else all day and applied to myself here: the edge has to be TRUE, not convenient, and the test is whether the consumer's ACCEPT depends on it.** I have spent the day telling seats that a predicate naming a capability nobody built is a defect; drawing an edge a predicate does not support is the same defect from the other side. **And it would have been invisible: every instrument passes on a false edge. `--check-schedule` orders it, `--check-cuts` accepts it, `--check-tables` would have happily agreed with the restatements I propagated. Nothing mechanical distinguishes a true edge from a plausible one.** |
| D-82 | 2026-08-29 | **T3's predicate had TWO defects and both are fixed. It described a capability the harness does not have.** | Verified by PM, not relayed. **(1) `--args` DOES NOT EXIST.** `harness/drive.mjs:1652` parses `--input` and nothing else; the clause failed on ARGUMENT PARSING before it ever reached the node. **(2) THE SIX-STATE LISTING HAD NO MODE THAT RUNS IT:** `--list` lists ONE state — whatever the page boots into — the only multi-state mode is the walk, and `ls harness/scenarios/` returns exactly one file, `happy.json`, which is S0→S1→S2→S3. **There is no S4 or S5 scenario and no six-state list mode.** Amended to what is actually runnable, **labelled honestly in two parts: FOUR states MEASURED in a real Chromium with exact NAME-SET matching at every flip (2→6→13→14, so the register is in each registered SET and not merely in a count), and TWO states DERIVED through the same compiled surface the browser is demonstrably registering from. This node MUST NOT claim six-in-a-browser and no document may restate it as such.** **I2 was EXACT rather than convenient about what it could claim — rounding up was available and nobody would have checked. That distinction is now IN the predicate, so the honesty survives the seat that made it.** Funding S4/S5 scenarios is a harness capability in I1's lane and would be a node with hours; **deliberately not funded — I1 holds the critical-path head, and two states derived from the compiled surface the other four are measured against is evidence enough for a cut-2 node.** **AND ONE DEFECT WAS L1's OWN BRIEF, WHICH I2 CORRECTLY OVERRULED: the brief said `candidates` must be NON-EMPTY; `violation.schema.json` allOf[0] says that if `fix_class` is `human_exception_required` or `not_reimbursable` then `candidates` has `maxItems: 0` — it MUST be empty.** I verified the block. **The schema is making a TRUE statement rather than an arbitrary one: when only a human can act there is no value the agent could supply, and offering a list of tools anyway is a way forward that does not exist.** A seat citing a frozen contract against its own dispatch brief is instruction-source discipline in the direction that is hardest to exercise. |
| D-83 | 2026-08-29 | **THE REPORT IS A COMMIT, NOT A MESSAGE. Binding, at the user's direction, and a MECHANISM rather than a fifth reminder.** | The user's instruction, relayed: back a mechanism fix, not another reminder, and land it as a binding rule. **FOUR REPORTS HAVE BEEN LOST IN FOUR DIFFERENT WAYS — a seat that finished and went idle (D-55), a seat still running whose message never arrived (D-67), a restarted session whose transcript was gone (D-74), and two composed as ordinary conversation text and never sent at all.** L1's formulation is the one that settles the design: *"a report composed but not transmitted is indistinguishable, from the receiving end, from a report never written."* **Every arrangement so far has depended on a CHANNEL between the seat and L1, and the channel has now failed in four distinct ways out of four. So the mechanism removes the channel: the five pit fields go in a `PIT:` block in THE FINAL COMMIT MESSAGE ON THE SEAT'S OWN BRANCH, and L1 reads them with `git log` at gate time.** **COMPOSING IS COMMITTING. There is no send step to forget and nothing between the seat and the gate that can drop it — the report arrives INSIDE the artifact L1 is already gating.** Written into `charters/L1.md` as clause 6e. It costs no new tool, no new file, no ownership row, and it rides the branch sweep (6d) that already exists. **If the block is absent at gate time L1 asks ONCE in the merge exchange; if it is still not in hand, PENDING and merge. Never reconstruct from the diff.** **The D-74 ban stands unchanged and the distinction is exact: retroactively relabelling an ordinary commit body as a pit is still forbidden. A block authored AS the five fields IS the report; a body that merely reads like one is not — and the difference is whether the seat wrote it as its report.** |
| D-84 | 2026-08-29 | **`--assert-flips` with no value silently overwrites another seat's evidence file. Route to I1 as a harness defect; it is the sprint's own family in a CLI.** | L1 measured it: `--assert-flips` with NO argument does not error — it falls through to the reachability gate and **OVERWRITES `evidence/H2-reachability.json`, which is I1's declared output**, under whichever seat happened to run the gate. It appeared in I2's working tree and I2 reverted it; nothing of I1's is in its commits. **The general shape is this sprint's own family arriving in a CLI: A FLAG WHOSE MISSING ARGUMENT CHANGES WHICH MODE RUNS.** It is the same defect as a check that runs but is not enforced, and a predicate that names a capability nobody built — **the operator's intent and the machine's behaviour diverge, and nothing announces it.** **Ruled: `harness/**` is I1's, the fix is argument-required validation on every flag that takes a value — a flag missing its argument must EXIT NON-ZERO, never fall through to a different mode — and it goes to I1 with the other harness work.** **W should file it: the evidence is a measured cross-seat write, and a mistyped gate run silently rewriting another seat's evidence file is exactly the shape the register exists for.** |
| D-85 | 2026-08-29 | **"A SINGLE SCHEMA IS NOT A SINGLE STATEMENT." I2's widening of its own T5 lesson, adopted.** | I2 read `violation.schema.json` by enumerating `properties` and `required` and stopped there. **The constraints that actually shaped the node are in `allOf`/`if`/`then`, and it did not see them until ajv rejected its first body. Worse, THE SAME FACT WAS STATED TWICE IN THAT ONE FILE and it read only one statement: `properties.rule_id.pattern` is `^[RS][0-9]{2,3}$` and `allOf[1].else.properties.rule_id.pattern` is `^R[0-9]{2,3}$`.** I verified both. **In its own words: at T5 it learned to look for the second independent statement of a fact, and that lesson saved that node — and here it applied the lesson ACROSS documents and not WITHIN one.** **Adopted as binding: a schema is a CORPUS, not a declaration. Read `allOf`, `if`/`then`/`else`, `oneOf` and `not` before claiming you know what a schema requires, and look for the second statement of a fact INSIDE a single file as well as across files.** **This is the second time today a seat has improved a lesson it wrote itself yesterday** — the first was UX widening D-40 into D-51 — and both improvements came from the seat that had to live with the earlier version. |
| D-86 | 2026-08-29 | **T3's THIRD defect — my D-82 fix was incomplete. `--url` is mandatory, and unbound the clause SILENTLY GRADES A DEPLOYED SITE.** | Verified: `drive.mjs:1821` throws *"--list and --exec require --url"*, and the only fallback is `readV5Origin()` reading `evidence/V5-origin.txt`, which holds **`https://webmcp-probe.onrender.com` — a DEPLOYED ORIGIN.** Unbound, the clause **either ERRORS or SILENTLY GRADES A REMOTE SITE RATHER THAN THE TREE BEING MERGED — and the second failure is the dangerous one because it goes GREEN. A predicate that passes against the wrong system is worse than one that fails.** Bound to a locally booted server on both spans. **SECOND TIME I HAVE AMENDED THIS PREDICATE AND SECOND TIME IT WAS INCOMPLETE: D-82 fixed the flag I had been shown and I did not run the amended clause myself.** Same shape as drafting a checker against the instance in front of me instead of the class — L1 has now corrected that in me four times. The substance was never in doubt. |
| D-87 | 2026-08-29 | **All five outstanding tickets adjudicated: 2 adopt, 3 debt. First use of D-79's split, and three of the five are against L1.** | **DEV-010 adopt** — T1 merged green while its outputs were unservable; plan wrong, work right, and my own draft of the `--check-orphans` arm would have passed the defect. **DEV-014 adopt** — `node:crypto` in the page graph: every hop fetched 200 and the graph failed to LINK, which no probe anyone had written could see. **DEV-011 debt** — four merge rows asserted pit files that did not exist while the row COUNT agreed with `graph.state.json.done` throughout; **a count agreeing while the content lies is the kind of green that feels like verification and is not.** **DEV-012 debt** — and what I priced is not the `git reset --hard` but that it was **FIRST REPORTED AS A SHARED-CHECKOUT HAZARD RATHER THAN AS L1's OWN RESET.** A hazard report and a self-report invite different fixes and only one prevents recurrence; misattributing your own action to an environmental condition consumes the finding and produces the wrong rule. **DEV-013 debt PLUS A MECHANISM** — `claude rm` destroyed two uncommitted files and is the **probable cause of the V2 and G2 pit rows standing PENDING**, making them gaps rather than omissions. **Second instance of DEV-008's family AFTER a ruling; a repeat does not get the same price twice without a mechanism.** New clause 6f: before `claude rm`/`stop`, the seat reads idle/done AND `git status --porcelain` is EMPTY — **verifying the seat's STATE and verifying its WORKTREE are two different checks, and only the second sees a draft pit.** |
| D-88 | 2026-08-29 | **I re-created the D-31 collision inside the node I wrote to fix a collision. `F7.outputs` was one entry short and it blocked the node's own accept.** | `F7.accept`'s first span runs `tests/acceptance/sign-install.test.mjs`; that path was in no node's `outputs`, so ownership clause (a) did not apply, clause (b) sent `tests/**` to QA, and the pre-commit hook blocked UX from committing **the file its own accept mandates.** F3 and F4 both list their test; **F7 was the outlier.** Corrected and verified — `check-ownership --seat UX` returns 0 violations on both paths. **This is exactly D-31, the ruling I opened the sprint with, re-created by me in a node created in one hour under time pressure.** L1's framing is the right one and I am recording it as a checklist gap rather than a lapse in care: **that is when a checklist item gets dropped, and it is an argument for having one rather than for being more careful.** **UX did the right thing in every available direction — did not edit `graph.json`, did not pass `--no-verify`, did not write the test somewhere it was permitted. It stopped and reported.** Fourth time today a seat refused a convenient fix that would have worked. |
| D-89 | 2026-08-29 | **The confirm_token has NO DOM DELIVERY CHANNEL, so the page cannot complete a signature — and it is an on-camera shot. Two of the three options are REFUSED.** | Verified in `server/sign.mjs`: `:168` `requireConfirmToken` defaults true (the shipped, enforced position); `:366` `/respond` rejects without a matching token; `stripTicketAndToken` at `:91` removes it from every agent-visible response; and `:21` documents `peekConfirmTokenForDialog` as *"this module's stand-in for the DOM"* — **test-only.** So F4's POST would answer 403 `E_NO_CONFIRM_TOKEN`, and **SB-10/SB-11 are on-camera shots of a human COMPLETING a signature that the page cannot today complete.** **RECONSIDERING `requireConfirmToken` IS REFUSED: the token is the human-presence proof, stripped from every agent-visible response precisely so only a human at the DOM can supply it. Deleting it removes the thing that makes the signature mean anything — the property this project exists to demonstrate. STAGING THE SHOT DIFFERENTLY IS REFUSED OUTRIGHT: that is filming a capability we do not have.** **Ruled: an S5 follow-up, 1.0 h, owner I3, landing BEFORE the shoot. The INVARIANT any implementation must preserve is that THE TOKEN REACHES THE DOM BY A CHANNEL THE AGENT CANNOT READ THROUGH THE TOOL SURFACE** — a session-scoped endpoint the surface does not expose, or server-side delivery into the page, are both admissible; anything an agent can call is not. **If the honest implementation needs a frozen contract amended, AMEND IT AND SAY SO — D-77 settled that a freeze catches accidental change and the sanctioned route for an intentional reviewed one is a bump. Do not route around a freeze with a filter.** **It surfaced at the glue and nowhere earlier because every test stubs the human, F7's included.** |
| D-90 | 2026-08-29 | **UX's rule, adopted and pushed to every seat: A NEGATIVE CONTROL SATISFIABLE BY THE TOOL BEING ABSENT IS NOT A CONTROL.** | UX's clause-3 negative control PASSED FOR THE WRONG REASON. The fixture passed `amount` as a STRING, so `amountCents` was null, the draft stayed DIRTY at S2, and **`submit_expense_report` was not on the surface at all.** The control asserted *"the submit must not complete"* — true, and **nothing whatever to do with the provider being uninstalled.** It now asserts `state() === "S3"` and that the tool IS on the surface before concluding anything from its refusal. **UX's formulation, which is a strict widening of the rule this entire sprint is built on: *"I have been checking that assertions CAN fail, and this is the next question — whether they fail FOR THE REASON NAMED. A control that can be satisfied two ways tests neither."*** **This is the third time today a seat has widened a rule it wrote itself** — D-40 into D-51, "a single schema is not a single statement" (D-85), and now this — **and all three came from the seat that had been pushing the earlier version hardest.** Goes into every dispatch brief the way clause 6e did. **And it arrived one node ahead of itself: D-89's confirm_token gap surfaced at the glue and nowhere earlier precisely because every test stubs the human — an assertion passing for a reason other than the one named.** UX also withdrew a mechanism it had offered during F3 (`usdCents` null blamed on the FX table; the real cause was `toCents()` handed a string) with the line **"'presumably' is a word that should not appear in a report."** |
| D-91 | 2026-08-29 | **I told L1 an edit was "in my worktree at HEAD" while it sat UNCOMMITTED, and a finished node stayed blocked. L1 checked instead of relaying, and was right when it measured.** | The F7 outputs fix was real and correct and **in a dirty working tree**. L1's measurement at the time is exact: my branch and main were both `828be16`, so merging moved nothing, and `check-ownership --seat UX` still returned a violation. **It is committed now at `b239e00` and the tree is clean — both facts are true, and L1's finding was correct when made.** **L1's own sentence about lost pit reports turns out to describe the authority's edits too: *"A CHANGE COMPOSED BUT NOT COMMITTED IS INDISTINGUISHABLE, FROM THE MERGING END, FROM A CHANGE NEVER MADE."*** Clause 6e removed the channel between a seat and the gate; **nothing removed the channel between an EDIT and the BRANCH — and I am the seat that just fell through it.** Written into `charters/PM.md`: **before telling L1 an edit is ready, run `git status --porcelain` and confirm it is COMMITTED — not written, not saved, COMMITTED.** This is D-56's recompute-in-the-turn rule extended from claims about the BOARD to claims about MY OWN TREE, and it is the fourth stale claim I have made today. **L1 also declined to commit `erp/graph.json` for me, correctly: it is mine, and L1 holding the pen on the authority is precisely what D-79 moved away from.** It would have been faster and it would have been wrong. |
| D-92 | 2026-08-29 | **The deploy is live and D1's starvation is over — but the free tier satisfies "exactly ONE instance" for a reason that DISAPPEARS at the upgrade, and `/version` is invalidated by every merge.** | **The user deployed: `https://outpocket.onrender.com`, 200, Origin-Agent-Cluster ABSENT, so D1's first clause passes.** D1 is now blocked on a MISSING FILE rather than a missing URL — `server/routes/version.mjs` is a declared D1 output (I4's to land) and the wiring into `createApp()` is `server/index.mjs` (S1's, I3's). Both dispatched. **(a) FREE TIER IS A RECORDED, TEMPORARY DIVERGENCE FROM D-08, not a waiver** — no card on file, and the user intends the paid tier before go-live so the 9/4–9/21 judging window does not hit cold starts. **THE TRAP IS THE "EXACTLY ONE INSTANCE" CLAUSE: free tier is single-instance BY NATURE, so it passes today for a reason that vanishes at the moment of the upgrade — the clause breaks precisely when someone does the right thing.** `evidence/D1-url.txt` must state WHICH reason it is, *"single by free-tier default"* or *"configured to one instance"*, and on upgrade the instance count must be EXPLICITLY configured and the evidence re-written. **A recorded reason that silently stops being true is this sprint's own pattern; this is the first instance where it stops being true as a CONSEQUENCE OF AN IMPROVEMENT.** **(b) `/version` EQUALS `git rev-parse HEAD` ONLY BETWEEN A DEPLOY FINISHING AND THE NEXT PUSH, and Render auto-deploys on push. Every merge invalidates it until Render catches up.** Not a defect — pinning the deployed commit is the point — **but it means D1 IS GATED LAST OR RE-RUN AT THE END. Ruled: D1's version clause is re-verified in the SAME FINAL PASS AS D6, against the final state, after the last merge.** D-60 put D6 last for the same class of reason: a check that certifies a state we then change certifies nothing. |
| D-93 | 2026-08-29 | **T3's FOURTH revision — and L1's observation is worth more than the fix: THE ACCEPT PREDICATE IS THE ONE ARTIFACT NOBODY EXECUTES BEFORE COMMITTING.** | `<port>` **is not a placeholder to a shell — it is a REDIRECTION.** `<` opens an input redirect and `>` closes with nothing to redirect to, so the line died at PARSE TIME before node was reached: `sh: -c: line 0: syntax error near unexpected token 'newline'`, exit 2. **A human reads `<port>` as "fill this in"; `sh` reads it as syntax.** Fixed to `--url "$URL"`, the house pattern `S9` has used green all sprint, bound by the gate runner. **THE PATTERN ACROSS ALL FOUR REVISIONS IS THE FINDING: `--args` did not exist → the flag was right but there was no origin → an origin was named but the token was invalid. EACH REVISION CORRECTED THE PREVIOUS AND INTRODUCED THE NEXT, AND EVERY ONE WAS CHECKED BY READING IT AND NONE BY RUNNING IT.** `node tools/accept-gate.mjs T3 --list` takes two seconds, prints the spans **as the shell will see them**, and would have caught all three. **This time I RAN it before committing, and it immediately showed a defect I had just introduced: my explanatory prose about `--args`/`--input`/`--url` had been backticked, and THE RUNNER DEFINES A SPAN AS BACKTICK-DELIMITED TEXT — so my explanations became five executable fragments and renumbered every span after them.** Stripped; T3 now lists four clean spans. **Then I proved the fix rather than read it: `sh -n` on the extracted span exits 0.** Written into `charters/PM.md` as a four-item checklist run before any predicate is committed — `--list` it, backtick only real commands, check the node lists its test file (F7's gap), and use `"$URL"`. **Not a rule about care: one command.** **L1 refused to substitute the value for the fourth time on one clause, with the fast path obvious, and its reason is exact: it would be grading I2 against a command L1 wrote, and every future gate on the node would be meaningless. The cost of four round trips is real and it is smaller than that.** |
| D-94 | 2026-08-29 | **The storyboard has a CATEGORY ERROR: three shots are filmed on surfaces that are not our page, and F0 gave them page selectors. A declared SURFACE per shot — in the storyboard, never a skip list in the tool.** | MEASURED: `data-agent-transcript`, `data-forced-call-result` and `data-commit-result` appear in **ZERO** files under `src/`, `server/` or `harness/` and zero times in `graph.json`. **They are not unbuilt features.** SB-04's refusal and SB-12's post-signature rejection both appear in **the agent's own client window**. **SB-08 is SELF-CONTRADICTORY rather than shorthand: its row reads *"the request is forced past the page with a hand-made call"* and its selector is `[data-forced-call-result]` — a hand-made call is curl, in a TERMINAL, and asserting our page displays the result DEFEATS THE ENTIRE POINT OF THE SHOT, which is that the request went around the page.** Adopted: every shot declares a `surface` — `page`, or an off-page surface named explicitly. Page shots resolve against the served DOM; off-page shots name their surface and are not resolved as selectors; **a shot with NO declared surface is a FAILURE, never a skip.** **DECLARED IN THE STORYBOARD, NEVER A SKIP LIST INSIDE `check-storyboard.mjs`: a silently skipped shot is the exemption that hides the bug — the `SWEEP_EXCLUDE` shape L1 made I4 rewrite — and the rule must live where a reader can argue with it.** **UX REFUSED TO MAKE ITS OWN GATE GREEN AND THE REFUSAL IS AFFIRMED, with the clearest structural argument anyone has offered today: `docs/STORYBOARD.md` is F0's output and UX-owned, so UX could have added the column and passed. F0 exists as a SEPARATE NODE precisely so the thing that WRITES the storyboard is not the thing that GRADES against it — and if F6 edits the storyboard so F6's tool passes, that split collapses, for its own convenience.** So I rule the column as a **correction to F0** and amend F6's resolution rule in the authority. **UX decides neither.** Whether the beats still carry their argument once relabelled is **L2's**. |
| D-95 | 2026-08-29 | **L1's cut-ladder premise is INVERTED — there is no ladder defect. But the F5 → F6 edge IS mislabelled, and re-labelling it hard converts an accidentally-correct ordering into an enforced one.** | L1 reported that F5 is cut before F6, so a fired ladder would leave F6 surviving and unsatisfiable. **Measured against the authority: `key(c) = Infinity when c is 0, else c`, and ranks fire 1 → 2 → 3 CUMULATIVELY. F6 is cut 1 and dies at rank 1; F5 is cut 2 and dies at rank 2. THE CONSUMER DIES BEFORE THE PRODUCER — which is exactly what the invariant's own words require, *"a node may not outlive the input it depends on."* F6 can never survive F5's deletion. No violation and nothing to fix there.** L1 verified the selectors and the cut numbers correctly; it was the direction of the ordering that inverted, and I would not have caught it without running it. **BUT THE EDGE IS STILL WRONG, FOR A DIFFERENT REASON.** `SB-01`'s `#surface-inspector [data-tool-row]` and `SB-02`'s `[data-policy-version]` are F5's inspector, and **F6's accept REQUIRES them to resolve. The invariant exempts soft edges *"because the consumer degrades gracefully"* — F6 DOES NOT DEGRADE, IT FAILS.** **THE GENERAL RULE: if a node's ACCEPT depends on another node's OUTPUT, the edge is HARD however gracefully the consumer appears to degrade. Soft describes a consumer that still PASSES without the producer.** Re-labelled hard; both checks pass today — invariant needs `key(F5) ≥ key(F6)` = `2 ≥ 1`, schedule needs `day(F5) ≤ day(F6)` = `3 ≤ 4`. **What it buys: as a soft edge NOTHING asserted that ordering, so had F5 slipped one day F6 would have become unsatisfiable and no instrument would have said so.** Third instance of D-76's class. |
| D-96 | 2026-08-29 | **D-89's accept predicate, which I owed. Three clauses, and the third is the one that matters.** | **(1) POSITIVE, END TO END:** with the page driven into the dialog state, a human decision POSTed to `/api/sign/{id}/respond` carries a confirm_token **the page obtained itself**, the request succeeds rather than returning 403 `E_NO_CONFIRM_TOKEN`, and the report commits. **`peekConfirmTokenForDialog` — which `server/sign.mjs:21` documents as the module's stand-in for the DOM — MUST NOT APPEAR IN THE PATH UNDER TEST**, or the node proves the test harness works and nothing else. **(2) NEGATIVE, AND IT IS THE INVARIANT:** for EVERY tool on the surface in that state, driven through the real tool surface, **no response body contains the confirm_token** — the agent cannot obtain it by any call available to it. **(3) THE NEGATIVE MUST FAIL FOR THE REASON NAMED — D-90 applied, and without it clause 2 is satisfied by there simply being NO TOKEN:** the same run must prove a token **exists** at that moment and that **the page can read it**. **Clause 2 minus clause 3 is exactly the trap UX found in its own F7 control** — a control satisfiable by the thing being absent is not a control — arriving one node later and from the other direction. L1's arrangement of having I3 propose a predicate alongside its green is right for speed, but the predicate is mine and this is it. |
| D-97 | 2026-08-29 | **L2 WAS NEVER SEATED. Nobody has ruled on quality for the entire sprint, and the seat's charter sat on disk being cited the whole time. Ruling: boot it NARROWLY, and record the gap permanently.** | **VERIFIED BY PM AGAINST THE LIVE ROSTER, not relayed: 90 peer sessions; L1, I1, I2, I3, I4, QA, UX and W are all up as background seats. THERE IS NO L2 AND THERE NEVER HAS BEEN ONE THIS SPRINT.** L1 traced the Day-1 dispatch: the boot set was five seats plus L1, and L2 was not among them — **not deferred, not dropped, simply never in the list.** **THIS IS THE LARGEST INSTANCE OF THE DAY'S OWN CLASS AND IT IS NOT A LOST REPORT OR AN UNCOMMITTED EDIT — IT IS AN ENTIRE ROLE THAT SILENTLY DID NOT EXIST while `erp/charters/L2.md` sat on disk being quoted.** An absence that produces no error, for two days. **THE MISS IS AT LEAST AS MUCH MINE AS L1's, and more so in one respect: my own charter names L2 in the escalation path AND in the tiebreak rule — *"if it is about whether it is worth shipping, L2 wins"* — and I invoked it one ruling ago by routing the storyboard beats question to a seat that could not receive it. I have cited L2 all sprint and never once checked that anybody was there.** L1's accounting of its own share is exact and I am not softening it, but it should not carry this alone. **CONSEQUENCES, stated rather than implied: (i) every merge this sprint was gated on the WRITTEN PREDICATE AND NOTHING ELSE — which is exactly what L1's charter instructs, and it was written assuming a second seat covered the other half; (ii) L1 has had NO ESCALATION PATH FOR WEAKNESS all sprint; (iii) `E4` — 2 h, cut 2, READY, undispatched — is L2's, and it is the node that builds C1's blind packet, the same packet whose contamination question consumed two of my rulings today.** **RULED: OPTION (b), BOOT L2 NARROWLY — and L1's own objection is what makes the narrow scope right rather than a compromise.** L1 argued a cold seat asked to rule on 41 merged nodes at hour 100 will rubber-stamp, which is the failure mode L2 exists to prevent. **Correct — so do not ask it that. SCOPE L2's REVIEW TO WHAT A JUDGE ACTUALLY SEES: the video, the four Devpost answers, the README, the live site. For THAT corpus a cold seat is not a liability, IT IS THE CORRECT EYE — a judge is also cold, has no accumulated investment, and reads the artifacts and not the graph.** L2 takes `E4` (already budgeted, no new hours), the SB-04/SB-08/SB-12 beats ruling, and ONE end-state review before D6 (~1.0 h). **NOT a running quality function — there is not enough sprint left for one, and pretending otherwise is how a rubber stamp gets manufactured.** **AND (c)'s SUBSTANCE IS RECORDED REGARDLESS, because it stays true: for 41 of 62 nodes, QUALITY WAS GOVERNED BY ACCEPTANCE PREDICATES ALONE. That is now a KNOWN and WRITTEN property of this submission rather than a silent one, and no document may describe this sprint as having had two rulers over its full length.** L1's refusal to absorb the role is affirmed and its reasoning is the right one: `TEAM.md` makes L1 and L2 deliberately asymmetric because *"two identically-prompted leads share failure modes and degrade into mutual confirmation rather than mutual accountability"* — **and the person who loses that asymmetry is the user, not L1.** |
| D-98 | 2026-08-29 | **D1's predicate PRINTED where it must ASSERT. Four green spans were consistent with a 500 and a mismatched sha — on a cut-0 node.** | `curl -s -o /dev/null -w '%{http_code}' $URL` **writes the code to stdout and exits 0 WHATEVER IT IS**, and the version clause said *"equals `git rev-parse HEAD`"* in **PROSE** while the runner executes spans separately and never compared them. **So the two clauses that matter most on the deployment node could not fail.** Corrected to `test "$(...)" = 200` and `test "$(curl -s "$URL/version")" = "$(git rev-parse HEAD)"`. **Verified under D-93's checklist rather than by reading: `accept-gate --list` shows four clean spans, `sh -n` exits 0 on both new ones, and I proved they FAIL in the failing direction — 500-vs-200 exits 1, 200-vs-200 exits 0.** **L1 graded D1 by checking the four clauses explicitly rather than trusting four exit-0s, and that is the only reason this was caught rather than banked.** A predicate that cannot fail is not a weaker predicate, it is a **decoration**, and this is the fourth artifact class this sprint has found one in: a lint that never ran, a hook git never invoked, a clause that passed on zero call sites, and now a probe that prints. **Also recorded on the node: DONE MEANS OBSERVED, NOT PERMANENTLY TRUE.** All four clauses held simultaneously against the live host, and **L1's very next push invalidated the version clause exactly as D-92(b) predicted.** The final-pass re-verification alongside D6 still binds. |
| D-99 | 2026-08-29 | **A bare `S2` or `S3` is AMBIGUOUS and the two draft states CROSS OVER between namespaces. F5's accept named them backwards. L1 was right and I nearly dismissed it.** | **MEASURED, and I had to check the compiler because the export alone misled me: internal `MEMBERSHIP.S2` is 13 tools with NO `submit_expense_report` — THE DIRTY DRAFT — and internal `S3` is 14 tools WITH it — THE CLEAN DRAFT. The export publishes `S2-emp-draft-clean` (14, has submit) and `S3-emp-draft-dirty` (13, no submit).** My first check used only the export, where the semantic labels are correct, and I was about to report L1's finding as inverted for the second time in two rounds. **Measuring the compiler directly reversed that: the export's labels are right, the NUMBERS cross over, and `tools/export-surface.mjs:21-22` and `:100-114` document the mapping DELIBERATELY — *"export S2-emp-draft-clean == compile.js internal S3"*. I2 knew and wrote it down.** So nothing is broken; **the hazard is that THE SAME NUMBER MEANS DIFFERENT SURFACES IN TWO NAMESPACES.** F5's accept attached `-emp-draft-clean` to internal S2 while driving `surfaceState()`, which returns the internal namespace — **backwards in the namespace the node actually drives.** Corrected to name internal ids with the export id and the tool count beside each. **RULE, and it is R-43's bare-`V6` ban in a second register: NO PREDICATE, DOCUMENT OR TEST MAY NAME A DRAFT STATE WITHOUT SAYING WHICH NAMESPACE.** Recorded in `id_collision_warnings` where R-43 lives. It has reached three documents; `register.js`'s stale `5/12/13/6` header is I2's to fix. **UX drove on the canonical ids and RECORDED THE DISCREPANCY IN ITS TEST HEADER rather than quietly picking the reading that made its life easier — which is how this surfaced at all.** |
| D-100 | 2026-08-29 | **I1 filed a defect against its own MERGED node, unprompted, and its rule is the sharpest of the sprint. Adopted beside D-90 — they are NOT the same rule.** | H6's grader `--selftest` drives all eight clauses to FAILURE and **never shows the grader ACCEPTING anything it should.** If `grade()` returned `pass:false` unconditionally, H6's selftest would go from 8/8 to a single failure — and in I1's words, *"I would have read that as a near-miss rather than the warning it is."* **I1's rule: A SUITE THAT ONLY TESTS ONE DIRECTION OF A DECISION CANNOT TELL A WORKING GUARD FROM A STUCK ONE, AND WHICH DIRECTION IT TESTS IS USUALLY DECIDED BY WHICH FAILURE YOU WERE AFRAID OF THAT DAY.** **Its self-diagnosis is the part that generalises and it is unusually honest: it had been afraid of fail-OPEN all week because that is what this sprint kept producing, and THAT FEAR IS EXACTLY WHY IT COULD NOT SEE THE FAIL-CLOSED HALF.** **D-90 says a control must not be satisfiable by the subject being ABSENT. D-100 says a SUITE must exercise BOTH DIRECTIONS of the decision. They are different rules and both are now binding.** V4 is the demonstration rather than the claim: I1 rebuilt its comparator selftest as fires/declines PAIRS, 11 cases to 20, then broke the comparator twice on purpose — **fail-open turned 1 red, fail-closed turned 13.** The suite now catches an instrument that cannot say NO *and* one that cannot say YES. **DISPOSITION: H6 is merged, green, and NOT WRONG — its live 5/5 runs exercise the accept path — but its SUITE is half-checked by today's standard. Follow-up when I1 is idle, NOT a re-open: 0.5 h, and it is a debt I am recording rather than a defect I am pricing.** Filing against your own merged work with nothing forcing it is the behaviour the deviation register exists to buy, and this is the second time I1 has done it. |
| D-101 | 2026-08-29 | **"A RULING THAT COMMISSIONS WORK BUT CREATES NO NODE COMMISSIONS WORK THAT CANNOT BE COMMITTED." Third instance, three causes, one shape — and the fix is ONE line, not a node.** | L1's sentence is the finding and it generalises past D-89. **Three instances now, each with a DIFFERENT cause: `F7`'s `outputs` was one entry short; `W` owned no nodes at all so clause (a) was empty by construction; and `D-89` is a RULING rather than a node, so there is no `outputs` array for clause (a) to match and clause (b) sends every path to whoever owns the glob.** **MEASURED, and it narrows L1's ask considerably: of the four paths D-89's work touches, `server/sign.mjs` and `src/page/sign-bridge.js` are ALREADY S5 outputs and `server/index.mjs` is S1's — all three I3's. ONLY `tests/acceptance/confirm-token.test.mjs` was declared by no node.** So the fix is to extend `S5.outputs`, not to create a node: **D-89 is an S5 follow-up, its artifacts are S5's outputs, and a node would have cost a schedule slot, a 63→64 count and a restatement cascade to buy nothing the outputs array already gives.** Verified: `check-ownership --seat I3` returns 0 violations on all four. **`DEV-016` is added on the DEV-E3 PRECEDENT ALREADY IN THAT ARRAY — a node may declare a SPECIFIC deviation ticket it authored, and clause (a) beats the `.team/deviations/**` glob D-79 gave W. The glob stays W's default; a named ticket is the node's. S5 did this before I ruled it.** **THE PART THAT INDICTS ME: today's other follow-ups — H2's modes, S1's static route, F1's mount, E1's `--suite`, F0's surface column — were ALL SAFE BY LUCK OF TOUCHING FILES THOSE NODES ALREADY DECLARED, and I did not check once.** `charters/PM.md` now carries the question to ask before ruling a follow-up: **does this work create a path no node declares?** — with three graded answers, cheapest first. |
| D-102 | 2026-08-29 | **I3 found a REAL DEFECT in merged F7 work while building D-89, read F7 in full first, and correctly ruled it NOT F7's fault. The seam class again — the fourth time.** | `src/page/sign-install.js:173` reads `awaiting.confirm_token` and `:164` reads `awaiting.request_id` from `bridge.beginSign()`'s return — **and `beginSign()` DELIBERATELY NEVER CARRIES EITHER**, by R-13/R-44's narrow `{status, ticket}`. **So today `mountSignDialog()` always receives `confirmToken:""` and `request_id:null`, and A REAL CLICK POSTS TO `/api/sign/null/respond`.** **I3 read F7's file in full before writing and said explicitly that this is not a defect in F7: F7 wired the seam to a field `beginSign()` was never going to supply. Two correctly-scoped modules and a seam nobody owned — the same class as the sign-glue gap D-80 created a node for, arriving one layer inside the fix for it.** **The implementation satisfies my D-89 invariant BY CONSTRUCTION RATHER THAN BY FILTERING, which is the distinction I asked for: `GET /api/sign/{request_id}/confirm-token` is session-scoped and NOT a registered tool, with a page-side-only `openForDialog()` companion — and `beginSign()`/`continueSign()` are BYTE-FOR-BYTE UNCHANGED, so the tool-facing R-13 contract holds exactly as documented.** Its test proves all three D-96 clauses against the REAL F4 dialog and REAL server, with `peekConfirmTokenForDialog` never in the path, **every tool on the REAL COMPILED SURFACE swept — 14 in that state, from T5's six-state model rather than a hardcoded 16** — and D-90's precondition proven in the same run against a temporarily reverted sabotage that correctly failed the sweep. **ONE LINE REMAINS AND IT IS UX's: `sign-install.js`'s `browserDialogPort.present()` must call `openForDialog()` instead of `beginSign()`. I3 flagged it precisely rather than editing outside its charter — `sign-install.js` is F7's, UX's.** **THE TEST DOES NOT NEED IT; THE LIVE PAGE DOES — so D4's on-camera beat needs it and D-89 IS NOT FINISHED UNTIL IT LANDS.** |
| D-103 | 2026-08-29 | **L2's rubric clause is NOT void — it is a deliberate anti-gaming design. Its added clauses move to `erp/RUBRIC-ADDENDA.md`, which L2 owns, and that makes the add-only constraint MECHANICAL.** | L2 measured rather than inferred — `check-ownership --seat L2 --file erp/RUBRIC.md` → DENY — and **deferred to the authority over its own charter without being told to, one hour after being seated.** **My first instinct was to void the clause, on the argument that a grader editing its own standard is exactly the collapse UX refused at D-94. Reading it changed my mind, and this is why I read it: the clause is ADD a clause, DATED, saying you added it, BEFORE the ruling that needs it, NEVER MID-ARGUMENT. That is not a leftover — "never mid-argument" is precisely the anti-gaming safeguard, and stating a criterion before you apply it is how a standard stays honest. A grader may not REWRITE its standard; declaring one in advance is a different act.** **REJECTED — a declared output for RUBRIC.md: two nodes declaring one file gives two seats write access to it and destroys the determinism the ownership rule exists for. That is my own reasoning from D-75 and it binds me here.** **REJECTED — L2 hands L1 the patch: it works, but L1 is the beneficiary of the current arrangement and said so, and under it L2 could ask L1 to REWORD an existing clause and L1 would have no basis to refuse.** **RULED: `erp/RUBRIC-ADDENDA.md`, owned and committed by L2 — D-79's shape, the author writes it — and it does what neither alternative does: IT MAKES THE ADD-ONLY CONSTRAINT MECHANICAL RATHER THAN HONOUR-SYSTEM.** Verified both directions: L2 now passes on the addenda file and **still DENIES on `RUBRIC.md`** — it cannot reword §1–§4 to make a verdict come out right, because it does not own the file, and it never needs another seat's hand. **L1 declined to design its own exception, saying it would rather carry it unresolved for an hour than pick the option that keeps the pen in its hand. That is the right refusal and it is the sixth of its kind this sprint.** |
| D-104 | 2026-08-29 | **L1's question SUPERSEDES mine. D-101's caught three instances and missed the fourth; L1's catches all four.** | Mine asked *"does this work create a path no node declares?"* — which misses `RUBRIC.md`, **because that path EXISTS and IS declared, just to somebody else.** **L1's: "DOES THE SEAT I AM GRANTING THIS TO OWN THE PATHS IT WILL HAVE TO WRITE?"** Adopted into `charters/PM.md` in place of mine, to be asked before ruling any follow-up, writing any charter clause, or creating any node. **The class in its final form: A DOCUMENT THAT GRANTS WORK CANNOT GRANT THE RIGHT TO COMMIT IT, BECAUSE OWNERSHIP LIVES IN A DIFFERENT DOCUMENT.** Four instances, **four different granting artifacts**: a NODE whose outputs list was one entry short (`F7`); a CHARTER for a seat that owns no nodes at all (`W`); a RULING that created no node (`D-89`); and a CHARTER granting an edit to a file another node declares (`L2`). **Three of the four are mine — I wrote F7's outputs, I ruled D-89, and I have cited L2's charter all sprint — and the fourth I ruled on this morning.** L1 has now improved my formulation twice in one day. |
| D-105 | 2026-08-29 | **UX's rule, recorded: "A COMMENT EXPLAINING WHY A DEFECT IS INTENTIONAL DOES NOT MAKE IT STOP BEING A DEFECT — IT MAKES IT STOP BEING REPORTED."** | The `skin.css` and favicon 404s carried **a sound justification in a comment beside them**, and that comment **discouraged every subsequent reader from asking.** What fixed them was **three people hitting them FROM OUTSIDE, none of whom had read the justification.** **This is the inverse of everything else the sprint has found: not an absence that produces no error, but a PRESENT, CORRECT, WELL-REASONED EXPLANATION that suppresses the report a defect would otherwise generate.** A justification is evidence about INTENT and none whatever about CONSEQUENCE, and the better the reasoning reads, the fewer people check whether it is still true. **Note UX found the favicon 404 by measuring for the CLASS rather than the item I named — the page's entire network log is now `/api/me → 401`, which is the signed-out ANSWER rather than a failure.** Also recorded from L2, and it is the axis nobody has ruled on for two days: **the surface compiles from (role, report-state, verdict) and POLICY IS NOT AN AXIS** — a policy edit revokes `submit_expense_report` INDIRECTLY by flipping the draft clean→dirty, which bears on whether SB-04's causal chain is filmable as described. And its judgement that `E_SNAPSHOT_MISMATCH`'s literal line is **"a better still frame than anything our page holds"** is a claim about the DEMO rather than about correctness — **exactly the thing that has had no ruler since Day 0.** |
| D-106 | 2026-08-29 | **A WHOLE-SITE OUTAGE: one malformed `POST /api/sign` killed the node process. Found by L1 running the shoot gate against production. Fixed at two layers, both verified independently.** | **Uncaught `CanonError: E_CANON_TYPE` from `digest()` on an undefined field, escaping the handler entirely.** Measured pre-fix: `/api/me` 200, one short body, then **EVERYTHING 000 — the server is gone.** **SEVERITY: any SIGNED-IN user, or the page itself on any bug, takes the site down with one request. The auth check runs before the crash so anonymous probes are safe — AND A JUDGE WILL BE SIGNED IN.** **THE DIAGNOSIS IS THE PART WORTH KEEPING: `POST /api/sign` returned 502 and L1's retries returned 401 `E_NO_SESSION`, which LOOKED LIKE A COOKIE PROBLEM AND WAS NOT — the instance had restarted and sessions are in-memory, so every session on the box was wiped. THE 502 AND THE 401s WERE ONE EVENT.** A second symptom that impersonates an unrelated defect is how an outage gets misfiled as a login bug. Fixed by I3 at two layers — field validation in `open()` returning 400 `E_BAD_SIGN_REQUEST` naming the missing fields, and **a top-level guard THAT DID NOT EXIST AT ALL.** **L1 proved the general guard alone suffices by disabling only the specific validation — and its FIRST ATTEMPT TO DISABLE IT SILENTLY FAILED TO APPLY and returned 400, which would have let it report the general guard as verified when it had not been tested. "A SABOTAGE THAT DOES NOT APPLY IS A GREEN THAT MEANS NOTHING" — the D-90 family in the tester's own hands.** **Also recorded: I3's FIRST DRAFT OF THE FIX WAS A SECOND DEFECT WEARING THE CLOTHES OF A FIX** — `typeof === "string"` rejected null, **null is a valid OCF-1 value** and the page legitimately sends null policy fields, and it broke three already-passing tests before committing. Caught by running the full suite as a matter of course. |
| D-107 | 2026-08-29 | **`E4` clause [1] was DECORATION, and the answer key had no path. Both fixed; L2 flagged both and refused to fix either, correctly.** | *"`ls -1` in it prints 2"* — **"in it" binds to nothing in a shell, so the extracted span was a bare directory listing that EXITS 0 FROM THE REPO ROOT ON ANY PACKET OR NONE.** Replaced with L2's proposed assertion, and **PROVEN BOTH DIRECTIONS BY ME BEFORE COMMITTING rather than read: the real run exits 0, a wrong count exits 1.** `tr -d " "` is load-bearing because **macOS `wc -l` emits leading spaces, so a naive comparison would fail closed for a reason unrelated to the packet** — a detail L2 caught that would have produced a confusing red on a correct packet. **AND I ADDED TWO JUNK SPANS WHILE EXPLAINING THE FIX, FOR THE SECOND TIME — my backticked prose became `ls -1` and `tr -d " "` as executable fragments. D-93 exists because I did this once; I did it again in the act of applying it, and only caught it by running `--list` as the checklist requires.** **THE ANSWER KEY: `EVAL.md` §8.3 required *"a pre-registered answer key held by L2 and never shown to C1"* and NO NODE DECLARED A PATH FOR IT, so the operational half — which `EVAL.md` itself calls the half that carries weight — had nothing to compare against. Now `evals/blind/answer-key.json`, an `E4` output, so clause (a) resolves it to L2.** **L2 declined to create a path the authority had not named, and that was right — it is the discipline that keeps `erp/VERDICTS.md` dead. FIFTH INSTANCE OF D-104's CLASS: the document that REQUIRES an artifact is not the document that can GRANT a path for it.** |
| D-108 | 2026-08-29 | **The packet CANNOT be identifier-free, and `EVAL.md` §8.2 described a check that can never pass. Corrected — and the consequence is a real reduction in what may be claimed.** | `artifacts/tools.export.json` carries `outpocket` inside its own **FROZEN** envelope id and `Countinghouse` in a tool description. **Neither is removable and neither is a mistake:** the first is fixed by the frozen schema, the second is **the product name a real agent reads, and stripping it would make C1 grade a surface we do not ship.** So E4 greps only the brief — the satisfiable reading — and **reports** the identifiers every run rather than blocking. **CONSEQUENCE, STATED RATHER THAN PATCHED, and it belongs in the record BEFORE anyone writes this up: BLINDNESS NOW RESTS ON `E8`'s TRANSCRIPT CHECK, WHICH *DETECTS* A BREACH RATHER THAN *PREVENTING* ONE. No write-up may say the packet is identifier-free.** **AND L2's RULE, WHICH BELONGS BESIDE D-90 AND D-100 RATHER THAN INSIDE THEM: PROVE THE DETECTOR STILL RETURNS THE POSITIVE ON THE REAL CORPUS.** It nearly shipped a half-tested detector: all four injected contamination cases fired and it would have called that proven; **the clause that caught the bug was the one asserting the REAL FILES STILL PASS** — its workflow-order pattern matched its OWN schema's *"in task order"*. **A DETECTOR THAT FIRES ON EVERYTHING IS EXACTLY AS SATISFIABLE AS ONE THAT FIRES ON NOTHING, AND THE FALSE-POSITIVE DIRECTION IS THE ONE THAT GETS AN INSTRUMENT DISABLED BY THE NEXT PERSON RATHER THAN MERELY IGNORED.** D-90: not satisfiable by the subject being absent. D-100: exercise both directions of the decision. **D-108: and prove it still passes what should pass.** |
| D-109 | 2026-08-29 | **THE SHOOT GATE HAS PASSED. A signature completed end to end on production, and the negative was run BEFORE the positive so the green cannot be a pass that would have happened anyway.** | Measured against `https://outpocket.onrender.com` at live `/version == HEAD`, evidence at `evidence/L1-shoot-gate.md`: login 200 · well-formed `POST /api/sign` 200 · `GET /confirm-token` 200 · **INVARIANT HELD — the token appears 0 times in the agent-visible record, and 0 occurrences of its value** · `POST /respond` with the WRONG token **403 `E_NO_CONFIRM_TOKEN`, the guard bites** · `POST /respond` with the token THE PAGE OBTAINED **200, `signed`, `signed_by:"Chen Xiao"`** · `/api/me` 200 after every step. **The D-89 invariant is satisfied on the live host, not in a test: the agent cannot read the token because no call returns it.** **AND L1 RAN THE WRONG TOKEN BEFORE THE RIGHT ONE DELIBERATELY, so the 200 is not a green from a check that would have passed regardless — D-100 and D-108 applied by the tester to its own run rather than to somebody else's node.** The outage fix is live too: a malformed `POST /api/sign` from a signed-in session returns 400 and **the process survives.** **I did not green-light this and did not need to — L1 held its own gate against its own schedule pressure, twice told me it would not ping the notice until it held, and pinged only when it did.** |
| D-110 | 2026-08-29 | **A VALIDATION ERROR THAT MASKS AN AUTHORIZATION ERROR MAKES THE AUTHORIZATION ERROR UNTESTABLE FROM OUTSIDE. This is D-90 at the API boundary and it is a rule about how negative tests are written.** | For most of L1's run, **a wrong token and a malformed body both returned 400 `E_BAD_REQUEST`** — so it could not tell which it had. Once the encoding was fixed the wrong token became **403 `E_NO_CONFIRM_TOKEN`, a different code entirely.** **The server is behaving correctly — validate-then-authorize is the right order — and that is exactly what makes this dangerous: FOR MOST OF THAT RUN L1 COULD NOT HAVE DISTINGUISHED "THE TOKEN GUARD WORKS" FROM "MY JSON IS BROKEN", AND ONLY ONE OF THOSE IS A PROPERTY OF THE PRODUCT.** **RULE, binding on every negative test that asserts a guard: PROVE THE REQUEST IS WELL-FORMED BEFORE CONCLUDING ANYTHING FROM ITS REFUSAL.** A test asserting "the wrong token is refused" is satisfied by a validation error, which is D-90's *"a control satisfiable two ways tests neither"* arriving at the API boundary — and it is invisible in a way the D-90 case is not, because **both outcomes are a refusal and the test author is expecting a refusal.** Not fixed and not on the shoot's path; **recorded because `E3`'s negative suite and `C3`'s red-team work both assert refusals against this API, and neither currently proves its requests are well-formed first.** |
| D-111 | 2026-08-29 | **`FORCE_COLOR` has a SECOND failure mode and it bit the seat that quotes the pit into every brief. Knowing a fact is not the same as recognising its second shape.** | `node -e 'console.log(x)'` **INSPECTS** its argument, so under `FORCE_COLOR=3` a value scraped out of JSON into a shell variable arrives as `\033[33m1\033[39m` — **and the request built from it is malformed for a reason nothing in the error mentions.** **This is worse than the documented grep case: there a match merely fails; here A CORRECT SERVER REJECTS A CORRUPT REQUEST AND THE SEAT BLAMES THE SERVER.** L1 lost two round trips to it **while quoting `kb/pits/L0.md` into every dispatch brief it sent today.** **The safe form is `process.stdout.write(String(x))` — it does not inspect and does not colour, for any type. The existing bare-string rule does NOT cover this, because `console.log(1)` on a NUMBER is an inspected value.** Written into `TEAM.md` §1 beside the first failure mode. **Recorded with it, and it is the same shape one register down: L1 GUESSED A ROUTE NAME AND AN ENUM VALUE — `/api/session` and `decision:"approve"` — instead of reading the authority, which says `/api/login` and `decision:"signed"` in a FROZEN schema. Two 400s of its own manufacture, after a day of telling seats to read the predicate rather than infer it.** Both are the same failure: **a rule you enforce on others is not thereby operating on you.** |
| D-112 | 2026-08-29 | **YES — the F0 edit is UX's to execute, under a PM mandate whose CONTENT is L2's. UX was right to ask rather than assume, and the answer turns on where the JUDGEMENT originates, not on whose hand holds the pen.** | **The F0/F6 split is between NODES, not seats.** UX has always been both author and grader at the seat level; it is the node boundary that makes the two acts distinct. **At D-94, UX would have DECIDED which shots are off-page in order to make its own gate pass — the grader choosing its own standard — and it refused. Here L2 has decided, and TRANSCRIBING A RULING IS A DIFFERENT ACT FROM MAKING ONE.** **SO THE MANDATE MUST BE COMPLETE, AND THIS IS THE BINDING HALF: EVERY VALUE UX WRITES MUST BE L2's, AND IF ANY IS MISSING UX STOPS AGAIN RATHER THAN FILLING IT IN.** The four surface labels are L2's. **`SB-04`'s REWRITE TEXT must be L2's words, not a relabel UX composes. `SB-07` cannot keep `#agent-banner`, and WHAT REPLACES IT — a different selector or none — IS L2's CALL.** A gap in the mandate is exactly a place where UX would be exercising judgement over the standard it grades against; **if it hits one it should refuse a second time, and it would be right a second time.** **AND THE SAFEGUARD IS MECHANICAL RATHER THAN TRUSTED, which is what makes authorising this cheap: F6's tool now FAILS when an off-page shot's selector RESOLVES on our page. A transcription that drifted in UX's own favour is caught by UX's own instrument — the grader cannot quietly pass itself.** L1 offered its read as input and explicitly declined to write the document, on the ground that it would be a third seat with standing in neither node. Correct, and the seventh refusal of its kind this sprint. |
| D-113 | 2026-08-29 | **UX fixed the CLASS rather than the instance: the defect was POSITIONAL PARSING, so ANY seventh column was invisible by construction. A capture group would have closed D-94 and left the next schema change to fail identically.** | Asked to make a `SURFACE` column visible, **UX did not add a seventh capture group.** It found that `check-storyboard`'s unanchored six-group regex parsed BY POSITION, and **moved to resolving columns BY NAME from the table's own header row** — so the column works in any position, and the next schema change does not repeat this. **Its test also asserts the OTHER fields survive an insertion, "because a parser that finds the new column while corrupting the duration has not been fixed"** — which is D-108's rule applied to a parser: proving the new thing works is not proving the old things still do. **And L2's finding (2) is MECHANISED rather than noted: off-page shots are NOT SKIPPED — an off-page shot whose selector RESOLVES on our page is a FAILURE.** **That INVERTS the assertion instead of exempting the shot, which is precisely what D-94 demanded when it refused a skip list: a silently skipped shot is the exemption that hides the bug, and an inverted assertion is a check.** `SB-07` will say so out loud the moment the column lands. |
| D-114 | 2026-08-29 | **"A SELF-TEST WRITTEN AT THE END OF BUILDING A TOOL INHERITS THE TOOL'S OWN CONTROL FLOW, AND THE CONTROL FLOW IS WHERE THE ASSUMPTION LIVES." UX's finding, and it explains three instruments in one day.** | UX found its own self-tests **UNREACHABLE BEHIND THE FAILURE THEY EXIST TO SURVIVE** — they ran after the parse gate, so the moment the storyboard stopped parsing, which is exactly the condition under test, **the tool's proof of its own correctness became unreachable.** It noticed only because the self-test output vanished. **THE COUNT IS THREE TODAY AND THEY ARE ALL THE SAME SHAPE: `--check-orphans`' corpus omitted `tests/` so it INVENTED violations; `merge-gate` reported a debt for a pit that had been delivered inside the very commit it was gating; and now a self-test gated behind the parse it exists to survive. IN EACH CASE THE FIX FOR A FAILING-OPEN INSTRUMENT WAS ITSELF FAILING OPEN.** UX says it no longer thinks that is coincidence and neither do I. **The mechanism its sentence names is why: a self-test written LAST is written INSIDE the author's model of the tool, and the author's model is exactly the thing the self-test cannot check. Every assumption that made the tool wrong is still in scope when the proof is written.** The remedy that follows is structural rather than diligent — **a self-test must be reachable on the tool's WORST path, not its happy one, and the way to get that is to write it BEFORE the path exists.** H6's grader was written before its runner and is the one instrument today that did not need this correction. |
| D-115 | 2026-08-29 | **"Is the packet ready to grade against" is NOT MINE — it is an ADMISSIBILITY question and `RUBRIC.md` §5 is the admissibility bar. ROUTED TO L2. What IS mine is the timing, and the timing is: fire tonight IF L2 rules it admissible.** | L1 offered me the ruling and offered to route it instead, correctly. **My charter is explicit — *"Ruling on quality. That is L2's scale, and you have no instrument for it"* — and `L2`'s charter names `erp/RUBRIC.md` §5 as the admissibility bar. Whether a verdict from a contaminated-but-detected packet is a verdict worth having is precisely that bar.** **I would have been wrong to rule it and it took L1 offering me the exit to notice — this is the same seat I told two hours ago that quality had gone unruled for 41 nodes because nobody checked whether L2 existed.** **THE QUESTION FOR L2, narrowly: the packet CANNOT be made identifier-free — `outpocket` sits in the export's own FROZEN envelope id and `Countinghouse` in a tool description, neither removable and neither a mistake — so blindness rests on `E8`'s transcript check, which DETECTS a breach rather than PREVENTING one (D-108). Is C1's verdict, produced under detection rather than prevention, admissible under §5?** **WHAT IS MINE AND IS RULED: (i) the packet is STABLE — the export is frozen post-T3 and D2/D3 are parked, so waiting buys no cleaner packet; (ii) it is ONE SHOT on the Codex quota and L1 was right to hold it rather than spend it before someone read D-107's consequence — holding a one-shot instrument until its validity is ruled is the correct instinct and I want it recorded as such; (iii) if L2 rules admissible, FIRE TONIGHT, because nothing improves by waiting and the user has ordered maximum utilisation.** |
| D-116 | 2026-08-29 | **The sign record is CORRECT BY DESIGN and needs no frozen-schema bump — and amending it would REDUCE the auditability it exists to demonstrate.** | L1 measured that `GET /api/sign/{id}` is **identical before and after signing** — `state`/`decision`/`signed_by` all undefined, because `stripTicketAndToken` projects exactly `$defs.sign_request` and `additionalProperties:false` forbids more — so an auditor holding a `request_id` cannot tell **from that record** whether it was ever signed. It declined to call it a defect and asked whether the day book is the intended durable witness. **MEASURED at `server/sign.mjs:503-516`, IT IS, and the chain entry is strictly stronger than the alternative: `kind:"commit"`, `source:"human"`, `actor: signed_by`, `label:"signed & submitted <report_id>"`, `payload_digest`, `recomputed_digest`, `prev`, `entry_digest`. It records WHO signed, THAT a human signed, WHAT was signed — and it is HASH-CHAINED.** **THE SENTENCE, for whoever a judge asks: the sign request is an EPHEMERAL CEREMONY RECORD; the durable witness is the HASH-CHAINED COMMIT ENTRY in the day book — which is the artifact an auditor actually reads, since `get_day_book` is on the `S5-aud` surface and the sign endpoint is not.** **AND THE INVERSION IS THE POINT: putting the outcome into the sign record would be WORSE. A mutable field on a request is not tamper-evident; a chained entry with `prev` and `entry_digest` is. Answering this by amending the frozen schema would REDUCE the auditability it was meant to demonstrate.** L1 was right to raise it, right not to call it a defect, and right that it needed answering deliberately — **the answer needed MEASURING rather than asserting, and it is now one sentence anyone can give.** |
| D-117 | 2026-08-29 | **L1 destroyed evidence by pushing on an inference, and asserted a capability of its own plan TWICE without testing it. Both self-reported before anyone found them. The second is the worse one and L1 says so first.** | **(1) EVIDENCE DESTROYED: the relay said the freeze could lift; L1 pushed within a minute; the correction — that a possibly-decisive record of the user's signature sat in server memory — arrived after the redeploy had wiped it.** L1's own accounting: *"I treated a lift as a green rather than confirming the user was out of the flow"*, and **a push is irreversible against a live host and it made one on an inference.** **RULED: a push that redeploys is a DESTRUCTIVE ACT against in-memory state, and a lift is not a confirmation. Before any push that redeploys while a human is in a flow, CONFIRM THE HUMAN IS OUT OF IT — not because someone said the freeze lifted, but because you asked and were answered.** This is D-71's family — a relayed clearance is not a measured state — with the loss made permanent by the irreversibility. **(2) A CAPABILITY ASSERTED TWICE, UNTESTED: L1 told the relay it would read the server before and after the click — it CANNOT, the record is session-OWNERSHIP-scoped and its session is refused 401 even with a valid different session. Then, correcting that, it offered a GET re-read as equivalent — AND THE GET IS IDENTICAL EITHER WAY (D-116), so it could never have answered the question either. BOTH CLAIMS WERE MEASURED ONLY AFTER BEING RELAYED TO THE USER.** L1's own judgement is right and I am not softening it: **the second is worse, because it had just been burned by exactly this and still did not test the replacement before offering it.** **A CORRECTION IS A CLAIM AND CARRIES THE SAME EVIDENCE BURDEN AS THE THING IT CORRECTS — and the moment right after being wrong is when that burden feels least like it applies.** |
| D-118 | 2026-08-29 | **The contract is NOT wrong and E3 is NOT wrong — A PRODUCER NODE WAS NEVER FUNDED. Implement it, sequenced AFTER the shoot, and the reason is a failure mode rather than caution.** | MEASURED: `grep` over `server/` and `src/` returns **ZERO occurrences of `E_POLICY_DIGEST_MOVED` OR `E_POLICY_VERSION_MOVED` — NEITHER HALF IS IMPLEMENTED** — while `signature.schema.json`'s `theFix` names (a) as the closure and `policy.schema.json`'s R-33 says *"neither half is sufficient alone."* `commit()` throws four other codes and `reconcile()` compares the REPORT, not the served policy, so **a policy swapped between sign and commit is not refused — the exact attack the contract says (a) closes.** **AND THE THIRD QUESTION ANSWERS ITSELF ON A MEASUREMENT: THE ONLY NODE MENTIONING EITHER CODE IS `E3` — THE CONSUMER. No producer node was ever funded.** That is **the consumer-specifies-the-producer family with a FROZEN CONTRACT as the consumer** — the seventh instance and the first where the granting artifact is a frozen schema. **So no amendment is warranted: the specification is correct and the build was never scheduled.** **SCOPE: implement as an S5 follow-up, I3, ~1.0 h, AFTER `D4` IS SHOT.** The reason is not artifact-stability nerves — **the demo's central beat runs through `commit()`, `server/sign.mjs` is the file that produced D-106's whole-site outage today, and the failure mode of a NEW REFUSAL CONDITION is REFUSING A LEGITIMATE COMMIT ON CAMERA.** The gap is not demo-reachable and not judge-reachable without write access to the served policy, **which the contract itself calls arguably outside the declared threat model** — so sequencing costs nothing that waiting does not already cost. **Implement under D-108: prove a LEGITIMATE COMMIT STILL SUCCEEDS, not only that the new refusal fires — here the false-positive direction breaks the product.** |
| D-119 | 2026-08-29 | **E3 STAYS RED AND UNMERGED. I am not relaxing the clause, and L1 was right not to ask me to.** | C4 says acceptance remains blocked until the server returns 409 `E_POLICY_DIGEST_MOVED`, and **L1 refused to relax a predicate to land a node — the banned behaviour, named as such in my charter, and it declined without being reminded.** **AFFIRMED: the clause stands as written.** E3's suite is honest against the contract and **the node is not at fault** — it is blocked on a producer that does not exist, which is exactly the disposition T2, F1, H5 and T3 all received today. **E3 is cut rank 2. If D-118's follow-up does not land before submission, E3 does not land, and THAT IS AN ACCEPTABLE OUTCOME — it is not a reason to weaken the clause.** A cut-2 node failing to land costs a rank-2 eval; a relaxed predicate costs the meaning of every future gate on that node. **AND C4 GRADED ITS OWN NODE RED UPSTREAM RATHER THAN PASSING A SUITE THAT WOULD HAVE GONE GREEN AGAINST A SERVER THAT DOES NOT DO WHAT THE CONTRACT SAYS.** It found the gap **by BUILDING the negative suite rather than by reading** — N-20 exposed it. **A Codex seat on a separate quota, with no stake in this register and no charter conversation behind it, refusing a convenient green.** Its own line is the sharpest statement of the finding: *"the contract names the code, but the commit path never compares against the served policy digest."* **That is the eighth refusal of a defensible shortcut this sprint and the first from a seat outside the resident set.** |
| D-120 | 2026-08-29 | **L2 REJECTED THE PREMISE OF THE QUESTION, and that is the finding: there was no detection. Both edits landed; C1 may fire.** | L1 asked whether a verdict produced under DETECTION rather than PREVENTION is admissible. **L2's answer: it is NEITHER — the transcript clause had nothing to read.** **VERIFIED BY PM on codex-cli 0.144.6: `--ephemeral` and `--json` are separate flags; the frozen command carried the first and not the second, and `-o` writes only the final message. SO THE RUN EMITS NO TRANSCRIPT, AND THE CLAUSE "the transcript shows ZERO tool calls outside $PACKET" WOULD HAVE BEEN REPORTED SATISFIED FROM THE ABSENCE OF EVIDENCE — this sprint's defining defect, sitting inside the blind eval's own blindness control.** Fixed: `--json` appended, stdout redirected to `evals/blind/C1-transcript.jsonl`, **declared as an E8 output so C1 owns it (D-101), and the clause now ALSO requires the file to be NON-EMPTY — an empty transcript FAILS rather than satisfies.** **SECOND EDIT: `outpocket` and `countinghouse` REMOVED from the verdict grep, and this is NOT a relaxation — it separates two token classes the original CONFLATED. Measured: both occur ONCE INSIDE the packet (the frozen envelope id; a tool description), so they are QUOTABLE FROM THE INPUT C1 WAS HANDED and are evidence of nothing. `/Users/`, `.mjs` and `src/` occur ZERO times in the packet, so any of them in a verdict IS evidence blindness broke.** **L2's urgency argument is D-108 with the quota on the line: the rubric schema ITSELF asks C1 for "the specific tools or descriptions that drove the score", and the FIRST tool description contains "Countinghouse" — so the schema INVITES the quoting that would trip the check, and with no second run a false positive costs not one number but the whole instrument.** **L2's reasoning on why the leak is not disqualifying, kept verbatim because it is what makes detection PROPORTIONATE rather than second-best: *"a name is a POINTER, not content, and turning a pointer into implementation knowledge requires a tool call outside the packet — which is precisely the observable act, so detection IS a proportionate control here. It is simply not armed."*** **AND I ADDED TWELVE JUNK SPANS WHILE EXPLAINING THIS, THE THIRD TIME TODAY — caught again only by running `--list` as D-93 requires. Both surviving spans then proven with `sh -n`.** |
| D-121 | 2026-08-29 | **THE THIRD CONDITION IS MINE TO CARRY TO SUBMISSION: if the rubric total is used anywhere, the disclosure goes with it — or the number is inadmissible.** | L2's ruling: **any use of the rubric total — README, Devpost, report — MUST disclose that the packet was NOT identifier-free and that blindness rested on an AFTER-THE-FACT TRANSCRIPT CHECK rather than prevention. If the disclosure is dropped, THE NUMBER GOES BACK TO INADMISSIBLE no matter how clean the run was, and L2 will say so at the end-state review.** **This is not satisfiable tonight and it is not L1's to satisfy — it is a standing obligation on D5, which writes the Devpost entry, and it is recorded in `D5.notes` so it reaches whoever drafts it rather than living in a message.** **The cheapest compliant option is not to cite the total at all**, and whoever drafts should know that before they reach for it. **This is D-108's shape applied to a claim rather than a check: a number that is true is not thereby admissible, and the condition that makes it admissible has to travel with it.** **PROCEDURAL NOTE ON L2, and it is the second time tonight a seat made a claim checkable instead of asking to be believed: it added §3.6 to `RUBRIC-ADDENDA`, DATED AND PUSHED BEFORE the ruling, applied to nothing earlier — *"the ordering is in git rather than in my prose."* That is D-103's add-only safeguard honoured with an artifact.** And it accepted L1's correction on `compile.js` without argument — *"you are right and I was wrong… the D-99 error, made by the seat that received D-99"* — and thanked L1 for NOT routing a wrong finding to I2, on the ground that it would have cost more than it was worth. |
| D-122 | 2026-08-29 | **D-118 is UNPARKED and dispatched tonight, sequenced LAST. Its blocking condition expired on its own terms — and L1 noticed, not me.** | I ruled D-118 to wait *"after D4 is shot"*, on the ground that **the failure mode of a new refusal condition is refusing a legitimate commit ON CAMERA.** The camera is off: the user completed the signature on production, `method:"signature-click"` is in the day book, and the shoot gate is closed in `evidence/L1-shoot-gate.md`. **The condition I named has been satisfied and the ruling should have unparked itself hours ago.** **THE CAP QUESTION IS ANSWERED BY MEASUREMENT AND IT IS NOT CLOSE: I3's ONLY open scheduled node is `S7` at 1.5 h — every other I3 node is done. S7 1.5 + D-118 1.0 = 2.5 AGENT-HOURS AGAINST A 6.0 `seat_day_hours_cap`.** L1 declined to assume and asked, **having been the seat that pushed past a stated limit before** — the right instinct, and the answer was two commands away. **SEQUENCING: L1's reason is ADOPTED IN PLACE OF MINE. A new refusal condition landing at 01:00, in the middle of QA's post-upgrade re-smoke and behind D3's idle window, would be ATTRIBUTED TO THE STARTER UPGRADE OR TO THE OUTAGE FIX RATHER THAN TO ITSELF.** That is D-110's family — **a signal attributable two ways tells you neither** — and it is a measurement-hygiene argument rather than a risk argument, which is a better reason than the one I originally gave. **Land it against a CLEAN BACKGROUND, after QA reports and after D3's window closes, and run its own verification BEFORE QA's next measurement so the boundary is unambiguous either way. D-108 still binds: prove a LEGITIMATE COMMIT STILL SUCCEEDS.** **AND THE PRACTICE IS NOW IN MY CHARTER: WHEN YOU PARK SOMETHING, NAME THE CONDITION THAT UNPARKS IT, NEVER A TIME.** D-118 was checkable, so L1 could verify it and re-ask; **a deferral dated "tomorrow" expires only when someone happens to think of it, which is this sprint's own absence-that-produces-no-error aimed at my rulings.** |
| D-123 | 2026-08-29 | **C1 fired and failed on a schema defect, and THE ONE SHOT IS NOT SPENT. My D-120 amendment was not the cause.** | The API rejected `rubric.schema.json`: **all six dimensions carry `$ref` with a SIBLING `description`, which the structured-output validator refuses.** Four transcript events, **zero tool calls**, no verdict, packet still 2 files. **THE RUN IS NOT CONSUMED** — the failure is upstream of any grading, so the instrument is intact and L2 has `seat/L2-schema-fix` open. **D-120's spans were clean and were not the problem, and I am recording that explicitly rather than leaving it to be inferred: the amendment was correct and the failure was downstream of it.** **AND THE FOUR TRANSCRIPT EVENTS ARE THEMSELVES THE PROOF THAT D-120's FIRST EDIT WORKED — before it, the run emitted no transcript at all, and the clause asserting zero tool calls would have been satisfied by an empty file. Tonight it produced four events and a countable zero.** The failing run is the first evidence that the control is armed. |
| D-124 | 2026-08-29 | **C1 ran. 9/12, `gate.passed: FALSE`, 5 of 8 tasks completable — THE INSTRUMENT WORKING, NOT FAILING. The fourth admissibility clause was disarmed BY MY OWN D-120 FIX, and the amendment checks the run's own transcript FIRST.** | **THE RESULT IS UNFLATTERING AND THAT IS THE POINT: a blind grader with no knowledge of this repo says the gate does not pass.** Three of four clauses PASS on measurement: verdict grep clean, transcript non-empty (11 events, 54,479 bytes), **and ZERO TOOL CALLS OUTSIDE THE PACKET — six `command_execution` events, three distinct commands, all relative paths resolving inside `-C $PACKET`.** **THAT THIRD CLAUSE DESERVES READING TWICE: had it said "zero tool calls" FLAT, it would have FAILED EVERY LEGITIMATE RUN and passed only a run that graded nothing — D-90 exactly. "Outside $PACKET" is what makes it a control rather than a trap, and tonight it cleared three in-packet reads a flat wording would have called a breach.** L1 caught itself misremembering its own clause as the flat version and corrected before reporting. **THE DEFECT IS MINE: D-120 added `--json` so the transcript control would have something to read — correctly, and it is the only reason that control is armed at all — AND THE SAME FLAG SUPPRESSES THE HUMAN-READABLE BANNER THE FOURTH CLAUSE DEPENDED ON. Two clauses of one accept could not both be satisfied by one run. THE FIX FOR ONE CONTROL DISARMED THE VERIFICATION OF ANOTHER**, and neither of us saw it because the clauses were written at different times for different reasons — D-114's family with a new shape. **AMENDED IN PREFERENCE ORDER, AND (i) IS NARROWER THAN THE ORIGINAL RATHER THAN WIDER: check `C1-transcript.jsonl` FIRST for an event carrying this run's model or reasoning-effort configuration — that is THE RUN THAT HAPPENED and beats any probe. ONLY IF ABSENT, fall back to EXISTENCE + EFFECT to the L0 gate-(4) standard: `config.toml` exactly two keys with `model_reasoning_effort = low`; `--strict-config` carried, which I VERIFIED on codex-cli 0.144.6 *"errors out when config.toml contains fields that are not recognized"*, so a run that STARTED had its keys read rather than silently ignored; and a same-`CODEX_HOME`, same-`--strict-config` probe differing ONLY in `--json` printing the banner.** **Not a relaxation, for the same reason the D-120 token split was not: it separates WHAT THE RUN DID from HOW WE CAN OBSERVE IT.** **L1 refused to write PASS beside evidence it would not call equivalent, saying it had asserted its own capability untested twice tonight and would not do it a third time on a one-shot instrument. That refusal is why this is a ruling and not a quiet substitution.** |
| D-125 | 2026-08-29 | **The 9/12 verdict's MEANING is L2's, not mine. I record the number; I do not interpret it.** | `gate.passed: FALSE` and `tasksCompletable: 5 of 8` is a claim about **whether this is good enough** — **L2's axis, `RUBRIC.md` its instrument, and my charter forbids me ruling on it.** I nearly interpreted it in the same breath as recording it, which is the D-115 error I made four hours ago and caught only because I had just written that row. **ROUTED TO L2 with the same shape as the admissibility question: it holds the rubric, it wrote `verifyVerdict`, and the end-state review is where a 9/12 either means something or does not.** **WHAT IS MINE AND IS RULED: the verdict is ADMISSIBLE evidence — three clauses pass on measurement and the fourth is amended to be satisfiable — and D-121's disclosure obligation now BINDS IN FORCE rather than in principle. If the total is cited anywhere, it travels with the statement that the packet was not identifier-free and that blindness rested on an after-the-fact transcript check. The cheapest compliant option remains not citing the total.** **And one thing the number already tells us that needs no rubric to read: a blind agent handed only the export and the tasks could complete FIVE OF EIGHT. That is a measurement of our TOOL DESCRIPTIONS, not of C1** — `EVAL.md` §8.3 says a `firstTool` mismatch is *"a finding about our descriptions, not about C1"*, and the same logic governs the completion count. **It is the most actionable output the eval was built to produce, and it arrived.** |
| D-126 | 2026-08-29 | **THE HEADLINE FIGURE I GAVE THE USER WAS WRONG. L2 corrected it under §2.4 — a figure from our own instrument carries its ACHIEVABLE MAXIMUM. There is ONE genuine shortfall, not three.** | I reported *"5 of 8 tasks completable"* and told the user **"the three tasks it couldn't complete are the most actionable output."** **VERIFIED against `evals/blind/tasks.md`, frozen and dated BEFORE the run: T4, T7 and T8 are deliberately written to be things the surface may well not do, drafted from the scenario BEFORE the tool list was read.** **T4 and T8 are genuinely unfulfillable — no registered tool takes a file-shaped argument, and an auditor session registers no write tool at all. THE ACHIEVABLE CEILING IS 6 AGAINST A PASS MARK OF ≥6: ZERO SLACK.** **The admissible form, ceiling in the same sentence: *"gate not passed: 5 of a possible 6, the other two tasks being deliberately unfulfillable by design."*** **MY INSTRUCTION SURVIVED THE CORRECTION AND MY NUMBER DID NOT — and the instruction is the part that mattered: T3 is real, it is OURS, and it is exactly what the eval was built to find.** `canConstructArgs` FALSE, missing `merchant` and an absolute receipt date — **NO REGISTERED TOOL RETURNS TODAY'S DATE, so "last Tuesday" is unresolvable from our surface, and no merchant is derivable from a taxi ride.** A measurement of our TOOL DESCRIPTIONS, exactly as `EVAL.md` §8.3 says. **I passed a raw count to the user without its ceiling — the same class as citing a rubric total without D-121's disclosure, which I had ruled four hours earlier. A NUMBER WITHOUT ITS DENOMINATOR IS A CLAIM, NOT A MEASUREMENT.** |
| D-127 | 2026-08-29 | **The reasoning-effort axis is UNEVALUABLE for this run — my branch (ii) measures a SIBLING. Downgraded to OUR-ESTIMATE, and re-running is FORBIDDEN.** | L2 ruled under §3.6(a): **a control is armed only if the artifact it inspects is PRODUCED BY THE RUN IT GRADES.** No event in the transcript names a model or an effort, so **branch (i) is absent — and my branch (ii)'s existence+effect pair measures a same-config SIBLING RUN, not the run that happened.** **The statement "the run executed at reasoning effort low" is now OUR-ESTIMATE and must carry those words wherever it appears. The six dimension scores are unaffected and stay MEASURED.** **L2 EXPLICITLY FORBADE CURING THIS BY RE-RUNNING, and the reasoning cuts against the instinct L1 and I both had: `EVAL.md` §8.5 forbids re-rolling after seeing the number, and A RE-FIRE NOW WOULD BE EXACTLY THAT WHATEVER THE MOTIVE. The cure would be worse than the defect.** **And L2 noted the risk POINTS TOWARD US: if the run executed above `low`, a more capable grader is §8.1's hazard — over-reading thin descriptions and FLATTERING our surface. That is why it is disclosed rather than waved through.** **For any FUTURE run only, now in the accept: pair the `--json` run with an immediately-following banner probe on the same `CODEX_HOME`, LABELLED A SIBLING MEASUREMENT rather than the run's own banner.** That is the strongest thing the accept can assert while `--json` suppresses the banner, **and calling it what it is costs nothing.** |
| D-128 | 2026-08-29 | **L1 read D-119 correctly: `seat/C4-E3` stays HELD even though it causes ZERO regressions. Confirmed, and the reasoning is worth stating because the temptation was legitimate.** | L1 trial-merged E3 and measured **206/0 with and without — zero regressions** — and **held anyway**, on the ground that D-119 says E3 stays unmerged and that merging a suite is not obviously the same as landing a node. **It asked me to confirm rather than act on its own reading. CONFIRMED: HOLD.** **A green suite that asserts a behaviour the server does not have is not evidence of anything, and landing it would put a passing test in the tree for a guarantee the product does not make — the tree would then be green ABOUT NOTHING, which is the exact class this sprint has spent three days finding.** **When D-118 lands and E3 goes actually green, merge it then.** **AND A CONTAINED FALSE ALARM I want recorded because it nearly was not: L1's first trial measured 3 failures and 39 VANISHED TESTS and it was ready to report a regression against another seat. IT WAS ITS OWN HARNESS — a fresh worktree has no `node_modules`, so every test importing `ajv` failed at load. The control run — `main` alone, same kind of worktree — gave the identical 164/3.** **It ran the control only because THE ERROR NAMED A PACKAGE RATHER THAN AN ASSERTION.** L0's pit arriving a second time in one night: **THE ENVIRONMENT IS PART OF THE TEST**, and a failure that names a dependency is a failure about the harness until proven otherwise. |
| D-129 | 2026-08-29 | **T3 needs a sentence in the README, recorded in `G2.notes` where the drafter will meet it — not a ruling, a thing the drafter must know before drafting.** | L2 flagged it explicitly NOT as a ruling: **the README does not exist, and `RUBRIC.md` §1's evidence rule refuses ruling on unwritten prose.** Recorded in the authority because **it exists nowhere else and would not survive the night in a chat log** — the same reason D-121's disclosure went into `D5.notes`. **L2's framing is better than a defect line and is what the drafter should write from: T3 IS NOT ONLY A DEFECT, IT IS THE HONEST COST OF THE THING WE ARE ADVOCATING. We keep flow control out of descriptions because the registration state machine IS the workflow — and T3 is a blind agent hitting the price of that choice. IT KNEW WHICH TOOL TO CALL. It could not resolve "last Tuesday" or invent a merchant, because nothing in the surface tells it and WE DELIBERATELY DID NOT PUT IT IN THE PROSE.** **A judge who thinks about our design for thirty seconds arrives at that objection unaided. FINDING IT IN OUR OWN TEXT SCORES; HAVING IT FOUND AGAINST US DOES NOT** — §4.1 requires that an unfixed thing be volunteered, and a **planned line beats one extracted at the end-state review after the text is set.** **AND THE POINT I HAD NOT SEEN UNTIL L2 SAID IT: AN INSTRUMENT THAT FOUND NOTHING WOULD BE WORTHLESS. T3 is the strongest available evidence that the blind run was A REAL TEST RATHER THAN A LAP OF HONOUR** — which is why it belongs **in the same paragraph as the 9/12, not buried under it.** Three obligations now travel together wherever a figure appears: **D-121's disclosure, D-126's ceiling (5 of a possible 6, two deliberately unfulfillable), and D-129's sentence.** Cross-referenced on `D5` in short form, **with the full text in one place so the two cannot drift.** **NO T3 FIX TONIGHT, and L2's reasoning is adopted: no registered tool returning today's date is A DESIGN GAP, NOT A BUG, and 01:00 is exactly when a design gap gets "fixed" into something worse. If it is fixed at all it should be by daylight and on purpose.** |
