# A. DEFECT TABLE

| # | Sev | Defect | Evidence |
|---|---|---|---|
| 1 | **HIGH** | **S10's freeze predicate is unsatisfiable as written.** `git log -1 --format=%s -- erp/contracts/violation.schema.json` must match `^freeze:`. L0 gate (5) commits that path with subject `bootstrap: outpocket sprint A` (`git add -A`), and §2.4 says the contract files are pre-existing and **no node lists one as an output** — so nothing ever produces a later commit touching that path. MEASURED in a scratch repo: after bootstrap the subject stays `bootstrap:`; `git commit --allow-empty -m 'freeze: contracts'` does **not** change `git log -1 -- <path>`. S10 is critical-path node #3, cut 0, Day 1, and it unblocks T1/S1/S4. The project already has the correct pattern one node over — S5's `git log -1 --format=%B -- erp/contracts/eval-case.schema.json` works because S5 actually edits the file. | `graph.json` S10.accept; reproduced in `/private/tmp/.../scratchpad/sign/gt` |
| 2 | **HIGH** | **R-30 landed in the CONTRACTS universe and in zero node accepts.** `CONTRACTS.md:1399` books "Rework: **S5**, **F4**, **E3**, **E9**". None of the four accepts in `graph.json` mentions decline-to-unlock, `E_ALREADY_ANSWERED`, or the one-shot guard. Grep across `graph.json`: 0 hits for `decline` / `ALREADY_ANSWERED`. Consequence: a code in the frozen rejection table has no implementing gate; F4 has no "already answered — start a new one" requirement; E9's bar is still "all five" attacks (see #3). | `graph.json` S5/F4/E3/E9 `accept` vs `CONTRACTS.md:1399` |
| 3 | **HIGH** | **`eval-case.schema.json` contradicts itself inside a frozen file.** `x-requiredCases.note` says "**ELEVEN** negative controls… the eleventh is neg-decline-to-unlock". The `negative_control` array holds **ten** and does **not** contain `neg-decline-to-unlock`. The description key `neg_decline_to_unlock` exists; the id was never added to the list. Frozen by S10 on Day 1. | `erp/contracts/eval-case.schema.json` `x-requiredCases` |
| 4 | **HIGH** | **N-20 is double-assigned across the two authorities.** `graph.json` E3.accept: "`neg-policy-content-swap`… asserted under the identifier **N-20** — THE N-NUMBER IS ASSIGNED HERE… the next free number is N-20 and it is now taken." `EVAL.md:763` assigns **N-20** to `neg-decline-to-unlock`. `neg-policy-content-swap` appears **nowhere in EVAL.md** (0 grep hits). E3's accept fails against the suite C4 builds from EVAL.md's table. | `graph.json:637`; `EVAL.md:763` |
| 5 | **MEDIUM** | **Ruling-number collisions R-24 and R-30.** `graph.json.rulings_applied`: R-24 = "D-17 has a home and a gate on Day 0", R-30 = "flag names, headless". `CONTRACTS.md` / `contracts/*.json` / `charters/C3.md`: R-24 = "the signature binds the policy's CONTENT", R-30 = decline-to-unlock. `graph.json` **contradicts itself**: E3.accept cites "ruling R-24" in the CONTRACTS sense. PLAN.md D-29 cites R-30 as the flag ruling while C3.md:178 cites R-30 as decline-to-unlock. | `graph.json:217,223,637` vs `CONTRACTS.md:1398,1399` |
| 6 | **MEDIUM** | **`survivingVector`'s rejection walk is incomplete, and omits the one code that fires.** It says "Walk **the whole of** x-rejectionCodes" and enumerates **12 of 14**, omitting `E_NO_CONFIRM_TOKEN` and `E_ALREADY_ANSWERED`. Its 7-field body fails the frozen `sign_respond_request` (**eight required fields, `additionalProperties:false`, `confirm_token` required**) → 403 `E_NO_CONFIRM_TOKEN` by `confirmToken`'s own text. The same stale numeral "**twelve codes**" is baked into frozen `eval-case.schema.json` `examples[1].observedToday` — on the exact rule R-30 had just written ("cited as the table, never as a numeral"). | `contracts/signature.schema.json` `x-signRequestState`; `eval-case.schema.json` `examples[1]` |
| 7 | **MEDIUM** | **Missing hard edge V5 → S10 (and V5 → G6).** V5 outputs `erp/contracts/probe-verdict.schema.json` on Day 0; S10 freezes the directory Day 1 and G6 ajv-validates *every* `*.schema.json` there. Only the schedule orders them, and `--check-schedule` proves `day(u)≤day(v)` **over hard edges only** — with no edge there is nothing to prove. If V5 slips one day, the ninth schema lands after the freeze with no scheduled deviation. G6 is cut 1, so at 2.5 h/day nothing validates it at all while V1 (cut 0) ajv-compiles it. | `graph.json` V5.outputs, S10.inputs=`['T6']`, G6.inputs=`['L0','S10','S11']` |
| 8 | **MEDIUM** | **PATHS.md Glob-owner column wrong on 4 rows** — the writing seat was copied into the glob column. `src/page/sign-bridge.js` says I3, longest glob `src/page/**` → **UX**; `tools/blind-home.sh` says L2, `tools/**` → **L1**; `evals/blind/C1-verdict.json` says C1, `evals/blind/**` → **L2**; `evals/redteam/report.md` says C3, `evals/**` → **C4**. All four writes are still legal under rule (a), but a seat reading PATHS.md and a seat reading its generated charter block (PATHS §7.1) will disagree. | `PATHS.md:126,303,384,385` vs `graph.json.file_ownership` |
| 9 | **MEDIUM** | **EVAL.md restates E9 at 3 h; graph.json says 3.5.** The v2.2.0 note names "E9 3.0 → 3.5" as one of its four hour moves. This is the **only** node-field mismatch in the corpus (GRAPH.md 68 rows/264 field checks clean, PLAN.md 68 rows/186 checks clean). | `EVAL.md:426` |
| 10 | **MEDIUM** | **EVAL.md restates the critical path as 28.5 h**, citing `graph.json.critical_path_A` as its source. Graph says 29.0; PLAN.md:462 and GRAPH.md:724 both carry the 28.5→29.0 move. Not a retraction — a live assertion. | `EVAL.md:441` |
| 11 | LOW | EVAL.md's report template (line 1427) offers `refused` in the Result column — the value R-27 bans and which makes `eval-case.schema.json` fail its own schema. Its ID column still reads "N-01 … N-19" though the table now runs to N-20. | `EVAL.md:1427` |
| 12 | LOW | S5's accept prose says "the confirm_token this node ships flips that case to **REFUSED**" in the same paragraph that bans the word. The gate itself correctly demands `enforced`. | `graph.json:509` |
| 13 | LOW | PATHS.md says "51 of **157** rows". I parse **165** master-table rows, **57** disagreeing. | `PATHS.md:18` |
| 14 | LOW | `graph.json.revision_note` still says "the **ten-entry** exemption list"; G0.accept and PATHS §2.4 say **EIGHT** (R-28 dropped two). | `graph.json:5` |
| 15 | LOW | RISK.md's BW-14 sweep claims "**14** bare vs **6** word-bounded, delta **8**". I measure **23 vs 11** (delta 12) over `erp/*.md`+charters, **26 vs 13** including `reviews/`. The qualitative claim reproduces exactly (only BW-14 of 32 differs; the whole delta is `f`+`our differentiators`); the counts are stale. The file itself says "Re-run this sweep whenever a row is added." | `RISK.md:214` |
| 16 | LOW | `erp/DECISIONS.md` — an L0 output gated by L0 gate (1) — has **no master-table row** in PATHS.md; it appears only in §6, the dead-names table, annotated "real". PATHS §7.3 calls a missing path "a defect in **this** file". | `PATHS.md:494` |
| 17 | LOW | G0's tokenizer admits directory prefixes as candidates at (t4), but the stated resolution rule ("appears in some node's `outputs` or is a pre-existing artifact under erp/") does not resolve them. **8 tokens** hang on reading "appears in outputs" as prefix-of-output: `webmcp-eval-kit/src` ×2, `webmcp-eval-kit/bin`, `tests/redteam`, `tests/acceptance`, `src/page`, `erp/contracts` ×2. A strict implementation exits 1 on Day 1 on a cut-0 node. | `graph.json` G0.accept (t4) |
| 18 | LOW | ~/.codex profile file **layout** is unspecified anywhere in erp/. MEASURED on codex 0.144.6: `[profiles.<name>]` inside `<name>.config.toml` silently yields `model: gpt-5.6-sol`, `reasoning effort: none` — gate (4) fails with no pointer at the cause. A flat top-level-keys file works (`reasoning effort: low`, grep PASS). Will cost Day-0 time. | measured, this machine |
| 19 | LOW | `capacity.agent_capacity_assumption` says "10 active node-owning seats"; **twelve** own horizon-A nodes. Disclosed in a GRAPH.md footnote; `graph.json` itself is uncorrected. | `graph.json.capacity` |
| 20 | LOW | G0's published run breakdown "157 in node outputs, **12** as output basenames". I reproduce the headline exactly (179 candidates, 19 exemption hits, 10 pre-existing, **0 unresolved**) but split the remainder 156/13. Cosmetic. | measured |

---

# B. HARD STOPS AND VERDICT

**Hard-stop count: 1.** Sequence: 22 → 16 → 5 → 3 → **1**.

The one stop is defect #1 — S10's `^freeze:` commit predicate, on Day 1, on critical-path node #3. Everything else in the Day-0 and Day-1 walk executes. I ran L0 gate (1)'s regex against PLAN's own D-17 row (matches, yields 2.5, equals `human_hours_available/5.5`); gate (2)'s spike suite (**24 tests, 23 pass, 1 fail, failure is exactly `auditor surface: read-only by construction`** — and the failure reason, `open_expense_report must be readOnly`, is precisely what T6's R-9 option (B) fixes); gate (3) (`ls erp/charters | wc -l` = 16); gate (4) (banner prints `reasoning effort: low`, grep passes — with the caveat in #18); gate (6) (`git remote get-url origin` matches the `\(\.git\)\?` alternation, and the MEASURED note that it prints *with* `.git` is correct); `gh` is authenticated as Caleb0796 with `repo` scope and both repos exist. I did **not** count #18 (codex layout — the gate catches it, ten minutes to resolve) or V5's Render provisioning (budgeted work, disclosed by R-18) as stops.

**Verdict: EXECUTABLE. The plan is sound enough to hand over; fix defect #1 before S10 runs on Day 1, and reconcile R-30 (#2–#6) before Day 3.**

---

# C. FOR THE PERSON HANDING THIS OVER

**What is genuinely solid — and it is a lot.** The graph is mechanically clean under every test I could construct: 68 nodes, 119 edges, acyclic, inputs↔edges bijective with zero asymmetry, 108 hard / 100 qualifying / 8 cross-horizon, **0 cut-invariant violations**, critical path recomputed from scratch at **29.0 h over exactly the published twelve nodes in the published order** (and the "27.0 h without T2→H3" claim reproduces node-for-node), every capacity figure and all five ladder rows reproducing to the decimal including `short_by`/`spare` and per-rank depth, 62/62 scheduled once, 0 backwards hard orderings, 0 backwards *soft* orderings too, peak seat-day exactly 6.0 on exactly the five named seat-days, and **35/35 PLAN.md gate blocks matching the accept fields**. PLAN's day table reproduces `schedule_A` day-for-day *and* reads topologically within each day. The falsification register's amputation sets are exact by id. The cryptographic layer is the strongest thing here: I wrote an OCF-1 canonicaliser from the §3 prose alone and reproduced **7/7 conformance vectors byte-for-byte, the policy digest `b7ccc1ff…` at exactly 2458 canonical bytes, the snapshot digest `535bb82f…` at exactly 1570, all five tamper digests including the consistent re-total and the same-version policy swap, the surface digest `630daf55…` with its 230+33+17=280 / ceil(280/4)=70 accounting, and both chain entries with `seq 1.prev == seq 0.entry_digest`** — 15 published values, zero misses. ajv-2020: 6/6 schemas compile, 17/17 examples valid, 9/9 invalid examples rejected, and the one `x-lintFailingExamples` instance is schema-valid on purpose and trips two of the 19 banned substrings, verbatim from `countinghouse/src/policy.js:133` — which is the correct line. The 19-rule count (15 line-level + 4 report-level at 193/197/199/218) reproduces, as do the 1,396 ported lines. The banned-phrase sweep is clean (exactly one of 32 patterns differs bare vs `\b`, and the BW-14 fixture gives 5 hits old / 3 hits new on both BSD `grep -E` and JS `RegExp`, lines 3-4-5, exactly as claimed); the "16 rules" and "eight frozen schemas" sweeps find only retractions outside `reviews/`. And **IR-16 reproduces exactly on the installed binary** — Chrome 152.0.7977.64, `--headless=new`, clean profile per launch: no flag → `undefined`, `WebMCP` → `object` + registerTool succeeds, `WebMCPTesting` → `object` + registerTool succeeds. The session owner's measurement stands; I got the same thing. I also confirmed IR-16b for free: on a LAN IP (10.0.0.185) `document.modelContext` is `undefined` **even with the flag**, and `navigator.modelContext` is `undefined` in all six launches, confirming the 150-removal rule.

**The first hour tomorrow.** Fix defect #1 before anything else, because it sits on Day 1's highest node and the fix has to be chosen, not discovered: either give S10 a real byte to change in `violation.schema.json` (the way S5's scheduled deviation gives itself one) or change the predicate to assert `git log -1 --format=%s -- erp/contracts/FREEZE.md` — a path S10 genuinely produces. Then spend the rest of the hour on the R-30 reconciliation, because it is one root cause with four faces: add `neg-decline-to-unlock` to the actual `negative_control` array in `eval-case.schema.json` (#3), settle the N-20 collision by giving `neg-policy-content-swap` its own row in EVAL.md (#4), decide whether the sign-gate ruling or the Chrome-flag ruling keeps the number R-30 and likewise R-24 (#5), and put `E_ALREADY_ANSWERED` into S5's accept and the sixth attack into E9's (#2), or the one-shot guard ships untested. All of that is text, none of it moves an hour or an edge, and doing it before the freeze is cheap while doing it after is a deviation ticket per file. The two one-line edits (#9 E9's hours, #10 the 28.5) take a minute and remove the last table drift in the corpus.

**The single thing most likely to go wrong that nobody has caught.** Not any defect above — it is defect #6's shape. The corpus has *two consistent internal accounts of whether the sign gate's surviving forgery is open*, and they live two JSON keys apart in the same frozen file. `survivingVector` says flatly "IT COMMITS" and walks twelve codes; `confirmToken` and `$defs.sign_respond_request` make `confirm_token` one of eight required fields under `additionalProperties:false`, so the exact body `survivingVector` posts is refused 403 `E_NO_CONFIRM_TOKEN` — and that code is one of the two the walk silently omits. `negativeControl` reconciles them temporally (open on today's empty build, enforced once S5 ships the token), and that reconciliation is correct — but it is stated in only one of the four places the claim appears, and it is stated in none of the places a seat will actually read: not in C3.md's target 6, not in the `provableClaim` framing, not in the README/video/Devpost language the whole R-13 apparatus exists to police. C3 has been ordered, in bold, to run the vector against the build in front of them and to "say so loudly" if it now refuses. On Day 5 it *will* refuse, because S5 landed the token on Day 3 — and the charter's own instruction tells the red team that a refusal means "something changed and nobody wrote it down." Someone will spend hours re-opening a closed question, or worse, will conclude the gate is stronger than `provableClaim` permits and write that sentence into the Devpost text, which is the one failure this project has built four separate instruments to prevent. Put the temporal qualifier into `survivingVector` itself — one clause, "before the confirm_token ships" — and add `E_NO_CONFIRM_TOKEN` to its walk.