# CONFIRMATION PASS — `/Users/calebwei/mcp/outpocket/erp/`

Everything below was executed. Nothing was accepted on a document's say-so. Scripts and evidence in `/private/tmp/claude-501/-Users-calebwei-mcp/67898feb-2bcd-4702-acaa-66b19b7e96db/scratchpad/` (`graphsuite.py`, `ocf1.py`, `tables.py`, `acceptpaths.py`, `chk/check3b.mjs`, `l0/contracts-check.mjs`, `l0/attacks.mjs`, `chrome/drive.mjs`, `chrome/probe1.mjs`).

## 0. What re-ran clean

| Check | Result |
|---|---|
| parse, 68 nodes, unique ids, 119 edges | clean |
| inputs↔edges bijection | 119/119 both directions, 0 dupes, 0 dangling |
| topological sort | acyclic, 68/68, roots `{G1, L0}` |
| cut invariant, hard + same-horizon | **0 violations over 100 qualifying edges** (cut 0 = +∞); 108 hard / 11 soft |
| critical path | **29.0 h**, same 12 nodes, same order — but only under the documented method (`hard edges only`); over all edges it is 30.5 h. Runner-up without `T2→H3` = **27.0 h** on exactly the path `critical_path_A` names |
| capacity | every figure reproduces: 118.0 / 10.5 / 107.5 / 5.375 / 15.875; all four ladder thresholds incl. `short_by` 2.125/1.525/0.3 and `spare` 0.275; per-rank freed hours 12.0/24.5/11.5/0.0; 48.0 h cuttable over 27 A-nodes; 35 cut-0 A-nodes |
| `schedule_A` | 62/62 once, 0 backwards edges (all kinds), peak agent seat-day **exactly 6.0** at the five named seat-days |
| `--check-freezes` | 10/10 `unblocks` have the hard edge |
| gate-block byte equality | **35/35 PLAN.md quoted gates byte-identical** to `graph.json.accept`; the quoted set is exactly the 35 cut-0 A-nodes |
| PLAN.md day table | 62/62 equal on day/owner/hours/cut |
| GRAPH.md lane tables | 68/68 equal on id/title/owner/inputs/hours/cut |
| ajv-2020 over `erp/contracts/` | 6/6 compile, **17/17 examples valid, 9/9 `x-invalidExamples` correctly rejected** |
| L0 gate (2), executed | `node --test tests/*.test.mjs` → **24 tests, exactly 1 failure, `auditor surface: read-only by construction`** |
| `npm test -- <name>`, measured both ways on node v22.23.1 | `"test":"node --test"` → exit 1, `Could not find 'surface'`; `"test":"node --test tests/*.test.mjs"` → whole suite, argument inert |
| banned-phrase sweep, whole tree, generated list | generator emits exactly 32 rows BW-01..BW-32; 166 hits, **0 live assertions** |
| `"16 rules"` | 17 occurrences, all retractions or reviews quoting the historic defect |
| R-7 from source | `countinghouse/src/policy.js` → 15 line-level + 4 report-level = **19** distinct codes |
| **published digests** | **21 of 21 reproduce** from an independent Python OCF-1 written only from CONTRACTS.md §3 prose — 7/7 vectors, 3 policy digests + 2 byte counts (2458/2457) + the `17bc4b2d` trap, **6/6 snapshot rows incl. the new swap row**, snapshot canonical bytes **1570**, and **both chain entries** |

## 1. The fourteen residual defects

| # | Defect | Status | Evidence (executed) |
|---|---|---|---|
| 1 | signed snapshot not committable | **CLOSED** | `attendees` is 3. Ran the frozen policy engine over the frozen snapshot: `blocking=0 warning=0 clean=true total=22850`, matches the carried verdict, **COMMITS**. Reverted to 2 → `CAP_MEALS block ln_3 "$186.40 exceeds the meal cap of $80.00 × 2 attendee(s) = $160.00"`, 422 `E_NOT_CLEAN`. Spike `LIMITS` == frozen `limits_cents` on all 8 keys. All six §7.2 digests, 1570 bytes and both chain entries moved together and all recompute. `x-invalidExamples` re-keyed to `ln_7`. §11 gains check 3b. |
| 2 | F4 accept vs frozen `sign_respond_request` | **PARTIAL** | `graph.json`, `PLAN.md:809`, `GRAPH.md:467` all carry the corrected 8-field wording, byte-identical. **`charters/UX.md:103-114` does not**: it still tells the UX seat, present tense, that `graph.json`'s F4 says "ONLY `{decision, reason}`", that the frozen schema requires **seven** fields (it is eight), and to *"Escalate to PM on Day 1… This is a `send-back` on the predicate."* CONTRACTS §7.3's correction box names UX.md as a site and is itself still present-tense. |
| 3 | H2 `--exec` not implementable | **PARTIAL — executable half still OPEN** | The research landed and is correct; I re-measured every claim in EVAL.md §2.3 (below). But `graph.json`'s H2 accept is **byte-unchanged**: still `getTools().length === 1`. Measured: `getTools().length` → `undefined`. No `WebMCP.enable`, no `toolsAdded`, no `invokeToolRoundTrip`, no `headless:false`. EVAL.md §2.5 says *"The replacement predicate is in §2.5.1 and is `graph.json`'s to adopt"* — it was not adopted, and `graph.json` is the declared authority for accept strings. H2's `notes` still pose the question §2.5 answers; `GRAPH.md:1077` still lists it as an open falsification. |
| 4 | S3 reads `.code` not `.id` | **CLOSED** | Ran the new predicate verbatim against the frozen document: **exit 0**. Ran the old form: **exit 1**. |
| 5 | Nothing pushes | **CLOSED as written — and it created NEW-1** | L0 gains gate (6) push + gate (7) clone-the-pushed-remote; `L0→G3` names `package-lock.json` *on origin/main*; `G1→G3` visibility edge added. Measured: `origin/main` = `7496b06`, 3 files, `erp/` untracked; both repos `private MIT` (anon `curl` → 404, `gh api` → private); `git remote get-url origin` matches gate (6)'s regex. |
| 6 | `npm test -- <name>` | **CLOSED** | Measured both ways. All five call sites now `node --test <path>`; surviving mentions are R-23's own retraction. L0 gate (2) reproduces exactly. |
| 7 | G0 `--check-accept-paths` self-fails | **PARTIAL** | Both named causes fixed. Re-implemented the checker to G0's own stated rules: 12 discarded, 18 exempted, 102 resolved in outputs, **7 still unresolved** — `E4:AGENTS.md`, `E4:auth.json`, `E4:config.toml`, `E5:no-net.mjs`, `E6:eval.yml`, `F4:CONTRACTS.md`, `L0:PLAN.md`. None falls in the three declared discard classes or the ten-entry exemption list. Exit 0 vs 1 now turns entirely on an unspecified tokenizer. (Also: 2 of the 10 exemptions — `provenance`, `tool-export` — are never used by any accept.) |
| 8 | D-17 homeless | **CLOSED** | `erp/DECISIONS.md` is an L0 Day-0 output; gate (1) is its gate and cross-checks `human_hours_available == v×5.5`. 13.75 = 2.5×5.5 verified. |
| 9 | E3 fails by construction | **PARTIAL — the fix is broken (NEW-2)** | S5 now files `.team/deviations/DEV-E3-eval-case-known-open.md`, lands the edit in the same commit, and gates it four ways. But the gate requires `controlStatus === 'refused'` and the frozen enum is `["enforced","known-open","not-runnable"]`. Ran ajv: `refused` → *"must be equal to one of the allowed values"*. |
| 10 | "eight frozen schemas" dead vocabulary | **PARTIAL** | G6's *predicate* is now correct (`every *.schema.json in erp/contracts/`) and the stale `canonical_bytes` catch is gone (I recomputed 2458 and 2457, both correct). CONTRACTS.md:85-95 declares the phrase dead as of 2026-08-28 — yet **`graph.json`'s G6 title is still "Contracts conformance runner over all eight frozen schemas"**, restated in `PLAN.md:624` and `GRAPH.md:208`, plus ~18 further sites. The authority breaks the rule the same round wrote. V5 adds a 9th contract file on Day 0, so "eight" is also numerically stale from Day 0. |
| 11 | RISK.md §2 worked example | **CLOSED, one residue** | Marked HISTORICAL, line numbers deleted, instruction explicitly withdrawn. Sweep confirms all 25 `structural guarantee` hits are bans/retractions. Residue: the section still bolds *"**Replacement text, to be pasted at all three sites by their owners:**"* two paragraphs after withdrawing exactly that instruction. |
| 12 | `registerTool` returns a Promise | **PARTIAL** | Corrected in `EVAL.md:1227`, `FACTS.md:504`/`:884`, `charters/I1.md:215`. **`charters/K2.md:89` still says it "returned `undefined` synchronously"** and hangs the performance caveat on it. Measured today: `[object Promise]`, `isPromise=true`, `isUndefined=false`. |
| 13 | chain `seq 0` unrecomputable | **CLOSED** | `x-knownChain` publishes both entries' inputs. Recomputed `seq 0` → `b0ab10c6…` ✓, `seq 1` → `a25d6903…` ✓, `seq1.prev == seq0.entry_digest` ✓, `seq0.payload_digest` == the policy digest `b7ccc1ff` ✓. |
| 14 | `falsification[8]` cites rank numbers | **CLOSED** | Names all 27 by id, "per R-12, which forbids naming rank numbers in an operational trigger". `GRAPH.md:1077` restatement matches. |

**5 closed · 6 partial · 3 closed-with-residue.** Zero remain in the form the audit described.

## 2. NEW defects this round introduced

**NEW-1 — HIGH. The pushed remote can never be green, so L0 gate (7) *and* G3 are both unsatisfiable.**
L0 gate (2) declares the tree it pushes has exactly 1 failing test (`auditor surface`, fixed by T6 on Day 1). L0 gate (7) then requires `git clone <remote> && npm ci && npm test` to **exit 0** on that same tree — one node demanding "exactly one failure" and "zero failures" of the same commit. Measured: that tree's `npm test` exits 1. The same trap hits G3 (Day 1, **cut 0**), which clones the same remote and requires *"0 fail with at least 24 tests"* — and gate (6) says **"NOTHING ELSE IN THIS GRAPH PUSHES"**, `TEAM.md:421` says "L1 is the sole pusher", and all 16 charters say "Never `git push`". `grep -rn "git push"` over the whole tree returns exactly one command, inside L0. T6's fix never reaches `origin`. Introduced by R-25's repair of defect #5.

**NEW-2 — HIGH. S5's scheduled deviation writes a value outside the frozen enum.** Gate: `if(e.controlStatus!=='refused') process.exit(1)`. Enum: `enforced | known-open | not-runnable`. Ran ajv both with and without `observedToday` rewritten — invalid either way. §11 check 1 (owner G6) is *defined* to fail when "any `examples[*]` fails validation against its own schema", and §11 says every check is wired into `npm test`. So Day 3 turns `npm test` red repo-wide. The enum's own word for what S5 means is `enforced`. One word; introduced by the fix to defect #9.

**NEW-3 — HIGH. `FACTS.md` IR-16(b) does not reproduce.** Published, graded **MEASURED 2026-08-29, "sixteen launches"**: `--headless=new`, no flag → `typeof document.modelContext` = `object`, `invokeTool` works. Seven launches today, Chrome 152.0.7977.64, clean dedicated `--user-data-dir`, page from `http://localhost:8731`:

| launch | `typeof document.modelContext` | CDP round trip |
|---|---|---|
| headed, no flag | `undefined` | no tools |
| headed, `--enable-features=WebMCP` | `object` | `Completed` |
| headed, `--enable-features=WebMCPTesting` | `object` | `Completed` |
| **`--headless=new`, no flag** | **`undefined`** | **no tools** |
| `--headless=new`, `WebMCP` | `object` | `Completed` |
| `--headless=new`, `WebMCPTesting` | `object` | `Completed` |
| `--headless` (old), no flag | `undefined` | no tools |

Five of six published rows reproduce, including IR-16(a) — the two flags **are** interchangeable. The row IR-16(b) is built on does not. Likely cause of the original error: **`WebMCP.enable` returns OK in every launch**, including no-flag with no page API at all, so a probe reading the CDP domain instead of the page API reads "on" when it is off. This is load-bearing twice: V0's Day-1 predicate is designed around it (`FACTS.md:129`), and EVAL.md §2.5's "Headed" bullet — one of the three things the replacement H2 gate must check — rests on it.

**NEW-4 — MED. `cdpDomainEnabled` in `evidence/H2-reachability.json` is a vacuous field.** Measured true with no flag and zero tools. Only `invokeToolRoundTrip` discriminates. §2.5.1 says so in prose and then lists both as if equal.

**NEW-5 — MED. One state transition has zero rejection codes, and decline-to-unlock still succeeds unclaimed.** `x-signRequestState` says *"A second respond on an already-answered record is refused; the machine is one-shot"* — none of the thirteen codes claims that condition (`E_NOT_SIGNED` is commit-only; `E_SIGN_IN_PROGRESS` is "every mutating **report** route"). The table's own rule is "NO TWO ENTRIES MAY CLAIM THE SAME CONDITION"; here one condition has none. Ran it: attacker with the token declines first → the human's genuine click is refused → commit returns `200 E_DECLINED`. Still absent from C3's target list (which has "race a second respond", not decline-first).

**NEW-6 — LOW. BW-14's generated pattern false-positives.** `our differentiator` matches the innocent "f**our differentiator**s", which occurs 8× in `erp/`. `erp/**` is excluded so it does not fire today — but D5 runs the same lint over `docs/DEVPOST.md`, and "three of our four differentiators are invisible server-side invariants" is exactly the sentence PLAN/GRAPH/RISK/UX/L2/RUBRIC all supply for copying.

**NEW-7 — LOW. Stale counts.** `charters/C3.md:150` tells the red team "All **eleven** rejection codes were walked" — the table is thirteen. `GRAPH.md:1077` still lists H2's page-API reachability as an open falsification EVAL.md §2.5 declares answered.

## 3. Signature attacks — what succeeds vs what the documents claim

Ran against a reference implementation of the §7.3 machine and all thirteen codes, built from the frozen contract.

| Attack | Result | Documents claim | Agree |
|---|---|---|---|
| One-request forgery (N-15) | **REFUSED** 409 `E_NOT_SIGNED` | refused, `E_NOT_SIGNED`, `enforced` | **yes** |
| Two-request `/respond` (N-16), scripted verbatim (7 fields) | **REFUSED** — fails `required` on `confirm_token` | both branches written down in S5's accept | **yes** |
| Same, 8th field with a guessed token | **REFUSED** 403 `E_NO_CONFIRM_TOKEN` | 403 `E_NO_CONFIRM_TOKEN`, REFUSED | **yes** |
| Same, attacker reads the dialog DOM (V3=yes) | **COMMITS** 200, `signed_by=chen`, chain len 1 | predicted exactly, "a direct function of open unknown V3" | **yes** |
| **Same-version policy content swap** (the audit's Invented C) | **REFUSED** 409 `E_POLICY_DIGEST_MOVED` (trap digest confirmed `17bc4b2d…` under unchanged `2026-08.1`) | R-24, `neg-policy-content-swap`, `enforced`, honest scope attached | **yes — newly claimed and newly closed** |
| **Decline-to-unlock** (the audit's Invented D) | **SUCCEEDS** — human's genuine click → 409; commit → 200 `E_DECLINED` | still not claimed anywhere | **no** |

The list the audit found disagreeing is now down to one item, and it is a nuisance requiring the token, not a forgery. The `policy_digest` gap the audit invented was adopted, implemented in the projection, given a code, given a negative control, and given an honest scope note — and I confirmed the digest actually moves.

## 4. Chrome, re-measured independently (152.0.7977.64)

Every claim in EVAL.md §2.3 reproduces: `executeTool('whoami',{})` → `TypeError: … not of type 'RegisteredTool'`; `executeTool(descriptor,{})` → `UnknownError: Failed to parse input arguments`; `executeTool(descriptor, JSON.stringify(args))` → OK, resolves to a JSON string; `getTools()` returns a Promise; descriptor own keys are exactly the seven listed; `Schema.getDomains` does **not** list `WebMCP` (35 domains); `WebMCP.enable` → OK; `toolsAdded` names the tools and carries `frameId`; `invokeTool` returns `{invocationId}` **and nothing else**; `toolResponded` carries `status:"Completed"` with the content; unknown name → `-32602 "Tool not found"`; `toolsAdded` spells annotations `readOnly`/`untrustedContent` while the page reads back `readOnlyHint`/`untrustedContentHint`; `consequentialHint` silently dropped; `navigator.modelContext` and `provideContext` undefined. **The research is sound. Only IR-16(b) fails, and only `graph.json` failed to receive any of it.**

## 5. Hard-stop count and verdict

Walking Day 0 (L0, V5) then Day 1 (16 nodes) from `erp/` alone:

1. **NEW-1** — L0 gate (7), and again at G3 on Day 1. Nothing can push T6's fix.
2. **Defect #3** — H2's first-hour gate, "gating everything else", runs `getTools().length === 1` = `undefined === 1`.
3. **Defect #7** — G0 `--check-accept-paths` on 7 unclassifiable tokens.

Plus V5's Render instance (host and domain named under R-18; the account itself still presumed), on the same basis the previous round counted it. And one *deferred* stop: **NEW-2** turns `npm test` red repo-wide on Day 3.

**22 → 16 → 5 → 3.**

**VERDICT: NOT-EXECUTABLE, but by a margin that is now measured in single hours rather than days.** The graph is provably sound and the cryptographic layer is now, as far as I can make it fail, complete: 21 of 21 published values recompute from prose, the frozen snapshot commits under the frozen policy, and the four named attacks plus the audit's own invented fifth all behave exactly as the documents say. The three remaining stops are all one-file edits. What this round actually got wrong is narrower and more embarrassing than what it fixed: **three of the six partial defects are the same failure — a correction that landed in the prose document and never landed in `graph.json` or in a charter.** `EVAL.md` retracted H2's predicate and told `graph.json` to adopt the replacement; it did not. `CONTRACTS.md` corrected F4 and named `charters/UX.md` as a site; UX.md still says send it back. `CONTRACTS.md` killed "eight frozen schemas" as dead vocabulary; `graph.json` still uses it in a node title. The corpus has a mechanical checker for restated *tables* (`--check-tables`) and none for restated *rulings*.

## 6. For a non-specialist

Yes — hand it to a fresh session tomorrow morning, with one instruction: **fix L0's own gate (7) before doing anything else.** The plan is in good shape. Everything a reviewer would poke at with a calculator now holds up: I recomputed every published number in the corpus from the written rules alone and all 21 came back identical, the one signed example that used to be self-contradictory is fixed and I proved it by running the rules over it, and the four security attacks the plan predicts all behave exactly as predicted. The first thing tomorrow's session will hit is at the very start, roughly ninety minutes in: the bootstrap step is written to push the code to GitHub and then check out a fresh copy and confirm all the tests pass — but the same step, three paragraphs earlier, says that copy deliberately contains one known failing test that a later task fixes. Those two sentences cannot both be satisfied, so the first task of the sprint fails itself, and the same knot re-ties on Day 1 at a second task (G3) because the plan says only one person is ever allowed to push, so the fix for that failing test never reaches GitHub for anyone to clone. It is a ten-minute edit — the bootstrap step should assert "exactly the one known failure", and something has to push after the fix — but nobody can start until someone makes it. Two other small landmines are worth clearing in the same sitting: the browser-reachability gate on Day 1 was rewritten correctly in one document and never copied into the file the working session actually reads, so as written it will fail on a technicality; and the Day-3 fix for the evaluation records writes a status word that the frozen schema does not allow, which will turn the whole test suite red three days in.