L0 — Day-0 bootstrap. Gate-by-gate evidence.
Recorded by L1, 2026-08-29, Day 0.
Machine: darwin 25.6.0 · node v22.23.1 · npm 10.9.8 · codex-cli 0.144.6
Every predicate below was executed VERBATIM from erp/graph.json L0.accept via
  node tools/l0-gate.mjs --run <n>
which extracts the backticked command from the authority and runs it with sh -c.
No predicate was retyped, paraphrased, or relaxed.

================================================================================
GATE (1)  PM RULING D-17, BEFORE ANY SEAT IS DISPATCHED            ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run 0   ->  exit 0

erp/DECISIONS.md CREATED on Day 0 (it did not exist; V6 appends to it on Day 2).
The D-17 row was lifted verbatim out of erp/RUNBOOK.md §2 "The D-17 row,
literally" by tools/l0-decisions.mjs — not hand-copied.

  | D-17 | 2026-08-28 | human_hours_per_day = 3.0 | ...

The predicate checks three things and all three hold:
  - regex ^\|\s*D-17\s*\|[^\n]*human_hours_per_day\s*=\s*([0-9.]+)   matched
  - captured value 3.0 is one of {2.5, 3.0}                           yes
  - graph.json capacity.human_hours_available == 3.0 x 5.5 == 16.5    yes

This gate is a RECORDING, not a decision. The user ruled 3.0 h/day on
2026-08-28, directly, before any seat was dispatched. Consequence of record:
16.5 h available against 15.875 required, 0.625 h spare, NOTHING IS CUT, all 62
horizon-A nodes stay in scope. The 27-node amputation set survives in
capacity.human_budget_sensitivity as a contingency, not as the plan.

================================================================================
GATE (2)  PORT, AND package.json's `scripts` BLOCK                 ***GREEN***
================================================================================
scripts block — required to be exactly:
  "scripts": {"test": "node --test", "start": "node server/index.mjs"}
Verified by literal fixed-string match against the span in L0.accept:
  grep -cF '"scripts": {"test": "node --test", "start": "node server/index.mjs"}' package.json
  ->  1

Port from ~/mcp/countinghouse/, line counts re-verified by wc -l and matching
erp/FACTS.md §11 exactly:
  src/tools.js      401      (FACTS §11: 401)
  src/erp.js        425      (FACTS §11: 425)
  src/policy.js     250      (FACTS §11: 250)
  src/scenarios.js  187      (FACTS §11: 187)
  src/samples.js    133      (FACTS §11: 133)
  tests/            290      (46 + 135 + 109; FACTS §11: 290)
  ---------------------
  src total       1,396      (FACTS §11: 1,396 lines of pure logic, no DOM)
PORTED, NOT REWRITTEN.

  node tools/l0-gate.mjs --run 11
  test -f src/tools.js && test -f tests/surface.test.mjs && node --test tests/*.test.mjs

  not ok 18 - auditor surface: read-only by construction
  # tests 24
  # pass 23
  # fail 1

24 tests, EXACTLY 1 failure, and the failure is the named one. This is the
known RED test. T6 fixes it on Day 1 by removing open_expense_report from the
auditor surface and replacing it with a side-effect-free get_report(report_id).
IT WAS NOT FIXED HERE. The predicate asserts the report, not exit 0; the run
exits 1 by construction and that is the specified outcome.

================================================================================
GATE (3)  TEAM TREE                                                ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run 12  ->  exit 0
  test -d .team/charters && test -d .team/contracts && test -d .team/log
    && test -d .team/deviations && test -f .team/lint/banned.txt

  node tools/l0-gate.mjs --run 13  ->  16
  ls .team/charters | wc -l        ->  16     (required: 16)

.team/charters is a SYMLINK to ../erp/charters, created here and by nothing
before. This is why L1's first boot — and only its first — reads
erp/charters/L1.md directly (R-15, TEAM.md §1, FACTS.md §14). Every later boot
and every other seat uses .team/charters/.

The 16 is the reconciled seat count: 15 agreed seats with four Codex positions,
the fourth of which (C4, eval engineer) is finally named rather than invented.

================================================================================
GATE (4)  PROFILES, R-3, existence AND effect                      ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run-clean 15  ->  exit 0

GREEN ON THE SECOND READING. THE FIRST READING WAS MINE, AND IT WAS WRONG.

This gate was reported RED for part of Day 0. The predicate is NOT defective,
erp/graph.json was never edited, and erp/FACTS.md §14 stands as written and
correctly graded MEASURED. Full account in
.team/deviations/DEV-L0-gate4-banner-grep.md, which is CLOSED and asks PM for
nothing.

What happened: `--run 15` exited 1, and the banner really did arrive carrying
ANSI SGR codes — ESC[1mreasoning effort:ESC[0m low — confirmed by cat -v, od -c
and spawnSync, and reproducible an hour later. The observation was sound. The
root cause inferred from it was not. The cause is FORCE_COLOR=3, exported by
the resident Claude session L1 runs inside, which makes codex colour its banner
even into a pipe. That is also why NO_COLOR=1 did not help: FORCE_COLOR wins.
MEASURED by tools/l0-forcecolor-probe.mjs, same invocation twice, env the only
difference:

  child env                    banner bytes                        gate's grep
  inherited (FORCE_COLOR=3)    [1mreasoning effort:[0m low         NO MATCH
  FORCE_COLOR removed          reasoning effort: low               MATCH

--run-clean removes FORCE_COLOR from the child environment and changes nothing
else; the command string is the same one extracted from erp/graph.json. A seat
normalising its own shell is not a seat editing the authority.

THE PROFILES WERE NEVER IN DOUBT.

All four ~/.codex/<name>.config.toml layers were written with FLAT TOP-LEVEL
KEYS per erp/FACTS.md §14 — no [profiles.*] table, no nesting — and all four
take effect. MEASURED by tools/l0-profile-evidence.mjs, evidence only, not a
substitute gate:

  profile     test -f   tomllib   banner (SGR stripped)      want
  verifier    PASS      PASS      reasoning effort: low      low
  builder     PASS      PASS      reasoning effort: medium   medium
  redteam     PASS      PASS      reasoning effort: high     high
  evaluator   PASS      PASS      reasoning effort: high     high

~/.codex/config.toml reads model_reasoning_effort = "ultra", so every one of
these four banners moved OFF the base config. That is a real effect check, not
an existence check, and it also confirms §14's layout warning: the [profiles.*]
failure mode is not what happened here.

R-4 is satisfied: builder, redteam and evaluator each set
sandbox_workspace_write.network_access = true; verifier does NOT, and verifier
is not used for C1's blind run in any case (E8, TEAM.md §1).

erp/graph.json IS UNTOUCHED, both when this gate read RED and now that it reads
GREEN. Relaxing a predicate to get a merge through is a banned behaviour for
this seat, and no relaxation was made — the amendment this ticket once asked
for is WITHDRAWN, not adopted.

================================================================================
GATE (5)  DEPENDENCIES, THEN COMMIT                                ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run 17  ->  exit 0
  node -e "...devDependencies; if(!d||!d.ajv) process.exit(1)"

ajv is declared HERE and nowhere else, because S10, G6 and V1 all invoke it on
Day 1 and no other node declares it. Declared as "ajv": "^8.17.1" — the v8 line
is the one that ships the 2020-12 build at ajv/dist/2020.

  node tools/l0-gate.mjs --run 18  ->  exit 0
  npm install && test -f package-lock.json && git add -A
    && git commit -m 'bootstrap: outpocket sprint A'

  npm install: found 0 vulnerabilities
  package-lock.json: present, 2452 bytes
  commit: 25 files changed, 2293 insertions(+)

package-lock.json is the point of this gate. npm ci fails with EUSAGE without
one, and four documented call sites run npm ci — G3's among them.

  node tools/l0-gate.mjs --run 19  ->  51        (required: > 30)
  git ls-files erp | wc -l

  node tools/l0-gate.mjs --run 20  ->  bootstrap: outpocket sprint A
  git log -1 --format=%s                        (required: matches ^bootstrap:)

51, not 50: erp/DECISIONS.md is the file gate (1) created.


================================================================================
GATE (6)  PUSH — THE FIRST PUSH                                    ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run 21  ->  exit 0
  git remote get-url origin | grep -q '^https://github.com/Caleb0796/outpocket\(\.git\)\?$'

The remote prints WITH the .git suffix, as the accept says it does, and the
optional-group regex matches it. An equality test against the bare URL would
have failed here.

  node tools/l0-gate.mjs --run 23  ->  exit 0
  git push -u origin HEAD:main
  c1bcceb..a1f650b  HEAD -> main

  node tools/l0-gate.mjs --run 24  ->  exit 0
  test "$(git rev-parse HEAD)" = "$(git ls-remote origin refs/heads/main | cut -f1)"

origin/main now carries the PRODUCT tree — package.json, package-lock.json,
src/**, tests/** and .team/** — which it did not before this gate. Committing
locally was not enough: G3 clones the GitHub URL on Day 1 and never sees this
working tree.

R-26(b): this is the FIRST push, not the only one. L1 carries a standing
obligation to push every merge to main. T6 fixes the RED test on Day 1 and G3
asserts zero failures against origin/main — that fix reaches the remote only
because L1 pushes it.

================================================================================
GATE (7)  LOCKFILE FROM A CLONE OF THE PUSHED REMOTE               ***GREEN***
================================================================================
  node tools/l0-gate.mjs --run-seq 28 29  ->  exit 0

Both spans run in ONE shell, because the first ends with `cd /tmp/l0` and the
second must execute inside that clone. Run separately, the second would assert
a property of the worktree instead — the exact mistake R-26(a) corrected.

  rm -rf /tmp/l0 && git clone https://github.com/Caleb0796/outpocket /tmp/l0
    && cd /tmp/l0 && test -f package-lock.json && npm ci
  ->  Cloning into '/tmp/l0'... added 5 packages, audited 6, 0 vulnerabilities

  npm test > /tmp/l0/out.txt 2>&1; node -e "...tests 24 / fail 1 / named..."
  ->  exit 0

  /tmp/l0/out.txt:
    not ok 18 - auditor surface: read-only by construction
    # tests 24
    # pass 23
    # fail 1

The clone is taken from the REMOTE, not from `.`, because the remote is the
tree G3 installs from. `npm ci` exits 0 — the EUSAGE this node exists to
eliminate is gone.

THIS GATE ASSERTS 24 TESTS WITH EXACTLY ONE NAMED FAILURE, NOT EXIT 0. That is
STRONGER than exit 0, not weaker: exit 0 is also satisfied by a suite that
silently stopped discovering tests, which is the failure `npm test -- <name>`
already produced once in this project (R-23). Zero failures on origin/main
becomes true only after T6 is merged and L1 pushes that merge; asserting THAT
is G3's job, which is why G3 carries a hard edge from T6.

================================================================================
RESULT: ALL SEVEN GATES GREEN. L0 ACCEPTED.
================================================================================
  main = a1f650b, pushed, remote sha verified equal to local HEAD.
  Committer/merger on main: L1 only.
  Pit: kb/pits/L0.md — all five keys.
  Deviations raised: 1 (DEV-L0-gate4-banner-grep), CLOSED by the seat that
    raised it, no amendment requested, erp/graph.json untouched throughout.

STILL OUTSTANDING, NOT PART OF L0: node V5's remaining output sits on branch
worktree-i1-v5-probe-verdict-schema (1d75053) — erp/contracts/probe-verdict.schema.json,
the NINTH contract file. S10 sha256-hashes erp/contracts/ on Day 1 expecting
nine. V5 is a separate node and gets its OWN merge, its own pit and its own
push; two nodes in one sha cannot be reported or reverted separately.
