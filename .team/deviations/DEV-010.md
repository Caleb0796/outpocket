ID: DEV-010
OPENED: 2026-08-29T15:23:54+0000
SEAT: I2
NODE: T1
CATEGORY: silent-descope
CLAIM: T1 merged with all gates green while its two declared outputs (src/page/tools/compile.js, src/page/tools/defs.js) were unservable in a browser — their upward imports resolved above the served document root and returned 404 — until D-66 widened the served root.
EVIDENCE:
  grep -n "erp.js\|policy.js" src/page/tools/compile.js src/page/tools/defs.js
  src/page/tools/compile.js:30:import { ErpError } from "../../erp.js";
  src/page/tools/defs.js:23:import { CATEGORIES, FX, fmtUsd, fmtMoney, policyForAgent } from "../../policy.js";

  grep -n '"id": "T1"' -A 20 erp/graph.json
  "D-66, PM 2026-08-29 — T1 IS NOT SENT BACK AND IS NOT AT FAULT, and its two upward
  imports are NOT edited. src/page/tools/compile.js and src/page/tools/defs.js import
  ../../erp.js and ../../policy.js, which is correct module code: the files exist, the
  paths resolve, and T1's tests import them from Node where they resolve fine. What was
  absent was a SERVING TOPOLOGY that could deliver them, which is S1's scope and not I2's.
  This is the seventh instance of the consumer-specifies-the-producer family and the fix
  is again in the producer."

  git log --oneline | grep -i "D-66\|widen"
  6855e8d S1: fix D-66 regression — inject <base href="/page/"> for the GET / alias
  bed3b49 S1 follow-up (D-66): widen served root from src/page/ to src/
  ca93d07 Merge PM D-66, D-67: widen the served root to src/; the page-graph arm reads the root from config
VERDICT:
VERDICT_NOTE:
