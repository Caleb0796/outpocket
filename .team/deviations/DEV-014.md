ID: DEV-014
OPENED: 2026-08-29T23:48:24+0000
SEAT: I2
NODE: S11
CATEGORY: silent-descope
CLAIM: S11 merged with tests/canonical.test.mjs green while src/canonical.js imports node:crypto, an import a browser cannot resolve, so the page's module graph (defs.js -> policy.js -> canonical.js -> node:crypto) fails to link and T2's registerTool call sites never run in the served page; fetch-based probes report 200 on every module and cannot see the link failure.
EVIDENCE:
  grep -n "node:crypto" src/canonical.js
  src/canonical.js:8:import { createHash } from "node:crypto";

  grep -n "^import" src/policy.js
  src/policy.js:15:import { canon, digest } from "./canonical.js";

  grep -n "^import" src/page/tools/defs.js
  src/page/tools/defs.js:23:import { CATEGORIES, FX, fmtUsd, fmtMoney, policyForAgent } from "../../policy.js";

  git log -1 --format=%H origin/main
  e1bf38bd88c79f3b04cab10e6c05bbad915cc8e5

  npm test (tail)
  # tests 139
  # pass 139
  # fail 0
VERDICT:
VERDICT_NOTE:
