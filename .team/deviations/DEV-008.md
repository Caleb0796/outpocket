ID: DEV-008
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: F0
CATEGORY: discipline
CLAIM: L1 ran `claude rm` on the UX seat while it was BUSY (not idle/done) in order to re-dispatch it, and destroyed F0's uncommitted outputs, costing roughly one hour of redone UX work.
EVIDENCE:
  cat .team/log/merges.txt
  "COSTS I INCURRED TODAY, recorded here because W files on evidence and this is
  the evidence: (a) I ran `claude rm` on a BUSY UX seat to re-dispatch it and
  DESTROYED F0's uncommitted outputs — ~1 h of UX work, re-done, no permanent
  loss. Never rm a seat that is not idle/done."
VERDICT: debt
VERDICT_NOTE: F0. Cost: approximately 1 h of UX work destroyed and re-done, no permanent loss. `claude rm` removes a seat's worktree along with its job state; it must never be run against a seat that is not idle/done.