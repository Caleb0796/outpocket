ID: DEV-017
OPENED: 2026-08-30T04:03:28+0000
SEAT: I4
NODE: G2
CATEGORY: silent-descope
CLAIM: G2 merged green (readme-credentials.test.mjs asserts only the two login lines and the two browser-open paths) while the repo-root README.md, the file a public GitHub landing page shows first, still opens with "Status: planning. No product code yet." and lists src/ and tests/ as "not yet created" — false at 48/62 nodes done with a live deploy, ahead of G1 flipping both repos public.
EVIDENCE:
  sed -n '1,10p;48,55p' README.md
  > **Status: planning.** No product code yet. The build plan lives in
  [`erp/`](erp/) and is the single source of truth for execution.
  | `src/` | Application source (not yet created) |
  | `tests/` | Test suite (not yet created) |

  python3 -c "import json; s=json.load(open('erp/graph.state.json')); print(len(s.get('done',[])))"
  48

  grep -n '"README.md"' erp/graph.json
  918:        "README.md",
  (G2.outputs)

  python3 -c "import json; g=json.load(open('erp/graph.json')); [print(n['accept']) for n in g['nodes'] if n['id']=='G2']"
  (accept text asserts only the login-line/role coverage and the two literal browser-open strings; nothing about the Status banner or the layout table)

  grep "MERGED G2" .team/log/merges.txt
  MERGED G2  ab4831b pits:PENDING accept:PASS

  find src tests -type f | wc -l
  46
VERDICT:
VERDICT_NOTE:
