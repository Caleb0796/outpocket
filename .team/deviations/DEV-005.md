ID: DEV-005
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: G5
CATEGORY: silent-descope
CLAIM: G5 merged to main with all three accept clauses PASS, while the delivered hook (.githooks/pre-commit-ownership) is never invoked by git — a staged path the hook should have blocked was committed successfully.
EVIDENCE:
  cat kb/pits/G5.md
  "AND THE HOOK DOES NOT FIRE. THIS NODE'S ACCEPT PASSES AND ITS PRODUCT IS INERT. I staged
  a path L1 may not write, committed, and the commit SUCCEEDED. The cause is not a bug in the script:
  git only ever invokes a hook named `pre-commit`. There is no `pre-commit-ownership` hook in
  git. `.githooks/pre-commit` exists and is G4's — it runs the Layer-0 lint, passed, and let the
  commit through."

  git -C /Users/calebwei/mcp/outpocket show d70c28d --stat | head -5
  commit d70c28db89114463152f72d306d2e562290bae22
  Merge: 677f32d 78a0c4b
      Merge G5: file-ownership pre-commit hook — and it does not fire
VERDICT: send-back
VERDICT_NOTE: THE ONLY SEND-BACK. G5.accept clause 3 is AMENDED: the hook must ACTUALLY FIRE — a real git commit staging a path the seat may not write must be REJECTED by git, with a non-zero commit and HEAD unchanged. The old clause proved only that the DIRECTORY was registered and that the script exits correctly WHEN INVOKED BY HAND. Git invokes hooks only by canonical name, so three clauses passed over an inert product. The fix is CHAINING, not logic: the script is correct and must be invoked from .githooks/pre-commit alongside G4's lint. DEADLINE Day 2, 09:00 PT. BRANCH: G5's, marked blocked.