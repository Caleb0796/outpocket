ID: DEV-018
OPENED: 2026-09-03T19:40:00+0000
SEAT: final audit
NODE: S10 / T5
CATEGORY: contract-runtime-drift
CLAIM: The runtime compiler used S2 for a dirty 13-tool draft and S3 for a clean
  14-tool draft, while the frozen export schema and public artifact used
  S2-emp-draft-clean and S3-emp-draft-dirty. The mismatch gave the same ids two
  meanings across runtime inspection, harnesses, and published evaluation data.
OWNER AUTHORIZATION: During the 2026-09-03 final audit, the project owner
  explicitly authorized fixing discovered bugs, testing, syncing main, and
  deploying the result.
CHANGE MADE:
  Runtime, inspector, harnesses, tests, and contracts now use S2 for the clean
  14-tool draft and S3 for the dirty 13-tool draft. The tool-export schema also
  defines app_commit as the last commit touching the derived surface sources,
  because an artifact cannot contain the hash of the commit that contains it.
  The two affected frozen hashes are re-recorded in erp/contracts/FREEZE.md.
GATE:
  npm test — 306 pass, 0 fail
  node harness/drive.mjs --assert-flips 2,6,13,14 — 7/7 clauses pass
  node harness/drive.mjs --fallback --scenario happy — 7/7 clauses pass
  node harness/rehearse.mjs --runs 5 — 5/5 runs pass
  sha256sum -c erp/contracts/FREEZE.md — all ten entries OK
VERDICT: adopt
VERDICT_NOTE: This removes a documented contradiction rather than changing the
  six public canonical states or any tool membership.
