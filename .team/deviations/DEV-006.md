ID: DEV-006
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: T1
CATEGORY: silent-descope
CLAIM: T1.accept clause 2 invokes tools/check-toplevel.mjs without its --selftest flag, so the tool's own real-zero-vs-unexamined-zero vacuity check never runs, and no later node's accept re-invokes check-toplevel once T2 introduces the first real registerTool call sites.
EVIDENCE:
  cat .team/log/merges.txt
  "WHAT ACTUALLY SURVIVES, and it is narrower and still worth PM's ruling:
     - T1's accept clause 2 does NOT invoke --selftest. The tool's own
       "is this zero a real zero" arm exists and no predicate exercises it.
     - T1 is the ONLY node whose accept invokes tools/check-toplevel.mjs.
     - T2, where registerTool call sites first appear, mentions check-toplevel
       0 times and iframe 0 times.
   So: the invariant is checked once, before it can fail, with the tool's vacuity
   arm switched off, and never again after T2 makes it falsifiable."

  grep -n '"id": "T1"' -A 12 erp/graph.json
        "id": "T1",
        ...
        "accept": "`node --test tests/surface.test.mjs` passes all ported surface tests in the new repo AND `find src/page -name '*.js' -print0 | xargs -0 node tools/check-toplevel.mjs` exits 0, finding zero registerTool call sites reachable from an iframe or worker entry file."
VERDICT:
VERDICT_NOTE:
