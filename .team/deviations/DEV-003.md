ID: DEV-003
OPENED: 2026-08-29T15:23:54+0000
SEAT: I4
NODE: G4
CATEGORY: scope-drift
CLAIM: G4's accept gate 1 (repo-wide sweep) was REJECTED on first submission, and the fix added two mechanisms not named in G4.accept's text — SWEEP_EXCLUDE and REGISTER_FILES — the first of which then required a second follow-up commit because its initial SWEEP_EXCLUDE principle was found to be partly wrong.
EVIDENCE:
  git -C /Users/calebwei/mcp/outpocket log --oneline | grep -i "G4\|SWEEP_EXCLUDE"
  0ff5a8d G4: narrow SWEEP_EXCLUDE to the verbatim-extract class only; rewrite pit under quote-nothing rule; add selftest regression
  eff8003 G4: generalize SWEEP_EXCLUDE from a literal list to a stated principle
  0f3cebd Merge G4: Layer-0 lint hook — banned identifiers, budget, wording, retracted claims
  e84cc49 G4: SWEEP_EXCLUDE — sweep must not walk files that quote bans to enforce them
  bd96226 Merge G4 follow-up: narrow SWEEP_EXCLUDE to the verbatim-extract class only

  cat kb/pits/G4.md (EARLIER section)
  "Gate 1 was rejected on the first submission: `node tools/lint-layer0.mjs` (clause 1,
  repo-wide sweep, no path on the command line) exited 1, tripped by three files, not one —
  ... The reviewer named the resolving shape precisely: clause 1 (sweep, exit 0) and clause 2
  (fixture named explicitly, exit 1) are the same file only for the fixture, and the fix is a
  swept-vs-named distinction, not a change to the fixture or to the predicate's four-entry
  EXCLUDE array. I added `SWEEP_EXCLUDE`, a third list distinct from both `EXCLUDE` (the
  predicate's literal array) and `REGISTER_FILES` (the RETRACTED.txt mechanism)"
VERDICT:
VERDICT_NOTE:
