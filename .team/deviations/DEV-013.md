ID: DEV-013
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: V2
CATEGORY: discipline
CLAIM: `claude rm` on the live seat+I1-V2 worktree destroyed two uncommitted files, most likely the V2 and G2 pit drafts — the same command family that destroyed F0's uncommitted outputs (DEV-008) — and is the likely reason those two pits are missing rather than merely unwritten.
EVIDENCE:
  git worktree list | grep -i "V2\|I1-V2"
  (no seat+I1-V2 entry present — the worktree that carried the branch no longer exists)

  grep -n "VECTOR, per mcp-6d" -A 10 .team/log/merges.txt
  "VECTOR, per mcp-6d: the seat+I1-V2 worktree was removed while carrying two
  uncommitted files, at the moment I absorbed that branch. If those were the
  pit drafts they died with the tree and the reflog will not have them — they
  were never committed. That is the same `claude rm`/reset family that cost F0
  an hour and orphaned V2's commit."
VERDICT:
VERDICT_NOTE:
