ID: DEV-011
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: G0
CATEGORY: discipline
CLAIM: L1's merge log (.team/log/merges.txt) asserted `pits:kb/pits/<node>.md` for H5, S9, V2 and G2 while none of those four pit files existed on main, and the V2/G2 rows also carried no sha, yet the row count agreed exactly with graph.state.json.done the whole time.
EVIDENCE:
  git log --oneline | grep -i "check-record\|correct four"
  2f3774f L1: correct four overstating merge rows, write S9's pit, add --check-record

  git show 2f3774f -- .team/log/merges.txt | head -30
  -MERGED H5  b446bc2 pits:kb/pits/H5.md accept:PASS
  +MERGED H5  b446bc2 pits:PENDING accept:PASS
  ...
  -MERGED V2   pits:kb/pits/V2.md accept:PASS
  -MERGED G2   pits:kb/pits/G2.md accept:PASS
  +MERGED V2  4b01533 pits:PENDING accept:PASS
  +MERGED G2  ab4831b pits:PENDING accept:PASS

  git show 2f3774f -- .team/log/merges.txt | tail -30
  "RECORD CORRECTION — MY LOG ASSERTED FOUR FILES THE TREE DOES NOT HAVE. ...
  THE ROW COUNT AND done COUNT AGREED EXACTLY THE WHOLE TIME. Both registers
  agreed while both overstated the tree. TWO REGISTERS AGREEING IS NOT TWO
  REGISTERS BEING RIGHT — clause 6c makes the record part of the merge and
  nothing checked the record.
  --check-record closes it: every MERGED row must carry a sha that resolves to
  a commit and a pit that exists or honestly reads PENDING, and the row set and
  graph.state.json.done must agree in BOTH directions. 24 rows, 24 done,
  3 PENDING, exit 0.
  Found by mcp-6d."
VERDICT:
VERDICT_NOTE:
