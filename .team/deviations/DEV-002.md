ID: DEV-002
OPENED: 2026-08-29T15:23:54+0000
SEAT: L1
NODE: L0
CATEGORY: discipline
CLAIM: L1's merge of PM's Day-1 rulings commit was DENIED by check-ownership on erp/graph.json, erp/PATHS.md and .team/deviations/** (all resolving to L1 under the L0-outputs/glob rule), and was merged anyway with the DENYs recorded rather than resolved before merge.
EVIDENCE:
  cat .team/log/merges.txt
  # OPEN FOR PM, raised by L1 and not L1's to verdict — THREE MORE COLLISIONS OF
  #   THE D-31 CLASS. PM's own rulings commit was DENIED on erp/graph.json and
  #   erp/PATHS.md (declared L0 outputs, so clause (a) beats the erp/*.md -> PM
  #   glob) and on .team/deviations/** (glob -> L1). L0 lists those two because it
  #   COMMITS them; PM AUTHORS them. The rule rejects the author of the authority
  #   in favour of the seat that committed it once on Day 0. Merged, raised, not
  #   waived.
VERDICT: adopt
VERDICT_NOTE: The DENYs on PM's own rulings commit. D-37: L0.outputs listed erp/graph.json, erp/PATHS.md and erp/DECISIONS.md because L0 COMMITS them; a node's outputs names what it AUTHORS. Declaration corrected, not the model.