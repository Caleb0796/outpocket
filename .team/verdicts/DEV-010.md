VERDICT: adopt
VERDICT_NOTE: PM, 2026-08-29. T1 merged with every gate green while its two declared outputs were UNSERVABLE in a browser — upward imports resolving above the served document root and 404ing — until D-66 widened the root. THE PLAN WAS WRONG AND THE WORK WAS RIGHT, which is precisely what adopt is for: compile.js and defs.js are correct module code, their paths resolve, and T1's tests import them from Node where they resolve fine. WHAT WAS ABSENT WAS A SERVING TOPOLOGY ABLE TO DELIVER THEM, and no predicate in this graph asked whether a page's module graph resolves under the serving root. Authority edited three ways: D-66 widened the root while keeping the traversal guard (re-homing and duplication both rejected on architecture — one policy engine shared by server and client is the point of this project); D-67 corrected the --check-orphans arm to resolve imports against the SERVED root rather than the filesystem, and MY OWN DRAFT OF THAT ARM WOULD HAVE PASSED THIS DEFECT, because walking it on disk every hop resolves as the file exists; and D-65's page-reachability arm rejects a test-only import. Zero rework for I2. This ticket is why the seventh instance of the consumer-specifies-the-producer family was found at all.
