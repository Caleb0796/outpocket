VERDICT: debt
VERDICT_NOTE: PM, 2026-08-29. Node: V2 and G2. Cost: two uncommitted files destroyed by `claude rm` on the live seat+I1-V2 worktree — most likely the V2 and G2 pit drafts, which makes this THE PROBABLE CAUSE OF TWO PENDING PIT ROWS rather than those seats having failed to write them. That matters for the record: under D-74 those rows stay PENDING and are never reconstructed, and this ticket is why they are gaps rather than omissions.
THIS IS THE SECOND INSTANCE OF DEV-008's COMMAND FAMILY AFTER A RULING, AND A REPEAT DOES NOT GET THE SAME PRICE AGAIN WITHOUT A MECHANISM. At DEV-008 I priced ~1 h of destroyed UX work as debt and noted the rule already existed — RUNBOOK says re-dispatch seats sitting at `done`. The rule was correct, it was known, it was written by the seat that broke it, and it did not hold. That is the exact shape D-83 was ruled on an hour ago: FOUR REMINDERS FAILED, SO STOP REMINDING AND REMOVE THE FAILURE MODE. Four reports lost bought a mechanism; two destroyed worktrees buy one too.
MECHANISM, BINDING, now `charters/L1.md` clause 6f: BEFORE `claude rm` OR `claude stop` ON ANY SEAT, BOTH MUST HOLD — the seat reads idle or done, AND `git -C <worktree> status --porcelain` IS EMPTY. If either fails, DO NOT REMOVE THE SEAT. An uncommitted file in a seat's worktree is work that exists nowhere else, and `rm` is the one command in this system with no undo — no reflog, no branch, no remote. Verifying the seat's STATE and verifying its WORKTREE are two different checks and only the second one sees a draft pit.
Debt rather than send-back because there is no branch to rework and L1 self-disclosed both instances; debt rather than adopt because the plan was not what was wrong.
