NODE:      E5
OWNER:     C4
INPUTS:    T5, E1
DO:        Run the accounting eval suite under a hard in-process network denial and write evals/accounting.json. YOUR PREDICATE HAS FOUR CLAUSES AND THREE OF THEM CAN PASS WHILE PROVING NOTHING, SO READ IT AS AN ADVERSARY BEFORE YOU BUILD. FIRST AND MOST IMPORTANT — PERMITTED IS NOT POSSIBLE, AND ABSENCE OF A CONNECTION IS NOT PROOF OF A DENIAL. If no-net.mjs fails to install its hooks — wrong import order, a prototype replaced after you patched it, an --import that silently no-ops — then the suite makes no network calls, exits 0, and your accept passes while the denial NEVER ARMED. Those two worlds are indistinguishable from the outside and only one of them is the claim. So the suite must carry a POSITIVE CONTROL: a deliberate connect attempt and a deliberate dns.lookup, both asserted TO THROW, run inside the same process as the eval. If the control does not throw, the run must FAIL LOUDLY rather than pass quietly. A denial you have not seen fire is a denial you are guessing about. SECOND: "two runs byte-identical" is satisfied perfectly by an empty result file, a constant, or a header with no per-case body. Byte-identical is a determinism check, and determinism over nothing is free. Assert the file contains a result PER CASE, that the case count is greater than zero and equals the suite's declared case count, and that pass/fail verdicts are actually present — then assert byte-identity on top of that. Report the case count in your five fields. THIRD: `! grep -rq 'api\.openai\|api\.anthropic' webmcp-eval-kit/src/` passes if that directory is empty, if it does not exist, or if you point it somewhere wrong — a negative grep over nothing succeeds. Assert the path exists and contains files BEFORE trusting the negative, and print how many files were scanned. A negative result is only as good as the proof that the search actually ran. FOURTH, the one clause that is already well-built: the header must record WHICH denial mechanism ran. Honour it literally — name the mechanism (prototype patch on net.Socket.prototype.connect and dns.lookup, in-process, via --import), not a boolean "network: false". A boolean records an opinion; the mechanism name records a measurement, and a later reader can check it. DO NOT WEAKEN THE PREDICATE TO GET GREEN. If it cannot be satisfied, stop and report why with the exact command and output; L1 escalates to PM and the predicate gets amended or the node gets cut. That is the process and it has already run twice today without anyone being penalised for it. Ownership: you write only under webmcp-eval-kit/ and evals/. Do not touch src/, tests/, harness/, erp/ or artifacts/tools.export.json — artifacts/tools.export.json in particular is a merged T5 output that E5's drift guard READS, and a node that edits the thing it is checking has proven nothing.
ACCEPT:    `node --import ./webmcp-eval-kit/test/no-net.mjs webmcp-eval-kit/bin/eval.mjs --suite accounting` writes evals/accounting.json and exits 0, where no-net.mjs throws from net.Socket.prototype.connect and dns.lookup — the syscall is denied, not an environment variable; two runs are byte-identical (diff exits 0); `! grep -rq 'api\.openai\|api\.anthropic' webmcp-eval-kit/src/` passes; and evals/accounting.json's header records WHICH denial mechanism ran.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/E5.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/C4-E5
DEADLINE:  2026-08-30T14:00-07:00
