NODE:      V4
OWNER:     I1
INPUTS:    V1
DO:        UNPARK V4. It is the last node still stamped Day 2 and you have been idle three hours; PM's D-73 already corrected its accept IN KIND so that everything it now asks for is machine-checkable and none of it needs a human or a browser. This is a desk job. YOU ARE THE REASON THE ACCEPT CHANGED, AND THAT IS THE PRECEDENT I WANT TO NAME BEFORE THE WORK: you read the original predicate, concluded it could not be satisfied honestly, and PARKED IT RATHER THAN MANUFACTURING A RUN 2. PM ruled you right and corrected the node rather than relaxing it. Three seats have now refused a convenient-and-defensible shortcut this week and yours was the first. THREE CLAUSES, ALL MECHANICAL. (1) evidence/V4-run1.json exists, records the observation AND NAMES ITS CLIENT, and is TRANSCRIBED from the existing measurement rather than re-measured — the D-48 precedent, recording a measured fact a file omitted is not editing evidence to make a gate green. Transcribe; do not re-run and do not round. (2) `node harness/compare-runs.mjs --selftest` exits 0, proving the COMPARATOR — that two runs naming DIFFERENT CLIENTS are refused with a distinct message, that missing observations do not compare equal, that a no-timeout run does not average with a numeric one, and that malformed run files are rejected. (3) evidence/V4.json records the second run as NOT TAKEN, names the reason, and names the standing conservative selection. CLAUSE 2 IS THE NODE AND IT IS AN INSTRUMENT, SO THE RULE UX WROTE TODAY BINDS IT: ARM NOTHING THAT HAS NOT DEMONSTRATED IT CAN STILL RETURN THE NEGATIVE — not "can it fire", can it DECLINE to. Three of my own instruments failed open in a single day, one of them inside the fix for another, so a --selftest that only proves the comparator ACCEPTS a good pair is two-thirds unchecked. Each of those four refusals needs its own case and each must be shown failing on demand. And per D-90: a case satisfiable by the input being ABSENT is not a case — "missing observations do not compare equal" must not pass merely because nothing was there to compare. THE NODE MUST NOT CLAIM WHAT IT DOES NOT HAVE, and this is the whole point of the correction: IT DOES NOT CLAIM A TWO-RUN AGREEMENT. What it establishes is ONE NAMED-CLIENT OBSERVATION and a comparator proven able to reject every way the comparison could pass while discovering nothing. Run 1's 22.267 s is the ChatGPT built-in browser's OWN CDP-wrapper timeout, a property of THAT client; on plain Chrome the harness IS the client, so its timeout is a parameter we choose and measuring it would be circular. Say that in evidence/V4.json in those terms rather than leaving a later reader to infer it. AND THE DESIGN CONSEQUENCE IS NOT AT RISK, so do not treat this as blocking anything: contingencies[4] is PROVISIONALLY SELECTED and S5 has SHIPPED the two-call handshake. A second run would upgrade "provisionally selected" to "fired"; it would not change one line of code. STANDING: FORCE_COLOR=3 is injected into every seat — `node tools/accept-gate.mjs V4 --run-clean`; and an unexplained exit 1 GETS RE-RUN BEFORE IT GETS A THEORY.
ACCEPT:    D-73, PM 2026-08-29 — THIS ACCEPT IS CORRECTED IN KIND, NOT RELAXED, AND V4 IS NOT MADE HUMAN-GATED. It previously read: 'Two independent runs write evidence/V4-run1.json and evidence/V4-run2.json, each recording the observed seconds until the client abandons a probe tool whose execute() never resolves, or the literal no-timeout-at-300s. Then compare-runs.mjs ... --tolerance 0.20 exits 0'. I1 read it carefully and concluded it cannot be satisfied honestly, and it is right. Run 1's 22.267 s is the ChatGPT built-in browser's OWN CDP-wrapper timeout - a property of THAT client. On plain Chrome over CDP THE HARNESS IS THE CLIENT, so its timeout is a parameter WE CHOOSE: measuring it would be circular, would discover nothing, and would then agree with itself. And a run 2 on a different client cannot be compared at 20 percent to run 1 anyway - compare-runs.mjs REFUSES two runs naming different clients, by I1's own design, because that is a client mismatch and not a measurement dispute. So the only faithful run 2 is a second BUILT-IN-BROWSER run, which needs a human, on a node that is NOT human-gated. WHAT THIS NODE NOW REQUIRES, all of it machine-checkable: (1) evidence/V4-run1.json exists, records the observation AND NAMES ITS CLIENT, and is TRANSCRIBED from the existing measurement rather than re-measured (the D-48 precedent: recording a measured fact a file omitted is not editing evidence to make a gate green); (2) `node harness/compare-runs.mjs --selftest` exits 0, proving the comparator itself - including that two runs naming DIFFERENT CLIENTS are refused with a distinct message, that missing observations do not compare equal, that a no-timeout run does not average with a numeric one, and that malformed run files are rejected; (3) evidence/V4.json records the second run as NOT TAKEN, names the reason, and names the standing conservative selection. THE NODE DOES NOT CLAIM A TWO-RUN AGREEMENT AND MUST NOT. What it establishes is one named-client observation and a comparator proven able to reject every way the comparison could pass while discovering nothing. THE DESIGN CONSEQUENCE IS ALREADY TAKEN AND IS NOT AT RISK: contingencies[4] is PROVISIONALLY SELECTED and S5 has SHIPPED the two-call handshake. A second run would upgrade 'provisionally selected' to 'fired'; it would not change one line of code.
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/V4.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/V4.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I1-V4
DEADLINE:  2026-08-31T10:00-07:00
