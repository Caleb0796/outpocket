NODE:      F3
OWNER:     UX
INPUTS:    F1
DO:        Build to the accept and nothing beyond it. Read any merged evidence your node depends on before writing — several answers landed today.
ACCEPT:    `node --test tests/acceptance/receipt-channel.test.mjs` — asserts zero registered tool has an inputSchema containing contentEncoding or format:'byte' or a property named file/data/base64, and that link_receipt with an unknown id returns a violation envelope with code RECEIPT_NOT_FOUND.
PIT:       kb/pits/F3.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/UX-F3
DEADLINE:  2026-08-30T23:59-07:00
