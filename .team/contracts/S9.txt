NODE:      S9
OWNER:     I3
INPUTS:    S1
DO:        Your input S1 is merged, including the static route added under D-50. S3's policy engine and S4's violation envelope are also in. Build to the accept and nothing beyond it.
ACCEPT:    boot the server, `curl -s $URL/api/state-digest > /tmp/d1`; restart; `curl -s $URL/api/state-digest > /tmp/d2`; `diff /tmp/d1 /tmp/d2` exits 0.
PIT:       kb/pits/S9.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/I3-S9
DEADLINE:  2026-08-30T23:59-07:00
