NODE:      E1
OWNER:     C4
INPUTS:    H2, S11
DO:        Port the OCF-1 canonicaliser into the eval kit as a VERIFIED PORT of src/canonical.js, not a reimplementation — R-11 makes S11's the only canonicaliser and yours must reproduce the same seven vectors byte-for-byte. Your inputs H2 and S11 are both merged.
ACCEPT:    `cd webmcp-eval-kit && npm ci && npm test` exits 0 — and `npm test` includes a vector suite asserting webmcp-eval-kit/src/canon.mjs reproduces ALL SEVEN vectors in erp/contracts/canonical-vectors.json byte-for-byte — AND `node webmcp-eval-kit/bin/eval.mjs --version` prints a semver AND `--selftest` runs the whole pipeline twice against a fixed local fixture, asserts the two result files are byte-identical AND asserts a non-zero tool count (a byte-identical-twice check passes happily against an empty surface).
PIT:       kb/pits/E1.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/C4-E1
DEADLINE:  2026-08-30T23:59-07:00
