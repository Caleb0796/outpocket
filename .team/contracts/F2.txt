NODE:      F2
OWNER:     UX
INPUTS:    S8, F1
DO:        Build src/page/ui/editor.js and tests/acceptance/editor.test.mjs. Every field cell carries data-source in {agent,human}, and after a human edit of an agent-written field the cell exposes BOTH data-source="human" AND data-prev-source="agent". THIS IS S8'S LEDGER MADE VISIBLE, and it is the claim a judge can check without reading a file: that the page SHOWS which values an agent chose and which a human chose, and shows when a human overrode an agent. S8 landed an hour ago — server/provenance.mjs and server/store.mjs — and I3 flagged explicitly that store.mjs is NOT wired into the HTTP write routes, so read store.mjs's header before you assume where the data comes from. THREE WAYS YOUR PREDICATE PASSES WHILE PROVING NOTHING, AND THE THIRD IS THE ONE I WOULD BET ON. FIRST, "every field cell carries data-source in {agent,human}" IS TRUE OF ZERO CELLS, and it is also true of a page that hardcodes data-source="agent" on every cell. Assert the cell count is non-zero and equals the real field count, and that BOTH values actually occur in the same rendered report — a renderer that can emit "human" and never does is a renderer, not a provenance display. SECOND, "after a human edit ... exposes both" is satisfied by a renderer that sets data-prev-source="agent" unconditionally. Show a field the human did NOT touch carrying NO data-prev-source in the same render — an attribute that appears everywhere records nothing. THIRD, AND THIS IS THE TRAP THAT HAS CAUGHT THIS PROJECT REPEATEDLY: THE ATTRIBUTE IS NOT THE CLAIM. A judge reads the PAGE, not the DOM. A cell carrying data-source="human" while showing nothing a person can see has satisfied your predicate and communicated nothing. Assert the provenance is VISIBLE — text, or a mark with a legend — exactly as you did in F5 when you asserted the simulation notice was visible text and not merely an attribute, "because a judge reads the page and not the DOM". That was your own line and this node is where it pays. AND YOUR OWN NEWEST RULE BINDS THIS NODE HARD: SETUP THAT FABRICATES ITS INPUTS IS AN ASSERTION ABOUT THOSE INPUTS. If the test seeds a report with hand-written provenance rather than driving a real agent write and a real human edit, THE TEST IS ASSERTING THE THING IT IS SUPPOSED TO CHECK. You found that exact defect in your own check-storyboard sign-open state four commits after writing it. Drive the real path if it is eight lines, as it turned out to be there; if it genuinely is not, SAY SO IN THE TEST rather than letting a fabricated fixture read as a measurement. Per D-100, show the renderer refusing to mislabel — a cell must not be able to show "human" for an agent write. And per D-108, prove the check still returns the POSITIVE on a real render, not only the negative on injected cases. STANDING: FORCE_COLOR=3 is injected — `node tools/accept-gate.mjs F2 --run-clean`; and process.stdout.write(String(x)) if you scrape a value into a shell variable.
ACCEPT:    `node --test tests/acceptance/editor.test.mjs` asserts every field cell carries data-source in {agent,human} and that after a human edit of an agent-written field the cell exposes both data-source="human" and data-prev-source="agent".
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/F2.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/F2.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/UX-F2
DEADLINE:  2026-08-31T14:00-07:00
