NODE:      H1
OWNER:     I1
INPUTS:    L0
DO:        Write tools/chrome.mjs and tests/acceptance/launcher.test.mjs. --scenario cdp prints --enable-features=WebMCP and NOT WebMCPTesting; --scenario manual prints --enable-features=WebMCPTesting. This is a HOUSE RULE ABOUT OUR OWN CONFIGURATION, not a claim about the browser: MEASURED 2026-08-28, the two flag names are INTERCHANGEABLE and FACTS.md IR-16(a) is CONFIRMED. What the convention buys is that a launcher log says which scenario produced a run. A graded run made under the other name is an UNLABELLED run, not a broken one.
ACCEPT:    `node --test tests/acceptance/launcher.test.mjs` — asserts `node tools/chrome.mjs --scenario cdp --print-flags` prints a line containing '--enable-features=WebMCP' and NOT 'WebMCPTesting', and that `node tools/chrome.mjs --scenario manual --print-flags` prints '--enable-features=WebMCPTesting'.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields with
           your merge request and L1 writes kb/pits/H1.md itself. The fields, in
           order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a legal ENTRY; no
           report at all is not, and L1 cannot merge you without one. Report it in YOUR
           OWN WORDS — L1 transcribes and names you, and a pit L1 composed is worse than
           no pit. EARLIER is the one that pays: what you would tell yourself an hour
           before you learned it. This resolves a contradiction that stood all of Day 1:
           no node lists a pit in outputs, so the ownership rule's clause (a) is empty by
           design and the glob kb/pits/** is L1's, which made the pit gate and the
           ownership gate reject each other on every merge of the sprint.
BRANCH:    seat/I1-H1
DEADLINE:  2026-08-29T23:59-07:00
