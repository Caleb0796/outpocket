NODE:      T3
OWNER:     I2
INPUTS:    T2
DO:        Build src/page/tools/absence.js — explain_missing_tool, registered on the live page in all six canonical states. YOUR ACCEPT WAS AMENDED THIS HOUR BY PM (D-75) AND THE AMENDMENT IS THE INTERESTING PART, SO READ IT BEFORE YOU BUILD. I held this node rather than dispatching it, because as originally written it could not be satisfied without breaking a frozen contract: erp/contracts/tool-export.schema.json pins x-requiredStates at 1/5/13/12/6/6, adding one tool to all six states makes them 2/6/14/13/7/7, and tools/export-surface.mjs reads those counts OUT OF THE FROZEN SCHEMA and dies — which would have turned your own just-merged T5 red and taken E2 and E5 with it. PM ruled reading (b): explain_missing_tool is REGISTERED ON THE LIVE PAGE and DELIBERATELY ABSENT FROM THE BLIND EXPORT, nothing unfreezes, and the reason is stronger than a stylistic one — E4 copies artifacts/tools.export.json into C1's packet and that file is C1's ENTIRE WORLD, so a tool whose description announces that absences are explainable HANDS A BLIND EVALUATOR THE MECHANISM IT IS BEING TESTED ON. That is contamination of the eval, not a tidiness preference. THE CONDITION IS BINDING AND IT IS WHY THIS IS SAFE: the exclusion must be A NAMED CONSTANT IN tools/export-surface.mjs WITH ITS REASON WRITTEN IN THE SOURCE — never an inline filter, never a silent omission. Both halves are now asserted by your accept, registered on the page AND absent from the export, so that if someone later "fixes" the filter T3 GOES RED INSTEAD OF THE EVAL GOING QUIETLY WRONG. export-surface.mjs is a T5 output and T5 is yours, so the constant lands in your own file. NOW THE NODE ITSELF, AND ITS OWN TRAP. Your predicate asks that the returned body validate against erp/contracts/violation.schema.json with code, severity, field, fix and candidates all PRESENT. PRESENT IS NOT USEFUL. A body carrying code:"UNKNOWN", field:"", fix:"contact your administrator" and candidates:[] validates, satisfies every named key, and is worthless to the agent that asked. So assert the CONTENT: for the worked case in your own accept, --args '{"name":"submit_expense_report"}', the answer must name the REAL reason that tool is absent from the state it was asked about, and candidates must contain the tools that would actually get the caller there — non-empty, and drawn from the live surface rather than a hardcoded list. Prove it discriminates by asking about TWO tools absent for DIFFERENT reasons and asserting the two answers DIFFER. If both questions get the same answer you have built a stub with good manners. LAST, THE ONE THAT WOULD EMBARRASS US MOST: explain_missing_tool must not report ITSELF as missing, and asking it about a tool that IS present in the current state must not fabricate a violation — a tool that explains absences by inventing them is worse than no tool. TWO STANDING LESSONS: FORCE_COLOR=3 is injected into every seat, so a predicate that greps a colourising tool's output fails here and passes in a bare shell — `node tools/accept-gate.mjs T3 --run-clean`; and an unexplained exit 1 GETS RE-RUN BEFORE IT GETS A THEORY, kb/pits/L0.md.
ACCEPT:    `node harness/drive.mjs --list` contains explain_missing_tool in all SIX canonical states (the script asserts count==1 per state, over S0-anon, S1-emp-home, S2-emp-draft-clean, S3-emp-draft-dirty, S4-emp-submitted, S5-aud) AND `node harness/drive.mjs --exec explain_missing_tool --args '{"name":"submit_expense_report"}'` returns a body validating against erp/contracts/violation.schema.json with all of code, severity, field, fix, candidates present. AND — D-75, PM 2026-08-29, READING (b) RULED — THE EXCLUSION IS ASSERTED HERE RATHER THAN FILTERED QUIETLY: `node tools/export-surface.mjs` still exits 0 with erp/contracts/tool-export.schema.json's x-requiredStates UNCHANGED at 1/5/13/12/6/6, AND `grep -c explain_missing_tool artifacts/tools.export.json` returns 0, AND the exclusion is a NAMED CONSTANT in tools/export-surface.mjs with the reason in the source, never an inline filter or a silent omission. explain_missing_tool is a META-TOOL: its job is to explain why a tool is ABSENT. The export is not a census of every registered tool - it is the BLIND-EVALUATION SURFACE that E4 copies into C1's packet, and C1's entire world is that file. A tool whose description announces that absences are explainable hands a blind evaluator the mechanism it is being tested on. So it is REGISTERED ON THE LIVE PAGE in all six states (the clauses above) and DELIBERATELY OUTSIDE THE COUNTED EXPORT (this clause), and both halves are now checked. Nothing is unfrozen: erp/contracts/tool-export.schema.json is additionalProperties:false, so the exclusion CANNOT be declared inside the artifact, which is precisely why it is declared here in the authority instead.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/T3.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I2-T3
DEADLINE:  2026-08-30T14:00-07:00
