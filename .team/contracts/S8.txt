NODE:      S8
OWNER:     I3
INPUTS:    S1, S4
DO:        Build server/provenance.mjs, server/store.mjs and tests/acceptance/provenance.test.mjs. Every expense-line field carries {value, source:'agent'|'human', ts, actor}, and a human edit after an agent write flips source to 'human' and appends TWO day-book entries with DISTINCT sources. THIS IS THE CLAIM THE WHOLE PRODUCT RESTS ON, WRITTEN DOWN PER FIELD: that you can tell, afterwards and from the record alone, which values a human chose and which an agent chose. Treat it that way rather than as a bookkeeping node. THREE WAYS YOUR PREDICATE PASSES WHILE PROVING NOTHING. FIRST, "every field carries {value, source, ts, actor}" IS TRUE OF ZERO FIELDS, and it is also true of a record where every source is hardcoded 'agent'. Assert the field COUNT is non-zero and equal to the line's real field count, and assert that BOTH values of source actually occur in the same report — a schema that permits 'human' and never emits it is a schema, not a provenance record. SECOND, "flips source to 'human'" is satisfied by a writer that sets 'human' unconditionally. The test must show the SAME field reading 'agent' BEFORE the human edit and 'human' AFTER, and must ALSO show a field the human did NOT touch still reading 'agent' in the same record — a flip that flips everything has recorded nothing. That is D-90: a control satisfiable by the subject being absent is not a control, and here the subject is DISCRIMINATION rather than presence. THIRD, "two day-book entries with distinct sources" — assert the two entries' ACTORS as well, and that they are ordered, because two entries with distinct sources and the same actor is one actor wearing two hats, which is precisely the thing an auditor would need to catch. AND PER D-100, EXERCISE BOTH DIRECTIONS OF THE DECISION: show the provenance writer REFUSING to mislabel — an agent write must not be recordable as 'human'. A suite that only proves the flip happens cannot tell a working recorder from one stuck at 'human'. FOUR SEATS SHIPPED ONE-DIRECTIONAL SUITES TODAY AND ALL FOUR THOUGHT THEY WERE DONE. NOTE THE FIELD IS FOUR-PART AND THE ts IS THE ONE THAT WILL BITE YOU: a wall-clock value inside a record that anything else digests or compares byte-for-byte breaks that comparison — H6 nearly shipped exactly this by putting per-step ms into a dump graded for byte-stability. A DERIVED MEASUREMENT AND THE THING IT MEASURES MUST NOT SHARE A CHANNEL WHEN ONE OF THEM IS GRADED FOR BYTE-STABILITY. Check what already digests a line before you add a timestamp to one. STANDING: FORCE_COLOR=3 is injected into every seat, so `node tools/accept-gate.mjs S8 --run-clean`; and if you scrape a value out of node into a shell variable use process.stdout.write(String(x)) — console.log COLOURISES INSPECTED VALUES and I lost two requests to a revision that arrived as an escape sequence (D-111).
ACCEPT:    `node --test tests/acceptance/provenance.test.mjs` — every expense-line field carries {value, source:'agent'|'human', ts, actor}; a human edit after an agent write flips source to 'human' and appends two day-book entries with distinct sources.
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/S8.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/S8.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I3-S8
DEADLINE:  2026-08-31T12:00-07:00
