NODE:      H5
OWNER:     I1
INPUTS:    L0, V0
DO:        Your inputs L0 and V0 are both merged. V0 came back alias-absent on the installed Chrome 152 with a negative-control arm, and V1 has since added a SECOND engine to the picture — the ChatGPT built-in browser is Chromium 151 and the alias is absent there too. Read evidence/V0.json and evidence/V1.json before you write anything; the answer you need may already be recorded.
ACCEPT:    `node --test tests/acceptance/banner.test.mjs` asserts the banner text matches /^Chromium \d+ · WebMCP (present|absent)( · simulated agent)?$/ and that a Chromium major below 153 additionally renders a node with [data-warn="chrome-lt-153"].
PIT:       kb/pits/H5.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/I1-H5
DEADLINE:  2026-08-30T23:59-07:00
