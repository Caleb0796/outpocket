NODE:      F4
OWNER:     UX
INPUTS:    S5
DO:        Build src/page/ui/sign-dialog.js and tests/acceptance/sign-dialog.test.mjs — the confirmation a human sees before they certify an expense report. READ YOUR PREDICATE AS AN ADVERSARY FIRST, BECAUSE ITS CENTRAL CLAUSE IS SATISFIED BY A LIE. The regex /you are certifying .+ if this is wrong, .+/i is matched by the literal string "You are certifying something. If this is wrong, something happens." That passes your accept, passes any reviewer skimming for green, and tells the person signing NOTHING. So the test that matters is not the one in your predicate: assert that the sentence NAMES THE ACTUAL THING BEING CERTIFIED — the report id, the total amount, the number of lines — read out of the live dialog state rather than hardcoded, and prove it by rendering TWO DIFFERENT reports and asserting the two sentences DIFFER. A certification text that is byte-identical across two different reports is a ritual, not a disclosure. SECOND TRAP, SAME FAMILY: "the dialog cannot be confirmed while that element is empty" is provable two ways and only one of them is worth anything. Asserting the button carries a `disabled` attribute checks what the markup SAYS. DISPATCH A REAL CLICK AND ASSERT NO POST HAPPENED — check what the page DOES. Those come apart the instant anything else re-enables the control, and this is the node where they come apart in front of a judge. THIRD: confirming POSTs to /api/sign/{requestId}. Assert the requestId in the POST equals the one the dialog was OPENED with, not merely that it is well-formed — a dialog that signs the wrong report is the exact failure this whole product exists to prevent, and "a POST happened" does not exclude it. MOUNT IT, AND YOU ALREADY KNOW WHY. F1 shipped a module nobody loaded and I told you it was merged when four commits were not on main; F3 was the tag that fixed it. `node tools/ready.mjs --check-orphans` will tell you whether your file is reachable from the page — run it before you report green, because a module nobody mounts is a file, not a feature. If index.html needs the tag and index.html is not yours to edit under the ownership rule, serve a copy of src/ and drive that, then report the tag you need. TWO STANDING LESSONS: every seat runs with FORCE_COLOR=3 injected by the harness, so a predicate that greps a colourising tool's output fails in a seat and passes in a bare shell — use `node tools/accept-gate.mjs F4 --run-clean`; and an unexplained exit 1 GETS RE-RUN BEFORE IT GETS A THEORY, see kb/pits/L0.md.
ACCEPT:    `node --test tests/acceptance/sign-dialog.test.mjs` — the element immediately preceding [data-signature-line] in DOM order has non-empty text matching /you are certifying .+ if this is wrong, .+/i; the dialog cannot be confirmed while that element is empty; and confirming POSTs to /api/sign/{request_id}/respond a body that VALIDATES against the frozen sign_respond_request — all eight required fields {schema, request_id, decision, reason, method, acknowledged_digest, acknowledged_revision, confirm_token} under additionalProperties:false — which carries NO signed_by, NO at, AND NO KEY FOR EITHER, both of which the server takes from the session cookie and its own clock; and whose confirm_token is read out of THIS dialog's rendered DOM, the only place it is ever delivered. AND — R-34, THE RECOVERY, AND IT IS A REQUIRED ASSERTION AND NOT A NOTE: when /respond answers 409 `E_ALREADY_ANSWERED` — the decline-to-unlock case, in which a holder of the confirm_token answers the request before the human's click lands — this dialog renders `already answered — start a new one` and offers a control that MINTS A FRESH SIGN REQUEST. The test asserts the rendered string and asserts that the control produces a NEW request_id. It may not be rendered as a transport error, as a retry of the same request_id, or as a silent no-op: the record is one-shot by construction, so a retry against the answered id can only fail again, and a human who is told nothing has had a signature cancelled without being shown that it was. The previous wording said the body carries ONLY {decision, reason}. That was unsatisfiable: such a body fails `required`, carries no acknowledged_digest for E_DIGEST_ACK_MISMATCH to check and no confirm_token for /respond to require. erp/CONTRACTS.md §7.3 names this defect, prescribes this wording and logs the correction in §15; the substantive point — no signed_by and no at, because the server takes both itself — is the part that was right and it survives verbatim.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/F4.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/UX-F4
DEADLINE:  2026-08-30T12:00-07:00
