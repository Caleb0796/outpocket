NODE:      S2
OWNER:     I3
INPUTS:    S1
DO:        Build server/authz.mjs and tests/acceptance/curl-403.sh. Your predicate is one of the better-written ones in the graph — it already forbids the obvious cheat by failing if the write-route table has a route the script does not cover, so there are no silent gaps. IT STILL HAS TWO WAYS TO PASS WITHOUT PROVING ANYTHING, AND BOTH ARE THIS SPRINT'S SIGNATURE BUG. FIRST AND WORST: A SERVER THAT RETURNS 403 TO EVERYONE PASSES YOUR ACCEPT PERFECTLY. Every route 403s an auditor cookie — yes, and it also 403s the employee who is supposed to be able to write, and your predicate cannot tell those apart. So every negative case needs a POSITIVE CONTROL BESIDE IT: the same route, the same body, an EMPLOYEE cookie, asserted NOT 403. Without it you have measured that the server says no, never that it says yes to the right person, and a totally broken authz layer is indistinguishable from a correct one. SECOND: 403 AND 404 ARE NOT THE SAME ANSWER. A route that does not exist, or whose path you typo'd, returns 404 — and if your script checks "not 200" or greps loosely it will bank a typo as a security control. Assert the status is 403 exactly, and assert the JSON body's code field is present AND is an authz code rather than whatever the 404 handler emits. THIRD, CHEAPEST TO MISS: assert the write-route table is NON-EMPTY before you iterate it. A table with zero routes satisfies "covers every route in the table" completely — for-loops over nothing exit 0, and exit 0 is also satisfied by a check that discovered nothing. That sentence has cost this sprint more than every other bug combined. Print the count and assert a floor. FOURTH, WORTH FIVE MINUTES: confirm the table is READ FROM THE SERVER'S OWN EXPORTED ROUTE TABLE rather than transcribed into the test — a hand-copied list drifts silently the first time someone adds a route, and then the no-gaps clause is checking your copy against your copy. TWO STANDING LESSONS: FORCE_COLOR=3 is injected into every seat, so any predicate that greps a colourising tool's output fails here and passes in a bare shell — `node tools/accept-gate.mjs S2 --run-clean` is the remedy; and an unexplained exit 1 GETS RE-RUN BEFORE IT GETS A THEORY, kb/pits/L0.md is the hour that bought that.
ACCEPT:    `bash tests/acceptance/curl-403.sh` exits 0: for every route in the server's exported write-route table, an auditor cookie receives 403 with a JSON body carrying a code field; the script fails if the table has a route it does not cover (no silent gaps).
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/S2.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I3-S2
DEADLINE:  2026-08-30T12:00-07:00
