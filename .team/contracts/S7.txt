NODE:      S7
OWNER:     I3
INPUTS:    S1, S11
DO:        Build server/chain.mjs and tests/acceptance/chain.test.mjs. Recompute the day-book chain from GET /api/daybook using src/canonical.js and verify every link; then FLIP A SINGLE BYTE of any entry's source field and assert verification fails AT THAT INDEX. THE NODE'S OWN NOTE IS THE POINT AND IT IS WORTH READING TWICE: "The source field must be inside the digest, OTHERWISE PROVENANCE IS DECORATION." You have just built S8's per-field ledger; this is what makes it un-forgeable rather than merely recorded. A provenance record an operator can edit without detection records nothing. TWO THINGS THIS PREDICATE ALREADY DOES WELL, so build to them rather than around them. It names the byte flip in the SOURCE field specifically — not any byte, the one that says whether a human or an agent did it — which is exactly the field an attacker would move. And it requires the failure to be located AT THAT INDEX, not merely detected: a chain that says "something is wrong somewhere" is far weaker than one that says which entry, and the index is what makes the day book an audit artifact rather than a checksum. WHERE IT CAN STILL GO HOLLOW. FIRST, "verify every link" IS TRUE OF A ONE-ENTRY CHAIN, and of a zero-entry one. Assert the entry count is non-zero and matches what the day book actually returned, and print it. A for-loop over nothing exits 0 and that shape has cost this sprint more than any other. SECOND, per D-108, PROVE THE VERIFIER STILL RETURNS THE POSITIVE ON THE REAL CHAIN — not only that a tampered chain fails. L2 nearly shipped a detector whose own pattern matched its own schema, and the clause that caught it was the one asserting the real corpus still passes. A verifier that rejects everything is exactly as useless as one that rejects nothing, and the false-positive direction is the one that gets an instrument switched off. THIRD, per D-100, exercise BOTH directions: a flipped byte must fail AND an honestly re-serialised entry must still pass. I3's own S6 work is the precedent — its five canonicalisation attacks proved an honestly-reformatted report is NEVER rejected, beside two real content changes that ARE caught. Same discipline, one artifact over. FOURTH, use src/canonical.js as the accept says — no second canonicaliser. That is the rule that has held all sprint and S11 exists to make it possible. ONE PIECE OF CONTEXT THAT MAKES THIS MORE THAN A CUT-3 NODE: PM ruled tonight (D-116) that THE DAY BOOK IS THE DURABLE WITNESS for a signature — the sign record is an ephemeral ceremony record, and get_day_book is on the auditor surface while the sign endpoint is not. So a judge asking "prove a human signed this" is pointed at the day book. YOUR CHAIN IS WHAT MAKES THAT ANSWER WORTH GIVING. STANDING: FORCE_COLOR=3 is injected — `node tools/accept-gate.mjs S7 --run-clean`; and process.stdout.write(String(x)) if you scrape a value into a shell variable.
ACCEPT:    `node --test tests/acceptance/chain.test.mjs` — recompute the chain from GET /api/daybook using src/canonical.js and verify every link; then flip a single byte of any entry's source field and assert verification fails at that index.
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/S7.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/S7.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I3-S7
DEADLINE:  2026-08-31T12:00-07:00
