NODE:      E2
OWNER:     C4
INPUTS:    E1, T5
DO:        Build evals/suites/capability.suite.json and evals/surfaces.expected.json, and make `--suite capability` grade the six canonical states by SET EQUALITY on tool names. YOUR PREDICATE IS ALREADY WELL-BUILT AND IT SAYS WHY IN ITS OWN TEXT — "never by count equality" — so read that clause as the finding it is rather than as a style note. THE REASON IS MEASURED AND IT IS IN THE FROZEN CONTRACT: S4-emp-submitted and S5-aud BOTH CARRY THE SAME NUMBER OF TOOLS AND ARE NOT THE SAME SET. A count assertion passes even if THE AUDITOR IS HANDED THE EMPLOYEE'S SURFACE, which is the single worst thing this product could do and the exact claim the demo makes. Set equality is the whole node. FOUR THINGS THAT CAN STILL PASS WHILE PROVING NOTHING. FIRST, "zero states skipped" is your own clause and it is the one I would attack: a suite that resolves no states skips none. Assert the graded count is SIX explicitly, print it, and make a run that graded fewer than six a LOUD FAILURE rather than a green shorter loop. SECOND, the expected sets must be TAKEN FROM artifacts/tools.export.json AT RUN TIME, not transcribed into surfaces.expected.json by hand — that file is a T5 output, merged, and if you hand-copy it your equality is checking your copy against your copy, which is the exact defect I3 designed curl-403.sh to avoid. If surfaces.expected.json must exist as a file, GENERATE it from the export and assert it still matches the export, so drift is an error rather than a silent narrowing. THIRD, the export now carries SEVENTEEN distinct tools and every state gained explain_missing_tool under D-77 — counts are 2/6/14/13/7/7 in EXPORT ids. BE CAREFUL WITH THE MIDDLE TWO: the export's S2-emp-draft-clean is the compiler's internal S3, and the export's S3-emp-draft-dirty is internal S2. THE NUMERALS CROSS OVER. Mapping by the digit publishes submit_expense_report on the surface carrying a blocking violation, and the state count, the id order and every digest still check out. That trap has now bitten three documents; do not let it bite a fourth. FOURTH, prove the suite can FAIL for the reason named: inject a single extra tool into one state and a single missing tool into another and show each exits 1 NAMING THE STATE AND THE TOOL. Per D-90, a control satisfiable by the subject being absent is not a control, so do not test "an empty state fails" and call it covered. AND PER TODAY'S NEWEST RULE, WHICH COST TWO SEATS THEIR SELFTESTS: A SUITE THAT ONLY TESTS ONE DIRECTION OF A DECISION CANNOT TELL A WORKING GUARD FROM A STUCK ONE. Show your grader ACCEPTING a correct six-state run as well as rejecting a broken one — a grader stuck at "fail" is as useless as one stuck at "pass", and two seats shipped one-directional suites today without noticing. Ownership: you write only under evals/ and webmcp-eval-kit/. Do NOT touch artifacts/tools.export.json — it is a merged T5 output and your suite READS it; a node that edits the thing it grades against has proven nothing. $URL needs a booted server or the live host https://outpocket.onrender.com.
ACCEPT:    `node webmcp-eval-kit/bin/eval.mjs --suite capability --url $URL` exits 0 reporting 6 of 6 canonical states matching the expected sets taken from artifacts/tools.export.json, by SET EQUALITY on tool names and never by count equality; zero states skipped; a single extra or missing tool in any state exits 1.
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/E2.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/E2.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/C4-E2
DEADLINE:  2026-08-31T12:00-07:00
