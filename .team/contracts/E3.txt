NODE:      E3
OWNER:     C4
INPUTS:    E1, T2, S2, S6
DO:        Build evals/suites/negative.suite.json. The suite exits 0 ONLY IF every must-fail case FAILED AS REQUIRED, including N-15 neg-commit-without-human returning 409 E_NOT_SIGNED, and the runner builds the pairing map from each case's `pairsWith` field and fails the run if any state in evals/surfaces.expected.json has an empty pairing. READ THE BINDING RULE FIRST, BECAUSE IT WAS WRITTEN FOR THIS NODE BY NAME. D-110: A VALIDATION ERROR THAT MASKS AN AUTHORIZATION ERROR MAKES THE AUTHORIZATION ERROR UNTESTABLE FROM OUTSIDE — so PROVE THE REQUEST IS WELL-FORMED BEFORE CONCLUDING ANYTHING FROM ITS REFUSAL. I learned this the hard way on production: a WRONG confirm_token and a MALFORMED body both returned 400 E_BAD_REQUEST, so for most of an hour "the token guard works" and "my JSON is broken" were indistinguishable — and only one of those is a property of the product. Once the body was correct the wrong token returned 403 E_NO_CONFIRM_TOKEN, a completely different code. YOUR N-15 IS THE EXACT SHAPE: asserting 409 E_NOT_SIGNED is satisfied by a malformed body that never reaches the sign gate at all, and IT LOOKS EXACTLY LIKE SUCCESS because both outcomes are a refusal and you are expecting a refusal. So every negative case must first show its request is WELL-FORMED — the cheapest form is a paired positive that succeeds with the same body shape, differing only in the one field under test. Assert the EXACT status AND the EXACT error code, never merely "not 200". SECOND: "every must-fail case failed" is TRUE OF ZERO CASES. Assert the case count is non-zero and equals the suite's declared count, print it, and make a run that graded fewer than declared a LOUD failure. A for-loop over nothing exits 0 and that shape has cost this sprint more than any other. THIRD, the pairing clause is well-built and worth honouring literally: a state with an EMPTY pairing must fail the run, so a case list that silently stops covering a state is caught rather than shrinking quietly. FOURTH, per D-100 and D-108 together: show the suite ACCEPTING a correct run as well as rejecting a broken one, and prove the detector still returns the POSITIVE on the real corpus — L2 nearly shipped a detector whose own pattern matched its own schema, and it was the clause asserting the REAL FILES STILL PASS that caught it. A DETECTOR THAT FIRES ON EVERYTHING IS EXACTLY AS SATISFIABLE AS ONE THAT FIRES ON NOTHING. Ownership: you write only under evals/ and webmcp-eval-kit/. Do NOT touch artifacts/tools.export.json, src/, server/ or tests/. $URL needs a booted server or the live host https://outpocket.onrender.com.
ACCEPT:    `node webmcp-eval-kit/bin/eval.mjs --suite negative --url $URL` exits 0 ONLY IF every must-fail case failed as required, INCLUDING N-15 neg-commit-without-human returning 409 E_NOT_SIGNED. The runner builds the pairing map from each case's `pairsWith` field and fails the run if ANY state in evals/surfaces.expected.json has an empty pair set — the old predicate `count(mustFail) >= count(capability states)` was 14 >= 7, trivially true, and permitted the actual situation in which S1-emp-home and S4-emp-submitted had no dedicated must-fail case at all. Every case declares `provingNode` and `brokenBy`. AND `neg-policy-content-swap` (ruling R-33) is asserted under the identifier N-20, expecting 409 `E_POLICY_DIGEST_MOVED` with controlStatus `enforced` — THE N-NUMBER IS ASSIGNED HERE, because R-33 deliberately left it to this file: N-01 through N-19 are all bound, erp/EVAL.md's control table running to N-19 after the anonymous-read control was renumbered off N-16 (graph.json's S5 accept binds N-16 to neg-respond-without-click and graph.json wins), so the next free number is N-20 and it is now taken. Its honest scope travels with it in the suite file and may not be dropped: the attack needs WRITE ACCESS TO THE SERVED POLICY DOCUMENT, which is arguably outside the declared N-04 curl-and-cookie model, so it is a weaker vector than neg-respond-without-click and must never be written up as a break of that model. The server-side half of the same fix — recompute the policy digest at LOAD and refuse to serve an unpinned (version, digest) pair — is S3's, assigned in S3's accept. AND N-16 neg-respond-without-click is asserted against erp/contracts/eval-case.schema.json examples[1] as S5's deviation DEV-E3-eval-case-known-open left it — controlStatus `enforced`, not `known-open` — so this node consumes the scheduled edit rather than tripping over the stale one. R-27: `refused` is not a member of the frozen enum [enforced, known-open, not-runnable]; the word for a control that now holds is `enforced`. AND `neg-decline-to-unlock` (ruling R-34) is asserted under the identifier N-21, expecting 409 `E_ALREADY_ANSWERED` on the human's genuine click after an attacker's decline, with controlStatus `enforced` once S5 lands the code on Day 3 and its severity — a nuisance-grade denial, not a forgery — carried in the case file and not droppable. R-35 SETTLES THE N-NUMBER COLLISION AND THIS ACCEPT IS ONE OF THE TWO PARTIES TO IT: N-20 was self-assigned twice, here to neg-policy-content-swap and in erp/EVAL.md's control table to neg-decline-to-unlock. graph.json is the authority, it assigned N-20 first and says so in this accept, so NEG-POLICY-CONTENT-SWAP KEEPS N-20 and NEG-DECLINE-TO-UNLOCK BECOMES N-21; erp/EVAL.md's table is regenerated against this, and neg-policy-content-swap — which appeared nowhere in it — gains its own row.
PIT:       D-83 / CLAUSE 6e, RULED 2026-08-29, BINDING — THE REPORT IS A COMMIT, NOT A
           MESSAGE. Put the five fields in a `PIT:` BLOCK IN YOUR FINAL COMMIT MESSAGE on
           your own branch. L1 reads them with `git log` at the gate and writes
           kb/pits/E3.md from them (D-31 still stands: you do NOT commit a pit FILE).
           COMPOSING IS COMMITTING. There is no send step to forget.
           WHY THIS CHANGED: four reports have been lost, in FOUR DIFFERENT WAYS — a seat
           that finished and went idle, a seat still running whose message never arrived, a
           restarted session whose transcript was gone, and two composed as ordinary
           conversation text and never sent. Every arrangement so far depended on a channel
           between you and the gate, and THE CHANNEL HAS FAILED IN FOUR WAYS OUT OF FOUR.
           A REPORT COMPOSED BUT NOT TRANSMITTED IS INDISTINGUISHABLE, FROM THE RECEIVING
           END, FROM A REPORT NEVER WRITTEN. So the mechanism removes the channel: the
           report rides inside the artifact being gated.
           Send it in your green message TOO if you like, but the commit is what counts.
           D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/E3.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/C4-E3
DEADLINE:  2026-08-31T12:00-07:00
