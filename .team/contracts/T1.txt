NODE:      T1
OWNER:     I2
INPUTS:    S10, T6
DO:        Port the tools.js surface compiler into the page as src/page/tools/compile.js and src/page/tools/defs.js, and write tools/check-toplevel.mjs. The ported surface must keep passing tests/surface.test.mjs. check-toplevel.mjs must find ZERO registerTool call sites reachable from an iframe or worker entry file — the registration has to live in the TOP-LEVEL document only.
ACCEPT:    `node --test tests/surface.test.mjs` passes all ported surface tests in the new repo AND `find src/page -name '*.js' -print0 | xargs -0 node tools/check-toplevel.mjs` exits 0, finding zero registerTool call sites reachable from an iframe or worker entry file.
PIT:       kb/pits/T1.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/I2-T1
DEADLINE:  2026-08-29T23:59-07:00
