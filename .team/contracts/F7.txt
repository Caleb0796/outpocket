NODE:      F7
OWNER:     UX
INPUTS:    T2, S5, F4, F1
DO:        Build src/page/sign-install.js: the glue that calls register.js's setSignatureProvider exactly once, so that an agent's submit_expense_report actually raises your F4 dialog and waits for a human decision. READ THE SEVERITY CORRECTLY BEFORE YOU START, BECAUSE I BRIEFED IT WRONG UPSTREAM AND PM CORRECTED ME. THE SECURITY PROPERTY IS ENFORCED AND TESTED SERVER-SIDE ALREADY — S12's sign-lock returns 423, S6's TOCTOU check returns 409 E_SNAPSHOT_MISMATCH, E3's N-15 asserts 409 E_NOT_SIGNED against the live URL. The server refuses an unsigned commit today. WHAT IS MISSING IS THE PAGE-SIDE DEMONSTRATION: the human step is enforced and nothing on screen shows it happening. This is a MISSING DEMO, NOT A MISSING GUARANTEE. It is still cut 0 and still outranks every remaining cut-2 and cut-3 node, because the video is the deliverable and SB-10 and SB-11 are on-camera shots — but do not build it in a panic as though the product were unsafe, and do not widen it into re-proving anything the server already proves. WHY IT IS YOURS AND WHY IT FELL THROUGH: register.js:97's own comment reads "F4 installs the real dialog by calling setSignatureProvider once." F4 is yours and correctly did NOT, because sign-dialog.js is deliberately DOM-only. sign-bridge.js (S5) is deliberately DOM-free. register.js (T2) exposed the seam. THREE MODULES EACH SCOPED CORRECTLY, AND THE GLUE BETWEEN THEM WAS NOBODY'S NODE — that scoping is why they are good modules, and it is exactly why nothing caught this. PM found a fifth piece of evidence I had missed: index.html:292 DESCRIBES the wiring, saying S5's sign-bridge calls the published dialog, which sign-bridge.js never does. THE CORPUS DOCUMENTS A CONNECTION THAT WAS NEVER BUILT — the same family as H3's comment table making an unmounted module look mounted, one level up. Fix that line while you are in there; prose asserting an integration is worse than silence, because it stops the next reader looking. THE SHAPE: construct createSignBridge, call beginSign, mount your dialog with the result, await the human decision, call continueSign, return {signed,reason} to register.js's hook, and call setSignatureProvider ONCE. YOUR ACCEPT'S THIRD CLAUSE IS THE ONE THAT MATTERS AND IT IS YOUR OWN FORMULATION TURNED ON YOUR OWN NODE — with the provider NOT installed the test must FAIL, so the suite cannot pass on a page where the wiring was silently removed. An assertion that something changed is only as strong as the evidence it could have not changed. And clause 2 is the T3 lesson applied here: a test proving only that the provider was INSTALLED proves a path exists and says nothing about whether the human decision is consulted, so stub the dialog to APPROVE and to DECLINE and assert the two outcomes DIFFER BY A NAMED REASON. PRESENT IS NOT USEFUL. Clause 1 is "exactly once" — not zero and not once per registration; a provider installed on every registration is a leak that will look fine until it does not. MOUNT IT: --check-orphans is in your accept for the first time in this project, and it now classifies referrers, so a test import will NOT satisfy it — the module must be reachable from src/page/index.html. You are the seat that found that false green; this is the first node gated on the fixed version. index.html is yours as an F1 output. STANDING: FORCE_COLOR=3 is injected into every seat, so use `node tools/accept-gate.mjs F7 --run-clean`; and an unexplained exit 1 gets RE-RUN before it gets a theory.
ACCEPT:    `node --test tests/acceptance/sign-install.test.mjs` passes AND `node tools/ready.mjs --check-orphans` exits 0 (the module is reachable from src/page/index.html, not merely present). THREE CLAUSES, AND THE THIRD IS THE ONE THAT MATTERS. (1) INSTALLED EXACTLY ONCE: loading the page's module graph calls register.js's setSignatureProvider exactly one time - not zero, and not once per registration. (2) THE WIRE CARRIES A DECISION, NOT MERELY A PATH: with the dialog stubbed to APPROVE, an agent-initiated submit_expense_report completes and the signed result carries the ticket from S5's continueSign; with the SAME wire and the dialog stubbed to DECLINE, the submit is REFUSED and the two outcomes DIFFER by a named reason. A test that only proves the provider was installed proves a path exists and says nothing about whether the human decision is consulted - PRESENT IS NOT USEFUL, the T3 lesson applied here. (3) NEGATIVE CONTROL: with the provider NOT installed, the same submit must reach the pre-D-80 behaviour and the test must FAIL - so the suite cannot pass on a page where the wiring was silently removed. An assertion that something changed is only as strong as the evidence it could have not changed.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/F7.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/UX-F7
DEADLINE:  2026-08-30T14:00-07:00
