NODE:      H6
OWNER:     I1
INPUTS:    H2, H3, H4
DO:        Build harness/rehearse.mjs and write evidence/rehearsal.json. This is the head of the critical path and everything left downstream — D4, D5, D6 — queues behind it. YOUR PREDICATE IS THE WEAKEST ONE YOU HAVE BEEN GIVEN AND YOU ALREADY KNOW WHY, BECAUSE YOU WROTE THE LESSON: a rehearsal that does nothing passes 5 of 5, finishes each run in well under 120 seconds, and writes per-step timings for steps that never ran. Every clause is satisfied by a harness that starts and stops. So the node is not "make it green"; it is "make 5/5 mean something". THE RULE YOU DREW ON H4 IS THE ONE TO BUILD FROM — WHEN A CHECK REPORTS SUCCESS, ASK WHAT IT WOULD HAVE PRINTED HAD IT FAILED, AND IF THE ANSWER IS "THE SAME THING", THE CHECK IS NOT A CHECK. Apply it to your own pass counter first: a run must be graded on OBSERVED END STATE, not on the absence of a throw, because in this codebase the error path returns normally by design — toolset.call hands back a readable content block for an unknown tool and an error envelope for a refusal. You were caught by that once already on H4, where seven ok flags sat on top of a demo that created no report. CONCRETELY, FOUR THINGS. (1) A RUN COUNTS AS A PASS ONLY IF IT REACHED THE STATE IT CLAIMS. Assert the surface actually reached S3 and that a report with the expected line count exists, read out of the dump — reuse harness/dump-state.mjs rather than writing a second observer, and make a run that stops short of S3 a FAILURE with a named code, the way you already made E_DEMO_INCOMPLETE. (2) PROVE THE RIG CAN REPORT A FAILURE AT ALL. Break something on purpose — point one run at a page with no mount, or seed a step that must refuse — and show rehearse.mjs reporting 4 of 5 with the failing run named. A 5/5 you have never seen become 4/5 is a number, not a measurement. Paste that run in your report. (3) PER-STEP TIMINGS MUST BE PER STEP. A single total split six ways is not a timing; the point of this file is that someone reading it later can see WHICH step is the slow one before it becomes a 120-second problem in front of a judge. Report the slowest step and its median across the five runs. (4) SAY WHAT THE FIVE RUNS DO AND DO NOT PROVE. Five runs of a SEEDED, deterministic demo mostly measures that the machine is not on fire — it is not evidence of flake resistance, because the input never varies. That is fine and it is worth having, but write the limit into evidence/rehearsal.json's own header rather than letting a later reader infer robustness we did not measure. If varying the seed across the five runs is cheap, do it and say so; if it is not, say why. ONE THING FROM UX, NOT BLOCKING THIS NODE BUT YOURS TO FIX AND WORTH FOLDING IN WHILE YOU ARE IN THESE FILES: in the configuration where WebMCP is ABSENT and ?demo=1 is set, the page is BOTH self-driving AND running a scripted seeded filing, and only the first is disclosed — H3's labelAsSimulated writes #agent-banner unconditionally and H4's label defers, so the demo label never appears in that run in either tag order. Your own H4 header says "an automated run that looks like a human filing is dishonest for the same reason an unlabelled self-driving agent is", and in that one configuration it does not say so. Both are true and the banner can hold both; UX's suggestion is to append rather than defer, so it reads the simulated-agent text AND "· demo mode, seed 7". UX also flagged, explicitly as a PREDICTION IT DID NOT RUN, that this should not affect the video because under --enable-features=WebMCP the real API exists, H3 refuses to shadow it, and the banner stays empty for H4 to fill — worth confirming on the rig rather than holding on inference, since "the demo is labelled" is an honesty claim. STANDING: FORCE_COLOR=3 is injected into every seat, so use `node tools/accept-gate.mjs H6 --run-clean`; and note your accept needs a booted server and a bound $URL — my merge gate refused H4 on exactly that and it was an environment failure, not a node failure.
ACCEPT:    `node harness/rehearse.mjs --runs 5` exits 0 and writes evidence/rehearsal.json showing 5 of 5 passes, each under 120 seconds, with per-step timings.
PIT:       D-31, RULED 2026-08-29 — DO NOT COMMIT A PIT FILE. You REPORT five fields and
           L1 writes kb/pits/H6.md from them. In order: TRIED, HAPPENED, CHANGED,
           EARLIER, GRADE. "No pit" is a legal ENTRY; no report at all is not, and L1
           cannot merge you without one.
           REPORT THEM IN THE SAME MESSAGE THAT SAYS YOUR ACCEPT IS GREEN, not after.
           Reports have been made and lost three times in this sprint, and a seat stopped
           before it reports takes its pit with it — one node's pit had to be written
           marked "NOT the seat's own report", which is worse than the node deserved.
           Report in YOUR OWN WORDS. L1 transcribes and names you; a pit L1 composed is
           worse than no pit. EARLIER is the field that pays: what you would tell
           yourself an hour before you learned it.
           (This block previously said "write it on your branch" — a contradiction with
           D-31 that survived a reset of mine and misled three seats, all of which
           correctly followed the ruling over the template and told me.)
BRANCH:    seat/I1-H6
DEADLINE:  2026-08-30T16:00-07:00
