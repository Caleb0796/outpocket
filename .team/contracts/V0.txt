NODE:      V0
OWNER:     I1
INPUTS:    L0
DO:        Probe the INSTALLED Chrome major (152.0.7977.64) for navigator.modelContext alias status and write harness/probe-v0.mjs plus evidence/V0.json. THE LAUNCH MUST CARRY THE FLAG — --enable-features=WebMCP or WebMCPTesting, MEASURED interchangeable — because a no-flag launch leaves document.modelContext undefined and a probe that omits it measures the flag, not the alias. Every boolean via CDP Runtime.evaluate on a live page; chromeMajor from the installed binary, never a user-agent string. AWAIT getTools() — it returns a Promise, so an un-awaited .length is undefined, and do not substitute !== 0, which passes against an empty surface. Record cdpDomainEnabled and never assert on it: WebMCP.enable returns OK in a flagless launch with no tools and no page API. NOTE THE OPEN ITEM: an evidence/V0.json already on disk matches NEITHER V0.accept's shape (chromiumMajor vs chromeMajor, method not 'cdp', missing flag/headless/toolCount/cdpDomainEnabled/invokeToolRoundTrip) NOR probe-verdict; it is recorded in kb/pits/V5.md under EARLIER and it is yours to replace, not to patch around.
ACCEPT:    `node harness/probe-v0.mjs` writes evidence/V0.json containing {chromeMajor:int, flag:string, headless:bool, navigatorAlias:bool, documentPresent:bool, toolCount:int, cdpDomainEnabled:bool, invokeToolRoundTrip:bool, method:'cdp'} and exits 0. chromeMajor must equal the major reported by the installed binary, never a user-agent string, and every boolean must be obtained via CDP Runtime.evaluate on a live page. THE LAUNCH MUST CARRY THE FLAG — --enable-features=WebMCP or WebMCPTesting, MEASURED 2026-08-28 interchangeable — because MEASURED the same day a no-flag launch leaves document.modelContext undefined both headed and under --headless=new, so a probe that omits the flag measures the flag and not the alias, and would report documentPresent:false as if that were a fact about Chrome 152. `headless` is RECORDED AND NOT CONSTRAINED: the flag works under --headless=new. documentPresent is `typeof document.modelContext === 'object'`; navigatorAlias is `typeof navigator.modelContext === 'object'`; toolCount is `(await document.modelContext.getTools()).length` and THE AWAIT IS LOAD-BEARING — getTools() returns a Promise (MEASURED), so an un-awaited `.length` is `undefined`, and `!== 0` must not be substituted because it passes against an empty surface. cdpDomainEnabled is RECORDED AND NEVER ASSERTED ON: MEASURED, `WebMCP.enable` returns OK in a launch with no flag, no tools and no page API at all, so it reads 'on' when the feature is off. Only invokeToolRoundTrip — an actual `WebMCP.invokeTool` whose matching `toolResponded` carries `status: 'Completed'` — discriminates, and it is the field that decides whether this probe ran against a live surface at all. Same shape as evidence/H2-reachability.json on purpose: one evidence format, one set of traps, two nodes.
PIT:       kb/pits/V0.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/I1-V0
DEADLINE:  2026-08-29T23:59-07:00
