NODE:      T2
OWNER:     I2
INPUTS:    T1, H1, F1, S1, S4
DO:        All five inputs are merged: T1, H1, F1, S1, S4. Build against the FROZEN erp/contracts/tool-surface.contract.md — the sixteen names, their per-state membership across S0..S5 and the read-only column are frozen under sha256sum -c erp/contracts/FREEZE.md and you may not edit them. S4's server/envelope.mjs is the violation envelope producer; use it rather than re-deriving the shape. DEV-006, ADOPTED: T1's top-level-registration clause MOVED TO THIS NODE, with --selftest MANDATORY — it was checked on Day 1 at the one moment it could not fail, on 0 of 0 call sites, and your node is where registerTool call sites first exist and the invariant becomes falsifiable.
ACCEPT:    `node harness/drive.mjs --assert-flips 1,5,12,13` exits 0: it drives a real Chrome with --enable-features=WebMCP against the served application, calls getTools() after each state transition and asserts the counts in order, and asserts that submit_expense_report disappears from getTools() after a blocking violation is introduced through the real policy engine. AND — DEV-006, adopt, PM 2026-08-29 — `node tools/check-toplevel.mjs --selftest` exits 0 AND `find src/page -name '*.js' -print0 | xargs -0 node tools/check-toplevel.mjs` exits 0. T1 is the only other node that invokes this tool, and T1 runs BEFORE any registerTool call site exists, so there the check can only pass VACUOUSLY: the invariant was verified once, at the one moment it could not fail, and never again. T2 is where call sites first appear and is therefore the first node at which it is falsifiable. `--selftest` is mandatory and was omitted from T1: it is the tool's own real-zero-versus-unexamined-zero arm, and until now no predicate in this graph exercised it.
PIT:       kb/pits/T2.md — REQUIRED BEFORE MERGE, no exceptions and no discretion.
           Five keys, in order: TRIED, HAPPENED, CHANGED, EARLIER, GRADE. "No pit" is a
           legal ENTRY; a missing FILE is not. One paragraph per key is enough. Write it
           on your branch. (kb/pits/** is glob-owned by L1 while the merge gate demands
           the file from you — that contradiction is L1's to carry to PM, not yours to
           resolve. Write the file; L1 absorbs the ownership finding.)
BRANCH:    seat/I2-T2
DEADLINE:  2026-08-30T23:59-07:00
