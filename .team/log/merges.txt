# Merge log — one line per merge to main, in the format erp/charters/L1.md specifies:
#   MERGED <node> <sha> pits:<path> accept:PASS
#
# This is NOT .team/log/inbound-PM.jsonl. That file is for inbound HUMAN messages
# (erp/TEAM.md §2), and `ls .team/log/inbound-*.jsonl` must contain only PM, L1 and L2 —
# putting merge lines there would corrupt what that invariant measures. PM reads this file
# at boot instead.
#
# L1 is the sole committer and sole pusher to main (R-26(b)). Every line below was pushed
# to origin/main and the remote sha verified equal to local HEAD before the line was written.

MERGED L0 475bf0f pits:kb/pits/L0.md accept:PASS
MERGED V5 7578174 pits:kb/pits/V5.md accept:PASS

# L0 — Day-0 bootstrap. All SEVEN gates green, run verbatim from erp/graph.json via
#   tools/l0-gate.mjs. First commit a1f650b (the bootstrap tree, gate (6)'s push);
#   475bf0f corrects the gate-4 record. Full gate-by-gate account in
#   evidence/L0-bootstrap.txt. One deviation raised and CLOSED by the raising seat,
#   .team/deviations/DEV-L0-gate4-banner-grep.md — it asks PM for NOTHING, and its
#   original requests (amend gate (4); re-grade erp/FACTS.md §14) are both WITHDRAWN.
#   erp/graph.json was never edited.
#
# V5 — probe origin. Owner I1; L1 merged and pushed because R-26(b) reserves both.
#   V5.accept re-run in L1's presence via tools/accept-gate.mjs, both clauses exit 0,
#   plus independent checks of the three prose clauses against the DEPLOYED page.
#   erp/contracts/ now carries NINE files, which is the set S10 freezes on Day 1.
#
# OPEN FOR PM, raised by I1 and not L1's to verdict: evidence/V0.json matches neither
# V0.accept's declared shape (chromiumMajor vs chromeMajor, method:"page-side feature
# detection…" vs 'cdp', and no flag/headless/toolCount/cdpDomainEnabled/invokeToolRoundTrip)
# nor the probe-verdict shape, and V0's second declared output harness/probe-v0.mjs does
# not exist. Recorded in kb/pits/V5.md under EARLIER. I1 has it as Day-1 work.

MERGED T6 745d560 pits:kb/pits/T6.md accept:PASS

# T6 — the RED test L0 shipped, fixed. Owner I2. Accept run verbatim via
#   tools/accept-gate.mjs T6 --run 0 IN I2'S OWN WORKTREE, exit 0:
#   tests/surface.test.mjs 9/9, the named 'auditor surface: read-only by
#   construction' ok, auditor set exactly {get_day_book, get_expense_policy,
#   get_open_report, get_report, get_session_scope, list_expense_reports}.
#   FULL SUITE 24 tests / 24 pass / 0 FAIL on origin/main — that is the tree
#   G3 clones, and R-26(c) is now satisfiable for the first time.
#   Distinct tool count 15 -> 16, as R-9 option (B) requires.
#
#   Gate 2 (Layer-0 lint) recorded N/A, NOT waived: G4 is still in flight and
#   tools/lint-layer0.mjs does not exist yet. T6 is re-lintable once G4 lands.
#
# OPEN FOR PM, raised by L1 and NOT L1's to verdict — THE PIT FILE IS
#   UNOWNABLE BY THE SEAT THAT MUST WRITE IT. Merge gate clause 6 requires
#   kb/pits/<node>.md; conventions.ownership_rule resolves kb/pits/** to L1
#   and NO node lists a pit file in outputs (erp/PATHS.md 2.8: "produced by no
#   node and budgeted in nobody's hours"). So every building seat that
#   satisfies gate 4 violates gate 3, mechanically and forever. Measured:
#     DENY  kb/pits/T6.md  [longest matching glob `kb/pits/**` -> L1, not I2]
#     OK    src/tools.js            [declared output of T6 (owner I2)]
#     OK    tests/surface.test.mjs  [declared output of T6 (owner I2)]
#   T6 was merged with the violation ON THE RECORD rather than waived, because
#   the alternative was blocking Day 1's leverage node on a contradiction that
#   is not I2's fault. Escalated to PM with three candidate resolutions.
#
# PROCESS FIX, caught by the peer session mcp-6d: the four dispatch records in
#   78fa31d carried no PIT line, so the merge gate was invisible from the
#   artifact each seat worked to. I2 wrote a pit from its charter; I3 did not.
#   That is not a difference in diligence. tools/contract.mjs now GENERATES a
#   PIT line and every contract has been regenerated.

MERGED S10 225daa6 pits:kb/pits/S10.md accept:PASS
MERGED S11 17680ea pits:kb/pits/S11.md accept:PASS
MERGED G4  0f3cebd pits:kb/pits/G4.md  accept:PASS
MERGED V0  31b6e4f pits:kb/pits/V0.md  accept:PASS
MERGED G3  2d35bee pits:kb/pits/G3.md  accept:PASS
MERGED G0  1af3dc3 pits:kb/pits/G0.md  accept:PASS

# Day 1. Seven merges (T6 above, then these six), every one gated in L1's
# presence with the accept run VERBATIM via tools/accept-gate.mjs, every one
# pushed with the remote sha verified equal to local HEAD before the line was
# written. origin/main carries all of them.
#
# S10 — THE SEAM IS FROZEN. sha256sum -c erp/contracts/FREEZE.md verifies TEN
#   files, not the nine S10.accept names: G6's accept is the tiebreaker and says
#   the .md contract files are covered by the FREEZE.md sha256 check instead. A
#   markdown contract nothing hashes is not frozen. R-20 confirmed BY COMPUTATION
#   at SEVEN write tools, from annotations.readOnlyHint !== true.
# S11 — the ONLY canonicaliser (R-11). Codepoint sort, not localeCompare, so a
#   stranger's clean clone under a different ICU sorts the same.
# G4 — REJECTED ONCE on a named clause and fixed properly. The sweep-vs-named
#   distinction is the only reading under which accept clauses 1 and 2 both hold.
# V0 — alias-absent on Chrome 152, with a negative-control arm nothing required.
# G3 — R-26(c) satisfied: a stranger's clean clone of origin/main is green,
#   40/40. L0 gate (7) asserts the OPPOSITE of the bootstrap commit and both are
#   true; T6's merge and L1's push are what carried the tree between them.
# G0 — held RED most of the day rather than relaxed. --check-tables found four
#   stale restatements of the G1 Day-1 -> Day-6 move on its first run; the files
#   are PM's; PM regenerated them (D-34) and the mode went green against the
#   authority, not against a weakened test.
#
# D-31 RULED, and it retires the contradiction recorded at T6: L1 WRITES
#   kb/pits/<node-id>.md from the seat's reported five fields. No seat commits a
#   pit; no ownership violation is recorded when one appears on a branch. Folded
#   into tools/contract.mjs so every contract now carries the ruling.
#
# OPEN FOR PM, raised by L1 and not L1's to verdict — THREE MORE COLLISIONS OF
#   THE D-31 CLASS. PM's own rulings commit was DENIED on erp/graph.json and
#   erp/PATHS.md (declared L0 outputs, so clause (a) beats the erp/*.md -> PM
#   glob) and on .team/deviations/** (glob -> L1). L0 lists those two because it
#   COMMITS them; PM AUTHORS them. The rule rejects the author of the authority
#   in favour of the seat that committed it once on Day 0. Merged, raised, not
#   waived.

MERGED T1  7e60d6d pits:kb/pits/T1.md accept:PASS
MERGED G5  d70c28d pits:kb/pits/G5.md accept:PASS

# CORRECTION TO THE T1 MERGE COMMIT (7e60d6d), AND IT IS OWED TO I2.
# That commit calls T1's second clause VACUOUS. That word is too strong and it
# reads as a defect in I2's node when the opposite is true. The peer session
# mcp-6d, which raised the finding, withdrew the characterisation itself after
# reading the tool rather than only running it, and it is right to.
#
# tools/check-toplevel.mjs carries a section headed VACUITY, in I2's own words:
# "The failure this checker exists to prevent is a green light over nothing
# scanned, so it prints its own coverage and refuses to report success when it
# examined no JavaScript at all." The run I quoted DOES print its coverage —
# 26 js/mjs scanned, 1 html — so it scanned real code and found zero call
# sites, which is CORRECT: T1 ports the compiler and T2 adds the registration.
# Its --selftest runs fixtures that DO contain violations, plus two decoys: prose
# that merely mentions the name, and a regex literal designed to desynchronise
# the blanker. I2 built the guard against the exact failure the word accused it
# of, and it is the most careful thing in the file.
#
# WHAT ACTUALLY SURVIVES, and it is narrower and still worth PM's ruling:
#   - T1's accept clause 2 does NOT invoke --selftest. The tool's own
#     "is this zero a real zero" arm exists and no predicate exercises it.
#   - T1 is the ONLY node whose accept invokes tools/check-toplevel.mjs.
#   - T2, where registerTool call sites first appear, mentions check-toplevel
#     0 times and iframe 0 times.
# So: the invariant is checked once, before it can fail, with the tool's vacuity
# arm switched off, and never again after T2 makes it falsifiable.
#
# AND THE FIX I FORWARDED TO PM AS OPTION (b) IS WRONG. "Make check-toplevel
# exit 1 on zero call sites" is wrong precisely BECAUSE the tool already
# distinguishes a real zero from an unexamined one, and a zero is legitimate
# today. The better shapes are: add --selftest to T1's clause so the arm runs,
# and put the find/check-toplevel clause into T2's accept where the call sites
# first exist. Still PM's ruling; still not mine.
#
# G5 — the accept passes and the product is INERT. git invokes hooks only by
#   canonical name; there is no pre-commit-ownership hook, and .githooks/pre-commit
#   is G4's and does not chain. Proven by committing a path L1 may not write and
#   watching it succeed. Filed for PM.
#
# COSTS I INCURRED TODAY, recorded here because W files on evidence and this is
#   the evidence: (a) I ran `claude rm` on a BUSY UX seat to re-dispatch it and
#   DESTROYED F0's uncommitted outputs — ~1 h of UX work, re-done, no permanent
#   loss. Never rm a seat that is not idle/done. (b) Enabling core.hooksPath for
#   G5 switched G4's lint hook on for every seat at once and blocked QA's G6
#   commit on a pre-existing failure. QA diagnosed it correctly against a clean
#   HEAD and recommended the fix that shipped.

MERGED F0  3007891 pits:kb/pits/F0.md accept:PASS
MERGED G6  3b5d051 pits:kb/pits/G6.md accept:PASS
MERGED H1  99ba3e8 pits:kb/pits/H1.md accept:PASS
MERGED S1  80ae482 pits:kb/pits/S1.md accept:PASS

# BOOKKEEPING CORRECTION, and the gap was mine. These four merges landed hours
# ago and their MERGED lines never got written, so this file carried ELEVEN rows
# while erp/graph.state.json.done carried FIFTEEN. HANDOVER section 1 names THIS
# file as the record — one line per node, with sha and pit — so any seat auditing
# from it undercounted by four. Found by the peer session mcp-6d, with the
# measurement attached rather than the assertion.
#
# All four pits exist and all four were verified before the rows were written:
#   F0  kb/pits/F0.md  landed 7b0c7d3 (rewritten once, to clear BW-33 ahead of
#                      the row, under the same rule I imposed on other seats)
#   G6  kb/pits/G6.md  landed 5f6c3c6, reported by QA under D-31
#   H1  kb/pits/H1.md  landed ea1b9db, and it is the one marked NOT THE SEAT'S
#                      OWN REPORT — I1 was stopped before it reported, so every
#                      field is labelled for provenance and GRADE is UNVERIFIED
#                      for anything about what the seat experienced
#   S1  kb/pits/S1.md  committed by I3 before D-31 reached it, not a violation
#
# WHY IT HAPPENED, since it is the same class as the W-branch miss earlier: both
# are bookkeeping that only happens if I remember to do it. The merge itself is
# gated by four mechanical clauses; the RECORD of the merge is gated by nothing.
# Two registers that must agree, with a checker on neither — which is precisely
# the shape --check-tables exists to fix one level up, and does not cover here.

MERGED H2  96bfd9f pits:kb/pits/H2.md accept:PASS
MERGED S3  e3bdd75 pits:kb/pits/S3.md accept:PASS

# CLAUSE 6c APPLIED TO ITSELF, TWENTY MINUTES AFTER I MERGED IT. D-49 rules that
# THE RECORD OF A MERGE IS PART OF THE MERGE. I merged that clause, then merged
# H2 and S3, then pushed a state file saying seventeen done while this file said
# fifteen — the identical two-registers-one-checker gap it was written to close,
# reopened by the person who wrote it in. Caught by my own end-of-batch count,
# which is now part of the loop rather than a thing I remember to do.
#
# H2 — CDP driver. Six clauses green, re-run by L1 against the merged tree.
#   I1's THROWAWAY SCOUT before designing is the method worth keeping: it removed
#   three guesses that would each have shipped as a wrong assertion (toolsAdded
#   fires ONCE with a tools ARRAY carrying frameId per tool; that channel spells
#   the annotations differently from the page JS; an unknown tool name is a
#   CDP-level rejection at send time, never a failed response). Every previous
#   revision of the brief had the calling convention wrong.
# S3 — policy engine. Four runnable spans green, 62/62, CHECK 3b still agreeing
#   in blocking, warning AND the identifying-quadruple set.
#   MY FIRST RUN OF contracts-check EXITED 1 AND I NEARLY WROTE IT UP AS A
#   REGRESSION S3 INTRODUCES INTO A MERGED GATE. Cause: ajv missing, because
#   node_modules is gitignored and a fresh scratch worktree has none. Fourth
#   false red from the environment today, same shape as Day 0's FORCE_COLOR pit,
#   and I got as far as drafting the theory before checking the environment.
#
# ADJUDICATION COMPLETE: all ten tickets carry PM's verdict, transcribed by L1
#   under clause 6b — 6 adopt, 2 debt (DEV-004 G0, DEV-008 F0), 1 send-back
#   (DEV-005 G5), plus the Day-0 ticket. 67% adopt.

# TWO FOLLOW-UPS MERGED — NOT new MERGED rows, because neither is a node and the
# row count must keep tracking graph.state.json.done. Both are scope on nodes
# already recorded above.
#
# G5 / DEV-005 — THE SPRINT'S ONLY SEND-BACK, CLOSED AHEAD OF ITS 09:00 DEADLINE.
#   .githooks/pre-commit now invokes pre-commit-ownership ahead of the lint;
#   ownership first, because a staged path a seat has no right to touch is
#   invalid regardless of its content. Gated against the AMENDED clause 3 — a
#   REAL commit, not a hand-invoked script — on a real seat branch:
#     seat I2 stages server/index.mjs (S1's, I3's)  -> exit 1, HEAD UNCHANGED
#     owned path with a banned identifier           -> exit 1, HEAD UNCHANGED
#     owned path, clean                             -> exit 0, HEAD ADVANCED
#
#   MY FIRST RUN OF THAT GATE WAS INVALID AND I NEARLY ACCEPTED IT. Scenario 1
#   reported exit 0 with HEAD advanced and I read it as the fix not working. Two
#   defects, both mine: `$?` was capturing an `echo` rather than the commit, and
#   core.hooksPath was stored ABSOLUTE, so the worktree ran MAIN's unchained hook
#   and proved nothing about the branch under test. A test that looks like proof
#   and is wired to nothing is the exact defect the send-back exists to close,
#   reproduced by the person gating it, one command later.
#
#   AND THE SECOND DEFECT WAS REAL BEYOND MY TEST: core.hooksPath absolute meant
#   EVERY WORKTREE IN THIS REPOSITORY RAN THE MAIN CHECKOUT'S HOOKS RATHER THAN
#   ITS OWN — a seat committing in its worktree was gated by whatever main
#   happened to carry at that moment. Set to the relative `.githooks`. It is a
#   config value, not a tracked file, so it needs setting once per clone and
#   belongs in the runbook.
#
# S1 static route / D-50 — THE CRITICAL-PATH BLOCKER. Unblocks T2 (S4 -> T2 -> H3)
#   and clears one of F1's two blockers. S1's own accept re-run verbatim and
#   unchanged at 8/8; new tests/acceptance/static.test.mjs 7/7; suite 69/69.
#   pageRoot is a PARAMETER rather than a hardcoded path, so the tests run against
#   an injected fixture instead of waiting on F1. Traversal refused plain AND
#   %2e%2e-encoded; /api/ proven unshadowed even when a same-named file exists on
#   disk. Test 7 DOCUMENTS that GET / is still 404 until F1 lands index.html and
#   names itself as the assertion to flip — a gap recorded as an assertion rather
#   than left as an absence.
#
# OPEN FOR PM, raised not waived: the D-50 scope addition produced an UNDECLARED
#   OUTPUT. tests/acceptance/static.test.mjs is DENIED — it matches tests/** -> QA
#   and no node lists it, so clause (a) is empty. Scope was added to S1 without
#   adding the file that scope produces to S1.outputs. Fourth instance of the
#   D-31/D-37 shape: the model is right and a declaration is short.

# I1's D-50 + D-48 follow-ups merged (not a new node row).
#   --smoke-login exists and FAILS DIAGNOSTICALLY: it names the ordering fact in
#   its own error text rather than erroring opaquely, which is what let F1's
#   span 0 be diagnosed in one run.
#   V1's optional fields TRANSCRIBED, and the boundary held exactly: three fields
#   the PNG proves, FOUR REFUSED as inference (tools, agentListed, agentSawTools,
#   registrationError), the mapping traced to probe/index.html rather than
#   asserted, and chromiumMajor flagged as UA-DERIVED rather than binary-read —
#   the caveat the schema itself carries. L1 verified mechanically before merging
#   that all five original values are byte-identical: D-43 HELD, additions only.
#   THE RESULT IS NOW IN THE RECORD RATHER THAN IN A PNG: the ChatGPT built-in
#   browser is CHROMIUM 151, the installed Chrome V0 measured is 152, and the
#   alias is absent on BOTH — HANDOVER section 3 rule 1 corroborated on a SECOND
#   engine, which is stronger than V0 alone. V0's notes said under R-8 that "151
#   is not present on this machine and the previous framing asked about a version
#   nobody can test." We have it now.
#
# F1 REMAINS HALF-GREEN AND THE BLOCKER HAS MOVED INSIDE IT. With the static
#   route and --smoke-login both merged, L1 measured in a detached worktree at
#   seat/UX-F1 + main: / -> 200 text/html, /ui/shell.js -> 200 text/javascript,
#   /api/me -> 401, span 1 still 2 elements — and a TRUSTED click on
#   [data-persona="ruiz"] leaves GET /api/me at 401. The page loads, the module
#   loads, the endpoint works, and the click is not reaching a POST. That is F1's
#   own defect and it is dispatched back to UX with the dependency evidence
#   attached so it does not re-derive it. Span 0 exists precisely to separate
#   "the API works" from "the page uses the API"; only the first was under test.

# F1 IS GREEN ON BOTH SPANS AND IS HELD ON ONE LINE THAT IS NOT UX's.
#   span 0: node harness/drive.mjs --smoke-login chen,ruiz -> EXIT 0, 2/2, and it
#   took the `dom` path rather than the `fetch` fallback, so the thing under test
#   was the SHELL and not the server. span 1: still exactly 2, measured in three
#   states (signed out, after a BUTTON login, after a CARD login).
#
#   MY LEAD WAS THE RIGHT FAMILY AND THE WRONG MECHANISM, and UX measured rather
#   than accepting it. I guessed the handler matched on event.target instead of
#   closest(). It already used closest() — and closest() walks UP the tree while
#   the <button data-login> sits BELOW the click, inside the <li data-persona>.
#   So [data-login] was structurally unreachable from a click on the card, no
#   matter how dispatched. Measured on the page: elementFromPoint at the card
#   centre returns the LI itself (the centre lands on its own padding, so no child
#   is involved at all), closest('[data-login]') -> null, closest('[data-persona]')
#   -> "chen". The symptom matched my prediction exactly for a different reason,
#   which is the most expensive kind of near-miss: a plausible story that produces
#   a working fix and teaches nothing.
#   UX also MEASURED the double-fire risk instead of arguing it away — instrumented
#   window.fetch and counted POSTs: button click 1, card-centre click 1.
#
#   HELD ON: tests/acceptance/static.test.mjs test 7, which flips 404 -> 200 now
#   that F1's index.html exists. That is I3's DELIBERATE TRIPWIRE firing exactly
#   as designed, and its own comment says so. The file resolves to QA by the
#   tests/** glob and does NOT invert under clause (a) — no node declares it — so
#   UX correctly refused to touch it and so did I. Dispatched to QA.
#   Verified independently: plain origin/main runs that file 7/7; it goes red only
#   with F1 merged. Cause is F1 landing and nothing else.

MERGED S4  f580670 pits:kb/pits/S4.md accept:PASS
MERGED F1  fbae697 pits:kb/pits/F1.md accept:PASS

# S4 — the violation envelope. Two things built so they CANNOT DRIFT: rule_id and
#   fix_class are LOOKED UP from S3's frozen POLICY_DOCUMENT rather than retyped
#   into a second rule table, and the x-fixLint scan reads its banned list LIVE
#   from the schema's own x-fixLint.bannedSubstrings. Same discipline as
#   extracting an accept predicate instead of retyping it.
#   "Every write route" had no HTTP write routes to point at — S2 and T2 build
#   those. I3 took neither easy reading (declare it vacuous, or wait) and wrote
#   ONE REAL SCENARIO PER RULE, R01..R19, each ajv-validated against the frozen
#   schema. And it corrected its own ASSERTION rather than the message when the
#   two disagreed: only `fix` was ever required to match the schema's worked
#   instance, because `fix` is what x-fixLint grades.
#
# F1 — MERGED TOGETHER WITH QA's TRIPWIRE FLIP, because neither is valid alone:
#   QA's commit asserts GET / -> 200 and is red without F1's index.html; F1 is red
#   against the old 404 assertion. Combined tree 82/82, tested before either was
#   pushed.
#   MY LEAD WAS THE RIGHT FAMILY AND THE WRONG MECHANISM. I guessed the handler
#   matched event.target instead of closest(). It already used closest() — and
#   closest() walks UP while the button sits BELOW the click, making [data-login]
#   structurally unreachable from a card click. UX measured the page rather than
#   implementing my story: elementFromPoint at the card centre returns the LI
#   itself, closest('[data-login]') -> null. Had it implemented my version the
#   node would have gone green and the geometry would have stayed unknown.
#   A DIAGNOSIS FROM THE PERSON HOLDING THE MERGE GATE IS THE EASIEST KIND TO
#   ACCEPT UNCRITICALLY, which is exactly why it needs measuring like any other.
#
# THE CRITICAL PATH IS UNBLOCKED AND DISPATCHED: T2's last missing input was F1.
#   14.0 / 29.5 h burned; T2 -> H3 -> H6 -> D4 -> D5 -> D6 remains.

MERGED H5  b446bc2 pits:PENDING accept:PASS

# CORRECTION TO THAT MERGE COMMIT, MADE HERE BECAUSE IT IS ALREADY PUSHED.
#   The message says spans 1 and 2 are "backticked FRAGMENTS, not commands".
#   THEY ARE NOT FRAGMENTS — THEY DO NOT EXIST. H5.accept contains exactly ONE
#   backticked span. `--show 1` and `--show 2` both print the runner's usage
#   line, and the exit 2 I saw was an OUT-OF-RANGE INDEX, not the new
#   non-command warning. I read a familiar exit code as a familiar cause without
#   checking which one it was — the same substitution of a plausible story for a
#   measurement that this log already records three times today, in a smaller
#   place. The gate itself is unaffected: the one real span exits 0, the suite is
#   100/100, lint 0, ownership 2/2.
#
# H5 — environment banner. Suite 82 -> 100 (18 new), no regressions.
#   FOUND BY THE CLAUSE-6d SWEEP AND NOT BY A REPORT, the fourth instance today.
#   D-55: a seat that finishes and goes idle is A PROCESS THAT HAS ENDED, so
#   push-based reporting fails at exactly the moment the report matters. The
#   sweep is pull-based and caught H5 and T2 within an hour of becoming 6d.
#
# T2 IS HELD, NOT REJECTED, AND ITS OWN SCOPE IS GREEN.
#   span 1  node tools/check-toplevel.mjs --selftest                 exit 0
#   span 2  find src/page -name '*.js' | xargs check-toplevel.mjs    exit 0
#           js/mjs scanned 40 · registerTool call sites 1
#           OK: 0 of 1 reachable from an iframe or worker entry
#           selftest: "ok  empty scan is a FAIL, not a pass"
#   DEV-006 IS VINDICATED: the clause PM moved off T1 now runs against ONE REAL
#   CALL SITE instead of zero, with the vacuity arm mandatory. W found it, PM
#   moved it, and it did the job one node later exactly as designed.
#   span 0 is `node harness/drive.mjs --assert-flips 1,5,12,13` and
#   `grep -c assert-flips harness/drive.mjs` returns 0. THIRD INSTANCE of the
#   consumer-specifies-the-producer defect, after --smoke-login (F1 -> H2) and
#   the S1 static route (T2/D1 -> S1). Escalated to PM under D-50; not sent back
#   to I2, whose own scope is green, and not invented into drive.mjs by me.
#
# THE FOURTH REGISTER, RAISED TO PM AND CURRENTLY UNMEASURED: my instruments
#   verify that PATHS exist (--check-accept-paths) and that ORDER holds
#   (--check-schedule). NOTHING VERIFIES THAT A MODE, FLAG OR SUBCOMMAND NAMED IN
#   A PREDICATE IS PRODUCED BY ANYTHING. Both checkers passed all three of these
#   happily — drive.mjs resolves as a declared H2 output every time. Three nodes
#   have paid for it today.

# --check-modes SHIPPED (D-59, G0 follow-up) AND PAID FOR ITSELF THE SAME DAY.
#   For every (script, --flag) pair in an accept: if the script EXISTS, the flag
#   literal must appear in it; if it does NOT, the producing node must be a HARD
#   INPUT of the node naming it. 31 pairs, 22 resolved on disk, 6 deferred, 3
#   violations — TWO UNKNOWN TO ANYONE THIS MORNING.
#     H3.accept names harness/drive.mjs --fallback, which does not exist. FOURTH
#       instance of the consumer-specifies-the-producer defect, on the node
#       immediately BEHIND T2 on the critical path.
#     E5.accept names webmcp-eval-kit/bin/eval.mjs --suite, produced by E1, with
#       no E1 -> E5 hard edge.
#   PM's note on the second one is the argument for buying the instrument rather
#   than reading more carefully: E2 and E3 name the same script and BOTH carry
#   their E1 edge, so E5 was the ODD ONE OUT rather than the pattern. A human
#   scanning E5 sees a well-formed node. ONLY SOMETHING COMPARING SIBLING NODES
#   AGAINST EACH OTHER SEES THE GAP.
#   THE LIMIT IS IN THE SOURCE: it checks FLAGS. S1's missing static route was a
#   CAPABILITY IN PROSE and is unreachable by it, permanently. Two of three.
#
# FOUR MISSING HARD EDGES OF ONE SHAPE DRAWN IN ONE EVENING — H2->F1, H2->T2,
#   H2->H3, E1->E5 — three of the four pointing at harness/drive.mjs.
#   124 edges, 113 hard, 105 qualifying, 0 violations. --check-modes is now down
#   to exactly the two flags I1 is writing, which is the instrument being right
#   rather than the tree being broken.
#
# D-61 CLOSED THE GAP L1 RAISED, and D-60 is what made it urgent: with the entry
#   bankable on day one, docs/DEVPOST.md could drift for two days while every
#   existing check stayed green, because every D6 clause verifies an ASSET THE
#   ENTRY POINTS AT and never the entry's own answers. D5 now writes
#   evidence/D5-entry-sync.txt with the sha256 of DEVPOST.md as last pasted;
#   D6 asserts that sha still matches. DELIBERATELY A SHA AND NOT A HUMAN
#   CONFIRMATION — "the operator confirms the answers still match" is a human
#   judgement standing as an acceptance predicate, the exact defect D4 was
#   rewritten to remove.
#
# V1's GATE: SWEPT AND THE VERDICT DOES NOT EXIST — no branch, no artifact.
#   Not D-55's "finished but unreported"; QA never ran the read. Re-dispatched
#   under D-46. Silence is not a pass.

MERGED S9  ba17b6c pits:kb/pits/S9.md accept:PASS

# I1's THREE MODES MERGED (D-58 + D-61). --check-modes now EXITS 0 on main:
#   "every mode named in an accept is produced by something". Both FAIL lines
#   the instrument found this evening are gone.
#   I1 RAN THE SELFTEST RED SIX WAYS BEFORE BELIEVING THE GREEN — wrong count at
#   flip 3, all-zero counts, wrong arity, non-numeral, no violation raised, and
#   violation-raised-but-submit_expense_report-still-present via two temporary
#   edits reverted and diffed back to zero. That last is the clause T2's accept
#   names explicitly, and I1 wanted to have SEEN it fail rather than believe it
#   would. An instrument only ever seen to pass is the vacuous-pass trap this
#   project priced four times today.
#
# T2 IS HELD ON A FIFTH INSTANCE OF THE CONSUMER/PRODUCER DEFECT, IN A REGISTER
#   NO INSTRUMENT REACHES. Not a missing flag and not a missing edge — A MISSING
#   MOUNT POINT. Measured:
#     src/page/index.html:159 loads ./ui/shell.js and NOTHING ELSE.
#     Nothing imports or loads src/page/register.js anywhere. Both index.html
#     and shell.js MENTION it in comments; neither loads it.
#     T2.outputs is [src/page/register.js] — its ONLY output. index.html and
#     shell.js are F1's (UX). So T2's accept requires an observable effect on a
#     page of which I2 owns not one byte, and nothing in T2's accept says who
#     mounts it.
#   --check-modes cannot see this: there is no --flag. --check-accept-paths
#   cannot: register.js resolves perfectly as a declared T2 output. This is the
#   CAPABILITY-DESCRIBED-IN-PROSE class PM named when it priced the instrument at
#   two of three, and it has now cost two nodes — S1's static route and this —
#   both found by a human driving a running system rather than by a checker.
#   Escalated to PM. T2 NOT sent back (I2's scope is green and DEV-006 is
#   vindicated on it); no script tag added by L1; UX not asked on L1's authority,
#   because specifying work into a node from outside is the act D-50 rules
#   against and this would be the fifth time today the fix was to do it again.
#
#   WORTH NAMING: I1's mode reported this correctly and by name — "register.js
#   EXISTS, so this is not the ordering fact — the page loaded it and still
#   registered nothing. That is a real failure." Without that line the next hour
#   goes on deciding which of the two it was.

MERGED V1  b13e898 pits:kb/pits/V1.md accept:PASS
# V1 — HUMAN-GATED, and its row was missing for ninety minutes and ~10 merges.
#   Mechanical gate: ajv 0, PNG non-empty, re-run on main. HUMAN SECOND GATE:
#   QA, under D-46, PASS — six panel-checkable fields matching exactly, and
#   chatgptModel explicitly NOT claimed because the page cannot detect its own
#   driving model. QA checked against the PANEL and not the copy-block, which is
#   the difference between a read and checking the file against itself.
#   V1's artifacts landed INSIDE I1's D-48 merge, so this node produced NO BRANCH
#   — clause 6d sweeps branches and had nothing to find, and the merge log had
#   nothing to hang a row on. QA DID report; the message never arrived. That is
#   not D-55's "finished without reporting" — it is a report sent and lost.
#   Caught by mcp-6d comparing merges.txt against graph.state.json.done.
#   GAP IN MY LOOP, recorded: 6d watches BRANCHES; a node whose artifacts arrive
#   inside another node's merge is invisible to it.

# SEVENTH INSTANCE, AND IT IS THE ONE NO INSTRUMENT REACHED.
#   T2 stayed red WITH the mount in place. Measured against the real served
#   origin: GET / 200, GET /register.js 200, GET /erp.js 404 — the page's module
#   graph dies at the first upward import. src/page/register.js:62 imports
#   ../erp.js; the static route serves src/page/ as root; I3's traversal guard
#   correctly refuses anything above it. THREE files reach above the root —
#   register.js -> ../erp.js, tools/compile.js -> ../../erp.js, tools/defs.js ->
#   ../../policy.js. T1 merged green because its tests import them from Node,
#   where the paths resolve. THE PAGE TREE HAS BEEN UNSERVABLE SINCE T1 LANDED
#   AND EVERY GATE PASSED.
#
#   AND D-65's ARM AS DRAFTED WOULD HAVE PASSED IT. Walk "reachable through
#   non-comment imports, transitively" ON DISK and every hop resolves, BECAUSE
#   THE FILE EXISTS. Green check, 404 browser. The class is not "is this file
#   referenced" nor "is it page-reachable" — it is DOES THE PAGE'S MODULE GRAPH
#   RESOLVE UNDER THE SERVING TOPOLOGY. Raised BEFORE the arm was built, which is
#   the only useful time. D-67 adopts it, and adds that the checker must READ THE
#   SERVED ROOT FROM THE SERVER'S CONFIG rather than hardcode it — a literal
#   would encode the shape of the defect and go stale the moment it is fixed.
#
#   FIX RULED TO I3 (D-66), 0.25 h: widen the served root src/page/ -> src/,
#   guard re-anchored, GET / routed to page/index.html. PM ruled out the
#   alternatives on ARCHITECTURE not cost — src/policy.js and src/erp.js are
#   imported by server/, tests/, harness/ AND the page, and ONE POLICY ENGINE
#   SHARED BY SERVER AND CLIENT IS THE POINT OF THIS PROJECT. Re-homing or
#   duplicating creates a second source of POLICY_VERSION truth. G1 makes the
#   repo public under MIT, so there is nothing in src/ to conceal; the guard
#   stops ESCAPE, not disclosure, and server/ is a sibling that must stay
#   unreachable — to be proven by the guard's own test, not assumed.
#
#   THE SHORTCUT I REFUSED, recorded because it was mine and convenient: I did
#   not move src/erp.js or src/policy.js — MY OWN L0 OUTPUTS — under src/page/ to
#   make a downstream node pass. It would have worked tonight and it would have
#   been the seventh time the fix for this class was to reach into another node.
#
# UX MOUNTED BOTH ORPHANS AND FOUND THE SECOND ONE ITSELF, before PM's D-64
#   reached it. It measured module EVALUATION ORDER with a deliberately
#   adversarial fixture — a second script 1370x larger than the first, so fetch
#   order and evaluation order would disagree if they ever could — rather than
#   citing the spec, and verified the document actually FETCHES register.js via
#   PerformanceResourceTiming rather than trusting that a tag exists. Its own
#   line is the summary of the night: A GREEN NODE IS NOT EVIDENCE THAT ITS
#   OUTPUT RUNS; IT IS EVIDENCE THAT ITS OUTPUT WORKS IF RUN.
#
# THE PAIRED GATE IS NOW A TRIO: I3's root widening + UX's mount + T2. None of
#   the three is valid alone. Merged locally to test, reset, NOT pushed.

MERGED V2  4b01533 pits:kb/pits/V2.md accept:PASS
MERGED G2  ab4831b pits:PENDING accept:PASS

# V2 — RECOVERED FROM A DANGLING COMMIT. seat/I1-V2 pointed at main and fe4e495
#   was on NO branch. Third shared-checkout incident today, after UX's checkout
#   moved HEAD under me and my own G5 commit landed on UX's branch. Found by sha,
#   branched, gated normally, nothing lost, I1 did nothing wrong. The peer session
#   mcp-6d independently anchored the same sha as insurance a moment later; both
#   anchors deleted after the merge.
#   THE EVIDENCE IS STRONG AND MY FIRST CHECK OF IT WAS WRONG. I probed for
#   before/after/gap keys, got null, and read that as a defective artifact. The
#   real keys are observationPair.before/.after/.gapMs. My probe asked the wrong
#   question — the third time today I drew a conclusion from a check that could
#   not have answered it (the T2 module-graph walk with a hardcoded base, the
#   D-41 grep against a retraction, and this).
#   What the evidence actually carries: toolCount 5 -> 6 with gapMs 3, a
#   documentNonce identical across both readings and navigationsSinceLoad 0
#   (two independent witnesses that ONE document instance was observed, since a
#   reload would discard the nonce and emit a second frameNavigated), the sixth
#   tool registered BY NAME over the CDP WebMCP domain rather than through
#   Runtime.evaluate, and the un-awaited getTools().length recorded as
#   "undefined" beside each reading to keep IR-18 visible. It also RETRACTS an
#   earlier 4048 ms gap from its own rig as a defect in that rig's push detector
#   rather than reporting it.
#
# G2 — README demo credentials and open-app paths. Accept 2/2, suite 102/102.
#
# CORRECTION TO THE V2 ENTRY ABOVE: I ORPHANED THAT COMMIT MYSELF. The reflog is
# unambiguous and I had blamed the shared checkout for all of it:
#   seat/I1-V2@{7}  branch: Created from HEAD
#   seat/I1-V2@{6}  commit: I1: V2 — the before/after pair …   <- I1's delivery
#   seat/I1-V2@{5..3}  merge worktree-s1-d66 / seat/UX-F1 / seat/I2-T2
#                      <- MY trio gating, run believing I was on main
#   seat/I1-V2@{2}  reset: moving to origin/main   <- MY reset, which orphaned it
# HALF of it is the shared checkout: HEAD@{7} reads "checkout: moving from main
# to seat/I1-V2" and I did not run that. The other half is mine: I ran three
# merges and a `git reset --hard` WITHOUT EVER CHECKING WHICH BRANCH I WAS ON.
# Every trio gating result was still valid — same content, same commands — but
# the bookkeeping was wrong for the whole sequence, and the reset destroyed a
# seat's delivery. I then reported the dangling commit as the shared-checkout
# hazard rather than as my own reset. The hazard is real and it put me on the
# wrong branch; it did not run reset --hard.
#
# NOTHING WAS LOST. fe4e495 was recovered by sha, gated normally and merged; the
# push was `HEAD:main`, so origin/main is correct throughout and never carried a
# wrong tree. Local `main` was stale at d41f303 while origin/main was ab4831b,
# and is now corrected. seat/I1-V2 is re-pointed at I1's own commit.
#
# AND THE HOOK CAUGHT IT, which is DEV-005 paying for itself within hours of the
# send-back: my record commit was BLOCKED because pre-commit-ownership resolved
# the seat from the branch name as I1 and denied .team/log/merges.txt and
# erp/graph.state.json. That gate exists because G5 shipped inert and PM sent it
# back; the chained hook I asked I4 for is what turned a silent wrong-branch
# commit into a refusal. It is the first time tonight an instrument stopped ME
# rather than reporting on someone else.
#
# THE RULE, and it is cheap: CHECK THE BRANCH BEFORE A DESTRUCTIVE GIT COMMAND,
# every time, not only when something looks odd. `git reset --hard` on a branch
# you have not verified is the one command in this loop that can destroy another
# seat's work, and I ran it four times tonight.

# RECORD CORRECTION — MY LOG ASSERTED FOUR FILES THE TREE DOES NOT HAVE.
#   H5, S9, V2 and G2 all carried `pits:kb/pits/<node>.md` and NONE of those
#   files exists on main. V2's and G2's rows also carried NO SHA — a shell
#   substitution that returned empty because I ran the grep for the merge subject
#   from the wrong branch, and I did not look at the line I had written.
#   Shas restored from the real merge commits: V2 4b01533, G2 ab4831b.
#   The three still-missing pits now read `pits:PENDING`, which is true, instead
#   of naming a file that is not there.
#
#   THIS IS THE ASSERT-VS-EXISTS CLASS IN MY OWN RECORD, and it is the fourth
#   time today the family has appeared: the authority asserted enforcement by a
#   registry row that did not exist (D-41); a rule permitted an edit whose
#   control was unverified (D-57); T1 was unservable while every gate passed; and
#   now the merge log names artifacts that were never written. Every time the
#   claim was reasonable and the thing behind it was absent.
#
#   VECTOR, per mcp-6d: the seat+I1-V2 worktree was removed while carrying two
#   uncommitted files, at the moment I absorbed that branch.
#   CORRECTION — I NARROWED THAT TO I1's PIT DRAFTS AND I WAS WRONG. I1 states it
#   never wrote to kb/pits/ at all this session (D-31 says report, do not commit),
#   that its only repo write all night was evidence/V2.json, and that fe4e495 is
#   1 file changed — which the tree confirms. So the two lost files are still
#   unidentified and the search does NOT narrow to I1. I inferred a convenient
#   explanation from a real observation and stated it as likely; the observation
#   stands and the attribution does not.
#
#   WHY IT WENT UNSEEN: clause 6c says the record of a merge is part of the merge,
#   and NOTHING CHECKED THE RECORD. The row count matched the done count, so both
#   registers agreed while both overstated the tree. Two registers agreeing is not
#   two registers being right.

# TWO CORRECTIONS FROM I1, BOTH ACCEPTED, BOTH MINE.
#
# (1) H5 WAS A DIFFERENT SESSION OF THE I1 SEAT and I asked the wrong one for its
#     pit. Verified: H5's commit 2d2854b carries
#     Claude-Session: …session_01GZnyPEuYaxqLRGj7m4jN4L; the session I asked is a
#     different id, dispatched tonight for V2 only. It REFUSED to reconstruct a
#     pit from a diff, and it is right to: that is the artifact D-31 exists to
#     prevent, and this project already carries one pit marked "NOT the seat's own
#     report" (kb/pits/H1.md) which was worse than the node deserved. H5's row
#     stays pits:PENDING, which is the honest state, and --check-record enforces
#     that PENDING may not silently become a filename.
#     Its commit body ends "the five fields are reported to L1" at 11:02 -0700, so
#     that session believed it had reported. That is the THIRD time today a report
#     was made and did not arrive — after QA's V1 verdict, durable in its own
#     transcript, and I4's and I3's. A seat still running cannot be relied on to
#     have reached me any more than one that has ended.
#
# (2) THE observationPair KEY PATH, recorded so the next reader does not repeat my
#     mis-grade: evidence/V2.json nests the pair under observationPair.before,
#     .after and .gapMs. V2's accept says "a pair of tool-count observations plus
#     the wall-clock gap" and names no keys, so a flat probe was a reasonable
#     guess — the mis-grade was as much the shape's fault as mine, which is I1's
#     framing and I accept it.
#
# OPEN FOR PM, raised by I1 and not L1's to edit: evidence/UNKNOWNS.md line 34
#   still reads "refreshes (localhost)" and "Not re-run remotely", both stale as
#   of 4b01533 — V2 was re-run on the REMOTE origin on Chrome 152. The row still
#   ends MEASURED so tools/check-unknowns.mjs stays green; it is wording, not a
#   broken gate, and UNKNOWNS.md is a V6 output and PM's.

MERGED E1  90dc51f pits:kb/pits/E1.md accept:PASS

# EXPOSURE I INTRODUCED BY SPLITTING THE TRIO, PINNED HERE SO IT CANNOT BE
# FORGOTTEN. src/page/index.html:182 is on main carrying
# <script type="module" src="./register.js"> while src/page/register.js is NOT on
# main. Every browser load 404s that module. Found by mcp-6d.
#
#   npm test stays green (114/114) because node tests never load the page — THE
#   EXACT GREEN-BUT-NEVER-RUNS GAP THAT PRODUCED SEVEN INSTANCES TODAY, and I
#   introduced an eighth with the fix for the seventh.
#   No accept catches it. D6.accept verifies the live URL returns 200 and the
#   first screen shows the env banner; both hold, because a 404 on one module
#   script does not stop the others evaluating — measured, F1's accept is 2/2 and
#   the banner mounts with register.js 404ing.
#   The real cost is a CONSOLE ERROR A JUDGE SEES IF THEY OPEN DEVTOOLS, on a
#   project whose pitch is that the page is honest about its own state.
#
#   WHY THE SPLIT WAS STILL RIGHT, and it is a trade rather than a free win: the
#   unmounted banner was a LIVE D6 FAILURE in an accept — the first screen would
#   have shown the literal placeholder through the demo and the video. This is
#   not in any accept. But "not caught by a gate" is exactly the reasoning this
#   project spent the day dismantling, so it is contained rather than argued
#   away: UX asked for a guard on the tag (its file, not mine), and the whole
#   thing is moot the moment T2 lands. I2 is working T2 now.
#
#   WHAT I DID NOT DO: revert the mount, which would reopen the D6 failure; or
#   edit src/page/index.html myself, which is UX's declared F1 output.

MERGED V3  dd6d662 pits:kb/pits/V3.md accept:PASS

MERGED T4  da68e78 pits:kb/pits/T4.md accept:PASS
# --check-modes CAUGHT THE NEXT INSTANCE AT THE MOMENT THE PRODUCER LANDED, which
#   is precisely what PM bought it for. E2, E3 and E5 all name
#   `webmcp-eval-kit/bin/eval.mjs --suite`; E1 shipped eval.mjs WITHOUT --suite,
#   and E1's own accept never asked for it (it asked for --version and --selftest).
#   Before E1 merged, the check correctly DEFERRED — the script did not exist and
#   the E1 -> E2/E3 hard edges were present. The instant the file appeared, the
#   flag's absence became visible. Ninth instance of consumer-specifies-producer,
#   and the first one caught at merge time rather than when a downstream seat was
#   dispatched into it.

MERGED S5  75861f9 pits:kb/pits/S5.md accept:PASS
# S5 — the human sign gate. Its accept MANDATES a scheduled frozen-file edit, and
#   both target paths resolve to L1: eval-case.schema.json by the erp/contracts/**
#   glob, FREEZE.md as a declared S10 output. I3 hit the ownership hook, DID NOT
#   BYPASS IT, filed DEV-E3-eval-case-known-open, left the edits as a patch and
#   handed them to the seat that owns them. FIFTH instance today of the tool/prose
#   gap behind DEV-001 and DEV-002, and the fifth time the seat that hit it
#   escalated rather than routing around the gate.
#   L1 applied it, VERIFIED BEFORE COMMITTING — controlStatus enforced,
#   observedToday absent, sha256sum -c FREEZE.md exit 0, contracts-check exit 0,
#   139/139 — and committed with the authorization id in the message, which is
#   what S5's accept greps for.
#   R-27 is why the naive fix was worse than the defect: controlStatus 'refused'
#   is NOT in the frozen enum, so setting it would have failed ajv and turned
#   npm test red repo-wide through CONTRACTS.md section 11 check 1.
#   OPEN, named by I3 rather than left: server/sign.mjs commit() does not yet
#   re-canonicalise against live state — S6's job, documented inline as NOT THIS
#   NODE — so the commit route does not catch a report edited between sign and
#   commit until S6 lands.
#
# E1 FOLLOW-UP (D-69): --suite shipped and --check-modes is GREEN again. Gated on
#   the ARTIFACT per D-71 — 28 occurrences in the committed file, not on the
#   report. AND IT REFUSES AN EMPTY SUITE (exit 2), which I asked for citing its
#   own selftest-proves-it-can-fail as the standard. C4 also declined to
#   manufacture a green: E2/E3 stay non-green until their suite files exist and
#   FAIL rather than pass vacuously.
#   C4's EARLIER is addressed to MY loop and is now in it: "run --check-modes
#   immediately when a producer artifact lands." That is exactly when this became
#   visible.

MERGED T2  0483c20 pits:PENDING accept:PASS
#   T2's PIT IS UNRECOVERABLE FROM I2's CURRENT SEAT, on the same footing as H5, and
#   I2 said so rather than writing one. T2 was already merged and green when that
#   session opened; everything it knows about the node -- 7/7 clauses, the walk,
#   submit appearing and vanishing -- it knows FROM MY SUMMARY, not from having done
#   it. Five fields written from that would be a seat TRANSCRIBING THE FOREMAN AND
#   SIGNING ITS OWN NAME TO IT, which is the exact failure D-31 exists to prevent
#   and worse than the PENDING row. Carried as unrecoverable; not waiting on I2.
# T2 — THE CORK IS OUT. 7/7 clauses, 1 -> 5 -> 12 -> 13, submit_expense_report
#   appearing at S3 and vanishing after a violation raised through the REAL policy
#   engine. Merged as a TRIO with I3's browser-safe canonicaliser and C4's
#   byte-identity re-sync; none of the three is valid alone.
#
#   ROOT CAUSE, and it is the sharpest instance of today's family: src/canonical.js
#   imported node:crypto, which a browser cannot resolve, so the page's module
#   graph FETCHED 200 ON EVERY HOP AND FAILED TO LINK. I probed that graph twice
#   and reported it healthy both times BECAUSE I WAS MEASURING FETCHES. Fetch
#   succeeds, linking fails, and no probe I wrote could see the difference.
#   Found by I2 in a file it does not own, which it did not touch.
#
#   C4's byte-identity test caught the knock-on at the earliest deterministic
#   boundary — the vindication of its choice to satisfy R-11 BY IDENTITY rather
#   than by agreement, verified here by blob hash: both files are
#   8fa0bedd4d8aeaad33c10e7140e678b6b72ffdaa.
#
#   DEV-006 VINDICATED: W found T1's clause passing on ZERO call sites, PM moved
#   it to T2 with --selftest mandatory, and it now scans a real call site where
#   the invariant can fail.
#
#   AND IT RETIRES THE DANGLING register.js REFERENCE I PUT ON MAIN when I split
#   the earlier gate. UX corrected my framing of that and was right: not "a
#   console error if a judge opens devtools" but a page with NO TOOLS AT ALL that
#   said nothing about it.

MERGED F3  e177cb5 pits:kb/pits/F3.md accept:PASS

MERGED S12 be34298 pits:kb/pits/S12.md accept:PASS

# kb/pits/H5.md IS UNRECOVERABLE AND THE ROW STAYS PENDING. I1 did not assert
#   that — it evidenced it. Commit 2d2854b carries a DIFFERENT session id of the
#   same seat; its body ends "No pit file committed, per D-31; the five fields are
#   reported to L1", so that session DID report and the report did not arrive.
#   I1 then swept every transcript under the project directory for an assistant
#   message containing both TRIED and GRADE together with H5/env-banner/banner.test:
#   ZERO HITS. Two transcripts reference that session id; neither carries the
#   report. So the fields exist nowhere reachable.
#   It declined to reconstruct from the diff and the commit body, which is what I
#   asked and what it had already refused once. PENDING is the honest state and
#   --check-record enforces that PENDING cannot silently become a filename.
#   FOURTH report today made and lost in transit — after QA's V1 verdict, I3's S9
#   and I4's. Reports are not a channel we can rely on; the branch sweep is.
#   RECOVERABLE AND NOT A PIT: 2d2854b's own body carries real technical content
#   (presence graded off document.modelContext never navigator.modelContext; the
#   warn node is a SIBLING of the banner text node because nesting puts its prose
#   into textContent and breaks the accept regex; --headless=new reports
#   HeadlessChrome/152.0.0.0 which matches only because it contains "Chrome/152").
#   That is the seat's own words and it stays where it is. IT MUST NOT BE
#   RELABELLED A PIT, and I1 was right to say so unprompted.
#
# V4 IS PARKED AT c98016f, NOT ABANDONED, and it is blocked on a HUMAN not on
#   time. Delivered and committed: harness/compare-runs.mjs (--selftest 11/11) and
#   evidence/V4-run1.json, run 1 TRANSCRIBED from evidence/V4.json rather than
#   re-measured, on the D-48 precedent.
#   NOT delivered: run 2. Run 1's raw error is a 22.267 s timeout that is THE
#   CHATGPT CLIENT'S OWN CDP-WRAPPER TIMEOUT — a property of that client. On plain
#   Chrome the harness IS the client, so its timeout is a parameter we choose:
#   measuring it would be CIRCULAR, would discover nothing, and would then agree
#   with itself. The only faithful run 2 is a second BUILT-IN-BROWSER run on the
#   remote origin.
#   compare-runs.mjs therefore carries a CLIENT-IDENTITY GUARD: two runs naming
#   different clients are REFUSED with a distinct message rather than folded into
#   "they disagreed by more than 20 percent", which would report a measurement
#   dispute where there is none. Its selftest also pins the three other ways the
#   gate could pass while discovering nothing — missing observations comparing
#   equal, a no-timeout run averaging with a numeric one, malformed run files.

MERGED V6  b6313e6 pits:kb/pits/V6.md accept:PASS

MERGED H3  8123246 pits:kb/pits/H3.md accept:PASS
# H3 — the in-page fallback agent, and the honesty defect it caught on the way.
#   Under --disable-features=WebMCP with the fallback mounted, the first screen
#   read "Chromium 152 · WebMCP present" ON A BROWSER WHERE WebMCP IS OFF, because
#   readEnv graded presence off whether document.modelContext EXISTED and the shim
#   installs exactly there. MOUNTING THE COMPONENT WHOSE PURPOSE IS TO BE HONEST
#   ABOUT BEING A SIMULATION MADE THE BANNER CLAIM REAL WebMCP WAS WORKING.
#   No test caught it: every existing test passed a real context, and the flip walk
#   was 7/7 green throughout because THE WALK CANNOT SEE THE BANNER.
#   I1's line, and it is today's lesson in a new register: A GREEN NODE IS NOT
#   EVIDENCE THAT ITS OUTPUT TELLS THE TRUTH.
#   Fixed STRUCTURALLY rather than cooperatively: the shim marks itself
#   __simulated and readEnv reads the marker, so the label survives the caller
#   forgetting to pass simulated:true — which happened immediately, because with
#   the tag in the earliest slot outpocketShell does not exist yet and
#   refreshEnvBanner never fires.
#   I VERIFIED THE REGRESSION TESTS CAN FAIL rather than taking the claim:
#   reverting the marker read turns EXACTLY tests 15 and 16 red; test 17 (a real
#   context is never labelled simulated) passes either way, which is what a
#   control is for.
#
# THE TAG, and two seats measuring the same thing on different trees.
#   UX measured that the tag BEFORE the shell leaves the env banner unlabelled
#   while #agent-banner still reads correctly — it does not throw, does not log,
#   and THE PAGE LOOKS CORRECTLY LABELLED. I1 measured both ends of the range and
#   got EXIT 0 either way. Not a contradiction: I1's marker fix makes the label
#   structural, so the constraint UX measured against the PRE-FIX module no longer
#   binds. Both were right about the tree they measured.
#   UX's rule is the keeper: A MODULE'S OWN STATEMENT OF ITS CONSTRAINTS IS SCOPED
#   TO THE CONCERNS ITS AUTHOR HAD IN MIND, AND A MOUNT IS WHERE A SECOND CONCERN
#   CAN APPEAR THAT THE AUTHOR NEVER HAD TO CONSIDER.

MERGED T5  c747c38 pits:kb/pits/T5.md accept:PASS
#   The blind surface export. All four accept clauses green bare, suite 161/161.
#   THE TRAP, AND WHY THE PREDICATE COULD NOT HAVE CAUGHT IT: the state ids cross
#   over at 2/3 — the export's S2-emp-draft-clean is compile.js's internal S3 and
#   S3-emp-draft-dirty is internal S2. Mapping by the digit would have published
#   submit_expense_report ON THE SURFACE THAT CARRIES A BLOCKING VIOLATION, and the
#   state count, the id order and EVERY DIGEST WOULD STILL CHECK OUT — all four
#   clauses are satisfied by an internally consistent export OF THE WRONG THING.
#   I checked the mapping independently rather than reading the code that implements
#   it: clean minus dirty is exactly [submit_expense_report], dirty minus clean is
#   empty. Correct direction.
#   app_commit is deliberately NOT `git rev-parse HEAD` — committing the artifact
#   would move HEAD and E5's drift guard would false-alarm forever.

MERGED F4  c526b56 pits:kb/pits/F4.md accept:PASS
#   The signing dialog. UNDISCLOSABLE THEREFORE UNSIGNABLE: when the facts cannot
#   be named the sentence returns null, [data-worst-case] renders empty, and the
#   POST is refused. A generic fallback sentence was the natural choice and would
#   have passed this accept forever.
#   The guard sits on the PATH THAT SENDS, not on the button: the test strips the
#   disabled attribute, calls submit directly, and asserts fetch.calls == [].
#   Three controls, because an assertion nobody has seen fail is a claim.
#   AND UX FOUND A FALSE GREEN IN MY OWN TOOL on the way (D-80): --check-orphans
#   counted a TEST import as a mount, so every node output had a referrer and a
#   module no page loads passed. It would not have caught the env-banner orphan
#   either. Fixed here; one true orphan now stands, S5's sign-bridge.js.

MERGED S2  e790b38 pits:kb/pits/S2.md accept:PASS
#   Authz. THE PREDICATE WAS PASSED PERFECTLY BY A SERVER THAT 403s EVERYONE, and
#   the fix is a POSITIVE CONTROL beside every negative: same route, employee
#   cookie, asserted not-403 AND pinned to the expected success status. Also 403
#   distinguished from 404 by exact status AND authz code; the route table asserted
#   NON-EMPTY before iteration (a for-loop over nothing exits 0); and the table READ
#   from the server's own export rather than transcribed.
#   I3 SABOTAGED ITS OWN authz.mjs TWICE -- always-throw, empty table -- to watch the
#   script fail before trusting it green.
#   And gating /api/sign broke its OWN merged S12 test; it fixed the FIXTURE rather
#   than weakening the gate to avoid touching prior work.

MERGED H4  55d3485 pits:kb/pits/H4.md accept:PASS
#   The seeded demo. Predicate was the weak kind -- two runs at the SAME seed compared
#   with diff, WHICH TWO EMPTY FILES PASS -- so the node was built around the
#   falsifiability rather than to the accept: seed 7 gives 3 lines/$4395, seed 8 gives
#   1 line/$1711, differing in title, count, merchants, dates and amounts.
#   SEEDED RATHER THAN SCRIPTED, deliberately: a hard-coded script is also
#   deterministic and would have passed forever while proving nothing.
#   I1's first run exited 0, reported ALL SEVEN STEPS ok, and had created NO REPORT.
#   toolset.call NEVER THROWS for the two failures that matter, so try/catch reports
#   success for a demo that did nothing. Caught by an incidental stderr line reading
#   "reached S1". THE LOG LINE WAS THE ONLY HONEST WITNESS IN THE ROOM.
#   Exclusion list carried IN the dump with paths and reasons; dates RELATIVISED not
#   excluded, which also keeps the dump stable across the 17 unattended midnights of
#   the judging window.
#   MY OWN ERROR, RECORDED: I pushed H4 without its mount tag because `git merge -F -`
#   cannot read a message from stdin, the merge errored mid-block, and I read the last
#   line of the output instead of every line. main briefly carried the module with
#   nothing loading it. Fixed in the next push; caught by re-running --check-orphans
#   rather than trusting the echo.

MERGED S6  46fe9d1 pits:kb/pits/S6.md accept:PASS
#   TOCTOU re-canonicalisation. THE PREDICATE'S OWN SCENARIO IS UNREACHABLE: S12's
#   lock refuses the "mutate then submit" mutation with 423 before S6 could run.
#   I3 PROVED THAT FIRST against the real server rather than quietly testing
#   something adjacent, then exercised the case the lock is NOT there to catch
#   through a bootstrap documented plainly as outside the lock.
#   False-positive safety is the half that matters and it is tested: five
#   canonicalisation attacks prove an honestly-reformatted report is NEVER rejected,
#   beside two real content changes that ARE caught. A check that rejects everything
#   is as useless as one that rejects nothing.

MERGED H6  12ea923 pits:kb/pits/H6.md accept:PASS
#   The rehearsal rig. Grader written BEFORE the runner, because "5 of 5 under 120s
#   with per-step timings" is satisfied by a rig that starts and stops.
#   Both falsifiability arms verified by me, not taken: --break-run gives 4/5 with
#   run 3 named and exit 1; --selftest drives all eight grading clauses to failure
#   on synthetic dumps, including one carrying ONLY the pre-seeded archive -- which
#   is exactly what a rehearsal that did nothing produces. That second arm exists
#   because the clauses that carry the weight CANNOT BE REACHED BY A LIVE RUN, and
#   an unexercised branch in a grader is indistinguishable from one that does not work.
#   I1 DID NOT BELIEVE ITS OWN GREEN: 0.6s per run including a Chrome launch is the
#   exact shape of a rig that did nothing, so it timed one invocation independently.
#   EARLIER is a new rule: A DERIVED MEASUREMENT AND THE THING IT MEASURES MUST NOT
#   SHARE A CHANNEL WHEN ONE OF THEM IS GRADED FOR BYTE-STABILITY. Putting per-step
#   ms in the dump would have broken H4's byte-identical accept and looked like
#   non-determinism in the demo.

MERGED F7  64363da pits:kb/pits/F7.md accept:PASS
#   The signature provider, installed. --check-orphans FULLY GREEN for the first time,
#   and by the right route: sign-bridge.js is closed by being IMPORTED, not exempted.
#   THE ORPHAN AND THE MISSING GLUE WERE ONE DEFECT SEEN FROM TWO SIDES.
#   Clause 2 drives the REAL tool through the double lock with only the human stubbed;
#   APPROVE completes with CH-0001 and S5's ticket, DECLINE refuses on the same wire
#   with the human's own words.
#   CLAUSE 3 PASSED FOR THE WRONG REASON FIRST: the fixture passed amount as a STRING,
#   toCents() returned null, the draft stayed dirty, and submit_expense_report was
#   never on the surface -- so "the submit must not complete" was true and tested
#   nothing. A NEGATIVE CONTROL SATISFIABLE BY THE TOOL BEING ABSENT IS NOT A CONTROL.
#   STILL OPEN: the page cannot COMPLETE a signature (403 E_NO_CONFIRM_TOKEN). D-89.

MERGED T3  4b10265 pits:kb/pits/T3.md accept:PASS
#   The absence register. BOTH DELIBERATE REDS FROM THE D-77 BUMP CLEARED HERE.
#   The predicate asks only that code/severity/field/fix/candidates be PRESENT, and
#   a body of code:"UNKNOWN", field:"", fix:"contact your administrator" satisfies it
#   and is worthless. It answers the same question SIX different ways across the six
#   states, names real validator codes, never reports ITSELF missing, and never
#   invents an absence for a present tool.
#   I2 OVERRULED MY BRIEF BY CITING THE FROZEN SCHEMA AND WAS RIGHT: allOf[0] forces
#   candidates EMPTY for human_exception_required, because when only a human can act
#   there is no value an agent could supply and a list of tools is a way forward that
#   does not exist.
#   FOUR states MEASURED in a real browser, TWO DERIVED -- and that distinction is now
#   IN the predicate, so the honesty survives the seat that insisted on it.
#   FOUR REVISIONS of this predicate, each checked by READING and none by RUNNING,
#   until PM ran `sh -n` and caught a fifth defect its own fix had introduced.

PARTIAL D1  7be7178 pits:kb/pits/D1.md accept:3-OF-4 (clause 4 deferred, D-92(b))
#   NOT counted as done. Header dump, Origin-Agent-Cluster ABSENT and 200 all pass on
#   the live host; /version==HEAD is verified in the FINAL PASS with D6, because Render
#   auto-deploys on push and a mid-sprint mismatch is expected.
#   Ownership checked PER SEAT: four apparent violations against I3 all trace to
#   commits authored by I4 on seat/I4-D1, and --seat I4 returns 0 on them. Ownership
#   binds branches, not keystrokes.
#   THE FIND: `Number(process.env.PORT) || 3000` treats 0 AS FALSY, so PORT=0 fell
#   through to 3000 and curl-403.sh + toctou.sh were ACCIDENTALLY BINDING 3000 EVERY
#   TIME while claiming ephemeral ports. Both passed throughout. A TEST THAT PASSES ON
#   THE WRONG PORT PASSES.
#   FIRST PIT DELIVERED UNDER CLAUSE 6e -- arrived inside the commit, no chasing.

MERGED E5  c522af8 pits:kb/pits/E5.md accept:PASS
#   The accounting eval under a network denial PROVEN TO HAVE FIRED. Three of four
#   accept clauses can pass while proving nothing and I verified each myself:
#   running the eval WITHOUT the preload exits 1 with E_NETWORK_DISABLED, so the
#   denial arms; byte-identity holds over 2 declared/2 emitted cases with verdicts
#   rather than over an empty file; and webmcp-eval-kit/src/ is non-empty so the
#   negative grep is a search that actually ran.
#   C4 added an arm I had not asked for: hooks REPLACED AFTER preload are rejected,
#   because a marker identifying a hook by its function NAME is satisfied by anything
#   wearing that name.
#   Header names the MECHANISM, not a boolean -- an opinion vs a measurement.

MERGED D1  c923725 pits:kb/pits/D1.md accept:PASS
#   GRADED IN THE WINDOW. All four clauses observed to hold SIMULTANEOUSLY against the
#   live host at c92372587a728300dd5bbd0686d2e31bcb01f7f0:
#     (1) Origin-Agent-Cluster ABSENT from evidence/headers.txt
#     (2) GET / returns 200            (checked EXPLICITLY -- the accept's own span
#         prints the code with -o /dev/null and exits 0 whatever it is, so exit 0 on
#         that span proves nothing on its own)
#     (3) live /version == git rev-parse HEAD, both c923725...
#     (4) evidence/D1-url.txt records ONE INSTANCE **with the reason** -- free tier,
#         single-instance BY NATURE, not a configured replica count
#   D-92(b) STILL BINDS: the version clause is RE-VERIFIED IN THE FINAL PASS WITH D6.
#   The next push to main moves the sha and Render redeploys, so this row records an
#   OBSERVATION AT A MOMENT, not a permanent property. DONE MEANS OBSERVED.
#   The PARTIAL D1 row above stays as history: the route and mount landed hours before
#   the clause could be graded.

MERGED V4  48fa7b8 pits:kb/pits/V4.md accept:PASS
#   The comparator, rebuilt because the STANDARD MOVED after it was written. I1 re-read
#   its own selftest against UX's fail-open rule and D-90 and failed it: 11 cases, every
#   one asking "does this refusal FIRE", not one asking "does it HOLD ITS FIRE when it
#   should". Rebuilt as fires/declines PAIRS, 11 -> 20.
#   Broken twice on purpose: fail-OPEN turns 1 red, fail-CLOSED turns 13 red. The suite
#   now catches an instrument that cannot say NO **and** one that cannot say YES.
#   D-90 case changed SHAPE, not assertions: it asserts the COUNTERFACTUAL -- the two
#   malformed docs ARE naively equal -- so the trap is proven reachable rather than
#   assumed. Without it the case is satisfied by absence.
#   FILED A DEFECT AGAINST ITS OWN MERGED H6 unprompted: that selftest is one-directional
#   too. A SUITE THAT ONLY TESTS ONE DIRECTION OF A DECISION CANNOT TELL A WORKING GUARD
#   FROM A STUCK ONE, AND WHICH DIRECTION IT TESTS IS DECIDED BY WHICH FAILURE YOU WERE
#   AFRAID OF THAT DAY.

MERGED F5  f7e4259 pits:kb/pits/F5.md accept:PASS
#   The inspector. Closes TWO of F6's five reds; only the three miscategorised shots
#   remain, with L2.
#   THE PREDICATE'S ONE HOLLOW PATH is both sides coming from the same place. Rows are
#   walked from the RENDERED DOM, tools from document.modelContext.getTools(). And the
#   panel reads modelContext rather than our registry ON PURPOSE: the registry is what
#   we TRIED to register, modelContext is what the browser HOLDS, and they differ
#   exactly when something is wrong.
#   FOUR IDENTICAL COMPARISONS ARE ONE COMPARISON -- the four counts are asserted NOT
#   ALL EQUAL, a guard UX added after noticing they happened to differ.

# D-98: D1's PREDICATE COULD NOT FAIL ON THE TWO CLAUSES THAT MATTER MOST, and I found it
#   while reporting the node GREEN. `curl -w '%{http_code}'` PRINTS the code and exits 0
#   WHATEVER IT IS; and the version clause said "equals git rev-parse HEAD" in PROSE while
#   the runner ran the two as separate spans and NEVER COMPARED THEM. FOUR GREEN SPANS WERE
#   CONSISTENT WITH A 500 AND A MISMATCHED SHA.
#   The only reason it was caught rather than banked is that I checked the four clauses
#   EXPLICITLY instead of trusting four exit-0s. A PREDICATE THAT CANNOT FAIL IS NOT A
#   WEAKER PREDICATE, IT IS A DECORATION -- the fourth artifact class this sprint has found
#   one in, after a lint that never ran, a hook git never invoked, and a clause that passed
#   on zero call sites.
#   Corrected by PM and proven in the FAILING direction: 500-vs-200 exits 1, 200-vs-200
#   exits 0. Re-run now: spans 0-2 green, span 3 RED because main moved under the deploy --
#   which is D-92(b) working, not a regression. D1's done status rests on the explicit
#   manual observation, and is re-verified in the final pass with D6.

FOLLOWUP D-89 47a724e pits:kb/pits/D-89.md accept:PASS (D-96 three clauses)
#   An S5 follow-up, not a node. THE PAGE CAN NOW COMPLETE A SIGNATURE.
#   The invariant is held BY CONSTRUCTION, not by filtering: a session-scoped route that
#   is NOT a registered tool (0 in defs.js, absent from the export surface), a page-only
#   openForDialog() companion, and beginSign()/continueSign() BYTE-FOR-BYTE UNCHANGED.
#   THE AGENT CANNOT READ THE TOKEN BECAUSE THERE IS NO CALL THAT RETURNS IT.
#   Clause 3 is why clause 2 means anything: "no tool leaks the token" is satisfied by
#   THERE BEING NO TOKEN, so the same run proves one EXISTS and is PAGE-READABLE.
#   AND THE F7 DEFECT IT REPAIRS: sign-install.js read three fields off a beginSign()
#   that never carries them, so a real click posted to /api/sign/null/respond. Root
#   cause: browserDialogPort HAD NO TEST, because every F7 test stubs the port -- EVERY
#   INJECTION POINT SPLITS THE CODE INTO A TESTED HALF AND A SHIPPED HALF, AND THE
#   SHIPPED HALF IS INVISIBLE TO EXACTLY THE TESTS THE INJECTION MADE POSSIBLE.
#   And: DEFENSIVE DEFAULTS ON A REQUIRED VALUE CONVERT A LOUD FAILURE INTO A WRONG ONE.

FOLLOWUP DEV-abort-rejection b26c737 pits:kb/pits/DEV-abort-rejection.md accept:N/A (not a node)
#   The unhandled AbortError QA found on the LIVE site. registerTool RETURNS A PROMISE
#   that rejects on abort; register.js discarded it and we revoke the previous
#   generation on every flip on purpose -- so every registration in the revoked
#   generation rejected unhandled. Cause PROBED on Chrome 152, not inferred.
#   QA's 12 and I2's 13 are the same bug: one rejection PER REGISTERED TOOL, so the
#   count tracks surface size and the deployed build predates T3. The discrepancy WAS
#   the evidence.
#   Fix is narrow: swallow only an AbortError whose controller is OURS. I sabotaged it
#   myself -- the pre-fix discard fails exactly tests 184 and 185, restored 197/197.
#   I2 NEARLY SHIPPED TWO FALSE NEGATIVES, BOTH THE BUG\'S OWN SHAPE: Playwright had no
#   WebMCP so it measured the __simulated shim, and a later run showed 0 exceptions
#   because the server had died of EADDRINUSE and the page never loaded.
#   A ZERO IS ONLY EVIDENCE IF YOU CAN SHOW THE THING THAT PRODUCES NON-ZERO WAS RUNNING.

FOLLOWUP DEV-sign-crash 38d6afb pits:kb/pits/DEV-sign-crash.md accept:N/A (not a node)
#   A WHOLE-SITE OUTAGE on the signature path. One malformed POST /api/sign killed the
#   Node PROCESS -- uncaught CanonError from digest() on an undefined field. Found while
#   running the shoot gate against production: the 502 and the subsequent 401s were ONE
#   EVENT, because the instance restarted and SESSIONS ARE IN-MEMORY.
#   Two fixes, each verified independently: field validation in open() (400 with the
#   missing names), and a TOP-LEVEL GUARD that did not exist at all -- so any throwing
#   handler now costs one client a 500 instead of the site.
#   I proved the general guard alone suffices by disabling only the specific validation:
#   500 not 000, CanonError still thrown, /api/me still 200. MY FIRST SABOTAGE SILENTLY
#   FAILED TO APPLY and returned 400 -- A SABOTAGE THAT DOES NOT APPLY IS A GREEN THAT
#   MEANS NOTHING.
#   The narrowing is undefined-ONLY because NULL IS A VALID OCF-1 VALUE; a stricter first
#   draft broke three passing tests and was caught before it shipped.

MERGED E4  50b8cd4 pits:kb/pits/E4.md accept:PASS (clause 1 verified BY HAND — see below)
#   C1's blind packet. Clause [1] is DECORATION: its prose says "`ls -1` IN IT prints 2",
#   "in it" binds to nothing in a shell, the extracted span is a bare `ls -1`, and it
#   EXITS 0 FROM THE REPO ROOT ON ANY PACKET OR NONE. Demonstrated. I verified the real
#   property instead: 2 entries, both byte-identical to source, and rev-parse inside the
#   packet exits 128 (outside any repo). With PM.
#   THE FINDING IS THE MIRROR OF D-90: prove the detector still returns the POSITIVE on
#   the real corpus, not only the negative on injected cases. L2's own workflow-order
#   pattern fired on its OWN schema ("in task order" != tool call order). A DETECTOR THAT
#   FIRES ON EVERYTHING IS EXACTLY AS SATISFIABLE AS ONE THAT FIRES ON NOTHING, and the
#   false-positive direction is the one that gets an instrument DISABLED.

MERGED E2  f31e3ba pits:kb/pits/E2.md accept:PASS (verified by hand — the gate cannot supply $URL)
#   Six canonical states by SET EQUALITY, never count. S4-emp-submitted and S5-aud carry
#   THE SAME NUMBER of tools and are NOT the same set, so a count assertion passes even
#   if THE AUDITOR IS HANDED THE EMPLOYEE'S SURFACE.
#   Expected sets read from artifacts/tools.export.json AT RUNTIME -- "transcribing it
#   would only check my copy against my copy".
#   The crossover cost C4 investigation time as well: exported S2-emp-draft-clean is the
#   compiler's internal S3. Four documents and a seat now. D-99 exists for this.

FOLLOWUP D-94-beats a338a5a pits:kb/pits/D-94-beats.md accept:N/A (a ruling, not a node)
#   All three off-page beats ENOUGH IF; F6 unblocks on DOCUMENTS ALONE, zero code.
#   THE TWO MECHANISM FINDINGS OUTRANK THE VERDICT: the SURFACE column appended as a
#   7th field is matched by NO group and SILENTLY IGNORED, so D-94 would have landed as
#   DECORATION and three miscategorised shots could then go GREEN. And SB-07 is a FOURTH
#   miscategorisation, green today, that would have stayed green after the ruling landed.
#   A SEAT DISCOVERING THAT THE RULING IT WAS HANDED WOULD NOT HAVE WORKED, BEFORE
#   EXECUTING IT -- found by running the parser regex of a node it was not reviewing.
#   SB-04 needs a REWRITE not a relabel: the surface compiles from (role x report-state x
#   verdict) and POLICY IS NOT AN AXIS, so an absent tool refuses nothing.

FOLLOWUP D1-deploy-observations 5d4c2f6 pits:kb/pits/D1-deploy-observations.md accept:N/A
#   Cold start MEASURED: 960s idle, then 200 in 0.414s -- AND THE HEDGE THAT MAKES IT
#   HONEST: a fast result is not proof the tier wakes fast, only that either it woke
#   fast OR IT NEVER SLEPT. A measurement that cannot distinguish its own two
#   explanations, said so by the seat that took it.
#   Deploy latency recorded as an upper BOUND (<=9m49s), not the unobserved flip.
#   And it corrected me: version.mjs was never lost -- I inferred that from an absence
#   on main and I4 checked instead of accepting it.

FOLLOWUP E4-answer-key 60bae7e pits:kb/pits/E4-answer-key.md accept:PASS (all four E4 spans)
#   The blind eval's operational half now has a key to compare against. Tasks
#   pre-register mismatchMeans -- what a MISS would tell us -- which bans quietly
#   RE-INTERPRETING a result, the cheaper cousin of re-rolling it.
#   Packet asserted BY NAME AND CONTENT, not count: "exactly two files" is satisfied by
#   THE ANSWER KEY REPLACING ONE OF THEM, the one substitution that flatters us.
#   L2's EARLIER refines D-108: the positive case is THE ONLY THING THAT NOTICES WHEN THE
#   AUTHOR CHANGES THE CONTRACT. A SUITE OF NEGATIVES CANNOT DISTINGUISH "STRICTER" FROM
#   "BROKEN", BECAUSE BOTH LOOK LIKE REJECTION.
#   AND ONE ROUTED CLAIM WAS WRONG: compile.js's header is NOT label-swapped -- it
#   describes INTERNAL ids and MEMBERSHIP confirms it. L2 compared against the EXPORT's
#   ids. The D-99 error, made by the seat that received D-99. Not routed to I2.

MERGED F6  33e100a pits:kb/pits/F6.md accept:PASS
#   13 of 13 shots resolving; storyboard is L2's ruling TRANSCRIBED, not UX's judgement.
#   I VERIFIED THE SAFEGUARD MYSELF: restoring SB-07's forbidden #agent-banner gives
#   "declared surface agent-client but its selector resolves on OUR page" -- exit 1, 0 on
#   restore. A transcription that drifted in UX's favour is caught by UX's own instrument.
#   MY FIRST SABOTAGE DID NOT APPLY (doubled backticks -> invalid selector -> 0 hits) and
#   LOOKED EXACTLY LIKE THE SAFEGUARD PASSING. Second time in one day.
#   AND MY OWN INFERENCE WAS THE NEAR-MISS: I told UX SB-12 was agent-client; L2 ruled
#   page. Had UX accepted my list, its own tool would have forbidden SB-12 a page selector
#   and F6 would have gone GREEN on a storyboard that had lost beat 3's page proxy.
#   A REFUSAL TO INFER DOES NOT ONLY PROTECT THE REFUSER -- IT FORCES THE DECIDER TO STATE
#   THE DECISION, AND STATING IT IS WHERE THE ERROR SURFACED.

FOLLOWUP F5-sweep bdebf08 pits:kb/pits/F5-sweep.md accept:N/A (a sweep, not a node)
#   Three instances of the presence-check class in ~17 candidates, ONE OF THEM UX'S OWN --
#   and it LOOKED correct, because it genuinely WAS document.modelContext (our shim).
#   THE FIX BROKE NO TEST, which is how the real gap surfaced: the F5 tests launch WITH
#   the WebMCP flag and CANNOT REACH THE BRANCH THE DEMO RUNS IN.
#   THE TESTED CONFIGURATION AND THE SHIPPED CONFIGURATION DIVERGE WHEREVER A TEST HAD TO
#   CHOOSE SOMETHING, AND THE CHOICE IS INVISIBLE FROM INSIDE THE TEST.
#   The CLEARED cases were measured too -- probe/ and the v3 fixture load no shim, so H2's
#   gate cannot be fooled. A classification, not a list of suspicions.

MERGED S8  490f421 pits:kb/pits/S8.md accept:PASS
#   Provenance per field. I3 read the FROZEN schema and found my dispatch undersold it --
#   the record is value_DIGEST, not value, so the ledger never stores a value twice -- and
#   built the mislabel refusal from the source<->tool<->actor triangle the schema already
#   states rather than inventing a parallel rule.
#   Every real record validated by a real ajv-2020 compile (17/17), not by its own
#   field-presence assertions.
#   I VERIFIED THE SABOTAGE MYSELF and my FIRST attempt was worthless -- a crude edit broke
#   the module LOAD, giving an import error dressed as a failing test. Disabling only the
#   mislabel guard gives 5 pass / 1 fail, and the one failure is the D-100 test built for
#   it. Second sabotage-that-proved-nothing I have written today.

FOLLOWUP promise-sweep 2d850b2 pits:kb/pits/promise-sweep.md accept:N/A (a sweep, not a node)
#   ONE instance and it is STRUCTURALLY BOUNDED: new AbortController() appears exactly
#   ONCE in product code, so the class has exactly one generator and it was already fixed.
#   Detector validated against the PRE-FIX file before the negative was trusted.
#   ROUTED: sign-dialog.js discards submitDecision from the human Sign/Decline handlers
#   with the fetch UNWRAPPED -- THE HUMAN CLICKS SIGN, THE REQUEST FAILS, AND NOTHING
#   VISIBLY HAPPENS, on the surface the camera films. And fallback-agent's shim NEVER
#   REJECTS, so it cannot reproduce the class -- a FALSE-NEGATIVE GENERATOR that nearly
#   convinced I2 there was nothing to find.
#   IN A CLASSIFICATION THE DANGEROUS CELL IS NOT THE INSTANCE, IT IS THE CLEARED ONE.

# D-92(a)'s TRAP FIRED, 2026-08-30 21:32 PDT, EXACTLY AS I4 WROTE IT TO.
#   The user upgraded to Render Starter. Good news twice over: D-08's recorded
#   divergence closes four days early, and the 09-04 -> 09-21 unattended judging window
#   no longer risks cold starts.
#   AND D1's CLAUSE 4 IS NOW UNSUPPORTED. "Exactly ONE instance" passed because the FREE
#   tier is single-instance BY NATURE -- a PROPERTY OF THE TIER, not a configured replica
#   count. That property is gone. evidence/D1-url.txt's opening line is now FALSE.
#   THE CLAUSE BROKE PRECISELY WHEN SOMEBODY DID THE RIGHT THING, which is what PM named
#   at D-92(a) and what I4 wrote the reason down to catch. A recorded FACT would have gone
#   silently stale; a recorded REASON announced its own expiry.
#   IT IS A CORRECTNESS DEPENDENCY, NOT BOOKKEEPING: S12's sign lock and S6's TOCTOU
#   closure are true BY CONSTRUCTION OF A SINGLE PROCESS. A second instance reopens the
#   lock race S6 closes.
#   Routed: I4 owns the file; the dashboard needs the relay and the user. D1 stays done on
#   its other three clauses and clause 4 is carried HONESTLY UNSUPPORTED until re-verified.

# D-92(a) RESOLVED, and the replacement reason is STRONGER than the one that expired.
#   Dashboard read post-upgrade, transcribed under the D-48 precedent:
#     AUTOSCALING OFF -- greyed, non-interactive, gated "requires a Pro plan or higher",
#       and the workspace is on Hobby. DOUBLY LOCKED: switched off AND structurally
#       unavailable without a deliberate plan purchase. IT CANNOT DRIFT ON SILENTLY.
#     MANUAL SCALING 1 -- slider and numeric input both 1, Save greyed, no pending change.
#   So "one instance" moved from an INCIDENTAL PROPERTY OF A TIER to an EXPLICIT
#   CONFIGURATION plus a STRUCTURAL GATE. The clause is supported again, by a better
#   reason than the one that expired.
#   S12's sign lock and S6's TOCTOU closure remain true by construction of a single
#   process under both facts.
#   THE WHOLE EPISODE IS THE ARGUMENT FOR RECORDING REASONS RATHER THAN FACTS: the tier
#   changed, the fact silently stopped holding, and the RECORDED REASON is what announced
#   its own expiry loudly enough for anyone to act on.

FOLLOWUP readme-and-D1-evidence d44313c pits:PENDING accept:N/A (not a node)
#   THE README TOLD A JUDGE THE PROJECT HAD NOT STARTED -- "Status: planning. No product
#   code yet", src/ and tests/ "not yet created" -- at 49/62 with a live deploy and a
#   completed production signature. Now "Status: live."
#   W found it; category SILENT DESCOPE, not typo: G2's test asserts only the two login
#   lines and NEVER LOOKED AT THE STATUS LINE, so the node passed and the unchecked claim
#   went stale. No instrument here could have caught it.
#   AND THE SEVERITY IS INVERTED FROM EVERY OTHER FINDING: two days of documents claiming
#   MORE than we measured, and this one claimed dramatically LESS.
#   I NEARLY DISMISSED IT -- my first check read three lines and I printed a label saying
#   W had a stale tree, while the grep in the same command contradicted me.
#   D1's evidence rewritten: one instance now holds by EXPLICIT CONFIGURATION plus a
#   STRUCTURAL GATE (autoscale needs a Pro plan; workspace is Hobby), replacing the
#   free-tier property the 21:32 upgrade retired. I4's recorded REASON announced its own
#   expiry -- a recorded FACT would have gone stale in silence.

FOLLOWUP DEV-017 57ec039 pits:PENDING accept:N/A (a deviation ticket)
#   SHA CORRECTED: the row first published b95f511, which is the README commit and NOT a
#   DEV-017 merge. `git merge -F -` failed (git cannot read a message from stdin), I echoed
#   MERGED unconditionally, and I pushed a record for a merge that had not happened. SECOND
#   TIME WITH `-F -` -- I documented the identical hazard in the H4 merge commit and then
#   repeated it. WORSE THAN H4: there the tree was wrong for one push; here THE RECORD WAS
#   FALSE ON MAIN, which is the artifact everything else is checked against.
#   W's README time-capsule finding, renumbered after an ID COLLISION THAT IS NOT ITS
#   ERROR: nothing allocates deviation ids, D-101 lets nodes declare their own tickets,
#   and W cannot see what another seat committed on a branch. Raised as its own item.
#   Category SILENT-DESCOPE and that is why nothing caught it: G2's test asserts the two
#   login lines and NEVER LOOKED AT THE STATUS BANNER.
#   SEVERITY INVERTED FROM THE WHOLE SPRINT: every other defect was a document claiming
#   MORE than we measured; this one claimed dramatically LESS.

MERGED E4 d2d0c86 pits:kb/pits/E4.md accept:PASS

MERGED E8 96ca184 pits:kb/pits/E8.md accept:PASS
#   ACCEPT SKIPPED IN THE GATE AND EXECUTED AT FIRE TIME INSTEAD: E8's span 0 IS the
#   blind run, so running it inside the merge gate would re-fire a one-shot instrument
#   and overwrite the artifact being merged. Span 0 exit 0 in L1's presence.
#   Fourth admissibility clause recorded against PM's branch (ii) -- existence + effect.
#   Branch (i) was checked FIRST and is ABSENT: the JSONL's distinct top-level keys
#   across all 11 events are item, thread_id, type, usage; zero mentions of the model
#   and no effort key on any event.
#   RESULT: R1..R6 = 1,1,2,2,1,2 (9/12); gate.passed FALSE; 5 of 8 tasks completable.
#   D-121 BINDS: any citation of this total travels with the disclosure that the packet
#   was NOT identifier-free and that blindness rested on an after-the-fact transcript
#   check. Without the disclosure the number is inadmissible. The MEANING of 9/12 is
#   L2's to rule -- not L1's and not PM's.

#   CORRECTION to the E8 row above, add-only, NOT a rewrite (D-103). L2 ruled the
#   "5 of 8 tasks completable" figure INADMISSIBLE AS WRITTEN under RUBRIC-ADDENDA
#   §2.4 -- a figure from our own instrument carries its achievable maximum.
#   evals/blind/tasks.md, frozen and dated BEFORE the run, declares T4/T7/T8 as
#   deliberately possibly-unfulfillable, and T4 and T8 are genuinely unfulfillable:
#   no registered tool takes a file-shaped argument, and an auditor session
#   registers no write tool at all. THE ACHIEVABLE CEILING IS 6 AGAINST A PASS MARK
#   OF >=6 -- zero slack, a perfection test on the completable subset wearing the
#   label of a three-quarters test. C1's 5 accounts for exactly T4, T8 and T3, so
#   THERE IS ONE GENUINE SHORTFALL, T3, NOT THREE.
#   ADMISSIBLE FORM, ceiling in the same sentence, never a bare "5 of 8" and never
#   a bare "gate: FAILED":
#     "gate not passed: 5 of a possible 6, the other two tasks being deliberately
#      unfulfillable by design"
#   THIRD DISCLOSURE ITEM, joining D-121's two rather than replacing them: L2 ruled
#   the reasoning-effort axis UNEVALUABLE for this run under RUBRIC-ADDENDA
#   §3.6(a) -- a control is armed only if the artifact it inspects is produced by
#   the run it grades. No event in the transcript names a model or an effort, so
#   the existence+effect pair recorded above measures a SIBLING run. "The run
#   executed at reasoning effort low" is OUR-ESTIMATE and must carry those words.
#   DO NOT RE-RUN TO CURE THIS: EVAL.md 8.5 forbids re-rolling after seeing the
#   number, and re-firing now would be exactly that whatever the motive.
#   Do NOT cite reasoning_output_tokens as evidence of the setting -- it is
#   consumption, not configuration.
#   PRE-REGISTERED AND BANKABLE: C1 picked list_receipts, not link_receipt. The key
#   said before the run that link_receipt would be the most important result in the
#   whole run. Seven of eight inside the frozen accepted set.
#   E8 ARTIFACT HASHES, recorded because I verified byte-identity through the merge
#   round trip and then did not write the hashes down -- the artifact was verified
#   and the VERIFICATION was not repeatable by anyone else. QA re-derived both from
#   a fresh clone of origin/main and they match exactly:
#     2b4749ea28fbca1821f83c85bb24f1fa835336b633f257e774bbfe41b81b72f0  evals/blind/C1-verdict.json
#     cf0271e686fbabac74ecd931e412e445a314441944581675a17b2441e09bcb27  evals/blind/C1-transcript.jsonl
#   This is a one-shot instrument that cannot be re-run (L2: re-firing would be
#   re-rolling after seeing the number, EVAL.md 8.5). Anyone can now re-derive the
#   check without asking L1.

MERGED S7 2db6594 pits:PENDING accept:PASS
#   Landed together with D-118 in one branch: they conflicted in commit() and I3
#   merged them rather than L1 resolving product code. Ordering: policy identity
#   (D-118), then S6 recon, then chain.append() (S7) only if both pass. I3 read the
#   auto-merged body line by line rather than treating a clean merge as a correct one.
#   NEW PROPERTY NEITHER ORIGINAL SUITE COULD SEE: a refused commit appends NOTHING
#   to the day book -- proven BYTE-IDENTICAL before/after, on both refusal paths, so
#   a 409 cannot leave a phantom chain entry. All four sabotages re-run against the
#   COMBINED code: disabling the policy check now also fails the new phantom-entry
#   test, a failure mode that DID NOT EXIST TO CATCH when that sabotage was first run.

FOLLOWUP D-118 2db6594 pits:PENDING accept:N/A (a PM ruling, not a graph node)
#   E_POLICY_DIGEST_MOVED, the refusal the frozen contract names and no node built.
#   Consumer-specifies-the-producer with a FROZEN CONTRACT as the consumer.

FOLLOWUP UX-F4-sign-dialog b65435f pits:PENDING accept:N/A (a routed defect, not a node)
#   Only a THROWN fetch was silent: a 4xx/5xx always rendered, because the response
#   came back and got read. The dialog was invisible in exactly the circumstance that
#   produces a thrown fetch -- a server that has just died, i.e. tonight's outage.
#   UX's general form: A COMPONENT'S TESTS CLUSTER AROUND THE OUTPUTS IT PRODUCES,
#   AND ITS WORST FAILURE IS USUALLY THE ABSENCE OF AN OUTPUT, WHICH HAS NO NATURAL
#   PLACE IN THAT LIST.

FOLLOWUP PM-rulings-D122-D131 753732b pits:PENDING accept:N/A (rulings)
FOLLOWUP L2-verdict-review a06806d pits:PENDING accept:N/A (a review and two addenda sections)

MERGED E3 5077b30 pits:kb/pits/E3.md accept:PASS
#   Held all day under D-119 and released by PM's standing authorisation ("when D-118
#   lands and E3 goes actually green, merge it then"). ACCEPT RE-RUN AT MERGE against
#   the DEPLOYED origin 079da49: 11 of 11 declared cases graded, 11 passed, zero
#   skipped, detector controls accepted the real corpus and rejected broken fixtures,
#   N-20 neg-policy-content-swap ENFORCED.
#   NOTE ON THE GATE, so nobody repeats the confusion: `node tools/merge-gate.mjs`
#   reported span 0 FAILING for this node, because it runs the accept against the
#   CURRENT tree and evals/suites/negative.suite.json exists only on the branch. The
#   predicate needs the MERGED tree. I merged locally, ran the accept there, and only
#   then pushed -- the predicate executed and succeeded in my presence, which is what
#   clause 6 asks for. The gate is right to run accept pre-merge for nodes whose
#   predicate does not depend on their own new files; E3 is the first node where it
#   cannot be.

MERGED E10 1331808 pits:kb/pits/E10.md accept:PASS
#   10 of 10 must-fail controls FLIPPED from refused to permitted under their declared
#   brokenBy mutation; 11 report rows. E3's eleven passes are now DETECTIONS rather than
#   absences of trouble -- D-108 applied to a whole suite.
#   FIRST NODE GATED WITH --accept-merged. E10's accept invokes a subcommand that exists
#   only on its own branch; in the base tree the gate would have reported a RED predicate
#   for a GREEN node.
#   C4's "five unrelated inspector failures" VERIFIED rather than taken: 221/0 here, zero
#   not-ok lines. Chrome was unavailable in its sandbox; the characterisation was right.

MERGED D3 adb1c80 pits:kb/pits/D3.md accept:PASS
#   ACCEPT EXECUTED BY I4 AT MEASUREMENT TIME, skipped in the gate. Re-running it now
#   would take 30 minutes AND FAIL: D3 measures a DEPLOYED state and the train moved the
#   served build, so /api/state-digest no longer matches S9's baseline. PM D-132: every
#   subsequent push invalidates it (D1's property under D-92(b)), so any re-run is stale
#   the moment the next push lands. STANDS WITH ITS BUILD NAMED (617a109); re-verifies in
#   the FINAL PASS with D1 and D6, against a fresh S9 baseline owned by that pass.
#   preCheck matchesS9Baseline TRUE, read before the clock and kept separate.
#   requestsMadeDuringIdleWindow 0. Cold request 200 in 280.5ms against a 10000ms bound.
#   Verified independently: ZERO pushes to main landed inside 22:09:45-22:39:45.
#   The 280.5ms is the Starter upgrade as a MEASURED FACT, not a pricing-page promise.

FOLLOWUP G2 adb1c80 pits:kb/pits/G2.md accept:N/A (pit debt cleared; the node itself merged at ab4831b)
#   G2's pit REFUSES to write TRIED and HAPPENED: the original work belongs to a different
#   session, and reconstructing a report from the diff is the artifact D-31 exists to
#   prevent. EVIDENCED-UNRECOVERABLE, same class as H5. Its EARLIER is measured and real:
#   readme-credentials.test.mjs asserts exactly the two things it was written for and
#   nothing else, so the stale status line sat ONE SCREEN ABOVE the text the test does
#   check, false for two days. THE GATE'S SILENCE MEANS "NOT CHECKED," NEVER "TRUE".
#   CORRECTION, add-only: the row above was first written as `FOLLOWUP G2-pit` and
#   --check-record REFUSED it -- "names kb/pits/G2.md, ANOTHER NODE'S PIT" -- because the
#   row id must match the pit basename. THAT OBJECTION PRINTED AND I COMMITTED AND PUSHED
#   ANYWAY, because the check and the commit were sequential statements with no guard
#   between them. That is DEV-014's pattern, in the ONE artifact that is itself the
#   instrument every other check validates against. The basename-identity check is one I
#   added to ready.mjs myself, and it did exactly its job; the defect was that nothing
#   stopped me walking past it.

FOLLOWUP S7 2db6594 pits:kb/pits/S7.md accept:N/A (pit debt cleared; node merged above)
#   Two clause-6e blocks, both I3's: S7 as built (56a875a) and the CONFLICT RESOLUTION
#   with D-118 (131ccec). The second is the one worth reading -- the merged commit()
#   produced a property NEITHER branch could test alone, and I3 wrote the phantom-entry
#   tests to cover it rather than treating a clean auto-merge as a correct one.

FOLLOWUP D-118 2db6594 pits:kb/pits/D-118.md accept:N/A (a PM ruling, not a graph node)
#   Supersedes the earlier D-118 row's pits:PENDING. The block was in history all along;
#   I recorded PENDING without checking whether it was recoverable.
